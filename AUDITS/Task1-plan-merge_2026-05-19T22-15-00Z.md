# Adversarial audit — `plan-merge-owed-backlog` (commit `9753160`)

Auditor: fresh-eyes adversarial pass. Audit UTC stamp: 2026-05-19T22:15:00Z (wall-clock; the audited commit's internal logical-UTC stamp is 2026-05-23T20:00:00Z per its commit message + receipt — `git show` shows the actual git wall-clock date is `Tue May 19 15:16:54 2026 -0400`. The forward-dated session stamps are a known project quirk and not in audit scope).

## Scope

In: the deliverables introduced by commit `9753160` (`plan-merge-owed-backlog` `[plan-update]`):
- `NEXT_STEPS.md` §8 matrix +4 sharpening rows (L13-ext (year-axis), H6-ext (heterogeneous-union), L13-ext (shared CSV), L17-ext (grep-scope)).
- `NEXT_STEPS.md` §15.D 3 prepended `[plan-update]` blocks (C8.18 model-clarification; C8.21 premise-correction; C8.22 scope-clarification).
- `LESSONS.md` +1 entry (2026-05-22T02:00:00Z; (A′) falsification addendum).

Out: re-litigating C8.X substance; the executing agent's self-justification (receipt, post-2026-05-23T20:00:00Z STATUS/DECISION_LOG/PRE_FLIGHT_LOG entries — refused per task brief); the canonical pipeline; the session's other 4 tasks.

## Checks performed

Each check: command/file actually run/read, raw result, and the auditor's reading of it.

### Check 1 — Source fidelity for the 4 new §8 rows

**Command set:**
- `Read LESSONS.md 1-65` (the 2026-05-22 addendum + the preserved 2026-05-20 entry).
- `Read FIX_LOG.md 38-160` (both 2026-05-23T02:00:00Z entries — L17 grep-scope + L13-ext shared-CSV).
- `git show 224444d:NEXT_STEPS.md | grep -n "^| L13-ext\|^| H6-ext\|^| L17-ext"` → 0 matches (the 4 rows did NOT exist pre-session). Post-session: lines 324-327.

Row-by-row fidelity:

- **L13-ext (year-axis)** — Class/Symptom/Caught-at/Specific-catch faithfully condense the 2026-05-20 LESSONS (A′) "Proposed matrix-row sharpening" block AND incorporate the 2026-05-22 addendum's strengthening ("a single ≤5000-row head-sample byte-equality is itself NOT a per-year proof"; "against the source's own file-level NCHS control total"). The example `RECWT@91 ... byte-identical to DMRACE@57 in 1988` is verbatim from LESSONS 2026-05-20 (A′). The "Caught at" carries an explicit caveat pointer to LESSONS 2026-05-22. **No nuance dropped; no silent strengthening/weakening.**
- **H6-ext (heterogeneous-union)** — `pa.Table.from_pylist`/`from_pandas`, "infer the Arrow schema from the FIRST record's keys only", "silently drops the late-arriving segment's columns (or crashes a chunked `ParquetWriter` at the segment boundary)", "explicit unified schema (derived from the layout, not from data order)", and the SMOKE-§9-#9 catch are all present verbatim in the LESSONS 2026-05-20 (A″) proposed sharpening. Faithful.
- **L13-ext (shared CSV)** — Class/Symptom/Caught-at/Specific-catch condense the FIX_LOG 2026-05-23T02:00:00Z `compare_external_targets_v3_linked.py` entry's "Could the §8 matrix have caught this earlier?" sharpening. The defense column adds "while keeping the L14 non-zero-exit on its owned surface", which the source FIX_LOG explicitly recommends ("L14-safe: the validator still exits non-zero on any per-row FAIL within its OWNED 2005-2023 surface"). Faithful.
- **L17-ext (grep-scope)** — Defense column near-verbatim from the FIX_LOG 2026-05-23T02:00:00Z `test_parquet_column_snapshot.py` "A §11 L17 sharpening is owed: …" clause. Sole departure: the source says `VERIFY-E pass` and the row says `VERIFY pass` (the `-E` suffix dropped). Not load-bearing — VERIFY-E is a project-internal sub-phase label that does not appear elsewhere in the §8 matrix.

**Verdict — Check 1:** PASS.

### Check 2 — Matrix self-consistency

**Command:** `grep -n "^| L[0-9]\|^| H[0-9]\|^| F[0-9]" NEXT_STEPS.md | head -50` (lines 309-327 post-session).

Pair-check each new row vs every existing row in its class family:

- L13-ext (year-axis) vs L13 (inventory CSV records file roles before column-content verification): new row sharpens the "downstream consumers to re-verify" prescription with the per-era-year + file-level-control-total verification. Compatible.
- H6-ext (heterogeneous-union) vs H6 (silent row drops in joins): new row addresses silent column drops (different defect; same H6 spirit of "silent loss at a stage boundary"). H6's "row-count assertion at every stage boundary" does NOT cover the H6-ext case (a column union drop with row count preserved), so the new row is non-redundant. Not contradictory.
- L13-ext (shared CSV) vs L13: new row narrows L13 to a specific sub-case (shared targets/inventory CSV + downstream consumer ownership). Compatible.
- L17-ext (grep-scope) vs L17: new row sharpens L17's PRE-FLIGHT grep-scope to include test-infra `EXPECTED_*`/`*_TOTAL`/`*_COUNT` literals. Compatible with L17's existing "Convention 2 `DESIGN: tracks-current-state` … bundle the minimal Edit into the same commit" prescription.

No new row contradicts L1-L17/H1-H10/F1-F5.

**Verdict — Check 2:** PASS.

### Check 3 — Append-only-supersede integrity

**Commands:**
- `git diff --numstat 224444d..9753160 -- NEXT_STEPS.md` → `10  0  NEXT_STEPS.md` (10 insertions, 0 deletions).
- `git diff --numstat 224444d..9753160 -- LESSONS.md` → `18  0  LESSONS.md` (18 insertions, 0 deletions).
- `diff <(git show 224444d:NEXT_STEPS.md) NEXT_STEPS.md | grep -c "^<"` → `0` (zero deletion-side lines).
- `diff <(git show 224444d:LESSONS.md) LESSONS.md | grep -c "^<"` → `0`.
- `git show 224444d:NEXT_STEPS.md | grep -F "**Goal.** Extend linked coverage from 2005-2023..." | md5` vs `grep -F ... NEXT_STEPS.md | md5` → `02a2b6649dc8d154a3557c6402e904fc` == `02a2b6649dc8d154a3557c6402e904fc` (C8.18 Goal line byte-intact).
- `git show 224444d:NEXT_STEPS.md | grep -F "soft-flag (ii), proposed-not-applied, queued for §11 human-merge"` vs `grep -F ... NEXT_STEPS.md` → both return the same DO-step-3 line byte-intact (the line whose stale-after-supersede status the new model-clar block "hereby resolves" but does not delete — correct §3 append-only-supersede behavior).
- `git show 224444d:LESSONS.md | sed -n '20,40p' | md5` vs `sed -n '38,58p' LESSONS.md | md5` → `bc28b327a5c0178100bdf9390837ca5e` == `bc28b327a5c0178100bdf9390837ca5e` (preserved 2026-05-20 LESSONS entry head byte-intact, just shifted +18 lines by the new prepended addendum).

All three "original preserved" claims hold byte-exact. The 4 §8 rows are appended after L17 (line 324+); the 3 §15.D blocks are prepended `>` block-quotes above each Task's `**Goal.**` line without touching the Goal/PRE-FLIGHT/DO/VERIFY bodies.

**Verdict — Check 3:** PASS.

### Check 4 — LESSONS addendum (A′) falsification claims vs DECISION_LOG 2026-05-22T02:00:00Z

**Files read:** LESSONS.md lines 20-34; DECISION_LOG.md lines 242-256.

Per-claim cross-check:

| Claim in 2026-05-22 LESSONS addendum | DECISION_LOG 2026-05-22T02:00:00Z |
|---|---|
| Σ(RECWT@91) over full 1983 den, 3,341,274 rows = 3,643,001 byte-exact vs guide "By occurrence" | "Σ(RECWT@91 over the full parsed 1983 den, 3,341,274 rows) = 3,643,001 == guide 'By occurrence' byte-exact" — match |
| 100% head-sample coincidence was a file-ordering artifact (den ordered by State-of-occurrence per p4; first 5000 records all VSCP weight-"1" and DMRACE@57≈"1"=White) | "the first 5000 records are all VSCP weight-1; `DMRACE@57`≈'1'=White, so the head-sample `byte91==byte57` coincidence is 75-81% on the FULL 1983-1987 file" — match |
| byte91 == byte57 equality only ~75-81% on FULL 1983-1987 file (not 100%) | "75-81% on the FULL 1983-1987 file, NOT the claimed 100%" — match |
| 1988 IS a genuine trailing-byte anomaly (100%, multi-valued); 1988 is a full file [Record count == by-occurrence] needing NO weighting | "1988 IS a genuine trailing-byte anomaly — 100%, multi-valued — but 1988 is a full file [Record count == by-occurrence] needing NO weighting → NULL stays correct there" — match |
| 1983-1984 → 6a-RECWT correct populated; 1985-1988 → 5c-iii NULL correct | "the 5c-iii `record_weight`=NULL is SUPERSEDED for 1983-1984 (correct 1985-1988)" — match |
| values ∈ {"1","2"} per p4 methodology | "values ∈ {'1':3,039,547;'2':301,727} exactly per p4" — match (LESSONS gives the set; DECISION_LOG gives the counts) |

Every load-bearing claim in the LESSONS addendum is grounded in the cited 2026-05-22 DECISION_LOG entry.

**Verdict — Check 4:** PASS.

### Check 5 — C8.18 model-clar block: each cited DECISION_LOG entry exists pre-this-session

**Command:** `grep -n "^## 2026-05-" DECISION_LOG.md | head -30` (all entries pre-2026-05-23T20:00:00Z are out of the forbidden-read scope).

Per cited timestamp:

| Block cites | DECISION_LOG entry exists |
|---|---|
| 2026-05-17T20:00:00Z (DO step 3 → 3a/3b) | line 526 — "DO step 3 PRE-FLIGHT — substrate format = Python `field_specs.py` … DO step 3 split → 3a (denominator) + 3b (numerator)" ✓ |
| 2026-05-19T05:00:00Z (DO step 5 → 5a/5b/5c — range start) | line 366 — "DO step 5 decomposed → 5a/5b/5c" ✓ |
| 2026-05-20T02:00:00Z (range end; 5c-iii + (A′) RECWT@91 finding) | line 278 — "DO step 5c-iii — the keyless 1983-1988 `link_segment` … (A′) RECWT@91 conservative-NULL Convention-3 finding" ✓ |
| 2026-05-21T12:00:00Z (DO step 6 → 6a/6a-RECWT/6b — range start) | line 260 — "DO step 6 decomposed → 6a/6a-RECWT/6b" ✓ |
| 2026-05-23T02:00:00Z (range end; 6b) | line 224 — "DO step 6b — the FULL canonical re-harmonize linked 1983-2023 → v4" ✓ |
| 2026-05-22T02:00:00Z (6a-RECWT; cited inline) | line 242 — "DO step 6a-RECWT … the bounded 1983-1984 RECWT root-cause fix" ✓ |
| 2026-05-23T05:00:00Z (DO step 7) | line 206 — "C8.18 DO step 7 — docs/metadata consumer-surface sync … tag `C8.18-complete`" ✓ |

Also for the C8.21 / C8.22 blocks (briefly):
- C8.21 cites DECISION_LOG 2026-05-23T16:00:00Z → line 142 ✓ ("the §15.D 'Stata 17+ supports import parquet' premise corrected").
- C8.22 cites DECISION_LOG 2026-05-23T18:00:00Z D1 → line 116 ✓ ("§15.D 'per-state' tabs structurally infeasible → substitute feasible race/age stratifications").

All 9 cited DECISION_LOG anchors exist in the pre-this-session log. Sub-Q42 4a/4b/4c is treated under Finding 1 below — see Findings.

**Verdict — Check 5:** PASS (substantively); see Findings for an incomplete-enumeration note.

### Check 6 — No stealth in-place edit outside the prepended blocks / appended rows

**Command:** `diff <(git show 224444d:NEXT_STEPS.md) NEXT_STEPS.md` — every hunk inspected.

Hunks:
1. `@@ -321,6 +321,10 @@` — 4 new lines inserted between L17 and the "If a new mistake class is encountered…" closing prose. No surrounding line changed.
2. `@@ -1396,6 +1400,8 @@` — 2 new lines (the `> **[plan-update] 2026-05-23 (model-clarification…**` block + 1 blank line) inserted between the existing 2026-05-17 [plan-update] block and the C8.18 `**Goal.**` line. The C8.18 Goal/PRE-FLIGHT/DO/VERIFY body byte-untouched (confirmed via the spot-check md5 + the diff's zero `^<` lines).
3. `@@ -1527,6 +1533,8 @@` — 2 new lines (the C8.21 premise-correction block + blank) inserted between the `### Task C8.21` heading and its `**Goal.**` line. C8.21 body byte-untouched.
4. `@@ -1559,6 +1567,8 @@` — 2 new lines (the C8.22 scope-clarification block + blank) inserted between the `### Task C8.22` heading and its `**Goal.**` line. C8.22 body byte-untouched.

LESSONS.md diff has one hunk inserting 18 lines (the 2026-05-22 entry + its `---` separator) between the file preamble and the existing 2026-05-20 entry. The 2026-05-20 entry is byte-shifted +18 lines but content-byte-intact (confirmed via md5).

No silent alteration to existing §15.D Goal/DO/VERIFY text. No silent alteration to existing matrix rows.

**Verdict — Check 6:** PASS.

## Findings

### Finding 1 — C8.18 model-clar block omits DO step 4 → 4a/4b/4c decomposition

**Class:** L11-adjacent (incomplete-claim, documentation completeness — not a stale roadmap claim per se, but a similar shape).

**Severity:** **minor**.

**Verbatim evidence — the omission:** The C8.18 model-clar block at NEXT_STEPS.md:1402 enumerates "**DO step 3 → 3a/3b** … **DO step 5 → 5a/5b/5c**, and **5c → 5c-i / 5c-ii (→ 5c-ii-a + 5c-ii-b) / 5c-iii** … **DO step 6 → 6a / 6a-RECWT / 6b** … **DO step 7** = the docs/metadata consumer-surface sync … `C8.18-complete` set. This block records the executed structure only…". DO step 4 is absent.

**Verbatim evidence — the omitted decomposition is in fact a witnessed within-task decomposition pre-this-session:** DECISION_LOG.md:436 — `## 2026-05-18T05:00:00Z — C8.18 DO step 4 PRE-FLIGHT — … DO step 4 split → 4a (1995-2002) + 4b (2003) + 4c (2004)`. Sibling DECISION_LOG entries witness each sub-step: 2026-05-18T14:30:00Z (4a), 2026-05-18T20:30:00Z (4b), 2026-05-18T23:00:00Z (4c).

**Why this is minor, not major:** the block's own framing is honest about its scope ("the substantive choices live in the cited append-only DECISION_LOG/RECEIPTS entries") and the omission does not alter scope, deliverables, or any byte-on-disk artifact. A reader trying to reconstruct the executed decomposition tree from this block alone would have an incomplete picture of step 4 but would still arrive at the correct deliverable (1995-2002, 2003, 2004 layouts authored). The soft-flag (ii) that motivated the block specifically called out step 3 → 3a/3b only, so the block's primary scope is satisfied; the additional voluntary documentation of steps 5/6/7 is incomplete by selectively omitting step 4.

**Remediation (optional, not blocking):** if a follow-up `[plan-update]` is ever generated for this block's scope, prepend a one-clause addition: "`DO step 4 → 4a/4b/4c` (4a 1995-2002 1989-rev layout; 4b 2003 DEFLATE64 transition; 4c 2004 dual-cert; DECISION_LOG 2026-05-18T05:00:00Z → 2026-05-18T23:00:00Z)". No canonical mutation required.

### No other findings

The 4 §8 rows are faithful to their LESSONS / FIX_LOG sources. The 3 §15.D blocks are correctly grounded in pre-this-session DECISION_LOG entries. The 2026-05-22 LESSONS addendum matches DECISION_LOG 2026-05-22T02:00:00Z byte-for-byte on every load-bearing claim. The diff is insertion-only (10/0 NEXT_STEPS.md, 18/0 LESSONS.md). The 3 high-risk L11 surfaces (C8.18 stale soft-flag (ii) note; C8.21 false "Stata 17+ supports import parquet"; C8.22 infeasible "per-state" slice) are addressed by the new prepended blocks rather than perpetuated. No L3/L6/L7/§3 violations surfaced.

## Verdict

**PASS-WITH-NOTES.**

The `[plan-update]` is a clean §11 human-merge: append-only on disk, byte-faithful to its append-only sources, and self-consistent with the existing §8 matrix. The one minor finding (DO step 4 → 4a/4b/4c omitted from the C8.18 model-clar block's voluntary enumeration of executed within-task decompositions) does not affect scope, deliverables, or any verifiable artifact, and the block itself explicitly defers substantive detail to the DECISION_LOG/RECEIPTS entries. No §7 condition is triggered. No new mistake class. No reproducibility regression.
