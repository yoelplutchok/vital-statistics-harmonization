# CONSOLIDATED AUDIT REPORT — Pre-D cleanup block + session integrity

- **Consolidator timestamp:** 2026-05-19T20:00:00Z (harness wall-clock)
- **Session under audit:** 5 Pre-D cleanup tasks + cross-cutting session integrity, commits `d913b91..17814d2`, repo append-only clock 2026-05-23T20:00:00Z–23:45:00Z
- **Audit method:** 6 independent fresh-eyes adversarial agents (no agent self-review), each refused to read the matching RECEIPT / DECISION_LOG / PRE_FLIGHT_LOG / STATUS entries for its task. All 6 outputs are preserved at `AUDITS/*.md`.
- **Consolidator role:** synthesis only; not itself an audit (does not re-execute checks; aggregates the per-audit findings into one decision-useful document).

---

## 1. Per-audit verdicts (top-line)

| # | Audit (file) | Target commit | Verdict | Findings |
|---|---|---|---|---|
| 1 | `Task1-plan-merge_2026-05-19T22-15-00Z.md` | `9753160` plan-merge-owed-backlog | **PASS-WITH-NOTES** | 1 minor (L11-adj) |
| 2 | `fetal-death-codebook-comparability-v240_2026-05-19T22-45-00Z.md` | `6eecda2` CODEBOOK+COMPARABILITY v2.4.0 | **PASS-WITH-NOTES** | **1 significant** (H8/L7) + 1 cosmetic + 1 soft + 1 out-of-scope obs |
| 3 | `file-inventory-imported-flag-v4_2026-05-19T23-00-00Z.md` | `84a9af3` file_inventory imported-flag | **1 FINDING** (cosmetic) | 1 cosmetic (L7) |
| 4 | `v3-linked-comparison-v4-verify_516b621_2026-05-19T22-45-00Z.md` | `516b621` v3-linked-comparison verify-current | **NO FINDINGS** | 0 (1 pre-existing portability smell, out of scope) |
| 5 | `convenience-benchmark-v4-scope_2026-05-19T22-45-00Z.md` | `17814d2` benchmark scope note | **NO L-CLASS FINDINGS** | 0 (2 minor non-L observations) |
| 6 | `Session-integrity_2026-05-19T22-30-00Z.md` | cross-cutting, 10 commits / 5 tasks | **PASS** | 0 |

**Aggregate:** 0 blockers, 0 §7 halts, 0 reproducibility / append-only / scope-creep violations. 1 significant finding (label-legend mismatch in CODEBOOK), 2 cosmetic, 1 minor completeness, 1 soft.

---

## 2. Independently-verified invariants (the strongest signals)

Each was re-executed by a fresh agent against primary evidence (re-hashing, re-running validators, re-grepping pre-edit/post-edit), NOT by trusting any narrative:

- **All 4 gate parquet SHAs byte-exact across the session** (Audit 6 re-hashed): `38e2cecb…` / `185c071e…` / `acb5c48a…` / `f630d8cf…`. **Zero canonical-data mutation.**
- **C8.20 generated appendix byte-identical**, marker-extracted: `b27640eeb6eda142…` at all three of pre-do / current / pre-edit (Audit 2). The Task-2 reordering invariant held.
- **H10 reproducibility proven**: v3-linked comparison `.md`/`.csv` re-rendered byte-identically from the v4 parquet (Audit 4: `diff -q` empty; `35 pass, 0 fail, 0 missing`).
- **Mutation guard structurally non-tautological** (Audit 4 read `tests/mutations/_runner.py` and confirmed the AND-of-conditions cannot pass on a silent validator failure).
- **No L10 back-fills**: every PRE-FLIGHT commit strictly precedes its COMPLETE commit (Audit 6 git-timestamp table).
- **Append-only integrity**: cumulative `0` deletions across NEXT_STEPS/LESSONS/DECISION_LOG/PRE_FLIGHT_LOG; STATUS's single `-1` is the H1 metadata "last updated" pointer, not a dated-section edit (Audit 6 traced it across all 5 commits).
- **Per-task numstats match receipt claims exactly**: Task 1 +28/-0, Task 2 +51/-37, Task 3 +19/-19, Task 4 0/0, Task 5 +7/-0 (Audit 6).
- **All 5 Phase-D deferrals accounted for**: 4 discharged via the 5 tasks; manuscript preserved as D.4 by **empty `git diff -- paper/draft_v2_hmd_styled.md`** (Audit 6 — the strongest possible proof).
- **All 4 §8 matrix sharpenings byte-faithful** to their LESSONS 2026-05-20 / FIX_LOG 2026-05-23 source proposals (Audit 1).
- **3 §15.D `[plan-update]` blocks correctly grounded** in 9 pre-this-session DECISION_LOG anchors (Audit 1 cross-referenced each).
- **LESSONS 2026-05-22 (A′) addendum matches DECISION_LOG 2026-05-22T02:00:00Z** byte-for-byte on every load-bearing claim (Audit 1's 6-row cross-check table).
- **C8.13 timing measurement was empirically pre-envelope-grow**: 2026-05-13 measurement vs C8.17 2026-05-14 / C8.18 2026-05-23 (Audit 5 traced the dates).
- **Convenience CSVs correctly scoped OUT of soft-flag (ee)**: `live_births_by_year.csv` NVSR-sourced static; `stratified_denominators.csv` reads only the (untouched-by-C8.18) natality parquet; `write_residents_only.py` outputs gitignored/reproducible build-side (Audit 5).
- **The 19 imported-flag rows are exactly the right set**: Audit 3 independently re-derived from C8.18 step 6b → matches the 19 flipped rows byte-for-byte; no row missed; no row over-flipped.

---

## 3. Consolidated findings (ranked by severity)

### Finding A — **SIGNIFICANT** (Task 2, H8 / L7 — label-legend mismatch)

**Source:** Audit 2, Finding #1.

**Where:** `fetal_death/CODEBOOK.md` line 35 (the v2.4.0 scope-note's "V2" rebroadening) interacting with per-variable Notes carrying legacy-1992-2002-only counts.

**What:** The scope note I added at L35 says the legacy `"V2"` label in per-variable Notes now means "the pre-2003-revision S-synthesized eras (1982-2002 = V3b+V3a+V2; 1989-revision behaviour for 1989-2002, 1978-revision for 1982-1988)". But three per-variable Notes carry quantitative counts that are **factually true only for the legacy 1992-2002 sub-slice** (700,704 rows), not the 1,310,738-row 1982-2002 union. A reader applying the L35 gloss gets a 80-100% wrong number against the parquet-derived appendix:

| Doc claim | If V2 = legacy 1992-2002 (true) | If V2 = 1982-2002 per L35 (false) |
|---|---:|---:|
| L97 plurality: "1,713 V2 plurality=9 rows" | 1,713 ✓ | **3,564** ✗ |
| L116 birthweight: "397,397 V2 rows have BW=9999" | 397,397 ✓ | **715,057** ✗ |
| L217 singleton: "V2: 1,713 blanks total" | 1,713 ✓ | **3,564** ✗ |

The auditor also notes I demonstrated awareness of this exact risk for `tabulation_flag` (L62: explicit "V2.0 1992-2022 slice" scoping + C8.20 pointer) but didn't apply the pattern to plurality / birthweight / singleton.

**Class:** H8 (doc-data drift); L7 (looks-right); label-legend mismatch.

**Severity:** **significant** — this is exactly the §7-#7 / H8 silent doc-vs-data drift class. Not release-blocking *by the audit's own grading* but warrants remediation before manuscript submission.

**Recommended remediation (either approach is fine, mine is the second):**
- **Approach 1 (cleanest):** drop the L35 V2-rebroadening gloss; keep "V2 = 1992-2002" everywhere; the cross-era V3b/V3a/V2.1 narrative paragraph already explains the new eras separately.
- **Approach 2 (preserves the umbrella framing):** apply the `tabulation_flag` pattern (explicit "V2.0 1992-2002 slice" scoping + Appendix C8.20 pointer) to the 3 affected per-variable Notes (plurality / birthweight / singleton).

---

### Finding B — **COSMETIC, REPEATED 19×** (Task 3, L7 — missing space)

**Source:** Audit 3, Finding #1.

**Where:** `natality/metadata/file_inventory.csv` col5 of every one of the 19 flipped rows.

**What:** My `record byte-width = C8.18 DO step 3 (per ` → `layout reconstructed at C8.18 (per` edit dropped the trailing space, producing `(perLinkCO83Guide.pdf)` instead of `(per LinkCO83Guide.pdf)`. Confirmed via 4 grep counts:

```
$ grep -c '(perLinkCO' natality/metadata/file_inventory.csv  → 19
$ grep -c '(per Link'  natality/metadata/file_inventory.csv  → 0
$ git show 7521366:…   | grep -c '(per LinkCO'                → 19  (pre-edit was correct)
```

**Class:** L7 (looks-right) — the agent's §7-gate analysis correctly declared L6 (invented numbers) clean (every SHA-anchored member byte-size preserved) but missed the L7 prose-typo check on the new wording.

**Severity:** **cosmetic** — purely human-readable metadata; no programmatic consumer parses it (Audit 3 Check 5 verified: only `tests/test_inventory_invariants.py` parses the CSV, and the natality parity test skips `*_linked` rows entirely via `int(year)` ValueError).

**Recommended remediation:** one `replace_all` edit `(perLinkCO` → `(per LinkCO`. 19 substitutions, no other consequence.

---

### Finding C — **MINOR** (Task 1, L11-adjacent — incomplete enumeration)

**Source:** Audit 1, Finding #1.

**Where:** `NEXT_STEPS.md` C8.18 model-clarification `[plan-update]` block.

**What:** The block enumerates the executed DO-step decomposition as 3→3a/3b; 5→5a/5b/5c→5c-i/5c-ii(-a/-b)/5c-iii; 6→6a/6a-RECWT/6b; 7 — **but omits DO step 4 → 4a/4b/4c** (4a 1995-2002 1989-rev layout; 4b 2003 DEFLATE64 transition; 4c 2004 dual-cert). Each sub-step is witnessed in DECISION_LOG 2026-05-18T05:00:00Z → 2026-05-18T23:00:00Z (Audit 1 located all 4 anchors).

**Class:** L11-adjacent (documentation-completeness; not a stale roadmap claim per se).

**Severity:** **minor** — the block's framing is honest about its scope (*"the substantive choices live in the cited append-only DECISION_LOG/RECEIPTS entries"*) and the omission affects no scope/deliverable/artifact. The soft-flag (ii) that motivated the block specifically called out step 3 → 3a/3b only; my voluntary additional documentation of steps 5/6/7 was incomplete by selectively omitting step 4.

**Recommended remediation (optional):** prepend one clause to the block: `**DO step 4 → 4a/4b/4c** (4a 1995-2002 1989-rev layout; 4b 2003 DEFLATE64 transition; 4c 2004 dual-cert; DECISION_LOG 2026-05-18T05:00:00Z → 2026-05-18T23:00:00Z)`. No canonical mutation.

---

### Finding D — **COSMETIC** (Task 2, parity — V3b "~200 bytes")

**Source:** Audit 2, Finding #2.

**Where:** `fetal_death/COMPARABILITY.md` era table (V3b row); `fetal_death/CODEBOOK.md` variable-availability matrix (V3b row).

**What:** The V3b row reads `"~200 bytes (1978-rev)"`. The exact value 200 (= 202 with CRLF) is in STATUS 2026-05-12T16:45:00Z. The tilde is unnecessary hedging that breaks parity with the V2 row's `"360 bytes (362 with CRLF)"` and the V1 2022-era's `"2,652 bytes"`.

**Severity:** **cosmetic.**

**Recommended remediation:** replace `"~200 bytes (1978-rev)"` with `"200 bytes (202 with CRLF)"`. Both files, 2 edits.

---

### Finding E — **SOFT** (Task 2, L77 lost verification count)

**Source:** Audit 2, Soft note #3.

**Where:** `fetal_death/CODEBOOK.md` L77 (`data_year` Notes).

**What:** My reword dropped the pre-edit verification count `"(verified 1,634,195/1,634,195 in V2.0)"` and did not add a v2.4.0 replacement. The unqualified `"Always equal to int(delivery_year) for every row"` is now an asserted invariant with no in-doc evidentiary backing. The C8.20 appendix `data_year` (i)-panel shows 0.00% null/blank across all 7 eras → `(verified 2,427,233/2,427,233 in v2.4.0)` would be byte-supported.

**Severity:** **soft** — not factually wrong, just lost evidentiary backing.

**Recommended remediation (optional):** add `(verified 2,427,233/2,427,233 in v2.4.0; see Appendix C8.20)`.

---

### Finding F — **UNDER-SPELLED-OUT IMPLICATION** (Task 5, transitively-superseded verdict)

**Source:** Audit 5, observation #1 (explicitly classed not-an-L-finding).

**Where:** `docs/PIPELINE_TIMING_BENCHMARK.md` line 146 (pre-existing C8.13 content; not edited this session).

**What:** The doc retains *"No edit recommended for the natality clause"* — a verdict that was true at C8.13 measurement time (88.45 min vs 90 min claim = -1.72%) but is **transitively superseded** by my scope note (since the verdict is derived from the now-superseded measurement, and a v4 re-measure on the 350M-record envelope could plausibly push it outside ±10%). My note's "superseded point-in-time figures" framing covers this by inference but doesn't explicitly call out that the verdict itself is contingent.

**Severity:** **under-spelled-out** — careful reader gets there by inference; casual one might not. Audit 5 explicitly does not class this as an L-finding.

**Recommended remediation (optional):** append one sentence to the scope note clarifying the verdict-level transitive coverage. Or accept the implicit coverage.

---

### Out-of-scope flags (NOT findings against this session)

The auditors flagged two stale items that belong to *different* files / earlier work, not this session's commits:

- **Audit 2 out-of-scope obs:** The C8.20 generated appendix `data_year` Schema note (CODEBOOK L359, inside the marker-delimited generated block — correctly not hand-edited this session) still reads stale V2.0 prose (`"1992-2022 — … verified 1,634,195/1,634,195 in V2.0"`). This is an upstream `scripts/_build_codebook_extensions.py` generator-input issue (the Schema-note source is `harmonized_schema.csv`'s `notes` column). Fix is regenerator-side, not commit-`6eecda2`-side.
- **Audit 5 obs #2:** `shared/helpers/build_stratified_denominators.py` module docstring (L5) still says *"natality v2.7.0 full harmonized parquet"* while canonical is now v3.0.0. Different file, separate documentation lag.
- **Audit 4 out-of-scope:** `external_validation_v3_linked_comparison.md` line 4 absolute path (pre-existing; last touched at C8.18 6b `127f101`, not this session).

These are **legitimately out of scope** for the 5 audited commits but are tracked here as future-work candidates.

---

## 4. Phase-D-entry readiness assessment

### What this audit set proves with high confidence
- Zero canonical data mutation this session (4/4 gate SHAs byte-exact, re-hashed)
- All 5 carried Phase-D deferrals discharged faithfully; manuscript preserved as D.4 (empty diff)
- Append-only state-file integrity intact across 10 commits
- No L10 back-fills; no scope creep; no §7 halts bypassed
- §11 plan-merge backlog cleared with byte-faithful sources
- Inventory-flag flip is canonical-truth correct; tests still green
- v3-linked comparison artifact already v4-current AND H10-reproducible from a SHA-stable parquet
- Convenience artifacts correctly scoped relative to soft-flag (ee)

### What the audit set found
- **No release blockers.** No §7 condition triggered. No HALT verdict in any of 6 audits.
- **1 significant finding** (Finding A — label-legend mismatch in CODEBOOK) that is *not* release-blocking by the audit's own grading but is exactly the §7-#7/H8 silent doc-vs-data drift class that warrants pre-submission remediation. This is the same pattern as the C8.20 audit finding that became C8.20-auditfix.
- **5 cosmetic / minor / soft findings** that are trivially fixable.

### Recommendation

**Phase D entry: not blocked, but recommended remediation pass first.**

Mirror the established **C8.20-auditfix precedent**: ship a small bundled remediation commit (one `[plan-update]`-class or doc-class commit) addressing Findings A + B + C + D + E in a single pass before authorizing Phase D. All are doc-only / metadata-only, fully reversible, byte-bounded, zero canonical mutation.

**Proposed remediation bundle** (estimated 1 short task, full five-phase discipline):
- **A.** Pick Approach 1 (drop L35 V2-rebroadening) or Approach 2 (rescope the 3 per-variable Notes). My recommendation: **Approach 2** (preserves the cross-era umbrella framing the auditor noted I already used successfully for `tabulation_flag`).
- **B.** `replace_all` `(perLinkCO` → `(per LinkCO` in `natality/metadata/file_inventory.csv` (19 substitutions).
- **C.** Prepend the missing DO step 4 → 4a/4b/4c clause to the C8.18 model-clarification block.
- **D.** Replace V3b `"~200 bytes (1978-rev)"` → `"200 bytes (202 with CRLF)"` in both docs.
- **E.** Add `(verified 2,427,233/2,427,233 in v2.4.0)` to CODEBOOK L77.

Finding F (Task 5 under-spelled-out verdict implication) can be folded in or left as-is — it's the lowest-severity item and Audit 5 explicitly does not class it as an L-finding.

### What the audit set does NOT cover

- The pre-Zenodo Phase-C scope itself (C8.16–C8.22 deliverables, excluding C8.20 which has its own prior audit). For full multi-agent coverage of those, the `/ultrareview` mechanism is the right pre-Phase-D mechanism.
- The canonical pipelines (natality v3.0.0, linked v4.0.0, fetal v2.4.0, matched-multiples). The H10 byte-exact gate parquet SHAs (Audit 6) attest to no-mutation this session, but full pipeline audit is out of scope.
- Phase D itself (externally irreversible; human-authorization-gated by design).

---

## 5. File pointers

The 6 source audits this consolidation synthesizes:

```
AUDITS/Task1-plan-merge_2026-05-19T22-15-00Z.md                           (15,157 b)
AUDITS/fetal-death-codebook-comparability-v240_2026-05-19T22-45-00Z.md    (15,376 b)
AUDITS/file-inventory-imported-flag-v4_2026-05-19T23-00-00Z.md             ( 9,248 b)
AUDITS/v3-linked-comparison-v4-verify_516b621_2026-05-19T22-45-00Z.md      ( 8,052 b)
AUDITS/convenience-benchmark-v4-scope_2026-05-19T22-45-00Z.md             (14,510 b)
AUDITS/Session-integrity_2026-05-19T22-30-00Z.md                          (13,985 b)
```

The prior C8.20 audit (`AUDITS/C8.20_20260519T152156Z.md`) is unrelated to this session.

---

## 6. Bottom line

**6 independent fresh-eyes adversarial audits. 0 blockers. 0 §7 halts. 1 significant finding (Finding A — label-legend mismatch; remediable with a small doc edit). 4 cosmetic/minor/soft findings (Findings B-E; all trivially fixable). 1 under-spelled-out observation (Finding F; optional).**

The internal Pre-D cleanup block is **substantively sound**: zero canonical-data mutation, append-only integrity intact, all 5 carried deferrals discharged faithfully, manuscript preserved as D.4 by empty-diff. Phase D is **not blocked**.

**Recommended:** ship a single small remediation bundle addressing Findings A–E (mirroring the C8.20-auditfix precedent) before authorizing Phase D — same shape as before: PRE-FLIGHT → SMOKE → DO → VERIFY → RECEIPT, all doc/metadata-only, zero canonical mutation, fully reversible. Then Phase D.
