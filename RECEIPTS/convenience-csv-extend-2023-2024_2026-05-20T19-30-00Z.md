# Receipt: convenience-csv-extend-2023-2024 (D-prep.8)

## 2026-05-20T19:30:00Z

### What was done

- Extended `fetal_death/live_births_by_year.csv` with 2023 (NVSR Vol 74 No 1) and 2024 (natality canonical `3,628,934`; NVSR final not yet published).
- Extended `JOINT_COVERAGE_YEARS` to 2005–2024 in `shared/helpers/build_stratified_denominators.py` (canonical + legacy column read paths).
- Rebuilt `fetal_death/stratified_denominators.csv` (4,990 rows; sha256 `ad2cfc2e…`).
- Updated `docs/JOINT_USE_GUIDE.md` convenience-year tables.

2003–2004 in `live_births_by_year` not refreshed (no user override; existing gap years unchanged).

### Verify results

- Per-year stratified sums = `live_births_by_year` for 2022–2024 (byte-exact).
- 2023 total `3,596,017` matches NVSR `resident_births` target (PASS).
- `pytest` `test_stratified_denominators_*` (2 tests): PASS.
- Gate parquet SHAs unchanged (PASS).

### Self-check

Could have used wrong residence filter column on v3 natality schema; verified via NVSR match on 2023 and parity test against direct parquet groupby.

### Forward-looking HALTs for next session

1. When NVSR 2024 final publishes, replace 2024 `source` cell with NVSR citation and reconcile if count differs.
2. `test_cross_product_join_parity.py` docstring still says "29 years" — cosmetic; behavior uses live CSV years.
