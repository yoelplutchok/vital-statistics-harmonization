# Receipt: C8.18 DO step 3a — cohort 1983-1991 DENOMINATOR/births `field_specs.py` layout authoring + SMOKE Tier-0/1 encoding + value-distribution verify
## 2026-05-17T23:30:00Z

### What was done

Executed C8.18 DO step 3a under the full five-phase discipline. Authored the **byte-exact additive pre-1990 cohort-linked denominator layouts** in `natality/scripts/01_import/field_specs.py`: `LINKED_BIRTH_1983_1988_FIELDS` (45-field, 91-byte, transcribed from the PRE_FLIGHT_LOG 2026-05-17T20:00:00Z state-on-disk 1983 layout — HALT 3, guide NOT re-read), `LINKED_BIRTH_1989_1991_FIELDS` + `LINKED_DEATH_1989_1991_FIELDS` (225-byte Denominator-PLUS, reconstructed byte-exact from the `LinkCO89Guide.pdf` **detail code-outline** pp20-47), and the reclen constants `LINKED_DEN_RECLEN_1983_1988 = 91` / `LINKED_DENOMPLUS_RECLEN_1989_1991 = 225`. Added an **additive** `_layout_for_linked_year` 1983-1991 branch to `parse_linked_year.py` (existing 2005-2013/2014-2020 return blocks byte-untouched — H10/HALT-13). Added the `DESIGN: tracks-current-state` SMOKE harness `natality/tests/test_linked_cohort_1983_1991_layout_smoke.py` (13 tests; Convention 1/2). Bundled the §15.D "layout-CSV → `field_specs.py`" fix-on-contact wording reconcile (HALT 7; C8.17-DO2 precedent). **Zero canonical parquet / schema / validation-CSV mutation** (11/11 gate parquet SHAs byte-exact at VERIFY). Two empirical findings: (1) **1983-1988 = 91-byte births-only denominator vs 1989-1991 = 225-byte denominator-PLUS** — the layout IS discontinuous across the 1989 birth-cert revision (HALT 4 confirmed, NOT assumed); (2) the pre-1990 public-use `.dat` encoding is **ASCII (NOT EBCDIC)** despite the guides' "IBM/EBCDIC 8-bit" tape note (HALT 5 resolved). SMOKE Tier 1 correctly caught that the `LinkCO89Guide` pp18-19 element-span summary is composite (`DBIRWT` is 79-82 grams, not the 79-85 element span = `DBIRWT@79-82` + `BIRWT12@83-84` + `BIRWT4@85`); the layout was rebuilt from the authoritative byte-level detail code-outline rather than loosening the assertion (§2 fail-closed; §9-#4).

### Inputs consumed

- `PRE_FLIGHT_LOG.md` 2026-05-17T20:00:00Z — 8 forward-looking HALTs (entry cheap-check; 8/8 verified) + the state-on-disk full 1983-1988 91-byte denominator layout (HALT 3; NOT re-read from the guide).
- `RECEIPTS/C8.18_step2_2026-05-17T18-30-00Z.md` + `DECISION_LOG.md` 2026-05-17T20:00:00Z (DO step 3 PRE-FLIGHT decisions: substrate = Python `field_specs.py`; DO step 3 → 3a/3b).
- `raw_docs/linked/LinkCO89Guide.pdf` (192 pp; SHA-anchored at DO step 2; text-extractable, no OCR — L12-extension re-confirmed) — p17 file characteristics (Denominator 225 B / 4,045,881), pp18-19 element-location matrix, **pp20-47 detail code-outline** (the byte-level authoritative layout).
- `raw_data/linked/LinkCO{83,85,88,89,90,91}.zip` members `LinkCO{YY}USden.dat` (read-only; first ~300 records each for SMOKE Tier 1; SHA-anchored at DO step 2).
- `natality/scripts/01_import/field_specs.py` + `parse_linked_year.py` (sibling substrate; the 2005/2014 LINKED specs).
- 11 gate parquets — SHA-verified byte-exact at PRE-FLIGHT entry cheap-check and re-verified at VERIFY-A.

### Outputs produced

Git-tracked (this commit):

- `natality/scripts/01_import/field_specs.py` — **purely additive** (zero deletion/modification lines; existing LINKED 2005/2014 tuple lists + constants byte-untouched): `LINKED_BIRTH_1983_1988_FIELDS` (45 fields), `LINKED_DEN_RECLEN_1983_1988 = 91`, `LINKED_BIRTH_1989_1991_FIELDS` (64 fields, locs 1-212), `LINKED_DEATH_1989_1991_FIELDS` (6 fields, locs 213-225), `LINKED_DENOMPLUS_RECLEN_1989_1991 = 225`, with source-citation header block.
- `natality/scripts/01_import/parse_linked_year.py` — additive `_layout_for_linked_year` 1983-1988 (`(91, BIRTH_1983_1988, [])`) + 1989-1991 (`(225, BIRTH_1989_1991, DEATH_1989_1991)`) branches + import additions; only the docstring + `ValueError` message lines changed (2005-2013/2014-2020 return blocks byte-identical; `test_2005_2014_branches_unregressed` PASS).
- `natality/tests/test_linked_cohort_1983_1991_layout_smoke.py` — NEW; `DESIGN: tracks-current-state`; 13 tests (5 SHAPE structural + 2 Tier-0 synthetic-with-L3-negative + 6 Tier-1 real-data parametrized).
- `NEXT_STEPS.md` §15.D Task C8.18 DO step 3/4 — "layout-CSV" → "`field_specs.py` layout" fix-on-contact wording reconcile + a `[fix-on-contact]` provenance note pointing at the soft-flag-(ii) §11 model-clarification.
- `PRE_FLIGHT_LOG.md` — C8.18 DO step 3a PRE-FLIGHT entry (2026-05-17T22:30:00Z; before any `field_specs.py` mutation; L10-safe) incl. the state-on-disk 1989-1991 denominator-plus layout.
- `RECEIPTS/C8.18_step3a_2026-05-17T23-30-00Z.md` — this file.
- `DECISION_LOG.md` + `STATUS.md` — appended (DO step 3a choices + new dated section).

No canonical parquet / `harmonized_schema.csv` / validation-CSV touched. Raw zips are gitignored + unchanged (read-only).

### Five-phase trace

- **PRE-FLIGHT**: ✓ `PRE_FLIGHT_LOG.md` 2026-05-17T22:30:00Z (per-sub-step, before any `field_specs.py` mutation; L10-safe). 8/8 forward-looking HALTs re-verified; 11/11 gate parquet SHAs byte-exact; 2 within-task scope decisions surfaced + resolved before DO (Convention 3).
- **SMOKE**: ✓ Tier 0 (synthetic 91-byte + 225-byte recovery + L3 position-shift negative checks: PASS) → Tier 1 (real `LinkCO{83,85,88}USden.dat` 91-byte + `LinkCO{89,90,91}USden.dat` 225-byte). Tier 1 **correctly FAILed first** on the pp18-19-summary-derived 1989-1991 layout (`DBIRWT@79-85` composite) → layout rebuilt byte-exact from the detail code-outline → re-run **13/13 PASS**.
- **DO**: ✓ additive `field_specs.py` + `parse_linked_year.py` + new SMOKE harness + §15.D fix-on-contact. Single commit (PRE-FLIGHT + DO + RECEIPT; C8.17 DO5a single-session-DO-step precedent).
- **VERIFY**: ✓ all 5 criteria (below).
- **RECEIPT**: ✓ this file.

### Verify results

- **VERIFY-A (zero canonical mutation)**: PASS — 11/11 gate parquet SHAs byte-exact vs PRE_FLIGHT_LOG 2026-05-17T20:00:00Z HALT 2 (natality `c8a740eb…a6237153`/`acb5c48a…28856974`; `.v28_baseline` `230efed2…33ccebac`/`e16ad532…77c41d44`; linked-derived `9b828a4d…5a08b777`; fetal-death `38e2cecb…99c5cf48`/`185c071e…a7968a09` via the `-build` tree; MM `adbec108…45dc1549`/`5c22308b…d39205d1`/`7c682668…edd61f5d`/`d98b4296…6a543261`). Threshold: 11/11 unchanged.
- **VERIFY-B (git scope)**: PASS — modified/new = `field_specs.py`, `parse_linked_year.py`, `test_linked_cohort_1983_1991_layout_smoke.py`, `NEXT_STEPS.md` §15.D, `PRE_FLIGHT_LOG.md`, + `RECEIPTS/`/`DECISION_LOG.md`/`STATUS.md`. NO `.parquet`/`harmonized_schema.csv`/validation-CSV. Threshold: scope ⊆ {scripts, test, plan, state}.
- **VERIFY-C (additive-only)**: PASS — `git diff field_specs.py` = zero deletion/modification lines (purely additive; 2005/2014 LINKED specs byte-untouched). `parse_linked_year.py` deletions = only the 1-line docstring (expanded) + the `ValueError` message (split + supported-years text); the 2005-2013/2014-2020 `return` blocks byte-identical; `test_2005_2014_branches_unregressed` asserts `_layout_for_linked_year(2008)[0]==900`/`(2018)[0]==1384`/`(1994)`→`ValueError` → PASS.
- **VERIFY-D (pytest count gate)**: PASS — canonical **4-dir** suite `fetal_death/ natality/ tests/ matched_multiples/` = **98 passed, 1 skipped, 1 xfailed in 315.76s**. Reconciles exactly: pre-change baseline 85P+1S+1XF preserved (zero regression — the 1 skipped + 1 xfailed unchanged; 85 still pass) **+** 13 new SMOKE all pass = 98P. Runtime 315.76s within the ~240-380s band. (A first 3-dir run returned 87P — investigated per §4.4 rather than rubber-stamped → found the canonical suite is 4-dir incl. `matched_multiples/`; the README's 3-dir "56 passed + 1 xfailed" line is stale → soft-flag (jj).)
- **VERIFY-E (SMOKE all PASS)**: PASS — 13/13. Encoding ASCII-not-EBCDIC confirmed (BIRYR == cohort year for all 300×6 sampled records under latin-1 decode); value-distribution sentinel-aware (CSEX∈{1,2} ~56/44; DPLURAL singleton-dominant; DBIRWT plausible grams excl. 9999 not-stated sentinel; GESTAT 37-41).

### Reproducibility

- `field_specs.py` constants are declarative; the SMOKE harness re-derives block size empirically + value-distribution-verifies → deterministic, re-running yields identical results.
- Zero canonical parquet rebuild (DO step 3a is layout substrate only). H10 gate: 11/11 SHAs byte-exact.

### Git

- Pre-DO rollback anchor: `C8.18-pre-do` @ `6632a15` (unchanged; the C8.18 DO rollback anchor).
- This commit: intermediate C8.18 DO step 3a — **NO tag** (Convention 5; `C8.18-complete` is the final-sub-step-only tag).

### STATUS.md updated

- New section dated 2026-05-17T23:30:00Z at the top (newest-first).

### Self-check (§10) — what could I have gotten wrong that VERIFY wouldn't catch?

1. **Composite-block sub-positions for 1989-1991** (`DELMTH@92-99`, `MEDRISK@101-117`, `OTHERRSK@118-128`, `OBSTETRC@130-136`, `LABOR@138-153`, `ABNORMNB@155-163`, `CONGENIT@165-186`, `FLRES@187-206`) are span-level from the detail code-outline; their internal sub-fields were NOT value-distribution-verified (only the anchor scalars around them: `DBIRWT@79-82`, `DPLURAL@87`, `OMAPS@88-89`, `DOW@208`, `CRACE@209-210`). A composite span could be off by a byte and the anchor checks would not catch it. *Mitigation*: non-overlap + in-bounds SHAPE checks pass, and the value-verified anchors tightly bracket the composites (everything before 92 and from 208 verifies). *Recommendation*: DO step 5 value-distribution-verify each composite sub-field against the detail code-outline before harmonizing them. Logged DECISION_LOG 2026-05-17T23:30:00Z residual risk.
2. **Non-anchor 1983-1988 fields** (recodes, geographic, father/prenatal) transcribed from the PRE_FLIGHT_LOG state-on-disk layout but not individually value-verified (BIRYR/MATCHS/CSEX/DPLURAL/DBIRWT value-verified on real 1983/85/88 data). Same DO-step-5 verify recommendation.
3. **CSEX@78 vs CSEXIMP@77 (1989-1991)**: resolved from detail p34 (`CSEXIMP@77`, `CSEX@78`); value-verified `@78` ∈ {1,2} with ~56% male (textbook ~1.05 sex ratio). Low residual risk (an imputed flag @77 would not show a 1/2 sex ratio).
4. **Encoding sampled only first ~300 records/file**: a mid-file encoding shift (extremely unlikely for a fixed-width NCHS public-use `.dat`) would not be caught here; DO step 5 full parse surfaces any anomaly via row-count + value-distribution.
5. **`_layout_for_linked_year(1983-1988)` returns `death_fields=[]`** — correct for the births-only denominator file, but the actual two-file numerator left-join (1983-1988) is DO step 5; a forward dependency, not a DO step 3a defect.

Each logged in `DECISION_LOG.md` 2026-05-17T23:30:00Z as a residual risk; recommended checks are DO-step-5 PRE-FLIGHT items (not added to VERIFY without human sign-off per §10).

### Forward-looking HALTs for next session (C8.18 DO step 3b)

1. `C8.18-pre-do`@`6632a15`; `C8.18-complete` NOT present (final-sub-step-only). `C8.17-complete` present. HEAD = this DO step 3a commit (after `a845af5`).
2. **11 gate parquet SHAs unchanged** (the same 11; fetal-death via the `~/Desktop/fetal-death-harmonization-build/` tree, soft-flag (hh)). Linked-derived `9b828a4d…` changes only at the later C8.18 re-harmonize DO step (3a/3b are layout-substrate only).
3. **Canonical pytest baseline is now `98 passed, 1 skipped, 1 xfailed`** (was 85P+1S+1XF; +13 from the new `test_linked_cohort_1983_1991_layout_smoke.py`, `DESIGN: tracks-current-state`). The canonical suite is **4 dirs**: `fetal_death/tests/ natality/tests/ tests/ matched_multiples/tests/`. The README's 3-dir "expected: 56 passed + 1 xfailed" line + `## Reproducibility` 3-dir invocation are **STALE** → soft-flag (jj) (pre-existing-L11-class docs; out-of-DO-step-3a-scope to fix).
4. `field_specs.py` `LINKED_BIRTH_1983_1988_FIELDS`(91)/`LINKED_BIRTH_1989_1991_FIELDS`+`LINKED_DEATH_1989_1991_FIELDS`(225)/reclen constants are byte-exact + real-data value-verified. **DO step 3b = the NUMERATOR layouts** (1983-1988 `LinkCO{YY}USnum.dat` 500-byte + 1989-1991 `LinkCO{YY}USnum.dat` 535-byte; ICD-9 underlying cause + 61-cause list + entity/record-axis multiple cause 261-504; + the NCHS certificate match key for the 1983-1988 two-file den/num join). DO step 3b must NOT re-derive the denominator (state-on-disk in `field_specs.py` + this receipt).
5. **Encoding = ASCII (verified, NOT EBCDIC)** — the 2005+ parser's `_slice_field` `.decode("latin-1")` works for pre-1990 public-use `.dat`; DO step 5 parser reuses it.
6. **Structural model (refined this step)**: 1983-1988 = pure two-file (91-byte births-only den + 500-byte num) → DO step 5 needs the num/den left-join on the cert match key; 1989-1991 = 225-byte Denominator-PLUS single-file (2005+-analogous) + 535-byte num. `_find_denomplus_member` (matches `"DENOM"`) still won't match `LinkCO{YY}USden.dat` (`"DEN"`) — a DO step 5 parser fix (per the DO step 3 PRE_FLIGHT_LOG).
7. §15.D DO step 3/4 "layout-CSV"→"`field_specs.py`" fix-on-contact APPLIED this commit; the broader §15.D model-clarification (3a/3b split; 1983-1988 pure-two-file vs 1989-1991 denominator-plus; num/den→denominator-plus-equivalent via match-key join) remains **soft-flag (ii)** proposed-not-applied for §11 human-merge (queued with the soft-flag-(w) batch).
8. **soft-flag (gg) refined**: 1989-1991 denominator-plus carries `UCOD@219-222` (ICD-9 underlying cause) + `UCODR61@223-225` (61 selected causes of infant death recode); the FULL entity/record-axis multiple-cause detail is the 535-byte numerator 261-504 (DO step 3b/5). `cause_recode_130` per-era availability + the exact ICD-9-era harmonized cause-column shape stay DO step 5/6 PRE-FLIGHT.
9. pytest runtime ~316s (in the ~240-380s band; the band is the gate, not the absolute number).

### Notes for next session

- **C8.18 DO step 3a CLOSED.** Denominator/births layouts authored byte-exact + real-data value-verified for both pre-1990 cohort eras. Next per the (soft-flag-(ii)-pending) plan: **DO step 3b — numerator (birth+death+ICD-9) layouts + the certificate match-key** for 1983-1988 (500-byte `*num*`) + 1989-1991 (535-byte `*num*`).
- The §15.D model-clarification `[plan-update]` (soft-flag (ii)) is owed to the human (§11; queued with the soft-flag-(w) batch); the on-disk PRE_FLIGHT_LOG/DECISION_LOG/RECEIPT are authoritative in the interim.
- SMOKE Tier 1's L13-extension catch (pp18-19 element summary ≠ byte-level layout) is the governing precedent for DO step 3b: reconstruct the numerator layout from the **detail code-outline** (pp20-47ish), value-distribution-verify, do not trust the pp18-19 element-span summary.
- `/tmp` scratch + the two background pytest output files are OS-cleanable + reproducible.
