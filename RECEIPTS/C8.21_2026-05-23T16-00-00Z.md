# Receipt: C8.21
## 2026-05-23T16:00:00Z

### What was done
Shipped `STATA_SAS_QUICKSTART.md` — a single cross-product pointer at the
monorepo root explaining how Stata and SAS users load the HVS Parquet data.
The version-proof primary path is the Parquet→CSV bridge (the already-shipped
`views.sql` + DuckDB `COPY`, verified against official docs, or
`quickstart.py` + pandas) then Stata `import delimited` / SAS
`PROC IMPORT … DBMS=CSV` (verified base syntax); native options (Stata
StataNow `import parquet`; Stata community `parquet` package, Unix-only; SAS
Viya ORC/Parquet `LIBNAME` engine) are documented secondarily with their exact
build requirements + verified official anchors. The §15.D premise "Stata 17+
supports `import parquet`" was found false at L9 PRE-FLIGHT and corrected (not
propagated). Wired into `docs/JOINT_USE_GUIDE.md` (section renamed + new
`### Stata / SAS`), `PROJECT_STRUCTURE.md` (which-file matrix + stale `§174`
anchor fixed → section name, L11), `docs/WORKED_EXAMPLE_FAQ.md` (new
language-access Q), `README.md` (new loading pointer). Docs-only.

### Inputs consumed
- `NEXT_STEPS.md` §15.D Task C8.21 (deliverable spec + Halt-flags L6/L9)
- L9-verified official docs: DuckDB CSV `COPY` (duckdb.org/docs/current/…/csv_export.html — WebFetch-confirmed exact syntax); Stata `[D] import` + `[D] import parquet` StataNow (stata.com manuals/statanow) + community `parquet` (github.com/mcaceresb/stata-parquet); SAS PROC IMPORT Statement + SAS Viya ORC/Parquet LIBNAME engines (documentation.sas.com)
- `views.sql`, `fetal_death/quickstart.py`/`.R`, `natality/quickstart.R` (sibling patterns); the 4 cross-link host docs

### Outputs produced
- `STATA_SAS_QUICKSTART.md` (NEW, monorepo root) — pointer; every code block verified-published syntax
- `docs/JOINT_USE_GUIDE.md` — "## Cross-language access" renamed `R, DuckDB, Stata, SAS` + new `### Stata / SAS` subsection
- `PROJECT_STRUCTURE.md` — which-file matrix: split R/Python vs Stata/SAS rows; Stata/SAS → `STATA_SAS_QUICKSTART.md`; stale `§174` → "Cross-language access" (L11)
- `docs/WORKED_EXAMPLE_FAQ.md` — new "## Loading the data in R, Stata, SAS, or SQL" Q (discoverability: previously had none)
- `README.md` — new "## Loading the data (Python, R, SQL, Stata, SAS)" pointer
- `PRE_FLIGHT_LOG.md`/`DECISION_LOG.md` 2026-05-23T16:00:00Z; this receipt; `STATUS.md` 2026-05-23T16:00:00Z
- **Zero canonical-state mutation** (no parquet/schema/validation-target/metadata-CSV/test/script). 3 gate SHAs byte-exact.

### Five-phase trace
- PRE-FLIGHT: ✓ (`PRE_FLIGHT_LOG.md` 2026-05-23T16:00:00Z — PROCEED; L9 anchors verified; Convention-3 snapshot; back-fill-free, precedes the first DO commit `e052dba`)
- SMOKE: ✓ (Tier 0 for a docs pointer per §15.D: file renders — markdown well-formed, 8 fence lines balanced; every code block = verified-published syntax; false "Stata 17+ supports" premise absent; every internal cross-link resolves)
- DO: ✓ (commit `e052dba` [pre-do: PRE-FLIGHT+DECISION_LOG] .. the C8.21-complete commit [new pointer + 4 cross-link edits + receipt + STATUS])
- VERIFY: ✓ (criteria below)
- RECEIPT: ✓ (this file)

### Verify results
- §15.D-1 pointer file exists at canonical path: PASS (`STATA_SAS_QUICKSTART.md`, repo root)
- §15.D-2 cross-link resolves: PASS — `WORKED_EXAMPLE_FAQ.md` + `JOINT_USE_GUIDE.md` + `PROJECT_STRUCTURE.md` + `README.md` all reference it; all internal markdown links in all 5 docs resolve (programmatic check, 0 broken)
- §15.D-3 no test-surface regression: PASS — no test reads the 5 changed docs (the 2 `git grep` hits in `test_canonical_filter_invariants.py`/`test_cross_product_join_parity.py` are **comment/docstring citations of the unchanged "Canonical analytic filters" section**, not content parsing); both re-run post-edit = **25 passed**; fast sanity 27 passed; docs-only ⇒ the heavy parquet suites cannot be affected (3 gate SHAs byte-exact; STATUS baseline 355P+1S+1XF intact)
- Scope (VERIFY-A): PASS — `git diff --name-only C8.21-pre-do` = the 4 cross-link docs + the new file only; no canonical-state files; `.claude/`+`AUDITS/` untracked, deliberately excluded
- L6/L9: PASS — every cited command is verified-published; every external URL is a WebFetch/WebSearch-returned official-domain anchor; the false §15.D premise is not shipped (`git grep "Stata 17+ supports"` → none)

### Reproducibility
- Pure docs; deterministic by construction. `git revert` the C8.21 DO commit fully removes the pointer + 4 cross-link edits; zero canonical artifacts touched ⇒ data envelope + reproducibility unaffected.

### Cross-product re-probe (if applicable)
- C8.22 / Phase D: unaffected — zero canonical-state mutation; settled envelope unchanged (3 gate SHAs byte-exact).

### Git
- Pre-DO tag: `C8.21-pre-do`, commit=`e052dba`
- Post-RECEIPT tag: `C8.21-complete`, commit=<the DO/receipt commit>

### STATUS.md updated
- New section dated 2026-05-23T16:00:00Z at the top marking C8.21 complete; next task C8.22

### Self-check
What could I have gotten wrong that VERIFY wouldn't catch?
1. **A cited official URL could 404 later or not show the exact command body.** *Mitigation:* every URL is a WebSearch/WebFetch-returned official-domain link (the DuckDB `COPY` body was WebFetch-confirmed verbatim); the load-bearing commands (`COPY … (HEADER, DELIMITER ',')`, `import delimited`, `PROC IMPORT DBMS=CSV`) are decades-stable base syntax independent of any one URL; natives are framed "confirm your build," not asserted. Residual: low.
2. **Parquet filenames assume the unpacked Zenodo flat layout.** *Mitigation:* the file states this explicitly and points to `views.sql`, which documents the identical assumption; consistent with the existing R/DuckDB docs. Residual: low.
3. **Stata `varnames(1)` / SAS `getnames=yes` option spelling.** *Mitigation:* both are long-stable documented options of `import delimited` / `PROC IMPORT DBMS=CSV`; cited to the official manual entries. Residual: very low.
(See §8 — class L6/L9; both Halt-flags actively satisfied. No new mistake class ⇒ no LESSONS entry.)

### Forward-looking HALTs for next session
- Forward-looking HALT 1: `C8.21-pre-do`@`e052dba` + `C8.21-complete` set ⇒ C8.21 CLOSED. Next = **C8.22 — Pre-computed cross-tab CSVs (C.5; ~1 session)** per KICKOFF Tier 3+5 + §15.D Task C8.22 (then Phase D).
- Forward-looking HALT 2: 3 gate parquet SHAs unchanged (`185c071ec76a`/`acb5c48a9abf`/`f630d8cf20db`) — settled pre-Zenodo envelope intact; C8.21 docs-only.
- Forward-looking HALT 3: pytest baseline unchanged at **355P+1S+1XF** (C8.21 added no test surface); memory-safe per-file split (soft-flag (kk)).
- Forward-looking HALT 4: C8.22 (cross-tab CSVs) is the first Tier-3+5 task that *generates* artifacts (CSV files) since C8.20-auditfix — it must apply the canonical filters (F1/F2), conserve row counts (H6), and auto-derive every cell (L6), per §15.D Task C8.22 Halt-flags.
- Forward-looking HALT 5: owed §11 human-merge items + Phase-D deferrals carry **unchanged** (C8.21 added zero canonical state, no LESSONS by design). The §15.D C8.21 "Stata 17+ supports import parquet" premise was corrected in-deliverable (DECISION_LOG 16:00:00Z) — a §11 plan-update to that one §15.D sentence is *optional* book-keeping (the deliverable/scope is unchanged); flagged for the next human plan-merge pass, not blocking.

### Notes for next session
- C8.21 CLOSED. Stata/SAS users now have a verified, version-honest entry point; R/Python/SQL/Stata/SAS access is discoverable from README + PROJECT_STRUCTURE + JOINT_USE_GUIDE + WORKED_EXAMPLE_FAQ.
- Next: **C8.22 (pre-computed cross-tab CSVs)**, then Phase D (D.1 Task 9 → D.2 Task 10 Zenodo → D.3 public-repo sync → D.4 manuscript). Tier 3+5 after C8.22 = 7/7.
- §2/§9-#2/L6/L9 honored (verified syntax before authoring; no invented commands/URLs; the false plan premise corrected, not propagated). §7-#17 respected (only the §15.D-named DO surface + the C8.17/C8.18-step7 honest cross-link-propagation precedent).
