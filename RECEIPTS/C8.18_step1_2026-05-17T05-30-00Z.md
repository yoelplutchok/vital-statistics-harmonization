# Receipt: C8.18 DO step 1 — cohort-vs-period publishing-design decision
## 2026-05-17T05:30:00Z

### What was done

Resolved the C8.18 cohort-vs-period publishing-design decision (the §15.D-prescribed DO-step-1 action; methodology-paper-level per Sub-Q42). The Convention-3 PRE-FLIGHT Field-value snapshot surfaced a **material §7-class substrate divergence**: the §15.D + EXPLORATION_REPORT §A.3 + KICKOFF task-plan premise *"HVS-linked from 2005 ships period-format only"* is **inverted** — the existing 2005-2023 linked product is the **cohort-linked** series (every validation target cites NCHS *cohort* linked-file guides; `NCHS_SOURCE_MANIFEST` §3 = "Linked-cohort raw zips"; parsers read `LinkCO{yy}US.zip`; `GETTING_STARTED:171` "follows each birth cohort"). The divergence was flagged in the C8.18 PRE_FLIGHT_LOG entry **before any DO mutation** (L10-safe) and resolved via **AskUserQuestion 2026-05-17 → Option A "Cohort-only backward extension"**: C8.18 will parse cohort-linked 1983-1991 + cohort-linked 1995-2004 and append to the existing cohort 2005-2023 series → a **41-year cohort-linked series (1983-2023, permanent 1992-1994 gap)**, linked v3 → v4; period-linked 1995-2004 is **dropped** from C8.18 scope (separate deferrable future product). C8.18 source scope corrected 29 → **19 zips (~1.85 GB)**. A coupled LLM decision settled ICD-9/10 handling at the §A.3 risk-(c) on-record default (null `underlying_cause_icd10` for 1983-1998; ICD-9 preserved within-era; 9→10 crosswalk deferred to a post-C8.18 task). A §15.D C8.18 scope-correction `[plan-update]` sub-entry is **proposed, not applied** (human-merged per §11; Convention 5 → separate commit). Decision-only: zero canonical-state mutation.

### Inputs consumed

- `RECEIPTS/C8.17_step7_2026-05-17T04-00-00Z.md` — 7 forward-looking HALTs (entry cheap-check; 7/7 verified).
- `NEXT_STEPS.md` §15.D Task C8.18 + Sub-Q42; `EXPLORATION_REPORT.md` §A.3 (lines 105-145); `KICKOFF.md` Tier-3+5 line.
- Substrate (read-only): `docs/NCHS_SOURCE_MANIFEST.md` §3; `natality/metadata/external_validation_targets_v3_linked.csv`; `natality/scripts/01_import/parse_linked_year.py` + `parse_linked_cohort_year.py`; `natality/scripts/03_harmonize/harmonize_linked_v3.py`; `natality/docs/{GETTING_STARTED,VALIDATION,COMPARABILITY}.md`; `natality/metadata/harmonized_schema.csv` (linked rows 86-95).
- 9 gate parquets — SHA-verified byte-exact at PRE-FLIGHT and re-verified at VERIFY (see below).
- AskUserQuestion 2026-05-17 (user-selected "Cohort-only extension (Recommended)").

### Outputs produced

Decision-only; no binary artifacts; no canonical artifact. Git-tracked state files MODIFIED:

- `PRE_FLIGHT_LOG.md` — C8.18 umbrella + DO-step-1 PRE-FLIGHT entry (prepended, L10-safe, pre-DO; 2026-05-17T05:30:00Z).
- `DECISION_LOG.md` — the C8.18 DO step 1 decision artifact (prepended; 2026-05-17T05:30:00Z).
- `STATUS.md` — new dated section (prepended).
- `RECEIPTS/C8.18_step1_2026-05-17T05-30-00Z.md` — this file (NEW).

### Five-phase trace

- **PRE-FLIGHT**: ✓ `PRE_FLIGHT_LOG.md` C8.18 entry 2026-05-17T05:30:00Z. 7/7 forward-looking HALTs from RECEIPTS/C8.17_step7 verified; all 9 gate parquet SHAs computed on-disk byte-exact (decision-only precondition holds); Convention-3 Field-value snapshot surfaced the inverted-premise §7-#12/#13 divergence → flagged before any DO mutation. pytest deferred (provably no test-surface mutation at a decision-only step; C8.17 DO-3/4/5b/6/7 precedent).
- **SMOKE**: ✓ Tier 0 = the corrected design-decision reasoning (3 options A/B/C re-derived under "existing product = cohort"; §15.D options (b)/(c) shown moot). Higher tiers belong to DO steps 3-7.
- **DO**: ✓ AskUserQuestion 2026-05-17 → Option A; decision + coupled ICD-9/10 default + proposed (not applied) §15.D `[plan-update]` recorded in DECISION_LOG. `C8.18-pre-do` tagged @ `6632a15` before the DO mutation.
- **VERIFY**: ✓ all criteria (below).
- **RECEIPT**: ✓ (this file) + DECISION_LOG + STATUS appended. No tag at this intermediate DO step (Convention 5 / C8.17 precedent; `C8.18-complete` only at the final C8.18 sub-step).

### Verify results

- **A — decision recorded + internally consistent**: PASS. DECISION_LOG 2026-05-17T05:30:00Z states cohort-only / 19 zips / period-1995-2004-dropped / 41-yr 1983-2023 / 1992-1994 gap / v3→v4 / ICD default-null / `[plan-update]` proposed-not-applied — no internal contradiction; the AskUserQuestion answer (Option A) matches the recorded choice. threshold: decision artifact exists and matches the user selection; value: exact match.
- **B — zero canonical-state mutation (H10; decision-only)**: PASS. `git status --porcelain` = exactly `M DECISION_LOG.md` + `M PRE_FLIGHT_LOG.md` (plus the new STATUS section + this receipt); zero `.py`/`.csv`/schema/test/script/parquet. Gate parquet SHAs re-verified byte-exact vs PRE-FLIGHT: canonical natality `c8a740eb…`/`acb5c48a…`; `.v28_baseline` `230efed2…`/`e16ad532…`; linked-derived `9b828a4d…`; fetal-death `38e2cecb…`/`185c071e…`; MM harmonized `adbec108…` (+ MM 1995-1997/1995-2000/2016-2020 untouched — git proves zero parquet writes). 9/9 unchanged.
- **C — plan-update proposed, NOT applied (§11)**: PASS. `git status` shows `NEXT_STEPS.md` + `KICKOFF.md` UNMODIFIED. The §15.D C8.18 scope-correction is documented as owed (soft-flag (ff)) for a separate human-merged `[plan-update]` commit per Convention 5; not silently actioned.
- **D — §7-class divergence handled per protocol**: PASS. The inverted-premise divergence was classified (§7-#12 + §7-#13), documented in PRE_FLIGHT_LOG before any DO mutation, and resolved via the exact §15.D DO-step-1 + Sub-Q42 AskUserQuestion mechanism — not silently worked around (KICKOFF convention-conflict rule satisfied).

### Reproducibility

Deterministic — decision-only state-file appends; no build step. The corrected premise is empirically re-checkable by re-reading the five substrate sources (all cite *cohort*, none *period*). Canonical artifacts unchanged (Criterion B) → the C8.17 reproducibility guarantee carries forward unaltered.

### Cross-product re-probe

9 gate parquet SHAs byte-exact (Criterion B) → natality_v2, fetal-death, linked-derived, matched-multiples provably untouched. No downstream task consumes DO step 1 output beyond the next session's reads. The linked-derived `9b828a4d…` will change only at the later C8.18 re-harmonize DO step (intended; not this step).

### Git

- Pre-DO tag: `C8.18-pre-do` @ `6632a15` (set at C8.18 start per §13 step 4; clean rollback point; not re-tagged at intermediate DO steps per Convention 5).
- Post-RECEIPT tag: **none** — DO step 1 is an intermediate C8.18 sub-step; `C8.18-complete` is set only at the final C8.18 sub-step (Convention 5 / C8.17 DO-step intermediate precedent).
- Commit: ~5-line summary per Convention 5; full narrative here.

### STATUS.md updated

New section dated 2026-05-17T05:30:00Z: C8.17 COMPLETE; C8.18 DO step 1 closed (cohort-only decision); next = C8.18 DO step 2 (download 19 cohort zips + cohort user-guide PDFs; SHA-verify) — gated on the human merging the §15.D C8.18 scope-correction `[plan-update]`.

### Self-check (§10) — what could I have gotten wrong that VERIFY wouldn't catch?

1. **The cohort-vs-period substrate read could be subtly wrong** — e.g., NCHS's 2016+ "period-cohort" combined file might mean the post-2016 HVS slice is actually period-flavored despite the cohort-labeled parsers. Mitigation: five independent substrate sources (manifest title, *every* validation-target guide citation, both parser docstrings, GETTING_STARTED, VALIDATION/COMPARABILITY weight-guidance) all converge on cohort; `parse_linked_cohort_year.py` explicitly reconstructs a cohort file from the period-cohort format. Residual risk: low; the convergence is total. Logged as residual in DECISION_LOG.
2. **The 19-zip count / filename convention could be off** (e.g., a `LinkCO9xUS` vs `LinkCO9x` boundary year, or 1983/1984 not actually public-use). VERIFY here does not download anything (DO step 1 is decision-only) — this is explicitly a DO step 2 PRE-FLIGHT obligation (HTTP-200 + SHA + L1-extension sibling-probe of all 19 URLs). Surfaced as a forward-looking HALT, not closed here. Plausible; bounded to DO step 2.
3. **ICD-9-era harmonized column shape** — defaulting `underlying_cause_icd10` null pre-1999 is safe, but the *positive* ICD-9 representation (own column? a `cause_icd_revision` flag? `cause_recode_130` ICD-9-era availability?) is not yet decided. Deliberately deferred to DO step 5/6 PRE-FLIGHT (soft-flag (gg)); recorded so it is not silently assumed.
4. **The §15.D `[plan-update]` not being merged before DO step 2** would leave NEXT_STEPS.md carrying the inverted premise (L11) while the on-disk decision says cohort-only. Mitigated by soft-flag (ff) + a forward-looking HALT making the merge a DO-step-2-entry precondition; the on-disk decision record is authoritative in the interim (state-on-disk principle).
5. **Dropping period-linked 1995-2004 could be regretted** if a downstream user specifically needs period-linked. Documented as a clean, well-scoped *future* candidate (separate task + `[plan-update]`), not a silent loss; the user explicitly chose Option A over Option B with that trade-off visible in the question.

Each residual risk logged in DECISION_LOG 2026-05-17T05:30:00Z.

### Forward-looking HALTs for next session (C8.18 DO step 2 PRE-FLIGHT)

1. `C8.18-pre-do` @ `6632a15` present; `C8.18-complete` NOT present (set only at the final C8.18 sub-step). C8.17 fully closed (`C8.17-complete`).
2. **9 gate parquet SHAs unchanged at DO step 2 entry** — canonical natality `c8a740eb…`/`acb5c48a…`; `.v28_baseline` `230efed2…`/`e16ad532…`; 7 non-natality-v2 (`38e2cecb`/`185c071e`/`9b828a4d`/`adbec108`/`5c22308b`/`7c682668`/`d98b4296`). The linked-derived `9b828a4d…` changes only at the later C8.18 re-harmonize DO step — **NOT** at DO step 2 (DO step 2 is download + SHA-verify only).
3. **Scope is 19 cohort zips, NOT 29** — 9 × `LinkCO{83..91}.zip` (no `US` suffix pre-1995) + 10 × `LinkCO{95..04}US.zip`. Period-linked `LinkPE*US.zip` 1995-2004 is OUT of C8.18 scope. DO step 2 PRE-FLIGHT probes exactly these 19 URLs (HTTP-200 + size + SHA) + their cohort user-guide PDFs via L1-extension sibling-derivation from the on-disk `LinkCO{05..15}Guide.pdf` naming + L12-extension text-layer probe.
4. **The §15.D Task C8.18 scope-correction `[plan-update]` is OWED (soft-flag (ff))** — premise period→cohort, 29→19 zips, DO-step-list re-sequence (period steps collapse), effort/VERIFY narrowing. It must be merged by the human (separate `[plan-update]` commit per §11/Convention 5) before or alongside DO step 2 so the plan and the on-disk decision agree. Until merged, this receipt + the DECISION_LOG + the C8.18 PRE_FLIGHT_LOG entry are authoritative for corrected scope.
5. ICD-9/10 = default-null + revision-tagged, crosswalk deferred (settled). The exact ICD-9-era harmonized cause column shape + `cause_recode_130` per-era availability for cohort 1983-1998 = DO step 5/6 PRE-FLIGHT question (soft-flag (gg); H7 fetal-death sibling-parity).
6. `tests/test_row_count_conservation.py` NATALITY pins (`201_161_456`/`range(1968,2025)`) must NOT be perturbed by C8.18; the LINKED pins (`74_943_824`/`range(2005,2024)`) get the Convention-2/L17 same-commit re-pin at the later C8.18 re-harmonize DO step (NOT DO step 2). B.12 latest = `v2_2026-05-16T08-00-00Z_columns.csv`; C8.18 re-snaps at re-harmonize. pytest baseline 85P+1S+1XF (~240-380s; count is the gate).
7. Tier 3+5 ≈ 2.5/7 (C8.16+C8.17 done; C8.18 in progress, DO step 1 of ~6-7 post-correction); cumulative Phase C ≈ 26 of 51-71 (effort-ceiling cap 86 intact).

### Notes for next session

- **C8.18 DO step 1 is closed (decision-only).** The cohort-vs-period question is settled: **cohort-only backward extension, 1983-2023 41-yr series, 19 zips, linked v3→v4**. Next per §15.D (re-sequenced): **DO step 2 — download the 19 cohort source zips + cohort user-guide PDFs, SHA-verify**.
- **Action owed to the human (not LLM-actionable):** merge the §15.D Task C8.18 scope-correction `[plan-update]` (soft-flag (ff)) — propose it alongside the soft-flag-(w) / §15.D-§1358 / DO-step-6/7-boundary next-`[plan-update]` batch. The corrected scope is fully specified in DECISION_LOG 2026-05-17T05:30:00Z.
- The §15.D 8-step DO list collapses under cohort-only: period-specific work in §15.D DO steps 4/5 disappears; expect ~6-7 DO steps total. The exact re-sequenced step list is part of the owed `[plan-update]`.
