# Receipt: C8.18 DO step 4c — cohort 2004 `field_specs.py` layout authoring (900-byte denominator-plus + 1259-byte numerator; 2003-revision transition continued; ICD-10 throughout) + SMOKE Tier-0/1 value-distribution verify
## 2026-05-18T23:00:00Z

### What was done

Executed C8.18 DO step 4c under the full five-phase discipline in one session (PRE-FLIGHT + SMOKE + DO + VERIFY + RECEIPT; the C8.18 DO step 3a/3b/4a/4b single-session-DO-step precedent). Authored the **byte-exact additive** cohort 2004 cohort-linked layouts in `natality/scripts/01_import/field_specs.py`, reconstructed FRESH from the `LinkCO04Guide.pdf` **DETAIL "Position/Len/Field" code-outline** (pp21-60; the L13-extension governing precedent — the pp18-19 "Linked 2004 Cohort Selected Data Elements and Locations" element-span SUMMARY is composite + NOT trusted, used only as an independent cross-check):

- `LINKED_BIRTH_2004_FIELDS` — 206 entries, locs 1-867 (REVISION@7 + IDNUMBER@10-14 + birth cert; identical in the den-plus AND the numerator's natality section per the shared LinkCO04Guide pp21-57 DETAIL). 1-575 structurally == `LINKED_BIRTH_2003` (value-verified byte-exact on real 2004 data); 576-867 reconstructed from the 2004 DETAIL (the 2004-specific 2003-rev F_* edit-flag region + relocated MRACE1E@800-823 / FRACE1E@835-858).
- `LINKED_DEATH_2004_FIELDS` — 16 entries, locs 868-900 (the den-plus death-derived "plus": FLGND@868 + AGED..UCODR130 + RECWT@893-900; == the `LINKED_DEATH_2005_2013` model, value-verified on real 2004 data).
- `LINKED_NUM_DEATH_2004_FIELDS` — 12 entries, locs 901-1259 (numerator-only mortality: FILLER + EANUM@903-904 + ENTITY@905-1044 (20×7) + RANUM@1047-1048 + RECORD@1049-1148 (20×5) + HOSPD/WEEKDAYD + DTHYR@1188-1191 + DOD_MM@1258-1259).
- `LINKED_DENOMPLUS_RECLEN_2004 = 900`, `LINKED_NUM_RECLEN_2004 = 1259`.

Added **additive** single-year-2004 `_layout_for_linked_year` (900-byte den-plus) + `_numerator_layout_for_linked_year` (1259-byte numerator = birth + den-plus-"plus" + numerator-mortality) branches to `parse_linked_year.py` (the existing 2005/2014 + 3a/3b/4a/4b return blocks + dispatcher behavior for the supported years byte-untouched — H10/HALT-13; verified by `test_prior_substep_dispatchers_unregressed` PASS). Added the NEW sibling `DESIGN: tracks-current-state` SMOKE harness `natality/tests/test_linked_cohort_2004_layout_smoke.py` (10 tests; Convention 1/2; kept separate from the shipped 3a/3b/4a/4b harnesses for sub-step isolation). **Zero canonical parquet / schema / validation-CSV mutation** (11/11 gate parquet SHAs byte-exact at VERIFY-A).

**Two material L13-extension findings, surfaced + resolved at the Convention-3 snapshot before any `field_specs.py` mutation:** (i) the prior `RECEIPTS/C8.18_step4b` forward-looking-note's *extrapolated* "2004 = 900/**1142**" numerator length was **FALSIFIED** — `LinkCO04Guide.pdf` p18 File Characteristics state Numerator US reclen = **1,259**, and exact zip-member-uncompressed-size arithmetic confirms byte-exact (den `4,118,956 × (900+2 CRLF) = 3,715,298,312` = `VS04LKBC.DUSDENOM`; num `27,763 × (1259+2) = 35,009,143` = `VS04LKBC.USNUMPUB`). (ii) the "2004 den-plus == `LINKED_BIRTH_2005_2013`" HYPOTHESIS was **FALSIFIED** — real `VS04LKBC.DUSDENOM` data shows the 2003-rev *cohort* layout (FILLER@1-6 + REVISION@7 + IDNUMBER@10-14); `LINKED_BIRTH_2003` value-verifies on real 2004 den-plus only for locs 1-575, then DIVERGES (2004 carries more 2003-rev F_* edit flags; `MRACE1E`@800 vs 2003's @683). Neither is a §7 halt — both were non-binding extrapolated/hypothesized `RECEIPTS/C8.18_step4b` forward-looking notes explicitly flagged for value-verification; the L13-extension "never assume same-model==same-layout; value-distribution-verify on real data" discipline is exactly the resolution mechanism (same class as the 4b 1259→1142 catch). Also resolved: `LinkCO04US.zip` = **DEFLATE** (stdlib `zipfile`; NOT DEFLATE64 like 2003 — no special tooling for 2004). Like DO step 3b/4a/4b, going straight to the DETAIL code-outline produced a layout that a **read-only builder probe + the new SMOKE Tier 1 verified on real data on the FIRST run** (no test loosened — §2 fail-closed; §9-#4; the builder value-verified every named anchor across den-plus + numerator, FAILS: NONE).

**One L17-class event resolved at VERIFY per the documented mechanism (NOT a §7 halt):** the full-suite VERIFY-D run surfaced 2 failures in the *2003 sibling* harness `test_linked_cohort_2003_layout_smoke.py` (`test_denominator_dispatcher_additive_branch` + `test_numerator_dispatcher_additive_branch`). That harness — `DESIGN: tracks-current-state` — had pinned `_layout_for_linked_year(2004)` / `_numerator_layout_for_linked_year(2004)` as raising `ValueError` (true at 4b authoring time, when 2004 was unconfigured). DO step 4c correctly configures 2004 → those `pytest.raises(ValueError)` on year 2004 became stale. This is a textbook §8 row L17 stale-pin. Per §4.2.1 + L17 specific-catch (c) + Convention 2 ("`tracks-current-state` → bundle the minimal Edit into the same commit as the canonical mutation"), the minimal Edit (`(1992, 1993, 1994, 2004)` → `(1992, 1993, 1994)` in both 2003-harness dispatcher tests + the explanatory comment) was bundled into this 4c commit. Re-run: the 2003 harness 10/10 PASS; full 4-dir suite **147 passed, 1 skipped, 1 xfailed** (zero regression; the L17 fix is the documented resolution, not a workaround). The 4a/3a/3b cohort harnesses were grep-scoped and carry no analogous stale 2004-raises pin.

### Inputs consumed

- `PRE_FLIGHT_LOG.md` 2026-05-18T23:00:00Z — per-sub-step PRE-FLIGHT (before any `field_specs.py` mutation; L10-safe). 9/9 forward-looking HALTs from `RECEIPTS/C8.18_step4b` re-verified; 11/11 gate parquet SHAs byte-exact; canonical pytest baseline `137 passed, 1 skipped, 1 xfailed in 222.97s` (4-dir) preserved; the FULL 2004 den-plus + numerator layout captured as state-on-disk substrate; the two 1142→1259 + LINKED_BIRTH_2005_2013-reuse material findings + four within-task scope decisions surfaced + resolved before DO (Convention 3).
- `PRE_FLIGHT_LOG.md` 2026-05-18T05:00:00Z (the C8.18 DO step 4 investigation-only checkpoint) + `DECISION_LOG.md` 2026-05-18T20:30:00Z + `RECEIPTS/C8.18_step4b_2026-05-18T20-30-00Z.md` (the 9 forward-looking HALTs).
- `~/Desktop/natality-harmonization/raw_docs/linked/LinkCO04Guide.pdf` (99 pp; 0 empty pages; text-extractable, no OCR — L12-extension confirmed this entry): p18 (file char: Den US 900 / Num US 1259 / Unl US 1259), pp21-57 (denominator-plus DETAIL locs 1-900; p57 divider "Here ends the denominator-plus file."), pp57-60 (numerator-only mortality DETAIL locs 901-1259). pp18-19 element-span SUMMARY = NOT trusted (L13-ext); used only as an independent cross-check (all listed positions matched the DETAIL + real data).
- `~/Desktop/natality-harmonization/raw_data/linked/LinkCO04US.zip` members `VS04LKBC.{DUSDENOM,USNUMPUB,USUNMPUB}` (DEFLATE, compress_type=8) — read-only, first ~500 records each via stdlib `zipfile` for the read-only builder probe + SMOKE Tier 1; SHA-anchored at C8.18 DO step 2.
- `natality/scripts/01_import/field_specs.py` + `parse_linked_year.py` (sibling substrate; the 3a/3b/4a/4b + 2005/2014 LINKED specs — `LINKED_BIRTH_2003` value-verified byte-exact on real 2004 data for 1-575; `LINKED_DEATH_2005_2013` death-"plus" model value-verified on real 2004 868-900 — pattern cross-checked, NOT re-derived).
- 11 gate parquets — SHA-verified byte-exact at PRE-FLIGHT entry cheap-check and re-verified at VERIFY-A.

### Outputs produced

Git-tracked (this commit):

- `natality/scripts/01_import/field_specs.py` — **purely additive** (`git diff --numstat` = 324 insertions / **0 deletions**; existing LINKED 2005/2014 + 3a/3b/4a/4b tuple lists/constants byte-untouched): the 2004 section-header source block + `LINKED_BIRTH_2004_FIELDS` (206, 1-867) + `LINKED_DEATH_2004_FIELDS` (16, 868-900) + `LINKED_DENOMPLUS_RECLEN_2004 = 900` + `LINKED_NUM_DEATH_2004_FIELDS` (12, 901-1259) + `LINKED_NUM_RECLEN_2004 = 1259`.
- `natality/scripts/01_import/parse_linked_year.py` — additive branches + minimal accuracy line-replacements (`git diff --numstat` = 53 insertions / **6 deletions**; the 6 deletions are docstring-paragraph + comment + 2 `ValueError` "Supported:" string replacements — accuracy, not behavior change: 5 new imports + the `_layout_for_linked_year` single-year-2004 branch + the `_numerator_layout_for_linked_year` single-year-2004 branch; the 2005/2014 + 3a/3b/4a/4b **return blocks + dispatcher behavior for supported years are byte-untouched**, asserted by `test_prior_substep_dispatchers_unregressed` PASS).
- `natality/tests/test_linked_cohort_2004_layout_smoke.py` — NEW; `DESIGN: tracks-current-state`; 10 tests (6 SHAPE structural [reclen constants, den-plus + numerator contiguity/tiling/in-bounds/non-overlap, both additive dispatcher branches incl. 1992-1994 gap → ValueError, prior 3a/3b/4a/4b/2005/2014 dispatchers unregressed] + 2 Tier-0 synthetic-with-L3-negative + 2 Tier-1 real-data den-plus + numerator/ICD-10 via stdlib `zipfile`).
- `natality/tests/test_linked_cohort_2003_layout_smoke.py` — **L17 bundled minimal Edit** (§4.2.1 / Convention 2; `tracks-current-state`): the two dispatcher-additive tests' stale `2004`-raises-ValueError pin removed (`(1992,1993,1994,2004)` → `(1992,1993,1994)`) + explanatory comments. No other change; the 2003 layout assertions are untouched.
- `PRE_FLIGHT_LOG.md` — C8.18 DO step 4c PRE-FLIGHT entry (2026-05-18T23:00:00Z; before any `field_specs.py` mutation; L10-safe) incl. the full state-on-disk layout description.
- `RECEIPTS/C8.18_step4c_2026-05-18T23-00-00Z.md` — this file.
- `DECISION_LOG.md` + `STATUS.md` — appended (DO step 4c 2-finding + 4-decision + 1-L17-event entry + new dated section).

No canonical parquet / `harmonized_schema.csv` / validation-CSV touched. Raw zips/guides are gitignored + out-of-tree (read-only, unchanged). §15.D NOT edited this step (the substrate-format wording was reconciled at the 3a commit; the broader model-clarification incl. the 4a/4b/4c decomposition + the 1142→1259 falsification + the LINKED_BIRTH_2005_2013-hypothesis falsification stays soft-flag (ii) §11 human-merge).

### Five-phase trace

- **PRE-FLIGHT**: ✓ `PRE_FLIGHT_LOG.md` 2026-05-18T23:00:00Z (per-sub-step, before any `field_specs.py` mutation; L10-safe). 9/9 forward-looking HALTs re-verified; 11/11 gate parquet SHAs byte-exact; canonical pytest 137P+1S+1XF/222.97s baseline preserved; the full layout reconstructed from the DETAIL code-outline + captured state-on-disk; the two 1142→1259 + LINKED_BIRTH_2005_2013-reuse material findings + 4 within-task scope decisions surfaced + resolved before DO (Convention 3).
- **SMOKE**: ✓ a read-only builder probe of the reconstructed layout on real DEFLATE `VS04LKBC.{DUSDENOM,USNUMPUB}` (the §2 fail-closed discipline) → Tier 0 (synthetic 900-byte den-plus + 1259-byte numerator recovery + L3 position-shift negative: PASS) → Tier 1 (real data via stdlib `zipfile`). **New harness 10/10 PASS first-run** (the layout was byte-exact first-run; no test loosened).
- **DO**: ✓ additive `field_specs.py` + `parse_linked_year.py` + new SMOKE harness + the L17 bundled minimal Edit to the 2003 sibling harness. Single commit (PRE-FLIGHT + DO + RECEIPT; the C8.18 DO 3a/3b/4a/4b precedent).
- **VERIFY**: ✓ all 5 criteria (below).
- **RECEIPT**: ✓ this file.

### Verify results

- **VERIFY-A (zero canonical mutation)**: PASS — 11/11 gate parquet SHAs byte-exact vs PRE-FLIGHT HALT 2 + the 4b receipt VERIFY-A: natality `c8a740eb…a6237153`/`acb5c48a…28856974`; `.v28_baseline` `230efed2…33ccebac`/`e16ad532…77c41d44`; linked-derived `9b828a4d…5a08b777`; fetal-death `38e2cecb…99c5cf48`(harm)/`185c071e…a7968a09`(deriv) (via the `~/Desktop/fetal-death-harmonization-build/` tree, soft-flag (hh)); MM `adbec108…45dc1549`/`5c22308b…d39205d1`/`7c682668…edd61f5d`/`d98b4296…6a543261`. Threshold: 11/11 unchanged.
- **VERIFY-B (git scope)**: PASS — modified/new = `field_specs.py`, `parse_linked_year.py`, `test_linked_cohort_2004_layout_smoke.py` (new), `test_linked_cohort_2003_layout_smoke.py` (L17 bundle), `PRE_FLIGHT_LOG.md`, + `RECEIPTS/`/`DECISION_LOG.md`/`STATUS.md`. NO `.parquet`/`harmonized_schema.csv`/validation-CSV. Threshold: scope ⊆ {scripts, test, plan, state}.
- **VERIFY-C (additive-only / no regression of existing behavior)**: PASS — `git diff --numstat` = `field_specs.py` **324/0** (purely additive; zero deletion/modification); `parse_linked_year.py` **53/6** (the 6 deletions are docstring-paragraph + comment + 2 `ValueError`-message line-replacements — accuracy improvements, NOT behavior change for supported years; the 2005/2014 + 3a/3b/4a/4b return blocks are byte-identical). The dispatcher behavior for all previously-supported years is unregressed: `test_prior_substep_dispatchers_unregressed` asserts `_layout_for_linked_year(1983)==91`/`(1989)==225`/`(1996)==230`/`(2002)==230`/`(2003)==783`/`(2008)==900`/`(2018)==1384` + `_numerator_layout_for_linked_year(1985)==500`/`(1990)==535`/`(2002)==535`/`(2003)==1142` — PASS (within the 147).
- **VERIFY-D (pytest count gate)**: PASS — canonical **4-dir** suite `fetal_death/ natality/ tests/ matched_multiples/` = **147 passed, 1 skipped, 1 xfailed in 335.95s**. Reconciles exactly: the 4b-close baseline 137P+1S+1XF preserved (zero regression; 1 skipped + 1 xfailed unchanged; 137 still pass — the L17 bundled Edit restored the 2 transiently-failing 2003-harness tests) **+** 10 new 2004 SMOKE all pass = 147P. Runtime within the ~240-380s advisory band (335.95s; the COUNT is the gate). [First VERIFY-D run before the L17 bundle was `2 failed, 145 passed, 1 skipped, 1 xfailed`; the 2 fails were the documented L17 stale-pin, resolved per §4.2.1/Convention 2 and re-verified clean.]
- **VERIFY-E (SMOKE all PASS)**: PASS — new 2004 harness 10/10. Tier 0 synthetic + L3 negative; Tier 1 real-data value-distribution (L13-extension, sentinel-aware) on real DEFLATE data via stdlib `zipfile`: den-plus reclen 900 + numerator reclen 1259 (block = data + CR/LF, byte-exact vs `LinkCO04Guide` p18 counts); DOB_YY == 2004 for all 300×2 records (encoding ASCII + alignment + block-size jointly); den-plus FLGND@868 ∈ {1,2} + REVISION@7 ∈ {S,A} (2003-rev transition continued) + BRTHWGT@467-470 4-digit grams + RECWT@893-900 "1.000000"; numerator FLGND@868 ≡ 1 (linked infant deaths only); AGED 000-366, EANUM/RANUM 00-20, DTHYR ∈ {2004,2005}, DOD_MM 01-12; UCOD@884-887 ≥80% ICD-10 alpha (2004 cohort = all ICD-10).

### Reproducibility

- `field_specs.py` constants are declarative; re-derivable from the `LinkCO04Guide.pdf` DETAIL "Position/Len" column via the documented extractor (state-on-disk `/tmp/c8_18_s4c/build_2004_layout.py` + `/tmp/c8_18_s4c/detail_parsed.json` + `/tmp/c8_18_s4c/field_specs_2004_block.txt` + the PRE_FLIGHT_LOG SMOKE Tier-0 description). The SMOKE harness re-derives block size empirically (data + CR/LF) + value-distribution-verifies → deterministic, re-running yields identical results.
- Zero canonical parquet rebuild (DO step 4c is layout substrate only). H10 gate: 11/11 SHAs byte-exact.
- Record lengths cross-checked byte-exact against the guide-stated counts (`LinkCO04Guide` p18: Den 4,118,956 / Num 27,763) + the zip-member uncompressed sizes (`3,715,298,312 / 902 = 4,118,956`; `35,009,143 / 1261 = 27,763`) + the real-data `file_size % block == 0` + CRLF-terminator probe.
- No new Python dependency added (2004 = DEFLATE, stdlib `zipfile`; preserves the C8.5a pinned-env SHA). The DEFLATE64 `7z` path remains a 2003-only concern.

### Cross-product re-probe (if applicable)

- N/A — DO step 4c is additive linked-layout substrate only; no cross-product artifact consumes the 2004 layout until DO step 5 (parser) / DO step 6 (re-harmonize). 11/11 gate parquet SHAs byte-exact (incl. fetal-death + natality + MM, untouched).

### Git

- Pre-DO rollback anchor: `C8.18-pre-do` @ `6632a15` (unchanged; the C8.18 DO rollback anchor).
- This commit: intermediate C8.18 DO step 4c — **NO tag** (Convention 5; `C8.18-complete` is the final-sub-step-only tag).

### STATUS.md updated

- New section dated 2026-05-18T23:00:00Z at the top (newest-first).

### Self-check (§10) — what could I have gotten wrong that VERIFY wouldn't catch?

1. **Composite spans + RESERVED/FILLER gap-fill** (`MEDRISK`@328-344, `OBSTETRC`@355-361, `LABOR`@374-389, `DELMETH`@395-401, `NEWBORN`@483-491, `CONGENIT`@504-525, `ENTITY`@905-1044, `RECORD`@1049-1148 + the `RESERVED_<n>`/`FILLER_<n>` gap-fills) are authored as single spans; the per-condition leaf sub-positions were NOT individually value-distribution-verified — only the bracketing scalar/anchor neighbors. A composite/reserved span could be off by a byte and the anchor checks + contiguous-tiling check would not catch it. *Mitigation*: composite extents are byte-exact from the DETAIL range-tokens, structurally identical to the value-verified 2003 sibling (1-525), and the den-plus tiles [1,900] and the numerator tiles [1,1259] with zero gap/overlap; the bracketing named anchors value-verify on real data. *Recommendation*: DO step 5 value-distribution-verify each composite's leaf sub-fields before harmonizing. Logged DECISION_LOG residual risk; soft-flag (gg).
2. **F_* edit-flag region 576-799** — the 2004-specific 2003-rev edit-flag cluster (where 2004 DIVERGES from 2003). The DETAIL parser cleanly enumerated the F_* scalars (F_MORIGIN…F_UCA_CLUB), value-verified on real data ({0,1} domain, plausible), with RESERVED-fill for the dense unenumerated F_URF_*/F_ULD_*/F_U_*/F_UAB_*/F_UCA_* leaf gaps. A non-anchor F_* scalar between two RESERVED gaps could in principle inherit a wrong start if a DETAIL token collided AND the monotonic-non-decreasing filter let it through. *Mitigation*: every enumerated F_* scalar value-verified on real data ({0,1}); MRACE1E@800-823 / FRACE1E@835-858 value-verified (blank in sample, consistent with the 2003 sibling); the region tiles cleanly. DO step 5 full-parse value-distribution surfaces any non-anchor scalar anomaly.
3. **Mid-file layout shift** sampled only the first ~500 records/member (extremely unlikely for a fixed-width NCHS public-use `.dat`; `file_size % block == 0` holds for the whole file — den 3,715,298,312/902=4,118,956, num 35,009,143/1261=27,763, both exact == guide p18 counts). DO step 5 full parse surfaces any anomaly via row-count + value-distribution.
4. **Birth/death split point 867/868**: `LINKED_BIRTH_2004` ends @867 (FILLER_859@859-867) + `LINKED_DEATH_2004` starts at FLGND@868. The exact internal boundary of the trailing `FILLER_859`@859-867 is an authoring choice (the DETAIL gap before FLGND@868); it does not affect parser correctness (birth+death concatenate to tile [1,900]/[1,1259]) and is filler either way. The DO-step-5 num↔den construction keys on `IDNUMBER`@10-14 (the LinkCO04Guide p18 num↔den-plus join key), in `LINKED_BIRTH_2004` — unaffected.
5. **`_find_denomplus_member` "DENOM" vs `VS04LKBC.DUSDENOM`**: `DUSDENOM` *contains* "DENOM", so the existing 2005+ member-finder would match the 2004 den-plus member (unlike 2003's `USDENPUB` which would NOT). This is a DO-step-5 parser concern to confirm (not 4c — layout substrate only; the SMOKE harness reads via its own member-substring match). Documented in the `field_specs.py` source block + the forward-looking HALTs.
6. **2003-revision "Vers*" semantics**: the byte position is constant, but a field's *populated-ness* depends on REVISION@7 (S=1989-unrev populates the 1989-vintage fields; A=2003-rev populates the 2003-vintage fields). The layout substrate is unaffected (fixed-width regardless); the per-revision null-pattern + the harmonized mapping are DO-step-5/6 concerns (H7 fetal-death sibling-parity for the 2003-rev transition; soft-flag (gg)). Documented.
7. **L17 forward-blindness for the next sub-step**: this 4c step's own `test_linked_cohort_2004_layout_smoke.py` does NOT pin any "future-year raises ValueError" (its `test_*_dispatcher_additive_branch` negatives are only the permanent 1992-1994 gap + 2005-for-numerator-dispatcher, both stable). So the L17 class that bit the 2003 harness will NOT recur from this harness at DO step 5/6. But: any later sub-step that configures a *new* year/parser path must re-scope the cohort harnesses' negative-year sets (the L17 forward-looking discipline). Logged as a forward-looking HALT + DECISION_LOG residual risk.

Each logged in `DECISION_LOG.md` 2026-05-18T23:00:00Z as a residual risk; the recommended checks are DO-step-5 PRE-FLIGHT items (not added to VERIFY without human sign-off per §10).

### Forward-looking HALTs for next session (C8.18 DO step 4c-close → DO step 5, or a remaining-sub-step)

1. `C8.18-pre-do`@`6632a15`; `C8.18-complete` NOT present (final-sub-step-only). `C8.17-complete` present. HEAD = this DO step 4c commit (after `a0128eb`).
2. **11 gate parquet SHAs unchanged** (the same 11; fetal-death via the `~/Desktop/fetal-death-harmonization-build/` tree, soft-flag (hh)). Linked-derived `9b828a4d…` changes only at the later C8.18 re-harmonize DO step (4c is layout-substrate only).
3. **Canonical pytest baseline is now `147 passed, 1 skipped, 1 xfailed`** (was 137P+1S+1XF; +10 from the new `test_linked_cohort_2004_layout_smoke.py`, `DESIGN: tracks-current-state`; the 2003-harness L17 bundled Edit kept the count net-additive — 137 still pass). The canonical suite is the **4-dir** `fetal_death/tests/ natality/tests/ tests/ matched_multiples/tests/`. README 3-dir "56 passed" line stays stale → soft-flag (jj).
4. `field_specs.py` `LINKED_BIRTH/DEATH/NUM_DEATH_2004_FIELDS` + reclen 900/1259 are byte-exact + real-data value-verified — state-on-disk. The cohort **1995-2004 layout substrate is now COMPLETE** (1995-2002 @ 4a; 2003 @ 4b; 2004 @ 4c; 1983-1991 @ 3a/3b). DO step 4c must NOT be re-derived; the 1983-1991 / 1995-2002 / 2003 / 2004 layouts are all state-on-disk in `field_specs.py` + the 3a/3b/4a/4b/4c receipts. **Next per the C8.18 DO list = DO step 5** (parser + `harmonize_linked.py` extensions per cohort era; the `_find_denomplus_member` "DENOM"/"DEN"/"NUM" support across all cohort eras + the DEFLATE64-at-2003 CLI-`7z`-stream parse-path wiring + the num↔den construction; ICD-9 default-null + revision-tagged per the DO step 1 coupled decision; `cause_recode_130` per-era reconciliation, soft-flag (gg)).
5. **Encoding = ASCII** — the 2005+ parser's `_slice_field` `.decode("latin-1")` works for the cohort `.dat` (re-confirmed at 4c on real 2004 data).
6. **Structural model**: cohort 1995-2004 = the three-file denominator-plus model. 1995-2002 done (230/535). 2003 done (783/1142; DEFLATE64 — CLI-`7z`-stream tooling). 2004 done (**900/1259** — the receipt-note "1142" extrapolation was FALSIFIED; the "den-plus == LINKED_BIRTH_2005_2013" hypothesis was FALSIFIED; **DEFLATE**, stdlib-fine). 2004 member names = `VS04LKBC.{DUSDENOM,USNUMPUB,USUNMPUB}` — `_find_denomplus_member` ("DENOM") *would* match `DUSDENOM` (unlike 2003's `USDENPUB`); the DEFLATE64-at-2003 `7z`-stream + the cross-era member-finder + the num↔den construction = DO step 5 parser concerns.
7. §15.D substrate-format wording reconcile already APPLIED at the 3a commit; the broader §15.D model-clarification (3a/3b split; 4a/4b/4c decomposition; structural model; the 4b 1259→1142 + the 4c 1142→1259 + LINKED_BIRTH_2005_2013-hypothesis falsifications; the DEFLATE64-at-2003-only `7z`-stream tooling decision) remains **soft-flag (ii)** proposed-not-applied for §11 human-merge (queued with the soft-flag-(w) batch).
8. **soft-flag (gg) refined**: the 2004 numerator carries `UCOD`@884-887 (ICD-10; 2004 cohort all ICD-10) + `UCODR130`@889-891 (130-cause infant recode) + `ENTITY`@905-1044 (20×7) / `RECORD`@1049-1148 (20×5) multiple cause + the per-condition flag blocks `MEDRISK`/`OBSTETRC`/`LABOR`/`DELMETH`/`NEWBORN`/`CONGENIT` as composite spans; per-condition decomposition + per-era cause-recode semantics + the 2003-rev "Vers*" null-pattern + the harmonized cause-column shape stay DO step 5/6 PRE-FLIGHT.
9. **L17 forward-looking discipline**: any later C8.18 sub-step (DO step 5/6) that configures a new parser/harmonize path or year must grep-scope ALL cohort SMOKE harnesses' negative-year/`pytest.raises` sets and bundle the minimal `tracks-current-state` Edit into the same commit (the §4.2.1/Convention-2 mechanism; this 4c step applied it to the 2003 harness). pytest runtime ~336s (in the ~240-380s band; the COUNT 147P+1S+1XF is the gate, not the absolute number).

### Notes for next session

- **C8.18 DO step 4c is CLOSED.** The cohort 2004 physical layout (900-byte den-plus + 1259-byte numerator) is authored byte-exact + real-data value-verified on real DEFLATE `LinkCO04US.zip` data (stdlib `zipfile`), state-on-disk in `field_specs.py` + this receipt + `PRE_FLIGHT_LOG.md` 2026-05-18T23:00:00Z. The receipt-note "2004 = 900/1142" extrapolation was FALSIFIED → numerator 1259 (guide p18 + byte-exact zip-member arithmetic); the "den-plus == LINKED_BIRTH_2005_2013" hypothesis was FALSIFIED → 2003-rev cohort layout. **The cohort 1995-2004 layout substrate is now COMPLETE (4a+4b+4c); with 3a/3b (1983-1991), the entire C8.18 1983-2004 cohort *layout* substrate is authored.** Next per the C8.18 DO list: **DO step 5 — parser + `harmonize_linked.py` extensions** (the cross-era member-finder incl. 2003 `USDENPUB`/`USNUMPUB` + 2004 `DUSDENOM`/`USNUMPUB` + the 1983-1991 `DEN`/`NUM` + the DEFLATE64-at-2003 `7z`-stream wiring + the num↔den construction; ICD-9 default-null + revision-tagged; `cause_recode_130` per-era — soft-flag (gg)).
- The L13-extension "reconstruct from the DETAIL code-outline, value-distribution-verify on real data, never assume same-model==same-layout" discipline produced a first-run-clean 4c layout AND cheaply caught BOTH the 1142→1259 and the LINKED_BIRTH_2005_2013-reuse assumption errors at the Convention-3 snapshot before any mutation; carry it to DO step 5.
- The L17 stale-pin recurrence (the 2003 harness pinning "2004 raises ValueError") was resolved per the documented §4.2.1/Convention-2 mechanism (minimal Edit bundled into the same commit). Carry the **L17 forward-looking discipline** (forward-looking HALT 9): any future sub-step configuring a new year/path must grep-scope all cohort SMOKE harnesses' negative-year sets.
- The §15.D model-clarification `[plan-update]` (soft-flag (ii)) is owed to the human (§11; queued with the soft-flag-(w) batch; now also folds the 4c 900/1259 layout + the 1142→1259 + LINKED_BIRTH_2005_2013-hypothesis falsifications); the on-disk PRE_FLIGHT_LOG/DECISION_LOG/RECEIPTs are authoritative in the interim.
- `/tmp/c8_18_s4c/*` scratch (builder + detail_parsed.json + field_specs_2004_block.txt + the pre-authoring probe) + background gate-SHA/pytest output files are OS-cleanable + reproducible.
