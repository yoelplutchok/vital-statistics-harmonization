# Receipt: natality_v28_rename
## 2026-05-12T13:35:02Z

### What was done

Renamed 4 natality harmonized columns from their v2.7.0 names to the canonical cross-product names that the fetal-death v2.0+ schema and the aliasing helper at `shared/helpers/canonical_join_keys.py` had been mapping to: `year → data_year`, `restatus → residence_status`, `maternal_race_bridged4 → maternal_race_bridged`, `maternal_hispanic_origin → hispanic_origin`. The rename ran end-to-end on the standalone natality build dir at `/Users/yoelplutchok/Desktop/natality-harmonization/` (4 columns renamed in `metadata/harmonized_schema.csv` + 5 cascading derivation_rule / raw_source_by_year cross-references; 9 scripts updated in 03_harmonize, 04_derive, 05_validate, 06_convenience, 07_figures; 6 docs updated for backtick + bare-word column refs; `.zenodo.json` bumped to v2.8.0; 2 metadata validation-target CSVs updated for column-header + notes text; 2 output-validation CSVs regenerated by the v2.8 pipeline). All 4 harmonized + derived parquets were re-derived end-to-end (V1 1990–2024 harmonize 7:05 wall + derive 5:26 wall; V3 linked 2005–2023 harmonize 5:46 wall + derive 3:01 wall; ~16 min total). Both NVSR validation gates passed: V1 183/183 byte-exact; V3 linked 33 byte-exact + 2 cells differ by 1 record (matching the Task 6 canonical framing for the 2015 `unweighted_infant_deaths` and `postneonatal_deaths` cells). The rename is value-preserving end-to-end: `paper_companion_results.csv` synthesis is byte-identical to the prior v2.7.0 commit when both notebook builders are rebuilt against v2.8 parquets. The aliasing helper at `shared/helpers/canonical_join_keys.py` is retained (NATALITY_TO_CANONICAL stays populated) so that downstream code reading the immutable v2.7.0 Zenodo deposit (10.5281/zenodo.19868835) can still alias to canonical names; for v2.8.0+ input the helper becomes a no-op via empty rename map. Partial version-string ripple landed (VERSION_ROADMAP, PROJECT_STRUCTURE, CITATION.cff, natality/README.md); the v2.7.0 Zenodo DOI remains the canonical citable artifact until the unified-HVS deposit task (KICKOFF.md sequence step 4) lands.

### Inputs consumed

- `/Users/yoelplutchok/Desktop/natality-harmonization/` build dir at HEAD `dcabd8c` pre-DO; tagged `natality_v28_rename-pre-do` at PRE-FLIGHT time.
- `metadata/harmonized_schema.csv` (pre-rename): 95 rows (94 data + header); rows 1–4 verified at PRE-FLIGHT as the 4 rename targets `year`, `restatus`, `maternal_hispanic_origin`, `maternal_race_bridged4`.
- `metadata/external_validation_targets_v1.csv`: pre-rename column header `year`; 183 validation-target rows.
- `metadata/external_validation_targets_v3_linked.csv`: pre-rename column header `year`; 35 V3 linked target rows.
- `shared/helpers/canonical_join_keys.py` (monorepo): `NATALITY_TO_CANONICAL` 4-entry dict declaring the canonical name mapping.
- 4 v2.7.0 natality parquets re-derived locally from raw NCHS yearly zips and the pre-rename metadata; their post-rename SHAs are recorded below as outputs.
- Per-year NCHS raw zips and yearly_clean cohort files at the build dir (unchanged from v2.7.0 state).

### Outputs produced

v2.8.0 build-dir parquets (4 files; all re-derived during the rename DO and verified at session-end):

- `output/harmonized/natality_v2_harmonized.parquet`: sha256=`230efed2ac34c794638aceaa777a31e62abffb6e8e6af94ed215970933ccebac`, rows=138,819,655, cols=71
- `output/harmonized/natality_v2_harmonized_derived.parquet`: sha256=`e16ad5323d68e28d401518f1ff56b12c09e43883e76022a9823d51a677c41d44`, rows=138,819,655, cols=84
- `output/harmonized/natality_v3_linked_harmonized.parquet`: sha256=`e1795ac615a6ee40b0d5813ac6f6c072692bc30808b746b3c3efb06cf5f357e7`, rows=74,943,824
- `output/harmonized/natality_v3_linked_harmonized_derived.parquet`: sha256=`9b828a4de4e59b17a1ca727e3dddc7ea7d748bb5281a98612f6fb9b85a08b777`, rows=74,943,824, cols=94

Sync target — monorepo `natality/` subdir (23 files, 116 ins / 116 dels, all pure renames):

- `metadata/harmonized_schema.csv` — 4 row renames + 5 cascading derivation_rule cross-refs.
- `metadata/external_validation_targets_v1.csv`, `external_validation_targets_v3_linked.csv` — column-header + notes-text renames.
- `output/validation/external_validation_v1_comparison.csv`, `external_validation_v3_linked_comparison.csv` — regenerated by v2.8 validators.
- `scripts/03_harmonize/harmonize_v1_core.py`, `harmonize_linked_v3.py`, `scripts/04_derive/derive_v1_core.py`, `scripts/05_validate/{compare_external_targets_v1.py, compare_external_targets_v3_linked.py, harmonized_missingness.py, key_rates_from_derived_core.py, validate_v1_invariants.py}`, `scripts/06_convenience/write_residents_only.py`, `scripts/07_figures/generate_paper_figures.py` — string-literal column-name renames at output-write and column-read sites.
- 6 docs (`CODEBOOK`, `COMPARABILITY`, `ABOUT_THIS_RELEASE`, `VALIDATION`, `FAQ`, `GETTING_STARTED`) — backtick and bare-word column refs.
- `.zenodo.json` — version `v2.7.0 → v2.8.0`.
- `README.md` — cosmetic LLM-mention scrub (pre-existing stash committed standalone this session) + 1 missed `restatus` doc ref + v2.8.0 not-yet-deposited note.

Monorepo top-level updates:

- `VERSION_ROADMAP.md` — table now distinguishes in-repo state (natality v2.8.0, fetal-death v2.1.0) from latest-deposited Zenodo state (v2.7.0, v2.0.0).
- `PROJECT_STRUCTURE.md` — `(v2.7.0 import)` → `(v2.8.0 in-repo state, last Zenodo deposit v2.7.0)`.
- `CITATION.cff` — note clarifies in-repo v2.8.0 vs deposited v2.7.0.
- `shared/helpers/canonical_join_keys.py` — docstring note that helper is a no-op for v2.8+ but retained for v2.7.0 backward-compat; smoke-verified.
- `notebooks/_build_joint_use_demo.py`, `_build_paper_companion.py`, `joint_use_demo.ipynb`, `paper_companion.ipynb` — rebuilt against v2.8 parquets; `paper_companion_results.csv` is byte-identical to the prior commit.

### Five-phase trace

- PRE-FLIGHT: ✓ (`PRE_FLIGHT_LOG.md` entry timestamp 2026-05-12T05:30:00Z, monorepo commit `f2c5b34`; PROCEED, no §7 halt; Convention 3 Field-value snapshot reconfirmed exact 61 string-literal rename surface across 18 files).
- SMOKE: ✓
  - Tier 0 (import-sanity): all renamed scripts import cleanly under the new schema; helper smoke-test confirms no-op for v2.8 input and full rename for v2.7.0 input.
  - Tier 2 (year 2022 harmonize to scratch path; ~50 sec wall): new column names present, old names absent on smoke output.
- DO: ✓
  - Build-dir commits `9fbc5b0` (the rename), `9be73dd` (doc backtick + validation regen), `6837b34` (cosmetic README scrub, standalone), `80c0380` (3 missed-restatus doc cleanup + v2.8 deposit note).
  - Monorepo commits `9a66b60` (sync), `a6b3d36` (notebooks rebuild), `5174552` (helper docstring), `50dd7b4` (version-string ripple).
- VERIFY: ✓
  - V1 NVSR (`compare_external_targets_v1.py`): 183/183 PASS byte-exact, 0 FAIL, 0 MISSING. Identical to v2.7.0.
  - V3 linked NVSR (`compare_external_targets_v3_linked.py`): 35 PASS in tolerance-aware framing; Diff breakdown 33 byte-exact + 2 Diff=1 cells at `(unweighted_infant_deaths, 2015)` and `(postneonatal_deaths, 2015)`. Matches Task 6 canonical framing EXACTLY.
  - Notebooks (Tier 4 cross-product): joint_use_demo executed cleanly (no FAIL/DIFF/error); paper_companion executed cleanly with the 2 pre-existing DIFFs (C03 fetal-death V2.1 count growth; C04 natality "~3.5M" wording precision) unchanged from prior v2.7.0 run.
  - `paper_companion_results.csv` byte-identical to prior commit (value-preserving rename verified end-to-end).
- RECEIPT: ✓ (this file).

### Verify results

- V1 NVSR validation: PASS, value=183/183 byte-exact, threshold=183/183 byte-exact.
- V3 linked NVSR validation: PASS, value=33 byte-exact + 2 Diff=1 = 35 total PASS within tolerance, threshold=33+2 matching Task 6 canonical framing.
- Re-derive determinism within session: PASS (each parquet derived once; SHAs match the locked values recorded mid-session in STATUS 2026-05-12T06:30:00Z; cross-session bit-stability is logged below as Self-check residual risk).
- Cross-product notebook rebuild: PASS, value=`paper_companion_results.csv` byte-identical to prior commit; 0 new DIFFs.
- Aliasing helper smoke (v2.7.0 vs v2.8 input both paths): PASS, v2.7.0 input renames to canonical, v2.8 input passes through.

### Reproducibility

- Re-running the harmonize → derive pipeline on a fresh check-out of build-dir HEAD `9fbc5b0` should produce the 4 parquet SHAs above bit-identically (forward-looking HALT below schedules a clean re-derive verification before the v2.8.0 Zenodo deposit lands).
- The v2.7.0 deposit at 10.5281/zenodo.19868835 is **immutable** and continues to serve as the byte-clean comparison baseline if a regression is suspected.
- `paper_companion_results.csv` byte-stability across the rename is the strongest end-to-end reproducibility signal: the column rename touches names but not values; the synthesis CSV's value content is unchanged.

### Cross-product re-probe

- `notebooks/joint_use_demo.ipynb`: rebuilt against v2.8 natality + v2.1.0 fetal-death + v3.0.0 linked; 19 cells executed cleanly; 8/8 NVSR 73-09 Table-4 age-band cells byte-exact for 2022.
- `notebooks/paper_companion.ipynb`: rebuilt against the same; 38 cells executed cleanly; 24 PASS + 20 CITE-ONLY + 4 L11 + 2 DIFF (both pre-existing).
- `paper_companion_results.csv`: byte-identical to prior commit.
- `shared/helpers/canonical_join_keys.py` smoke: v2.8 input passes through unchanged; v2.7.0 input renames to canonical.

### Git

- Pre-DO tag: `natality_v28_rename-pre-do`, monorepo commit=`f2c5b34`, build-dir commit=`dcabd8c` (set at PRE-FLIGHT 2026-05-12T05:30Z).
- Post-RECEIPT tag: `natality_v28_rename-complete`, monorepo commit=(post this commit), build-dir commit=`80c0380` (to be tagged).

### STATUS.md updated

- New section dated 2026-05-12T13:35:02Z marking natality v2.8.0 column rename complete (steps 1–14 of 14 done).

### Self-check

What could I have gotten wrong that VERIFY wouldn't catch?

1. **Re-derive determinism not verified across sessions for v2.8 specifically.** The 4 v2.8 parquet SHAs were locked at the end of the prior session and confirmed unchanged at session-start of this session, but no fresh re-derive of the full pipeline (from raw zips through `harmonize` → `derive`) has been executed this session to verify bit-identical output. v2.7.0 bit-stability was verified at each prior natality release; v2.8.0 bit-stability is *expected* (no derivation logic changed, only schema-output names) but currently unverified for this specific schema. Forward-looking HALT 1 below.

2. **V1-era column-level byte-clean regression not exhaustively verified at the per-column level.** The 183/183 V1 NVSR validation passed (functional verification at the aggregate level). 84 of 84 columns at the cell level have not been cross-compared against the v2.7.0 Zenodo deposit's bytes. Expected: 4 columns differ by name; all values bit-identical to v2.7.0. A column-by-column SHA check (project v2.7.0 deposit's parquets through the canonical-renamed schema and compare to v2.8 parquets) is the strongest possible verification; deferred per L17 SHAPE-not-VALUE convention. Forward-looking HALT 2.

3. **Internal Python variable names in `scripts/03_harmonize/*.py` still use `restatus` as a local variable.** These are not column names; they're local variables holding the raw RESTATUS pyarrow array post-read, used briefly before being written out to the `residence_status` output column. A reader might be confused by the name mismatch between input-side variable name and output-side column name. Mitigation: convention is local variables follow raw NCHS field names (RESTATUS → `restatus`); output columns follow the canonical names (`residence_status`). Documented here for traceability.

4. **Downstream user projects break on v2.8.** Two known consumer projects on the user's Desktop (`multiple-gestation-linked-imr`, `lbw-imr-divergence`) hard-code v2.7.0 column names. Per DECISION_LOG 2026-05-12T03:25Z HALT 3, compatibility updates in those projects are explicitly out of v2.8 scope. Users should pin to v2.7.0 (Zenodo immutable at 10.5281/zenodo.19868835) until they migrate.

5. **Cosmetic edits bundled with v2.8 close-out.** Three orthogonal changes shipped under the "v2.8 rename" close-out umbrella: (a) the pre-existing LLM-mention README scrub (committed standalone before any close-out work), (b) the 3 missed `restatus` doc refs (mostly v2.8 schema cleanup, with one DOI-block note), (c) the v2.8.0 deposit note (genuinely v2.8 close-out). All three were intentional; documented in commit messages. Not a regression risk.

6. **The 5 demographic/filter columns named in fetal-death v2.1.0 (`tabulation_flag` Int8, `residence_status` Int8, etc.) used to match natality v2.7.0 column names imprecisely.** Under v2.8 the natality `residence_status` column matches the fetal-death `residence_status` column byte-exact on naming. Joint-use code can now reference one column name across both products without an aliasing pass. The aliasing helper is no longer NECESSARY for v2.8 input but is RETAINED for v2.7.0 input backward-compat. A future deprecation of the helper (when v2.7.0 Zenodo deposit access is no longer common) is post-submission work; not in this receipt's scope.

7. **`paper_companion_results.csv` byte-identical-to-prior is a strong signal but not a proof of zero regression.** The synthesis CSV captures 55 enumerated manuscript claims. A regression that affects a value NOT enumerated in the 55 claims would not surface here. The 183 V1 NVSR + 35 V3 linked NVSR validation is the broader safety net; combined coverage is high but not 100%.

8. **Q19/Q20 still gate Task 7 (post-v2.8).** v2.8 close-out does not depend on those answers; Task 7 PRE-FLIGHT cannot start until they land. Flagged for the next session.

### Forward-looking HALTs for next session

Convention 4. The next session's PRE-FLIGHT must verify:

1. **`natality_v28_rename-complete` tag exists on both repos** at the expected commits (monorepo: this RECEIPT commit; build-dir: `80c0380`). If either tag missing, halt and tag before further work.
2. **The 4 v2.8 parquet SHAs above are unchanged at next-session start.** If `output/harmonized/*.parquet` SHAs in the build dir drift between sessions, someone re-ran a script and the locked baseline is at risk; halt and investigate.
3. **A clean re-derive verification is worth scheduling before the v2.8.0 Zenodo deposit lands** (KICKOFF step 4 / Task 10). The natural moment: just before the Zenodo upload, do a fresh re-derive from raw zips + compare new SHAs to the 4 SHAs in this receipt. If they match, the v2.8 pipeline is bit-stable; deposit. If not, investigate. Recorded here so the Task 10 PRE-FLIGHT will see it.
4. **`paper_companion_results.csv` synthesis CSV byte-stability is recorded as the rename-validation gold standard.** Future natality/fetal-death schema-only mutations (renames, dtype-only changes, doc-only changes) should preserve this CSV byte-identical; if a future task changes it, that's the audit signal to investigate why.
5. **Aliasing helper is retained for v2.7.0 backward-compat.** If a future task proposes to neuter `NATALITY_TO_CANONICAL` (empty the dict), it should first confirm that no consumer reads the v2.7.0 Zenodo deposit through the helper. The smoke-test inline in this session's `canonical_join_keys.py` commit `5174552` documents the dual-path behavior; rerun before neutering.
6. **Internal Python variable `restatus` in `harmonize_v1_core.py` + `harmonize_linked_v3.py` is intentional** (raw-field-name convention for local variables; output column name is `residence_status`). If a future grep finds these and flags them as missed renames, this receipt is the rebuttal.
7. **Q19 (Task 7 scope: V3a-only vs V3a+V3b) and Q20 (post-v2.8 sequence) remain open** for human direction. Task 7 PRE-FLIGHT cannot start until both land.

### Notes for next session

- v2.8.0 ships in repo but **is not deposited to Zenodo**. v2.7.0 (10.5281/zenodo.19868835) remains the canonical citable artifact. Zenodo deposit is KICKOFF.md sequence step 4.
- The natality build dir at `/Users/yoelplutchok/Desktop/natality-harmonization/` is HEAD `80c0380` (post-doc-cleanup); monorepo `natality/` subdir mirrors this state.
- Two pre-existing manuscript DIFFs (C03 fetal-death V2.1 record count from 1.63M → 1.74M; C04 natality "~3.5M" wording) await the manuscript re-pass at KICKOFF.md sequence step 6. The v2.8 close-out did not introduce these; they were DIFF in the prior commit too.
- Per KICKOFF.md sequence: next task is Task 7 (fetal-death V3 backward extension), gated on Q19/Q20.
