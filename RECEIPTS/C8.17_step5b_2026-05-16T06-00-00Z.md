# Receipt: C8.17 DO step 5b — `harmonize_v1_core.py` extension for pre-1990 era_tags
## 2026-05-16T06:00:00Z

### What was done

Authored the pre-1990 (1968–1988) harmonization path in
`natality/scripts/03_harmonize/harmonize_v1_core.py` — the second half of the
§15.D-bundled DO step 5 (DO step 5a shipped the parser + 22 `_raw.parquet`
files at 2026-05-15T03:45:00Z). The DO step 5b entry cheap-check surfaced
**four material divergences** from the DO step 5a receipt's assumed
architecture; all four were resolved at the cheap-check moment via
AskUserQuestion 2026-05-15 (Q1 Option A + Q2 Option A) plus two LLM
plan-amendments, documented in a PRE-FLIGHT addendum **before any DO
mutation** (L10-safe; task1 addendum precedent). The change is **purely
additive to the harmonizer**: a new `is_pre1989` branch inside `main()`'s
batch loop computing all 71 canonical output arrays for 1968–1988, a new
`_mrace1digit_to_bridged4()` helper (H7 sibling parity with the fetal-death
V3b B3 1-digit MRACE recode), a new `_to_int_or_null_safe()` helper (robust
against pre-1990 non-numeric sentinel chars), and a `_raw`/`_core`
input-path conditional. **1989 routes through the existing, byte-untouched
`is_pre2003` path** (1989 `_raw` layout == V2 1990-2002 layout). The
existing 1990-2002 and 2003+ code-path bodies are byte-untouched (4
deletions total, all at the era-dispatch boundary: 1 path line, 1 comment,
1 era-detect line, `if`→`elif`). DO step 5b writes **no canonical
artifact** — harmonized output went to `/tmp` scratch for SMOKE only; the
canonical 1968-2024 re-harmonize + version bump remain DO step 6.

### Inputs consumed

- `~/Desktop/natality-harmonization/output/yearly_clean/natality_{1968,1971,1975,1985,1989}_raw.parquet` (DO step 5a output; SMOKE substrate)
- `natality/metadata/harmonized_schema.csv` (canonical enums: `certificate_revision`, `maternal_race_bridged`, `marital_status`, etc.)
- `fetal_death/scripts/03_harmonize/harmonize.py:399` (H7 sibling B3 1-digit MRACE map, 1985 user-guide frame)
- `~/Desktop/natality-harmonization/output/harmonized/natality_v2_harmonized.parquet` (71-col schema-identity reference)

### Outputs produced

- `natality/scripts/03_harmonize/harmonize_v1_core.py` — MODIFIED, 1403→1684 lines (+289/−4). Additive: `_to_int_or_null_safe` (L77), `_mrace1digit_to_bridged4` (L241), `_raw`/`_core` path conditional (L617), `is_pre1989` era discriminator (L629), `is_pre1989` branch (L653–855), `if is_pre2003:`→`elif is_pre2003:` (L857). Existing 1990+/2003+ bodies byte-identical.
- `PRE_FLIGHT_LOG.md` — MODIFIED, +50 lines (DO step 5b PRE-FLIGHT addendum; prepended newest-first).
- No canonical parquet written (SMOKE output → `/tmp/c8_17_step5b/`, not shipped).

### Five-phase trace

- **PRE-FLIGHT**: ✓ Entry cheap-check 13/14 HALTs PASS (HALT 9 pytest deferred to VERIFY per C8.17 DO step 3/4 precedent); H10 SHA gate intact. Convention-3 Field-value snapshot surfaced 4 divergences (D1 monolithic main() not a dispatcher; D2 `_core` vs `_raw` path; D3 1989 collapses into is_pre2003; D4 no pre-1989 `certificate_revision` enum value) → resolved via AskUserQuestion 2026-05-15 (Q1 Opt A in-place branch + 1989→existing path; Q2 Opt A `unrevised_1968`) + 2 LLM amendments (D2 path conditional; marital=null). Documented in `PRE_FLIGHT_LOG.md` addendum 2026-05-15T05:30:00Z **before the first DO mutation**.
- **SMOKE**: ✓
  - Tier 0 (hand-computed fixture): `_mrace1digit_to_bridged4` full 1-digit frame {0/4/5/6/8→4, 1→1, 2→2, 3→3, 7/9/null→null}; `_dmeduc_years_to_cat4` 88/99 sentinels→null; `_pldel_to_facility`; CSEX recode — all match hand-computed expected.
  - Tier 1 (200-row subset × 5 years): schema byte-identical to canonical 71-col; row-count conservation (1000); per-sub-era distributions; **caught a real bug** — pre-1990 MONPRE has non-numeric sentinel `'&'` crashing the hard cast → fixed with `_to_int_or_null_safe` (regex-guarded coercion; additive helper, 1990+ paths untouched). Re-run PASS.
  - Tier 2 (full single year per sub-era + 1989, 11.79M rows, ~77s): **H6 row-count conservation byte-exact** (1968=1,750,782 / 1975=2,232,406 / 1985=3,765,064 / 1989=4,045,693 == `_raw`); `certificate_revision` 1968-1985=`unrevised_1968`, 1989=`unrevised_1989`; `maternal_race_bridged` historically plausible (White 79-84%, Black 15-17%, AIAN ~1%, API 0.5%→3.3% 1968→1989, null tiny); maternal_age 10-49; birthweight 227-9999; education null 1968 (no DMEDUC) / populated 1975+; **hispanic_origin all-null 1968-1985, populated 1989** (confirms 1989→is_pre2003/ORMOTH routing).
- **DO**: ✓ harmonize_v1_core.py additive edits (commit pending; tagged parent `C8.17-pre-do`@`12fc20e`).
- **VERIFY**: ✓ (criteria below).
- **RECEIPT**: ✓ (this file).

### Verify results

- **Criterion A — H10 reproducibility gate**: PASS. 4 canonical + 4 matched-multiples parquet SHAs **byte-exact** vs DO step 5a receipt (`38e2cecb…`/`185c071e…`/`e16ad532…`/`9b828a4d…` + `adbec108…`/`5c22308b…`/`7c682668…`/`d98b4296…`). DO step 5b wrote zero canonical artifacts.
- **Criterion B — pytest SHAPE invariant (Convention 1)**: PASS. Cache-cleared `pytest fetal_death/tests/ natality/tests/ tests/ matched_multiples/tests/ -p no:cacheprovider` = **85 passed, 1 skipped, 1 xfailed in 238.25s** (count exact; timing within 150-380s system-variance band; cf. DO 5a 152.55s / DO 4 251.86s). No test-surface mutation.
- **Criterion C — scope (HALT 13 / §7-#17)**: PASS. `git diff` = only `harmonize_v1_core.py` (additive) + `PRE_FLIGHT_LOG.md` (addendum). The existing `is_pre2003`/`else` bodies + output-assembly are byte-untouched (4 deletions, all at the dispatch boundary). 1990+ behavior provably unchanged (1990+ → `suffix=core`, `is_pre1989=False`, `is_pre2003="DMAGE" in cols` — identical to pre-edit).
- **Criterion D — SMOKE ladder**: PASS (Tier 0/1/2 above).

### Reproducibility

Re-running `harmonize_v1_core.py --years <pre-1990 set> --yearly-parquet-dir ~/Desktop/natality-harmonization/output/yearly_clean --out <scratch>` is deterministic and idempotent (pure pyarrow.compute transforms over the immutable `_raw` parquets). No regression: H10 gate byte-exact; pytest SHAPE unchanged.

### Cross-product re-probe

DO step 5b touches only the natality harmonizer's new pre-1990 branch; no canonical parquet, schema CSV, or validation target changed. fetal-death / linked / matched-multiples + the 1990-2024 natality canonical parquet are byte-identical (Criterion A). Downstream DO step 6 (canonical 1968-2024 re-harmonize + version bump) consumes this branch.

### Git

- Pre-DO tag: `C8.17-pre-do` @ `12fc20e` (parent; set at C8.17 PRE-FLIGHT close).
- Post-RECEIPT tag: **NONE** — intermediate DO step; no tag per Convention 5 precedent (C8.16 sub-step 2 + C8.17 DO 1/2/3/4/5a). `C8.17-complete` set only after the final DO step.
- Commit: ~5-line summary per Convention 5; full narrative here + DECISION_LOG 2026-05-16T06:00:00Z + STATUS 2026-05-16T06:00:00Z.

### STATUS.md updated

New section dated 2026-05-16T06:00:00Z (prepended; DO step 5b close; DO step 6 queued).

### Self-check (§10) — what could I have gotten wrong that VERIFY wouldn't catch?

1. **`marital_status` = null for 1968-1988 discards LEGITIM signal.** Verified the schema sources marital from DMAR (absent in natality pre-1990 public-use) and the fetal-death sibling uses DMAR even in 1982-1988; LEGITIM (legitimacy) ≠ marital_status. Conflating them is a §7-#19 hazard. Residual risk: a downstream user may *expect* a pre-1990 marital proxy. Mitigation: documented as a DO-step-6 candidate (`legitimacy_status` as a distinct column, not shoehorned). VERIFY can't catch a "should we have surfaced LEGITIM differently" judgment — flagged here + DECISION_LOG.
2. **`certificate_revision = unrevised_1968` is not yet in the canonical schema enum.** Correct per AUQ Q2; DO step 5b writes no canonical parquet so no schema violation occurs. Residual risk: if DO step 6 forgets to widen the `harmonized_schema.csv` enum + bump the schema version, the canonical re-harmonize would emit a value the schema validator rejects. Mitigation: forward-looking HALT 6 below + STATUS Notes.
3. **`preterm_recode3` from pre-1990 GESTREC3 maps only {1,2,3}; the pre-1990 `0` code → null.** The pre-1990 GESTREC3 `0` semantics ("not on certificate"/"not computable") were inferred from distribution, not a user-guide cross-read. Residual risk: if `0` actually means a valid preterm class in some year, those rows are wrongly nulled. Low impact (preterm_recode3 is `partial`-nullable; gestational_age_weeks is the primary signal). DO-step-6 user-guide refinement flag.
4. **`prenatal_visits` nulled for all 1968-1988** despite TPRENVIS existing 1972+. Conservative (TPRENVIS sentinel coding unverified at author+SMOKE). Residual risk: lost real signal. DO-step-6 refinement candidate (documented).
5. **`gestational_age_weeks` domain-clamped to {17..47, 99}; pre-1990 `00` → null.** Matches schema enum `17-47|99` but the V2 path does NOT clamp (it trusts clean 1990+ DGESTAT). Mild asymmetry vs the 1990+ path; defensible because pre-1990 DGESTAT carries an extra `00` code. SMOKE distributions plausible; flagged for DO-step-6 cross-era continuity review.
6. **`maternal_age` no sentinel-null beyond blank (parity with V2).** If pre-1990 DMAGE uses a numeric sentinel (e.g., 99=unknown) unlike 1990+, those would survive as age=99. Tier 2 showed max age 49 (no 99 leak in the probed years) — but not all 22 years were Tier-2'd. DO step 6 full-envelope validation will surface any sentinel leak via the maternal_age domain check. Flagged.

### Forward-looking HALTs for next session (C8.17 DO step 6 PRE-FLIGHT)

Re-verify byte-exact; halt if any fails.

1. **`C8.17-pre-do`@`12fc20e`; `C8.17-complete` NOT present.** (No tag added DO step 5b.)
2. **4 canonical + 4 matched-multiples parquet SHAs byte-exact**: `38e2cecb…`/`185c071e…`/`e16ad532…`/`9b828a4d…` + `adbec108…`/`5c22308b…`/`7c682668…`/`d98b4296…`. (DO step 6 WILL change `natality_v2_*` SHAs — that is the canonical re-harmonize + version bump; the *other 6* must stay byte-exact.)
3. **`harmonize_v1_core.py` carries the `is_pre1989` branch (L653) + `_mrace1digit_to_bridged4` (L241) + `_to_int_or_null_safe` (L77) + `_raw`/`_core` path conditional (L617).** Existing `is_pre2003`/`else` bodies byte-untouched. No further harmonizer edits expected at DO step 6 unless full-envelope validation surfaces a sentinel leak (self-check 6).
4. **22 `natality_<year>_raw.parquet` present** at `~/Desktop/natality-harmonization/output/yearly_clean/` (DO step 5a; SHA prefixes per DO 5a receipt). DO step 6 harmonizes all 22 + the 35 `_core` (1990-2024) in one `--years 1968-2024` run.
5. **1989 routes through `is_pre2003`** (verified Tier 2: cert=`unrevised_1989`, Hispanic populated). DO step 6 must NOT special-case 1989 into the pre-1989 branch.
6. **`certificate_revision` enum widening + schema-version bump is OWED at DO step 6.** `harmonized_schema.csv:5` enum must add `unrevised_1968` (per Anti-Pattern #6: schema edit requires version bump OR DECISION_LOG-referencing comment row). natality v2.8.0 → v2.9.0 (or v3.0.0 if the cert-revision boundary triggers a major bump per H10 cascade — DO step 6 decision). `years_available` cells for affected columns extend to `1968-…`.
7. **Cache-cleared pytest 85P+1S+1XF** (150-380s system-variance band). DO step 6 will re-pin C8.1 `tracks-current-state` smoke EXPECTED_ROW_COUNT/EXPECTED_YEARS to the 1968-2024 envelope (that re-pin is the expected behavior per L17/Convention 2, bundled with the parquet rebuild — NOT a regression).
8. **B.12 snapshot regression baseline** (`tests/snapshots/v1_2026-05-13T21-00-00Z_columns.csv` sha=`b6fe22d6…`) byte-exact through DO step 5b; DO step 6 re-snaps post-rebuild → new `tests/snapshots/v2_<UTC>_columns.csv`.
9. **DO step 6 substrate**: this branch + the 22 `_raw` + 35 `_core` parquets. Budget 1-2 sessions per §15.D. Sub-Q42: if DO step 6 exceeds 2 sessions, file `[plan-update]` sub-entry (cumulative cap 86; current ~23.5).
10. **`_to_int_or_null_safe` is is_pre1989-only**; the 1990+ paths still use `_to_int_or_null` byte-exact (HALT 13). DO step 6 must preserve this separation.
11. **Soft-flag (w)** carries: NEXT_STEPS.md §15.D §1358 "1978-revision birth cert from 1985" wording-reconciliation deferred to next `[plan-update]`. **Soft-flag (x)** carries (DO step 4 STATUS narrative envelope-sum arithmetic slip). **Soft-flag (z)** carries (1990+ parser bulk-vectorization candidate). NEW **soft-flag (aa)**: 4 conservative-null fields with documented DO-step-6 refinement candidates (LEGITIM→legitimacy_status, TPRENVIS→prenatal_visits, GESTREC3 `0` semantics, maternal_age sentinel-leak full-envelope check).
12. **6 DECISION_LOG design choices** (DECISION_LOG 2026-05-16T06:00:00Z) — DO step 6 inherits these as settled (do not re-litigate; re-open only if full-envelope validation contradicts).
13. **Tier 3+5 progress ≈ 2.0 of 7 tasks**; cumulative Phase C ≈ 23.5 of 51-71 sessions (within Q33 cap 86).
14. **No KICKOFF.md / NEXT_STEPS.md edit at DO step 5b** (Convention 5; §15.D wording soft-flag (w) carries).

### Notes for next session

- DO step 6 = the canonical re-harmonize: run `harmonize_v1_core.py --years 1968-2024` to the canonical out path, preserve the v2.8 parquet as `.v28_baseline.parquet` forward-stability anchor (§15.D DO step 6), bump the schema version + widen the `certificate_revision` enum, re-derive (04_derive), re-validate (existing 183/183 NVSR preserved + new pre-1990 cells incrementally), re-snap B.12. Then DO step 7 = docs (CITATION.cff/ABOUT_THIS_RELEASE/README/PROJECT_STRUCTURE) + C8.1 smoke re-pin.
- The four conservative-null fields (self-check 1/3/4 + soft-flag (aa)) are deliberate author+SMOKE scope boundaries, NOT bugs — DO step 6 may refine against the per-era user guides if the cross-era continuity review wants the extra signal.
- `/tmp/c8_17_step5b/` (scratch parquets + smoke scripts) is OS-cleanable; reproducible from the harmonizer + `_raw` parquets.

### §10 self-check answered: yes (6 residual risks above; logged in DECISION_LOG 2026-05-16T06:00:00Z).
