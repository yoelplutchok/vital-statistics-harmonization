# Receipt: C8.18 DO step 5b — the 1983-1988 self-contained-numerator + aggregate-denominator two-file construction + a cross-era numerator-member finder
## 2026-05-19T08:00:00Z

### What was done

Executed C8.18 **DO step 5b** under the full five-phase discipline in one session (PRE-FLIGHT + SMOKE + DO + VERIFY + RECEIPT; the C8.18 DO step 3a/3b/4a/4b/4c/5a single-session-DO-step precedent). DO step 5b is the second of the §15.D DO step 5 → **5a/5b/5c** per-concern decomposition (the 3a/3b + 4a/4b/4c precedent; §2 cheap-before-expensive / §9-#8): **5a** (shipped 2026-05-19T05:00:00Z) = the cross-era denominator-member finder; **5b** (this session) = the keyless 1983-1988 two-file construction + the symmetric numerator-member finder; **5c** = `harmonize_linked_v3.py` per-cohort-era field mapping (soft-flag (gg)).

5b added **purely additively** to `natality/scripts/01_import/parse_linked_year.py` (`git diff --numstat` = **139 insertions / 0 deletions**; two insertion-only hunks at function boundaries):

1. **`_find_numerator_member(zip_path)`** (NEW) — the symmetric `"NUM"` sibling of 5a's `_find_denomplus_member`: the UNIQUE member whose upper-name contains `"NUM"` and not `"UNL"`/`"UNM"`; zero/>1 → `RuntimeError` (§2 fail-closed). One rule, NO behavior-preservation constraint (unlike 5a — no shipped path calls it; the canonical v3 2005-2023 build reads only the denominator-plus). Cross-era-correct by name analysis for all 6 cohort eras (`LinkCO{yy}USnum.dat` 1983-1991; `LinkCO{yy}US{Num,NUM}.dat` 1995-2002; `VS0{3,4}LKBC.USNUMPUB` 2003/2004 — den `DENOM`/`USDEN`/`USDENPUB` + unlinked `USUnl`/`USUNL`/`USUNMPUB` never contain `"NUM"`).

2. **`_iter_two_file_1983_1988(zip_path, year, max_rows)`** (NEW) — the 1983-1988 **self-contained-numerator + aggregate-denominator** construction. 1983-1988 carry **NO record-level public-use key** (the C8.18 DO step 3b byte-confirmed finding); a record-level join is impossible and fabricating a proxy key would violate §2 fail-closed. The per-year `_raw` representation is the **lossless union** of two source segments discriminated by a synthetic `link_segment` column: `link_segment="den"` = every 91-byte `LinkCO{yy}USden.dat` record via `LINKED_BIRTH_1983_1988_FIELDS` (the aggregate birth denominator — ALL live births; one row per birth; no death section); `link_segment="num"` = every 500-byte `LinkCO{yy}USnum.dat` record via `LINKED_BIRTH_1983_1988_FIELDS` (locs 1-91) + `LINKED_NUM_DEATH_1983_1988_FIELDS` (locs 194-500, the ICD-9 mortality section) — the self-contained linked-infant-death set (MATCHS∈{1,2}; 92-193 numerator-only reserved, unenumerated per 3b). Lossless; H6 row-count conservation (den == guide-stated births; num == guide-stated infant deaths). `max_rows` caps **each segment independently** (refined at SMOKE — see below).

3. A top-of-`iter_parsed_records` `if 1983 <= year <= 1988: yield from _iter_two_file_1983_1988(...); return` branch — the 1989-2004/2005+ single-member body **byte-untouched** (§9-#7-safe; the 5a behavior-preserving discipline; the canonical 2005-2015 `parse_linked_year` path byte-identical, 2016-2023 uses `parse_linked_cohort_year`).

Plus a NEW sibling `DESIGN: tracks-current-state` SMOKE harness `natality/tests/test_linked_cohort_5b_numfinder_smoke.py` (19 tests; Convention 1/2; sub-step isolation). The `_layout_for_linked_year`/`_numerator_layout_for_linked_year` dispatchers + the 3a/3b/4a/4b/4c + 2005/2014 specs + `_find_denomplus_member` (5a) + `run_parse`/`_slice_field` are **byte-untouched** (zero deletion lines; H10/HALT-13). **Zero canonical parquet / schema / validation-CSV mutation** (11/11 gate parquet SHAs byte-exact at VERIFY-A).

**The 1983-1988 keyless one-row-per-birth-model methodology decision** (flagged by the 3b/5a receipts as a 5b PRE-FLIGHT concern) was resolved at the Convention-3 snapshot **before any code mutation**: lossless den+num union with a `link_segment` discriminator (NO fabricated record-level join); the harmonized `infant_death`/`record_weight` semantics for the keyless era = DO step 5c (den segment = one row per birth, `infant_death` null/unknown per-record since un-linkable; num segment = the within-era infant-death detail surface — a **documented within-era structural difference**, not a silent deviation: COMPARABILITY / harmonized_schema notes at 5c/6 + the manuscript Coverage paragraph = the known Phase-D **D.4** item). H7 sibling-parity: mirrors the fetal-death discipline of preserving source record structure + documenting within-era comparability rather than fabricating cross-era linkage. There is essentially ONE defensible no-double-count construction (the NCHS-canonical pre-2005 cohort method); within-task (NOT a Q42/Phase-B-2 trigger; NOT the methodology-paper-level cohort-vs-period question, resolved at DO step 1). Folded into soft-flag (ii) §11 human-merge.

**One Convention-3 divergence surfaced + resolved between PRE-FLIGHT and final DO (L10-safe, dated PRE_FLIGHT_LOG addendum 2026-05-19T08:30:00Z; the Task-1 2026-05-11T17:58:10Z precedent):** the new SMOKE was authored FIRST + run RED (ImportError vs the un-modified parser — §9-#9/L3), the DO functions authored, SMOKE re-run → **16 passed / 3 failed** (the 3 Tier-1 `..._real` tests: a single shared `max_rows` counter fills entirely from the multi-million-row den segment and never reaches num). **Root-cause verdict (§2/§9-#4 — fix the design, do NOT loosen the test):** not a parser bug and not a test to loosen — a genuine design defect of the shared-counter choice for a two-segment construction. `max_rows` is sampling-only (`max_rows=None` = the full lossless DO step 6 parse, identical under both); the faithful generalization for a multi-segment stream is **per-segment** (up to N den + up to N num). Refined `_iter_two_file_1983_1988` to a per-segment cap + updated its docstring + renamed/​rewrote the one affected Tier-0 test (`..._maxrows_shared_counter` → `..._per_segment_cap`); SMOKE re-run → **19 passed first-run** (no test loosened; the strengthened construction now samples both segments). Not a §7 halt (a within-task design refinement surfaced + resolved at the cheap-check SMOKE moment — exactly the Convention-3 mechanism).

### Inputs consumed

- `PRE_FLIGHT_LOG.md` 2026-05-19T08:00:00Z (per-sub-step PRE-FLIGHT; before any code mutation; L10-safe) + the 2026-05-19T08:30:00Z addendum (the shared→per-segment `max_rows` refinement). 9/9 forward-looking HALTs from `RECEIPTS/C8.18_step5a` re-verified; 11/11 gate parquet SHAs byte-exact; canonical pytest baseline `164 passed, 1 skipped, 1 xfailed in 339.45s` (4-dir) preserved; the methodology decision + 3 within-task scope decisions surfaced + resolved before DO (Convention 3); L17 grep-scope of all 6 cohort harnesses clean (no stale pin).
- `RECEIPTS/C8.18_step5a_2026-05-19T05-00-00Z.md` + `RECEIPTS/C8.18_step3b_2026-05-18T02-00-00Z.md` (the structural model: 1983-1988 = pure two-file, NO record-level public-use key; the 500-byte numerator IS self-contained; the 91-byte denominator is births-only).
- `natality/scripts/01_import/parse_linked_year.py` (`_find_denomplus_member` 5a + the cohort dispatchers — read-only, byte-untouched) + `zip_text_stream.iter_lines_from_zip` (the streaming reader; 1983-1988 = `ct=8` DEFLATE, stdlib path) + `field_specs.py` `LINKED_BIRTH_1983_1988_FIELDS` (BIRYR@2-5, MATCHS@1) + `LINKED_DEN_RECLEN_1983_1988`=91 + `LINKED_NUM_DEATH_1983_1988_FIELDS` (194-500) + `LINKED_NUM_RECLEN_1983_1988`=500 (state-on-disk 3a/3b; read-only).
- `~/Desktop/natality-harmonization/raw_data/linked/LinkCO{83,85,88}.zip` + `LinkCO05US.zip` — read-only, first ≤400 records/member (SMOKE Tier 1); SHA-anchored at C8.18 DO step 2.
- 11 gate parquets — SHA-verified byte-exact at the PRE-FLIGHT entry cheap-check + re-verified at VERIFY-A.

### Outputs produced

Git-tracked (this commit):

- `natality/scripts/01_import/parse_linked_year.py` — **purely additive** (`git diff --numstat` = **139 / 0**; two insertion-only hunks `@@ -126,6 +126,47 @@` [`_find_numerator_member`] + `@@ -329,11 +370,109 @@` [`_iter_two_file_1983_1988` + the 1983-1988 `iter_parsed_records` branch]; zero deletion lines — `_find_denomplus_member`/dispatchers/1989-2004+2005+ body/`run_parse`/`_slice_field` byte-untouched, grep-confirmed).
- `natality/tests/test_linked_cohort_5b_numfinder_smoke.py` — NEW; `DESIGN: tracks-current-state`; 19 tests (Tier 0: 5 per-era numerator resolution + 4 fail-closed L3 negatives + 1 den/unl-not-matched + 4 two-file-construction shape/conservation/per-segment-cap/bad-len + 1 position-shift negative; Tier 1: 3 real `LinkCO{83,85,88}` two-file construction + 1 full-year den≫num + 1 the-2005-regression-unchanged).
- `PRE_FLIGHT_LOG.md` — the C8.18 DO step 5b PRE-FLIGHT entry (2026-05-19T08:00:00Z) + the 2026-05-19T08:30:00Z per-segment-`max_rows` addendum (both before final DO close; L10-safe).
- `RECEIPTS/C8.18_step5b_2026-05-19T08-00-00Z.md` — this file.
- `DECISION_LOG.md` + `STATUS.md` — appended (the keyless construction methodology + the per-segment refinement + new dated section).

No canonical parquet / `harmonized_schema.csv` / validation-CSV touched. Raw zips/guides gitignored + out-of-tree (read-only, unchanged). §15.D NOT edited this step (the broader model-clarification — incl. the 1983-1988 keyless construction model + `_find_numerator_member` + the per-segment refinement — stays soft-flag (ii) §11 human-merge, the 3a/3b/4a/4b/4c/5a precedent).

### Five-phase trace

- **PRE-FLIGHT**: ✓ `PRE_FLIGHT_LOG.md` 2026-05-19T08:00:00Z (per-sub-step, before any code mutation; L10-safe) + the 08:30:00Z addendum. 9/9 forward-looking HALTs re-verified; 11/11 gate parquet SHAs byte-exact; canonical pytest 164P+1S+1XF/339.45s baseline preserved; the methodology decision + 3 scope decisions + the L17 grep-scope (clean) surfaced + resolved before DO (Convention 3).
- **SMOKE**: ✓ NEW harness authored FIRST (§9-#9). Run vs the **un-modified** parser = **collection ImportError** (`cannot import name '_find_numerator_member'`) — 0 pass: proves the harness genuinely requires the DO code (L3/§9-#5, not a rubber-stamp). After DO = **16 passed / 3 failed** → the per-segment `max_rows` design defect surfaced (PRE_FLIGHT_LOG addendum 08:30:00Z; §2/§9-#4 fix the design, not the test) → after the refinement **19 passed first-run** (no test loosened). Tier 0 = synthetic real-`.zip` per-era resolution + L3 fail-closed negatives + the two-file construction shape; Tier 1 = the real `_iter_two_file_1983_1988` end-to-end path on `LinkCO{83,85,88}` + the 2005 regression.
- **DO**: ✓ additive `_find_numerator_member` + `_iter_two_file_1983_1988` + the top-of-`iter_parsed_records` 1983-1988 branch + the per-segment-`max_rows` refinement + the new SMOKE harness. Single commit (PRE-FLIGHT + DO + RECEIPT; the C8.18 DO 3a/3b/4a/4b/4c/5a precedent).
- **VERIFY**: ✓ all 5 criteria (below).
- **RECEIPT**: ✓ this file.

### Verify results

- **VERIFY-A (zero canonical mutation)**: PASS — 11/11 gate parquet SHAs byte-exact vs PRE-FLIGHT HALT 2 + the 5a receipt VERIFY-A: natality `c8a740eb…`/`acb5c48a…`; `.v28_baseline` `230efed2…`/`e16ad532…`; linked-derived `9b828a4d…`; fetal-death `38e2cecb…`(harm)/`185c071e…`(deriv) (via the `~/Desktop/fetal-death-harmonization-build/` tree, soft-flag (hh)); MM `adbec108…`(harm)/`5c22308b…`/`7c682668…`/`d98b4296…`. Threshold: 11/11 unchanged.
- **VERIFY-B (git scope)**: PASS — modified/new = `parse_linked_year.py`, `test_linked_cohort_5b_numfinder_smoke.py` (new), `PRE_FLIGHT_LOG.md`, + `RECEIPTS/`/`DECISION_LOG.md`/`STATUS.md`. NO `.parquet`/`harmonized_schema.csv`/validation-CSV. Threshold: scope ⊆ {scripts, test, plan, state}.
- **VERIFY-C (additive / behavior-preserving)**: PASS — `git diff --numstat` = `parse_linked_year.py` **139 / 0** (purely additive; two insertion-only hunks at function boundaries; **zero deletion lines** → `_find_denomplus_member` + `_layout_for_linked_year` + `_numerator_layout_for_linked_year` + the 1989-2004/2005+ `iter_parsed_records` body + `run_parse`/`_slice_field` byte-untouched); `field_specs.py` **0 / 0** (not in the diff). The canonical 2005-2015 `parse_linked_year` selection is byte-identical (the 1983-1988 branch never fires for the canonical v3 build); locked by `test_tier1_2005_regression_unchanged` (no `link_segment` key) + the 6 prior cohort harnesses' `*_dispatcher_unregressed` tests (PASS within the 183).
- **VERIFY-D (pytest count gate)**: PASS — canonical **4-dir** suite `fetal_death/ natality/ tests/ matched_multiples/` = **183 passed, 1 skipped, 1 xfailed in 341.54s**. Reconciles exactly: the 5a-close baseline 164P+1S+1XF preserved (zero regression; 1 skipped + 1 xfailed unchanged; 164 still pass — incl. all 6 prior cohort harnesses, byte-untouched dispatchers) **+** 19 new 5b SMOKE all pass = 183P. Runtime 341.54s within the ~240-380s **advisory** band (soft-flag (kk) — the cohort harnesses accumulate real-zip Tier-1 I/O; per §8 L17 / HALT 9 the **COUNT is the gate**, PASS; not a §7 halt; reproducibility intact — 11/11 gate SHAs byte-exact).
- **VERIFY-E (SMOKE all PASS)**: PASS — new 5b harness **19/19** first-run after the per-segment refinement. Tier 0: per-era numerator resolution (1983-1991/1995-2002 mixed+upper/2003/2004+) + 4 fail-closed L3 negatives (den+unl-only, 2003-den+unm-only, empty, ambiguous-two-NUM — all RAISE) + the den/unl-not-matched discriminator + the two-file construction (segment tagging + H6 row-count conservation + per-segment cap + bad-length skip + position-shift L3 negative). Tier 1 (real SHA-anchored zips): the real `_iter_two_file_1983_1988` resolves den (`USden.dat`, 5a) + num (`USnum.dat`, new finder), parses both segments with `BIRYR`==cohort year (encoding+alignment+reclen jointly) + num `MATCHS`∈{1,2} + den≫num; the 2005 single-member path carries no `link_segment` (regression-lock).

### Reproducibility

- `_find_numerator_member` is deterministic (pure function of the zip namelist). `_iter_two_file_1983_1988` is a deterministic two-pass read (den then num) of fixed-width members; re-running yields identical rows. The SMOKE harness re-derives resolution + construction from synthetic + real archives → deterministic.
- Zero canonical parquet rebuild (DO step 5b is parser substrate only; the re-harmonize is DO step 6). H10 gate: 11/11 SHAs byte-exact.
- No new Python dependency (1983-1988 members = `ct=8` DEFLATE → stdlib `zipfile`; the C8.5a pinned-env SHA untouched).
- The SMOKE Tier 1 is re-runnable from the SHA-anchored cohort zips; the construction model + the per-segment refinement are state-on-disk in `PRE_FLIGHT_LOG.md` 2026-05-19T08:00:00Z + the 08:30:00Z addendum.

### Cross-product re-probe (if applicable)

- N/A — DO step 5b is additive linked parser-plumbing only; no cross-product artifact consumes it until DO step 5c (harmonize) / DO step 6 (re-harmonize). 11/11 gate parquet SHAs byte-exact (incl. fetal-death + natality + MM, untouched). The canonical v3 2005-2023 linked product is unperturbed.

### Git

- Pre-DO rollback anchor: `C8.18-pre-do` @ `6632a15` (unchanged; the C8.18 DO rollback anchor).
- This commit: intermediate C8.18 DO step 5b — **NO tag** (Convention 5; `C8.18-complete` is the final-sub-step-only tag).

### STATUS.md updated

- New section dated 2026-05-19T08:00:00Z at the top (newest-first).

### Self-check (§10) — what could I have gotten wrong that VERIFY wouldn't catch?

1. **The harmonized 1983-1988 IMR semantics are NOT verified at 5b** (5b is parser substrate; VERIFY confirms parse correctness — BIRYR==year, num MATCHS∈{1,2}, den≫num, H6 conservation — but not the harmonized `infant_death`/`record_weight` rate encoding). A wrong 5c harmonized encoding could double-count or mis-rate. *Mitigation*: the construction is lossless + no-double-count at the parser level (the `link_segment` discriminator makes provenance explicit); the methodology is fully documented (DECISION_LOG + PRE_FLIGHT). *Recommendation*: DO step 5c/6 PRE-FLIGHT must value-distribution-verify the harmonized 1983-1988 cohort IMR vs the NCHS published cohort Linked File report tables (the §15.D VERIFY criterion "per-year cohort counts 1983-2004 match … byte-exact"). Logged DECISION_LOG residual risk.
2. **`_find_numerator_member` cross-era correctness for 1995-2004 is asserted via synthetic member-name sets** (Tier 0), not exercised end-to-end on the real 1995-2004 numerator (5b's Tier-1 is 1983-1988-only — that is the two-file construction; the 1995-2004 numerator is the den-plus path's richer-multiple-cause surface, read at 5c/6). A real 1995-2004 `USNUM` member-name surprise would not be caught at 5b. *Mitigation*: fail-closed RAISE on zero/>1; the 5a real-data probe already confirmed the 1995-2004 member-name conventions; DO step 5c/6 full-parse surfaces any mis-resolution as a row-count/reclen mismatch. Logged DECISION_LOG residual risk.
3. **The 1983-1988 den + numerator-natality-section layouts are the 3a/3b state-on-disk layouts**, not re-derived at 5b; only the BIRYR/MATCHS anchors + reclen + encoding are jointly value-verified at 5b Tier 1. A latent 3a/3b non-anchor layout error would not be caught at 5b. *Mitigation*: 3a/3b already value-distribution-verified the full layouts field-by-field on real data (state-on-disk + the 3a/3b receipts). *Recommendation*: DO step 5c/6 full value-distribution-verifies the per-era field map. Logged DECISION_LOG residual risk (carry of the 3b self-check).
4. **Behavior-preservation for the canonical 2005-2023 path is asserted via the additive diff (139/0, byte-untouched body) + the SMOKE 2005 regression-lock + the 164-baseline-preserved dispatcher-unregressed tests**, not a full 19-year re-harmonize (DO step 6). A latent perturbation visible only in the full re-harmonize would not be caught here. *Mitigation*: the 1983-1988 branch only fires for 1983-1988 (never parsed by the canonical v3 build; 2005-2015 = the byte-untouched body, 2016-2023 = `parse_linked_cohort_year`); 11/11 gate SHAs byte-exact. *Recommendation*: DO step 6's 2005-2023 byte-clean regression vs the v3 baseline is the durable §15.D check. Logged DECISION_LOG residual risk.
5. **The per-segment `max_rows` refinement** changes the sampling semantics vs the originally-documented shared counter. *Mitigation*: `max_rows` is sampling-only; the canonical DO step 6 parse uses `max_rows=None` (lossless, byte-identical under both); the refinement is documented (PRE_FLIGHT_LOG addendum 08:30:00Z + docstring + DECISION_LOG); the construction is brand-new at 5b so no canonical/prior path depends on the old semantics.
6. **`link_segment` exists only on 1983-1988 rows** (1989+ untouched, behavior-preserving). DO step 5c must unify the v4 harmonized schema across eras; if 5c drops the segment distinction the keyless-era den/num provenance is lost. *Mitigation*: documented as a 5c/6 forward item (PRE-FLIGHT + this receipt's Forward-looking HALTs); the within-era structural difference is a known D.4 manuscript-Coverage item. Logged DECISION_LOG residual risk.

Each logged in `DECISION_LOG.md` 2026-05-19T08:00:00Z as a residual risk; the recommended checks are DO-step-5c/6 PRE-FLIGHT items (not added to VERIFY without human sign-off per §10).

### Forward-looking HALTs for next session (C8.18 DO step 5c)

1. `C8.18-pre-do`@`6632a15`; `C8.18-complete` NOT present (final-sub-step-only). `C8.17-complete` present. HEAD = this DO step 5b commit (after `506581c`).
2. **11 gate parquet SHAs unchanged** (the same 11; fetal-death via the `~/Desktop/fetal-death-harmonization-build/` tree, soft-flag (hh)). Linked-derived `9b828a4d…` changes only at the later C8.18 re-harmonize DO step 6 (5a/5b/5c are parser/harmonize substrate only).
3. **Canonical pytest baseline is now `183 passed, 1 skipped, 1 xfailed`** (was 164P+1S+1XF; +19 from the new `test_linked_cohort_5b_numfinder_smoke.py`, `DESIGN: tracks-current-state`). The canonical suite is the **4-dir** `fetal_death/tests/ natality/tests/ tests/ matched_multiples/tests/`. Runtime ~340s clean (advisory band, creeping as cohort Tier-1 accumulates real-zip I/O; the COUNT is the gate) → soft-flag (kk). README 3-dir line stays stale → soft-flag (jj).
4. **`parse_linked_year` now exposes `_find_numerator_member`** (cross-era `"NUM"`-not-`UNL`/`UNM`, fail-closed) **+ `_iter_two_file_1983_1988`** (the 1983-1988 lossless den+num union with the `link_segment` discriminator; per-segment `max_rows`) **+ a top-of-`iter_parsed_records` 1983-1988 branch**. State-on-disk; do NOT re-derive. **DO step 5c = `harmonize_linked_v3.py` per-cohort-era field mapping** (the cohort raw names `MATCHS`/`IDNUMBER`/`BIRYR`/`DMAGE`/… are entirely disjoint from the 2005+ names the harmonizer reads — a large mapping surface) **+ the harmonized 1983-1988 one-row-per-birth / `infant_death` / `record_weight` encoding for the `link_segment="den"`/`"num"` segments** (the keyless-era methodology: den = one row per birth, `infant_death` null/unknown per-record since un-linkable; num = the within-era infant-death detail surface; a **documented within-era structural difference** — manuscript Coverage = D.4) **+ ICD-9 default-null/revision-tagged + `cause_recode_130` per-era** (soft-flag (gg)).
5. **The 1983-1988 keyless construction methodology is RESOLVED + state-on-disk** (`DECISION_LOG.md` 2026-05-19T08:00:00Z + `PRE_FLIGHT_LOG.md` 2026-05-19T08:00:00Z + the 08:30:00Z per-segment addendum): lossless den+num union, NO fabricated record-level join, `link_segment` discriminator. 5c implements the harmonized semantics; do NOT re-open the no-key construction (it follows necessarily from the NCHS public-use data structure — there is one defensible no-double-count form).
6. **Encoding = ASCII** — `_slice_field` `.decode("latin-1")`; re-confirmed at 5b on real `LinkCO{83,85,88}` den + num members.
7. §15.D substrate-format reconcile applied at the 3a commit; the broader §15.D model-clarification — now ALSO folding the **1983-1988 keyless self-contained-numerator + aggregate-denominator construction model + `_find_numerator_member` + the per-segment `max_rows` refinement** alongside the 3a/3b + 4a/4b/4c + 5→5a/5b/5c decomposition + the DEFLATE64-at-2003-already-wired finding + the 4b/4c falsifications — remains **soft-flag (ii)** proposed-not-applied for §11 human-merge (queued with the soft-flag-(w) batch). On-disk PRE_FLIGHT_LOG/DECISION_LOG/RECEIPTs authoritative in the interim.
8. **soft-flag (gg)**: the numerator-only richer multiple-cause detail (`ENTITY`/`RECORD` axes; `cause_recode_130` per-era; ICD-9 1983-1998 vs ICD-10 1999+ underlying; the 2003-rev "Vers*" null-pattern) + the harmonized cause-column shape stay DO step 5c/6. For 1983-1988 the FULL 500-byte numerator (incl. the ICD-9 mortality section per the 3b layout) is now parsed into the `link_segment="num"` rows; 5c decides the harmonized cause representation.
9. **L17 forward-looking discipline**: any later C8.18 sub-step (5c/6) that configures a new parser/harmonize path or year MUST grep-scope ALL **7** cohort SMOKE harnesses' negative-year/`pytest.raises` sets and bundle the minimal `tracks-current-state` Edit into the same commit. 5b added NO new dispatcher year (only `_find_numerator_member` [no year arg] + the 1983-1988 branch; 1983-1988 already configured by 3a/3b; 1992-1994 still raise via the unchanged dispatchers) → introduced NO stale pin; the new 5b harness pins no future-year-raises case. The manuscript Coverage re-paragraph (1983-2023 linked incl. the keyless 1983-1988 within-era structural note) = Phase-D **D.4**. pytest COUNT 183P+1S+1XF is the gate; runtime band advisory (soft-flag (kk)).

### Notes for next session

- **C8.18 DO step 5b is CLOSED.** `parse_linked_year._find_numerator_member` (cross-era, fail-closed) + `_iter_two_file_1983_1988` (the 1983-1988 lossless den+num union with the `link_segment` discriminator; per-segment `max_rows`) + the behavior-preserving `iter_parsed_records` branch are state-on-disk + this receipt + `PRE_FLIGHT_LOG.md` 2026-05-19T08:00:00Z + the 08:30:00Z addendum. The keyless one-row-per-birth-model methodology is RESOLVED (lossless union, no fabricated join; the harmonized semantics + the within-era structural-difference documentation = 5c/6 + D.4). Next per the 5→5a/5b/5c decomposition: **DO step 5c — `harmonize_linked_v3.py` per-cohort-era field mapping + the keyless-era harmonized encoding + ICD-9 default-null/revision-tagged + `cause_recode_130` per-era** (soft-flag (gg); the largest mapping surface; ~1-2 sessions), then DO step 6 (re-harmonize linked 1983-2023; v3→v4) + DO step 7 (docs/metadata; tag `C8.18-complete`).
- The §2 cheap-before-expensive + the 3a-substrate-first / §9-#8 / §9-#9 discipline governed 5b (verify the low-risk plumbing increment; isolate the keyless construction into its own SMOKE'd sub-step; write the fixture before the code; fix the design — not the test — when SMOKE surfaces the shared-counter defect). Carry it.
- The §15.D model-clarification `[plan-update]` (soft-flag (ii)) is owed to the human (§11; queued with the soft-flag-(w) batch; now also folds the 1983-1988 keyless construction model + `_find_numerator_member` + the per-segment refinement); the on-disk PRE_FLIGHT_LOG/DECISION_LOG/RECEIPTs are authoritative in the interim.
- Runtime advisory-band creep (~340s clean) = soft-flag (kk); environmental cohort-Tier-1 I/O, not a canonical regression (11/11 gate SHAs byte-exact; the COUNT 183P+1S+1XF is the gate).
- `/tmp/c8_18_s5b_*` scratch (gate-SHA + pytest output files) is OS-cleanable + reproducible.
