# Receipt: C8.18 DO step 7 — docs/metadata consumer-surface sync (linked v3→v4: 1983-2023; 149,386,620 rows; permanent 1992-1994 gap) + tag `C8.18-complete` (the FINAL C8.18 sub-step)
## 2026-05-23T05:00:00Z

### What was done

Propagated the linked **v3 → v4.0.0** backward extension (1983-2023; 149,386,620 records; permanent 1992-1994 NCHS-linkage gap; +3 within-era cohort columns) — the canonical mutation shipped at C8.18 DO step 6b — into the consumer-facing documentation + metadata surface, and **closed C8.18** by tagging `C8.18-complete`. **Docs-only: zero parquet / `harmonized_schema.csv` / validation-CSV-row / test / script / metadata-CSV mutation** (re-verified — 10/11 gate parquet SHAs byte-exact, the linked-derived gate = the v4 `f630d8cf…` unchanged since 6b, `.v3_baseline` = `9b828a4d…` preserved; `git diff` scope is 16 markdown/json/cff docs + `PRE_FLIGHT_LOG.md`). §15.D scopes DO step 7 to 6 named files + the C8.18-specific cross-product `docs/COMPARABILITY.md`; a Convention-3 PRE-FLIGHT Field-value snapshot surfaced ~10 more consumer files carrying stale linked-v3 strings, resolved under the **C8.17 step 7 "Option A — Honest propagation" precedent** + the user's standing 2026-05-23 "make all relevant decisions yourself" authorization (documented in DECISION_LOG; NOT a §7-#17 halt — precedented + standing-authorized). 16 content docs edited. Three plan-mandated/precedented deferrals were taken (manuscript Coverage re-paragraph → Phase-D D.4; `file_inventory.csv` `imported`-flag refresh → Phase-D metadata-sync; the script-generated `external_validation_v3_linked_comparison.md` full-v4-surface regeneration → validator/Phase-D follow-up) and recorded as forward-looking HALTs. VERIFY-D (residual stale-string audit) caught **6 stale lines I missed** in the first DO pass (NCHS_SOURCE_MANIFEST ×4 incl. footer counts; FAQ ×1; VALIDATION reproduce ×1) — all corrected in-DO-scope (the C8.17 step7 `quickstart.R` VERIFY-D-catch precedent; L7 / no-rubber-stamp working as designed). This closes **C8.18** in full (DO steps 1–7 all shipped).

### Inputs consumed

- `RECEIPTS/C8.18_step6b_2026-05-23T02-00-00Z.md` — the 8 forward-looking HALTs (entry cheap-check; all verified).
- `RECEIPTS/C8.17_step7_2026-05-17T04-00-00Z.md` — the sibling docs-only-version-propagation precedent (Option-A Honest-propagation; discriminating principle; the manuscript-D.4 / convenience-parquet deferral pattern).
- `STATUS.md` 2026-05-23T02:00:00Z + `NEXT_STEPS.md` §15.D DO step 7 + KICKOFF "Current planned sequence".
- 11 gate parquets — SHA-verified at PRE-FLIGHT (`/tmp/c8_18_s7_gate_sha.txt`) and re-verified at VERIFY-A (10 byte-exact; linked-derived = the v4 `f630d8cf…`; `.v3_baseline` = `9b828a4d…`).
- Authoritative v4 numerics derived (L6, not invented): `pq.read_schema` (harmonized 81 cols / derived 97 cols / 149,386,620 rows); `tests/snapshots/v3_2026-05-23T02-00-00Z_columns.csv`; `harmonized_schema.csv` (97 rows; the gap in `years_available`); `file_inventory.csv` counts (43 FD + 95 nat[57+38 linked] + 3 MM = 141 zips).
- The pre-edit content of all 16 scoped files (Field-value snapshot in `PRE_FLIGHT_LOG.md` 2026-05-23T05:00:00Z).

### Outputs produced

Docs-only; no binary artifacts. Git-tracked files MODIFIED (17; `git diff --name-only`):

- **Tier A (§15.D-named + C8.18-specific):** `natality/.zenodo.json` (description/notes — linked 149.4M/1983-2023/v4-companion; version+title preserved natality-anchored, Zenodo patch = Phase-D D.2), `CITATION.cff` (in-repo-state notes: + linked v4.0.0 clause), `natality/docs/ABOUT_THIS_RELEASE.md` (V3/v4 section rewrite + the 4 framing notes + output table + validation summary + key-policies linked bullets + reproduce block), `natality/README.md` (coverage/metrics/columns/validation/output-table/zenodo-paragraph/reproduce/honest-validation), `PROJECT_STRUCTURE.md` (natality subproject line), `README.md` (mirror line + four-products Linked row + the `docs/` manifest-count line — fix-on-contact L11), `docs/COMPARABILITY.md` (cross-product timeline + joint-coverage envelope + the Linked-cohort-only pointers expanded with the 4 framing notes; the WRONG "pre-2005 NOT processed" line superseded).
- **Tier B (Option-A consumer files):** `CHANGELOG.md` (new C8.18/linked-v4.0.0 entry under v1.1-WIP Data extensions; [v1.0] history untouched), `VERSION_ROADMAP.md` (Currently-shipped linked row; Planned section deliberately untouched — pre-existing-stale soft-flag (dd)), `docs/NCHS_SOURCE_MANIFEST.md` (header total/inventory-count, the stale "later DO step"/"still 2005-2023 only" caveat → 6b-shipped, the footer envelope/zip-count lines — all fix-on-contact), `natality/docs/GETTING_STARTED.md` (load-comment/output-table/reproduce), `natality/docs/FAQ.md` (coverage/why-1983/file-picker col-count), `natality/docs/VALIDATION.md` (header + a new "Pre-2005 cohort validation" subsection + reproduce; the 2005-2023 measured prose preserved), `natality/REPRODUCING.md` (deposit-contents + source-URL coverage), `natality/docs/CODEBOOK.md` (header/intro + record-layout-eras table + death-side columns table: 3 new column rows + era-aware notes; the deep per-variable historical body remains C8.20-deferred), `natality/docs/COMPARABILITY.md` (header + a new "Pre-2005 cohort backward extension" subsection + the `infant_death` keyless note).
- `PRE_FLIGHT_LOG.md` — DO step 7 PRE-FLIGHT entry (prepended, L10-safe, pre-DO).

**Deliberately NOT touched** (precedent-consistent / plan-mandated deferrals): `paper/draft_v2_hmd_styled.md` (manuscript Coverage = Phase-D D.4 — KICKOFF "re-paragraphed ONCE at D.4 after all data lands" + C8.17 step7 precedent + C8.19 still pending); `natality/metadata/file_inventory.csv` `imported` flags (Phase-D metadata-sync; out of §15.D named scope; 6b deliberately didn't flip; has its own invariant test); `natality/output/validation/external_validation_v3_linked_comparison.{md,csv}` (script-generated by `compare_external_targets_v3_linked.py`; the 6b fix-on-contact scoped it to the 2005-2023 owned surface; full-v4-surface regen = validator/Phase-D follow-up); `docs/PIPELINE_TIMING_BENCHMARK.md` + `notebooks/README.md` (measured / unrebuilt artifacts — the C8.17 step7 precedent left them untouched; the soft-flag (ee) refresh now also covers linked v4); `natality/quickstart.R` + `docs/JOINT_USE_GUIDE.md` (no stale linked-coverage string — verified).

### Five-phase trace

- **PRE-FLIGHT**: ✓ `PRE_FLIGHT_LOG.md` 2026-05-23T05:00:00Z (L10-safe; precedes the first DO mutation). 8/8 6b forward-HALTs verified; 11-gate SHA proof (10 byte-exact + linked-derived `9b828a4d…`→`f630d8cf…` v4 by design + `.v3_baseline`==`9b828a4d…`; mtimes predate DO-7); git clean on `main`@`127f101`; Convention-3 Field-value snapshot of every scoped doc; the 3 scope decisions documented.
- **SMOKE**: ✓ Per-file exact old→new string enumeration via targeted Reads (L1-clean — every file Read-verified; exact unique old-strings captured for surgical Edits). Discriminating principle defined (SWAP coverage/version/count/framing; PRESERVE validation-scope + historically-correct + unrebuilt-artifact + the `natality_v3_linked_*` schema-family filename).
- **DO**: ✓ 16 content docs + the PRE-FLIGHT entry edited via surgical `Edit`. Zero parquet/schema/test/script/metadata-CSV mutation (VERIFY-A).
- **VERIFY**: ✓ all 5 criteria (below); VERIFY-D caught + corrected 6 missed stale lines in-DO-scope (re-swept clean).
- **RECEIPT**: ✓ (this file) + DECISION_LOG + STATUS appended; `C8.18-complete` tag.

### Verify results

- **VERIFY-A — git scope docs-only + 11-gate unchanged**: PASS. `git diff --name-only` = 16 docs (`.md`/`.cff`/`.json`) + `PRE_FLIGHT_LOG.md`; zero `.py`/`.csv`/`harmonized_schema`/`/tests/`/`/scripts/`/`metadata/`/parquet. 10/11 gate parquet SHAs byte-exact vs the 6b receipt; linked-derived = the v4 `f630d8cf…` (unchanged since 6b — no rebuild at DO step 7); `.v3_baseline` = `9b828a4d…` preserved (do NOT restore). Parquet mtimes (May 13/18) predate DO-step-7 work. Threshold: zero non-doc files; value: zero.
- **VERIFY-B — 1992-1994 gap documented in schema + CODEBOOK + ABOUT (manuscript = D.4-deferred)**: PASS. `harmonized_schema.csv` (6b-set; 11 hits), `natality/docs/CODEBOOK.md` (9 hits — header/intro + record-layout-eras + death-side rows), `natality/docs/ABOUT_THIS_RELEASE.md` (3 hits — section + the dedicated pre-2005 note). The §15.D criterion's manuscript clause is satisfied via the documented Phase-D / D.4 deferral (the C8.17 step7 manuscript-Coverage-deferral precedent; `paper/draft_v2_hmd_styled.md` NOT in `git diff`).
- **VERIFY-C — pytest count gate (Convention 1; HALT-9 deferred-to-VERIFY; memory-safe per-dir + tests/-per-file split per the 6b receipt — the single-process full-suite OOM-SIGKILLs on the 2 GB v4)**: PASS — **347 passed, 1 skipped, 1 xfailed, 0 failed, 0 error** — byte-identical to the 6b baseline (zero regression). Authoritative per-segment (every segment's full summary printed — the 6b self-check #6 lesson: never hand-summate): `fetal_death/` 12P+1XF; `natality/` 265P; `matched_multiples/` 11P; `tests/` top-level 6 files 53P (`test_canonical_filter_invariants` 12 + `test_cross_product_join_parity` 13 + `test_inventory_invariants` 3 + `test_parquet_column_snapshot` 6 + `test_row_count_conservation` 16 + `test_source_zip_sha_stability` 3); `tests/mutations/` 6P+1S (the 1 skipped = `test_validate_linked_parquets_mutation.py`, pre-existing). 12+265+11+53+6 = 347P; +1S +1XF. `test_inventory_invariants` 3P confirms `file_inventory.csv` untouched (the `imported`-flag deferral did not perturb its invariant test). Logs `/tmp/c8_18_s7/pt_*.log` (the initial driver glob missed the `tests/mutations/` subtree — caught by reconciling against the 6b authoritative baseline `/tmp/c8_18_s6/pytest_FINAL2_6b.log`, re-run as `pt_mutations_dir.log`; no hand-summation of an ambiguous split — the §4.4/self-check-#6 discipline). Docs-only changes provably cannot perturb the parquet/schema/CSV-reading test surface (no test reads any edited `.md`/`.cff`/`.json`; the C8.17 step7 VERIFY-C precedent) — empirically confirmed.
- **VERIFY-D — residual stale-string audit**: PASS (after 6 in-DO-scope corrections). The first DO pass left 6 stale lines (`docs/NCHS_SOURCE_MANIFEST.md` L205 "still covers 2005-2023 only", L237 footer coverage, L239 "122 zip × 64 = 7,808", L240 "76 rows", L241 envelope/joint-coverage; `natality/docs/FAQ.md` L18 "(94 columns)"; `natality/docs/VALIDATION.md` L284 `harmonize_linked_v3.py --years 2005-2023`); VERIFY-D caught all 6 (L7 / no-rubber-stamp — the C8.17 step7 `quickstart.R:8` precedent), corrected in-DO-scope, re-swept clean. Every remaining `74,943,824`/`2005-2023`/`94`-string residual is a documented intentional retention: (a) CHANGELOG L22 — the C8.18-entry provenance equation `149,386,620 = 74,442,796 + 74,943,824` + "byte-clean 2005-2023 slice"/"schema-additive over the v3 2005-2023 surface" (correct context; the C8.17 `201,161,456 = …` provenance-equation precedent); (b) CHANGELOG L87 — `[v1.0]` append-only release history (the C8.17 "[v1.0] history untouched" precedent); (c) the v3-validator 2005-2023 owned-surface scope + the `invariants_report_v3_linked_2005_2023.*` filenames + the measured 2005-2023 prose (birthweight offset / age-at-death / IMR-trend) + the annotated unrebuilt residents_only convenience numbers — all validation-scope/measured/unrebuilt-artifact intentional retentions per the discriminating principle.
- **VERIFY-E — version/framing internal consistency**: PASS. Robust per-file loop: `149,386,620` present across the headline-count surface (CHANGELOG/CITATION/README/VERSION_ROADMAP/natality README/ABOUT/GETTING_STARTED); `1983–2023` across all 16 content docs; `v4.0.0`/linked-v4 across the version surface; the 1992-1994 gap across all surfaces; the canonical filename `natality_v3_linked_*` retained everywhere (zero `natality_v4_linked`); no edit claims linked v4 is on Zenodo (every version line says pending / not-yet-deposited). `natality/.zenodo.json` validates as JSON; `CITATION.cff` notes is a well-formed double-quoted scalar (no internal `"`).

### Reproducibility

Deterministic + idempotent — docs-only text edits; no build step. Re-running VERIFY (`git diff --name-only`, `shasum`, `git grep`, `pytest`) reproduces the same results. Canonical artifacts unchanged (VERIFY-A), so the C8.18 DO-step-6b reproducibility guarantee carries forward unaltered.

### Cross-product re-probe

10 non-linked-derived canonical parquet SHAs byte-exact (VERIFY-A) → fetal-death, natality-v2, matched-multiples provably untouched. The linked-derived gate is the v4 `f630d8cf…` set at 6b (unchanged by this docs-only step). No downstream task depends on DO step 7 docs output beyond the next session's reads + the Phase-D deferrals it records.

### Git

- Pre-DO rollback anchor: `C8.18-pre-do` @ `6632a15` (unchanged; set at C8.18 start; not re-tagged at intermediate DO steps per Convention 5).
- Post-RECEIPT tag: **`C8.18-complete`** — set at this commit (DO step 7 is the FINAL C8.18 sub-step; C8.18 DO steps 1–7 all shipped → unblocks C8.19).
- Commit: ~5-line summary per Convention 5; full narrative here.

### STATUS.md updated

New section dated 2026-05-23T05:00:00Z at the top marking **C8.18 COMPLETE** (DO steps 1–7 all shipped; linked is v4 1983-2023/149,386,620/permanent 1992-1994 gap); next task = **C8.19** (perinatal-record pre-joined parquet) per KICKOFF Tier 3+5 sequence.

### Self-check (§10) — what could I have gotten wrong that VERIFY wouldn't catch?

1. **A paraphrased stale coverage/count string VERIFY-D's regexes didn't cover** (e.g., a spelled-out "nineteen cohort years", a "2005 onward" written without a year-range, a column count embedded in prose). *Mitigation*: VERIFY-D swept `74,943,824|74.9M|74.9 million|122 zip|76 rows|7,808|(94 columns)|still covers …2005-2023|2005-2023 linked-cohort coverage|19 contiguous cohort years 2005-2023|harmonize_linked_v3.py --years 2005-2023` over the union of edited files and caught 6 real misses; the headline v4 numerics were positively confirmed present (VERIFY-E robust per-file loop). Residual risk: a paraphrase the patterns missed — plausible-but-low (the consumer surface uses the canonical numeric forms).
2. **The `natality_v3_linked_harmonized.parquet` 81-col / `…_derived` 97-col split could be mis-stated relative to the harmonized_schema 97-row count** (schema rows ≠ a single parquet's column count: the schema includes V2-only + linked-only + derived rows). *Mitigation*: counts taken from `pq.read_schema` on the actual v4 parquets (81 / 97) — the authoritative source — not the schema-row count; the "78 v3 + 3" / "94 v3 + 3" framing is the derivable provenance (78+3=81, 94+3=97). Logged.
3. **The manuscript-D.4 deferral could be a mis-drawn judgment** if a reviewer reads §15.D / the 6b forward-HALT #4 "DO step 7 = … + the manuscript D.4 Coverage re-paragraph" as "do the re-paragraph now". *Mitigation*: KICKOFF "Manuscript Coverage paragraph re-paragraphed ONCE at D.4 after all data lands" + STATUS "Carried-forward (Phase D): manuscript Coverage re-paragraph = D.4" + the C8.17 step7 precedent (manuscript NOT edited; the receipt's "manuscript-facing headline counts" meant the ABOUT/zenodo/README consumer surface, not `paper/`) + C8.19 still pending ("all data" not landed). The framing notes ARE carried into COMPARABILITY/ABOUT/CODEBOOK now; the manuscript Coverage *paragraph* is the single-pass D.4 deliverable. Recorded as a Phase-D forward-HALT. A defensible reading either way — logged in DECISION_LOG so it is auditable / re-openable.
4. **The `file_inventory.csv` `imported`-flag deferral leaves a one-bit H8 docs-vs-data drift** (`imported=false` for the 19 pre-2005 cohort rows that ARE harmonized into v4). *Mitigation*: out of the §15.D DO-step-7 named scope; the 6b canonical step deliberately did not flip it; flipping it in a docs-only step would mutate a metadata CSV with its own `test_inventory_invariants.py` (widening VERIFY-A blast radius / §7-#17). The `docs/NCHS_SOURCE_MANIFEST.md` narrative now states the harmonize SHIPPED at 6b + the flag-refresh is a tracked Phase-D follow-up (so no doc *claims* it's unharmonized). Recorded as a forward-HALT. The exact C8.17 step7 metadata/convenience-refresh deferral pattern.
5. **A judgment mis-draw on the discriminating principle** — e.g., I preserved the PIPELINE_TIMING_BENCHMARK / notebooks/README / residents_only-convenience numbers as measured/unrebuilt artifacts; if a reader expects those to track the v4 envelope they may read them as stale. Documented as deliberate (the C8.17 step7 self-check #2 precedent — measured/unrebuilt artifacts are genuinely v3-2005-2023 artifacts; rebuilding is a C8.10/C8.13/convenience-class task) + soft-flagged + a Phase-D forward-HALT.
6. **The VALIDATION.md pre-2005 subsection / COMPARABILITY framing prose could mis-state the cohort_num residual** (years, deltas, the "same NCHS class" claim). *Mitigation*: every number is transcribed from the 6b receipt + DECISION_LOG 2026-05-23T02:00:00Z (1989 Δ+1, 1998 Δ+40, 2002 Δ−8; den/resident-births byte-exact 19/19; IMR ±0.02 19/19; weighted 1983-1984 3,639,113/3,669,268; the Task-6 class) — no invented figures (L6). Residual risk: a transcription slip — mitigated by the prose pointing to `external_validation_targets_v3_linked.csv` + `RECEIPTS/C8.18_step6b_*.md` as the machine-readable source of truth (a reader recomputes from there, not from the prose).

Each logged in `DECISION_LOG.md` 2026-05-23 as a residual risk; the recommended follow-ups are Phase-D forward-HALTs (not added to any VERIFY without human sign-off per §10).

### Forward-looking HALTs for next session (C8.19 PRE-FLIGHT) (Convention 4)

1. `C8.18-pre-do` @ `6632a15`; **`C8.18-complete` IS now present** (set at this DO step 7 commit). **C8.18 is fully closed (DO steps 1–7).** Next per KICKOFF Tier 3+5 sequence: **C8.19 — perinatal-record pre-joined parquet** (methodology research; PRE-FLIGHT may surface a too-sparse-to-be-useful §7 halt → defer to v1.1 methodology-paper subproject per §15.D).
2. **11-gate SHA state for C8.19 PRE-FLIGHT**: linked-derived = the **v4 `f630d8cf…`** (the C8.18 canonical mutation; NOT `9b828a4d…` — that is now the preserved `.v3_baseline`, do NOT "restore" it); the other 10 byte-exact (`38e2cecb`/`185c071e`/`c8a740eb`/`acb5c48a`/`230efed2`/`e16ad532`/`adbec108`/`5c22308b`/`7c682668`/`d98b4296`). C8.19 (perinatal-record) consumes the **v4 linked + v2.4.0 fetal-death + v3.0.0 natality + matched-multiples** envelope — the final pre-Zenodo data envelope is now settled.
3. **pytest baseline = 347 passed, 1 skipped, 1 xfailed (0 failed/error)** via the memory-safe per-dir + `tests/`-per-file split incl. the `tests/mutations/` subtree (soft-flag (kk); COUNT is the gate; the single-process full-suite OOM-SIGKILLs on the 2 GB v4). Unchanged from the 6b baseline (docs-only). README "56 passed" remains stale (soft-flag (jj)).
4. **Phase-D deferrals recorded by C8.18 DO step 7** (NOT C8.19-blocking; carry to Phase D):
   - **Manuscript Coverage re-paragraph = D.4** — must carry, in one pass after C8.19 lands: natality 1968-2024 (201,161,456) + linked v4 1983-2023 (149,386,620; permanent 1992-1994 gap) + the keyless-1983-1988 within-era structural difference (IMR = count(num)/count(den)) + the 1983-1984 50%-non-VSCP RECWT-weighted-sample + the documented numerator-file residual class (1989/1998/2002, the Task-6 class) + fetal-death 1982-2024.
   - **`file_inventory.csv` `imported`-flag refresh** false→true for the 19 pre-2005 cohort rows (Phase-D metadata-sync; re-run `test_inventory_invariants.py` after).
   - **`external_validation_v3_linked_comparison.{md,csv}` full-v4-surface regeneration** (the validator is currently scoped to its 2005-2023 owned surface; the pre-2005 cohort per-year reconciliation lives in `RECEIPTS/C8.18_step6b_*.md` + `external_validation_targets_v3_linked.csv`).
   - **Convenience-parquet + benchmark refresh to the v4 envelope**: `natality_v3_linked_residents_only.parquet` (v3 2005-2023 slice → v4 1983-2023), `docs/PIPELINE_TIMING_BENCHMARK.md`, `notebooks/README.md` linked-notebook descriptions, `notebooks/maternal_age_stratified_imr.ipynb` (C8.10a, v3-2005-2023 artifact) — soft-flag (ee) extended to linked v4.
5. **Scope-breadth precedent**: C8.18 DO step 7 followed the C8.17 step 7 "Option A — Honest propagation" pattern under standing authorization (mechanical swaps on the consumer surface; substantive bodies — CODEBOOK per-variable historical panels, the manuscript Coverage paragraph — deferred to C8.20 / D.4). Future docs-sync sub-steps should follow the same pattern; a `[plan-update]` reconciling §15.D DO-step-7's "6 files" wording with the realized ~16-file consumer surface is owed (soft-flag (ii) family; §11 human-merge — NOT bundled with any DO commit).
6. **Owed §11 human-merge items carried** (on-disk logs authoritative meanwhile): the §15.D model-clarification `[plan-update]` (soft-flag (ii); now also folds the C8.18 DO-step-7 scope-breadth + the 3 deferrals); the LESSONS 2026-05-20 §8-row sharpening + the 2026-05-22 (A′) addendum + the L13-extension-shared-CSV sharpening + the L17 grep-scope-must-enumerate-test-infra-count-constants sharpening (FIX_LOG 2026-05-23).
7. **C8.5b (Dockerfile) / C8.7b (orchestrator) remain DEFERRED.** Tier 3+5 ≈ 3/7 tasks done (C8.16 + C8.17 + **C8.18**); C8.19 (perinatal-record), C8.20 (CODEBOOK extensions), C8.21 (Stata/SAS pointer), C8.22 (cross-tab CSVs) remain. Cumulative Phase C tracked in STATUS (effort-ceiling cap 86 intact).

### Notes for next session

- **C8.18 is COMPLETE** (DO steps 1–7). The linked product is **v4.0.0: 1983-2023; 149,386,620 records; permanent 1992-1994 NCHS-linkage gap; canonical parquet filename `natality_v3_linked_*` retained**. DO step 7 was docs-only (zero non-doc mutation; 10/11 gate SHAs byte-exact; the linked-derived = the v4 `f630d8cf…` set at 6b). The consumer surface now consistently states the v4 envelope + the 4 framing notes (keyless 1983-1988; weighted 1983-1984; cohort_num residual; 1992-1994 gap); the manuscript Coverage paragraph + `file_inventory.csv` `imported` flags + the generated validation MD + the convenience/benchmark artifacts are the recorded Phase-D deferrals.
- Next per KICKOFF Tier 3+5 sequence: **C8.19 — perinatal-record pre-joined parquet** (the final data envelope is now settled: natality v3.0.0 + linked v4.0.0 + fetal-death v2.4.0 + matched-multiples).
- VERIFY-D did its job (L7 / no-rubber-stamp): it caught 6 stale lines the first DO pass missed; all corrected in-DO-scope (the C8.17 step7 `quickstart.R` precedent). The §4.4 "no successful receipt until VERIFY-C is authoritatively confirmed; commit gated on it" discipline (the 6b self-check #6 lesson) governed the commit gate.
- `/tmp/c8_18_s7/*` scratch (the gate-SHA + pytest split logs) is OS-cleanable + reproducible.
