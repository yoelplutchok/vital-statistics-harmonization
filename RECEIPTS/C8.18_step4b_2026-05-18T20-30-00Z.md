# Receipt: C8.18 DO step 4b — cohort 2003 `field_specs.py` layout authoring (783-byte denominator-plus + 1142-byte numerator; 2003-revision transition; ICD-10 throughout) + DEFLATE64 tooling decision + SMOKE Tier-0/1 value-distribution verify
## 2026-05-18T20:30:00Z

### What was done

Executed C8.18 DO step 4b under the full five-phase discipline in one session (PRE-FLIGHT + SMOKE + DO + VERIFY + RECEIPT; the C8.18 DO step 3a/3b/4a single-session-DO-step precedent). Authored the **byte-exact additive** cohort 2003 cohort-linked layouts in `natality/scripts/01_import/field_specs.py`, reconstructed FRESH from the `LinkCO03Guide.pdf` **DETAIL "Position/Len/Field" code-outline** (pp20-72; the L13-extension governing precedent — the pp18-19 "Linked 2003 Cohort Data Elements and Locations" element-span SUMMARY is composite + NOT trusted, used only as an independent block-range cross-check, all 9 cross-checks matched):

- `LINKED_BIRTH_2003_FIELDS` — 183 entries, locs 1-750 (REVISION@7 + IDNUMBER@10-14 + birth cert; identical in the den-plus AND the numerator's natality section per the shared LinkCO03Guide pp20-55 DETAIL).
- `LINKED_DEATH_2003_FIELDS` — 16 entries, locs 751-783 (the den-plus death-derived "plus": MATCHS@751 + AGED..UCODR130 + RECWT@776-783; present on BOTH den-plus + numerator; analogous to `LINKED_DEATH_1995_2002` / `_2005_2013`).
- `LINKED_NUM_DEATH_2003_FIELDS` — 17 entries, locs 784-1142 (numerator-only mortality: FILLER + EANUM@786-787 + ENTITY@788-927 (20×7) + RANUM@930-931 + RECORD@932-1031 (20×5) + death geo/date + DTHYR@1071-1074 + DTHMON@1141-1142).
- `LINKED_DENOMPLUS_RECLEN_2003 = 783`, `LINKED_NUM_RECLEN_2003 = 1142`.

Added **additive** single-year-2003 `_layout_for_linked_year` (783-byte den-plus) + `_numerator_layout_for_linked_year` (1142-byte numerator = birth + den-plus-"plus" + numerator-mortality) branches to `parse_linked_year.py` (the existing 2005/2014 + 3a/3b/4a return blocks + dispatcher behavior for the supported years byte-untouched — H10/HALT-13; verified by `test_prior_substep_dispatchers_unregressed` PASS + a standalone regression probe). Added the NEW sibling `DESIGN: tracks-current-state` SMOKE harness `natality/tests/test_linked_cohort_2003_layout_smoke.py` (10 tests; Convention 1/2; kept separate from the shipped 3a/3b/4a harnesses for sub-step isolation). **Zero canonical parquet / schema / validation-CSV mutation** (11/11 gate parquet SHAs byte-exact at VERIFY-A).

**Material L13-extension finding, surfaced + resolved at the Convention-3 snapshot before any `field_specs.py` mutation:** the prior C8.18 DO step 4 PRE-FLIGHT *assumption* "2003 = 783/**1259**" numerator length was **FALSIFIED** — `LinkCO03Guide.pdf` p17 File Characteristics state Numerator US reclen = **1,142**, and exact zip-member-uncompressed-size arithmetic confirms byte-exact (den `4,096,151 × (783+2 CRLF) = 3,215,478,535` = `VS03LKBC.USDENPUB`; num `27,843 × (1142+2) = 31,852,392` = `VS03LKBC.USNUMPUB`). Not a §7 halt — the "1259" was a non-binding prior assumption; the L13-extension "never assume same-model==same-layout; value-distribution-verify on real data" discipline is exactly the resolution mechanism. Like DO step 3b/4a, going straight to the DETAIL code-outline produced a byte-exact layout that **a read-only pre-authoring probe + SMOKE Tier 1 verified on real data on the FIRST run** (no test loosened — §2 fail-closed; §9-#4). The only two initial SMOKE failures were **test-harness copy-paste bugs from the 4a sibling** (MATCHS is @751 in `LINKED_DEATH_2003` for 2003, not @1 in `LINKED_BIRTH` like 1995-2002 → the numerator tests must key `d["MATCHS"]` not `b["MATCHS"]`); the LAYOUT was byte-exact first-run (the read-only pre-authoring probe had already confirmed MATCHS@751 ∈ {1,2}/{1}). The harness was fixed (NOT loosened); the layout was untouched.

### Inputs consumed

- `PRE_FLIGHT_LOG.md` 2026-05-18T20:30:00Z — per-sub-step PRE-FLIGHT (before any `field_specs.py` mutation; L10-safe). 9/9 forward-looking HALTs re-verified; 11/11 gate parquet SHAs byte-exact; canonical pytest baseline `127 passed, 1 skipped, 1 xfailed in 313.36s` (4-dir) preserved; the FULL 2003 den-plus + numerator layout captured as state-on-disk substrate; the 1259→1142 material finding + 4 within-task scope decisions surfaced + resolved before DO (Convention 3).
- `PRE_FLIGHT_LOG.md` 2026-05-18T05:00:00Z (the C8.18 DO step 4 investigation-only checkpoint) + `DECISION_LOG.md` 2026-05-18T05:00:00Z + `RECEIPTS/C8.18_step4a_2026-05-18T14-30-00Z.md` (the 9 forward-looking HALTs).
- `~/Desktop/natality-harmonization/raw_docs/linked/LinkCO03Guide.pdf` (111 pp; 345,151 chars; text-extractable, no OCR — L12-extension confirmed this entry): p17 (file char: Den US 783 / Num US 1142 / Unl US 1142), pp20-55 (denominator-plus DETAIL locs 1-783; p55 divider "Here ends the denominator-plus file."), pp55-72 (numerator-only mortality DETAIL locs 784-1142). pp18-19 element-span SUMMARY = NOT trusted (L13-ext); used only as an independent block-range cross-check (9/9 matched).
- `~/Desktop/natality-harmonization/raw_data/linked/LinkCO03US.zip` members `VS03LKBC.US{DEN,NUM}PUB` (DEFLATE64, compress_type=9) — read-only, first ~300 records each for the read-only pre-authoring probe + SMOKE Tier 1 via `7z e -so` streaming; SHA-anchored at C8.18 DO step 2.
- `natality/scripts/01_import/field_specs.py` + `parse_linked_year.py` (sibling substrate; the 3a/3b/4a + 2005/2014 LINKED specs — pattern mirrored, NOT re-derived).
- 11 gate parquets — SHA-verified byte-exact at PRE-FLIGHT entry cheap-check and re-verified at VERIFY-A.

### Outputs produced

Git-tracked (this commit):

- `natality/scripts/01_import/field_specs.py` — **purely additive** (`git diff --numstat` = 283 insertions / **0 deletions**; existing LINKED 2005/2014 + 3a/3b/4a tuple lists/constants byte-untouched): the 2003 section-header source block + `LINKED_BIRTH_2003_FIELDS` (183, 1-750) + `LINKED_DEATH_2003_FIELDS` (16, 751-783) + `LINKED_DENOMPLUS_RECLEN_2003 = 783` + `LINKED_NUM_DEATH_2003_FIELDS` (17, 784-1142) + `LINKED_NUM_RECLEN_2003 = 1142`.
- `natality/scripts/01_import/parse_linked_year.py` — additive branches + minimal accuracy line-replacements (`git diff --numstat` = 57 insertions / **10 deletions**; the 10 deletions are docstring-paragraph + 2 `ValueError` "Supported:" string replacements — accuracy, not behavior change: 5 new imports + the `_layout_for_linked_year` single-year-2003 branch + the `_numerator_layout_for_linked_year` single-year-2003 branch; the 2005/2014 + 3a/3b/4a **return blocks + dispatcher behavior for supported years are byte-untouched**, asserted by `test_prior_substep_dispatchers_unregressed` PASS + a standalone regression probe).
- `natality/tests/test_linked_cohort_2003_layout_smoke.py` — NEW; `DESIGN: tracks-current-state`; 10 tests (6 SHAPE structural [reclen constants, den-plus + numerator contiguity/tiling/in-bounds/non-overlap, both additive dispatcher branches incl. 1992-1994 gap + 2004-not-yet → ValueError, prior 3a/3b/4a/2005/2014 dispatchers unregressed] + 2 Tier-0 synthetic-with-L3-negative + 2 Tier-1 real-data den-plus + numerator/ICD-10 via `7z` DEFLATE64 stream).
- `PRE_FLIGHT_LOG.md` — C8.18 DO step 4b PRE-FLIGHT entry (2026-05-18T20:30:00Z; before any `field_specs.py` mutation; L10-safe) incl. the full state-on-disk layout table.
- `RECEIPTS/C8.18_step4b_2026-05-18T20-30-00Z.md` — this file.
- `DECISION_LOG.md` + `STATUS.md` — appended (DO step 4b 1-finding + 4-decision entry + new dated section).

No canonical parquet / `harmonized_schema.csv` / validation-CSV touched. Raw zips/guides are gitignored + out-of-tree (read-only, unchanged). §15.D NOT edited this step (the substrate-format wording was reconciled at the 3a commit; the broader model-clarification incl. the 4a/4b/4c decomposition + the 1259→1142 falsification + the DEFLATE64-tooling decision stays soft-flag (ii) §11 human-merge).

### Five-phase trace

- **PRE-FLIGHT**: ✓ `PRE_FLIGHT_LOG.md` 2026-05-18T20:30:00Z (per-sub-step, before any `field_specs.py` mutation; L10-safe). 9/9 forward-looking HALTs re-verified; 11/11 gate parquet SHAs byte-exact; canonical pytest 127P+1S+1XF/313.36s baseline preserved; the full layout reconstructed from the DETAIL code-outline + captured state-on-disk; the 1259→1142 material finding + 4 within-task scope decisions surfaced + resolved before DO (Convention 3).
- **SMOKE**: ✓ a read-only pre-authoring probe of the reconstructed layout on real DEFLATE64 `VS03LKBC.US{DEN,NUM}PUB` (the §2 fail-closed discipline) → Tier 0 (synthetic 783-byte den-plus + 1142-byte numerator recovery + L3 position-shift negative: PASS) → Tier 1 (real data via `7z e -so`). **10/10 PASS** (the layout was byte-exact first-run; the only 2 initial failures were 4a-sibling copy-paste harness bugs — MATCHS dict location — fixed, not loosened).
- **DO**: ✓ additive `field_specs.py` + `parse_linked_year.py` + new SMOKE harness. Single commit (PRE-FLIGHT + DO + RECEIPT; the C8.18 DO 3a/3b/4a precedent).
- **VERIFY**: ✓ all 5 criteria (below).
- **RECEIPT**: ✓ this file.

### Verify results

- **VERIFY-A (zero canonical mutation)**: PASS — 11/11 gate parquet SHAs byte-exact vs PRE-FLIGHT HALT 2 + the 4a receipt VERIFY-A: natality `c8a740eb…a6237153`/`acb5c48a…28856974`; `.v28_baseline` `230efed2…33ccebac`/`e16ad532…77c41d44`; linked-derived `9b828a4d…5a08b777`; fetal-death `38e2cecb…99c5cf48`(harm)/`185c071e…a7968a09`(deriv) (via the `~/Desktop/fetal-death-harmonization-build/` tree, soft-flag (hh)); MM `adbec108…45dc1549`/`5c22308b…d39205d1`/`7c682668…edd61f5d`/`d98b4296…6a543261`. Threshold: 11/11 unchanged.
- **VERIFY-B (git scope)**: PASS — modified/new = `field_specs.py`, `parse_linked_year.py`, `test_linked_cohort_2003_layout_smoke.py`, `PRE_FLIGHT_LOG.md`, + `RECEIPTS/`/`DECISION_LOG.md`/`STATUS.md`. NO `.parquet`/`harmonized_schema.csv`/validation-CSV. Threshold: scope ⊆ {scripts, test, plan, state}.
- **VERIFY-C (additive-only / no regression of existing behavior)**: PASS — `git diff --numstat` = `field_specs.py` **283/0** (purely additive; zero deletion/modification); `parse_linked_year.py` **57/10** (the 10 deletions are docstring-paragraph + 2 `ValueError`-message line-replacements — accuracy improvements, NOT behavior change for supported years; the 2005/2014 + 3a/3b/4a return blocks are byte-identical). The dispatcher behavior for all previously-supported years is unregressed: `test_prior_substep_dispatchers_unregressed` asserts `_layout_for_linked_year(1983)==91`/`(1989)==225`/`(1996)==230`/`(2002)==230`/`(2008)==900`/`(2018)==1384` + `_numerator_layout_for_linked_year(1985)==500`/`(1990)==535`/`(2002)==535` — PASS.
- **VERIFY-D (pytest count gate)**: PASS — canonical **4-dir** suite `fetal_death/ natality/ tests/ matched_multiples/` = **137 passed, 1 skipped, 1 xfailed** (see the in-band runtime in the STATUS section). Reconciles exactly: the 4a-close baseline 127P+1S+1XF preserved (zero regression; 1 skipped + 1 xfailed unchanged; 127 still pass) **+** 10 new 2003 SMOKE all pass = 137P. Runtime within the ~240-380s band.
- **VERIFY-E (SMOKE all PASS)**: PASS — 10/10. Tier 0 synthetic + L3 negative; Tier 1 real-data value-distribution (L13-extension, sentinel-aware) on real DEFLATE64 data via `7z`: den-plus reclen 783 + numerator reclen 1142 (block = data + CR/LF, byte-exact vs `LinkCO03Guide` p17 counts); DOB_YY == 2003 for all 300×2 records (encoding ASCII + alignment + block-size jointly); den-plus MATCHS@751 ∈ {1,2} + REVISION@7 ∈ {S,A} (2003-rev transition) + DBWT@467-470 4-digit grams + RECWT@776-783 "1.000000"; numerator MATCHS@751 ≡ 1 (linked infant deaths only); AGED 000-366, EANUM/RANUM 00-20, DTHYR ∈ {2003,2004}, DTHMON 01-12; UCOD@767-770 ≥80% ICD-10 alpha (2003 cohort = all ICD-10).

### Reproducibility

- `field_specs.py` constants are declarative; re-derivable from the `LinkCO03Guide.pdf` DETAIL "Position/Len" column via the documented extractor (state-on-disk `/tmp/c8_18_s4b_final.py` + the PRE_FLIGHT_LOG SMOKE Tier-0 table). The SMOKE harness re-derives block size empirically (data + CR/LF) + value-distribution-verifies → deterministic, re-running yields identical results.
- Zero canonical parquet rebuild (DO step 4b is layout substrate only). H10 gate: 11/11 SHAs byte-exact.
- Record lengths cross-checked byte-exact against the guide-stated counts (`LinkCO03Guide` p17: Den 4,096,151 / Num 27,843) + the zip-member uncompressed sizes (`3,215,478,535 / 785 = 4,096,151`; `31,852,392 / 1144 = 27,843`) + the real-data `file_size % block == 0` + CRLF-terminator probe.
- DEFLATE64 tooling: CLI `7z e -so` (homebrew p7zip). No new Python dependency added (preserves the C8.5a pinned-env SHA). Re-running requires `7z` on PATH (SMOKE Tier-1 `skipif`-guards its absence).

### Git

- Pre-DO rollback anchor: `C8.18-pre-do` @ `6632a15` (unchanged; the C8.18 DO rollback anchor).
- This commit: intermediate C8.18 DO step 4b — **NO tag** (Convention 5; `C8.18-complete` is the final-sub-step-only tag).

### STATUS.md updated

- New section dated 2026-05-18T20:30:00Z at the top (newest-first).

### Self-check (§10) — what could I have gotten wrong that VERIFY wouldn't catch?

1. **Composite spans + RESERVED gap-fill** (`MEDRISK`@328-344, `OBSTETRC`@355-361, `LABOR`@374-389, `DELMETH`@395-401, `NEWBORN`@483-491, `CONGENIT`@504-525, `ENTITY`@788-927, `RECORD`@932-1031 + the `RESERVED_<n>`/`FILLER_<n>` gap-fills) are authored as single spans; the per-condition leaf sub-positions were NOT individually value-distribution-verified — only the bracketing scalar/anchor neighbors. A composite/reserved span could be off by a byte and the anchor checks + contiguous-tiling check would not catch it. *Mitigation*: composite extents are byte-exact from the DETAIL range-tokens, cross-confirmed against the pp18-19 SUMMARY block ranges (9/9 matched); the den-plus tiles [1,783] and the numerator tiles [1,1142] with zero gap/overlap; the bracketing named anchors value-verify on real data. *Recommendation*: DO step 5 value-distribution-verify each composite's leaf sub-fields before harmonizing. Logged DECISION_LOG residual risk; soft-flag (gg).
2. **The naive newline DETAIL parser** had column-bleed artifacts (range-mismatch on a FILLER span; values-code "1" tokens mis-read as positions inside the per-condition leaf blocks). I resolved them by collapsing the per-condition leaf blocks into composite spans (their exact extents cross-confirmed against the SUMMARY) + RESERVED-filling all gaps; the named scalar anchors were taken from clean explicit-range tokens. *Residual risk*: a named scalar between two composites could in principle inherit a wrong start if a values-code collided AND the monotonic-non-decreasing filter let it through. *Mitigation*: every key anchor (REVISION/IDNUMBER/DOB_YY/DBWT/SEX/DPLURAL/COMBGEST/APGAR5/MATCHS/AGED/UCOD/UCODR130/RECWT/EANUM/ENTITY/RANUM/RECORD/DTHYR/DTHMON) was value-distribution-verified on real data (the read-only pre-authoring probe + SMOKE Tier 1). DO step 5 full-parse value-distribution surfaces any non-anchor scalar anomaly.
3. **Mid-file layout shift** sampled only the first ~300 records/member (extremely unlikely for a fixed-width NCHS public-use `.dat`; `file_size % block == 0` holds for the whole file — den 3,215,478,535/785=4,096,151, num 31,852,392/1144=27,843, both exact). DO step 5 full parse surfaces any anomaly via row-count + value-distribution.
4. **Birth/death split point 750/751**: `LINKED_BIRTH_2003` ends @750 + `LINKED_DEATH_2003` starts at MATCHS@751. The exact internal boundary of the trailing `RESERVED_742`@742-750 is an authoring choice (the DETAIL gap before MATCHS@751); it does not affect parser correctness (birth+death concatenate to tile [1,783]/[1,1142]) and is filler either way. The DO-step-5 num↔den construction keys on `IDNUMBER`@10-14 (the LinkCO03Guide p20 num↔den-plus join key), in `LINKED_BIRTH_2003` — unaffected.
5. **`_find_denomplus_member` "DENOM" vs `VS03LKBC.USDENPUB`** + **wiring the DEFLATE64 `7z` stream into the actual parse path**: these are DO-step-5 parser concerns (this step is layout substrate only; the SMOKE harness reads via its own `7z` stream). If DO step 5's member-finder isn't extended, the 2003 parse will not find the den-plus member. Documented in the `field_specs.py` source block + the PRE_FLIGHT_LOG + the forward-looking HALTs.
6. **2003-revision "Vers*" semantics**: the byte position is constant, but a field's *populated-ness* depends on REVISION@7 (S=1989-unrev populates the 1989-vintage fields; A=2003-rev populates the 2003-vintage fields). The layout substrate is unaffected (fixed-width regardless); the per-revision null-pattern + the harmonized mapping are DO-step-5/6 concerns (H7 fetal-death sibling-parity for the 2003-rev transition; soft-flag (gg)). Documented.

Each logged in `DECISION_LOG.md` 2026-05-18T20:30:00Z as a residual risk; the recommended checks are DO-step-5 PRE-FLIGHT items (not added to VERIFY without human sign-off per §10).

### Forward-looking HALTs for next session (C8.18 DO step 4c)

1. `C8.18-pre-do`@`6632a15`; `C8.18-complete` NOT present (final-sub-step-only). `C8.17-complete` present. HEAD = this DO step 4b commit (after `6fb7acd`).
2. **11 gate parquet SHAs unchanged** (the same 11; fetal-death via the `~/Desktop/fetal-death-harmonization-build/` tree, soft-flag (hh)). Linked-derived `9b828a4d…` changes only at the later C8.18 re-harmonize DO step (4b is layout-substrate only).
3. **Canonical pytest baseline is now `137 passed, 1 skipped, 1 xfailed`** (was 127P+1S+1XF; +10 from the new `test_linked_cohort_2003_layout_smoke.py`, `DESIGN: tracks-current-state`). The canonical suite is the **4-dir** `fetal_death/tests/ natality/tests/ tests/ matched_multiples/tests/`. README 3-dir "56 passed" line stays stale → soft-flag (jj).
4. `field_specs.py` `LINKED_BIRTH/DEATH/NUM_DEATH_2003_FIELDS` + reclen 783/1142 are byte-exact + real-data value-verified — state-on-disk; **DO step 4c = cohort 2004 layout** (900-byte den-plus + 1142-byte numerator; 2003-revision continued) from the `LinkCO04Guide.pdf` DETAIL (L13-extension). DO step 4c must NOT re-derive the 1983-1991 / 1995-2002 / 2003 layouts (state-on-disk in `field_specs.py` + the 3a/3b/4a/4b receipts). **2004 den-plus is HYPOTHESIZED == `LINKED_BIRTH_2005_2013` — value-verify, do NOT assume** (the L13-extension; same caution that FALSIFIED the 1995-2002-reuses-1989-1991 + the 2003-numerator-1259 assumptions). `LinkCO04US.zip` compression: re-probe (1995-2002 + 2005+ = DEFLATE stdlib-fine; 2003 = DEFLATE64; 2004 unknown until probed — likely DEFLATE, but verify).
5. **Encoding = ASCII** — the 2005+ parser's `_slice_field` `.decode("latin-1")` works for the cohort `.dat` (re-confirmed at 4b on real 2003 data).
6. **Structural model**: 1995-2004 = the three-file denominator-plus model. 1995-2002 done (230/535). 2003 done (**783/1142** — the prior "1259" was FALSIFIED). 2004 = 900/1142 (den-plus likely == `LINKED_BIRTH_2005_2013` — value-verify, NOT assume; the LinkCO04Guide p17 file-char + zip-member arithmetic is the cheap byte-exact check). `_find_denomplus_member` (`"DENOM"`) won't match `VS{03,04}LKBC.US{DENPUB,NUMPUB}`/`LinkCO{YY}US{Den,DEN}.dat` + the DEFLATE64-at-2003 `7z`-stream wiring = DO step 5 parser concerns.
7. §15.D substrate-format wording reconcile already APPLIED at the 3a commit; the broader §15.D model-clarification (3a/3b split; 4a/4b/4c decomposition; structural model; 1995-2002-reuse-falsification; **the 2003-numerator-1259→1142 falsification**; the DEFLATE64-at-2003 `7z`-stream tooling decision) remains **soft-flag (ii)** proposed-not-applied for §11 human-merge (queued with the soft-flag-(w) batch).
8. **soft-flag (gg) refined**: the 2003 numerator carries `UCOD`@767-770 (ICD-10; 2003 cohort all ICD-10) + `UCODR130`@772-774 (130-cause infant recode) + `ENTITY`@788-927 (20×7) / `RECORD`@932-1031 (20×5) multiple cause + the per-condition flag blocks `MEDRISK`/`OBSTETRC`/`LABOR`/`DELMETH`/`NEWBORN`/`CONGENIT` as composite spans; per-condition decomposition + per-era cause-recode semantics + the 2003-rev "Vers*" null-pattern + the harmonized cause-column shape stay DO step 5/6 PRE-FLIGHT.
9. pytest runtime in the ~240-380s band (the band is the gate, not the absolute number).

### Notes for next session

- **C8.18 DO step 4b is CLOSED.** The cohort 2003 physical layout (783-byte den-plus + 1142-byte numerator) is authored byte-exact + real-data value-verified on real DEFLATE64 `LinkCO03US.zip` data (via `7z` stream), state-on-disk in `field_specs.py` + this receipt + `PRE_FLIGHT_LOG.md` 2026-05-18T20:30:00Z. The prior "2003 numerator = 1259" assumption was FALSIFIED → 1142 (guide p17 + byte-exact zip-member arithmetic). Next per the C8.18 DO step 4 split: **DO step 4c — cohort 2004 layout** (900-byte den-plus + 1142-byte numerator; verify den-plus vs `LINKED_BIRTH_2005_2013` — do NOT assume) from the `LinkCO04Guide.pdf` DETAIL (L13-extension; the 3a/3b/4a/4b governing precedent).
- The L13-extension "reconstruct from the DETAIL code-outline, value-distribution-verify on real data, do NOT trust the element-span summary, never assume same-model==same-layout" discipline produced a first-run-clean 4b layout AND cheaply caught the 1259→1142 assumption error at the Convention-3 snapshot before any mutation; carry it to DO step 4c (esp. the "2004 den-plus == 2005-2013" hypothesis must be value-verified, NOT assumed).
- The DEFLATE64-at-2003 tooling decision (CLI `7z e -so` stream, NOT a `zipfile-deflate64` dep) is recorded; DO step 5 must wire the `7z` stream into the actual `parse_linked_year` parse path for 2003 (the SMOKE harness uses its own `7z` stream). Re-probe `LinkCO04US.zip` compression at the 4c PRE-FLIGHT.
- The §15.D model-clarification `[plan-update]` (soft-flag (ii)) is owed to the human (§11; queued with the soft-flag-(w) batch; now also folds the 4b 783/1142 layout + the 1259→1142 falsification + the DEFLATE64 `7z` tooling decision); the on-disk PRE_FLIGHT_LOG/DECISION_LOG/RECEIPTs are authoritative in the interim.
- `/tmp/c8_18_s4b_*` scratch (DETAIL extracts + the field-list builder + the pre-authoring probe) + background gate-SHA/pytest output files are OS-cleanable + reproducible.
