# Receipt: C8.18 DO step 5c-ii-a — the 1995-2002 cohort birth-side map (the 1989-rev sibling of the 5c-i 1989-1991 reference era)
## 2026-05-19T17:00:00Z

### What was done

Executed C8.18 **DO step 5c-ii-a** under the full five-phase discipline in one session (PRE-FLIGHT + SMOKE + DO + VERIFY + RECEIPT; the C8.18 DO step 3a/3b/4a/4b/4c/5a/5b/5c-i single-session-DO-step precedent). §15.D **DO step 5c-ii** ("the 1995-2002 + 2003 + 2004 cohort birth-side maps") was decomposed at the Convention-3 PRE-FLIGHT snapshot — before any code mutation — into two separable concerns of materially different risk + recode family (the 5→5a/5b/5c + 5c→5c-i/5c-ii/5c-iii + 4→4a/4b/4c + 3→3a/3b per-concern precedent + §2 cheap-before-expensive + §9-#8): **5c-ii-a** = the **1995-2002** cohort birth-side map (the 1989-revision birth certificate == natality V2 1990-2002 == the 5c-i-verified 1989-1991 reference era's recode family; the lowest-risk verifiable increment) + **5c-ii-b** = the 2003 + 2004 cohort birth-side maps (the 2003-revision dual-certificate transition; an entirely different raw field-name family — the natality V1-core `is_2003revised` analog; materially higher risk; its own SMOKE'd sub-step). This session shipped **5c-ii-a**.

5c-ii-a added **additively** to `natality/scripts/03_harmonize/harmonize_linked_v3.py` (`git diff --numstat` = **259 insertions / 18 deletions**; the 18 deletions are ALL the `_harmonize_cohort_batch` docstring reword (14) + the NotImplementedError message-string reword (4) — **0 logic-deletion lines**, grep-confirmed; the existing 5c-i 1989-1991 inline body + the 2005+ `_harmonize_batch` body are byte-untouched, anchors intact):

1. **`_harmonize_cohort_1995_2002(batch, year)`** (NEW) — the 1995-2002 cohort denominator-plus birth-side map. The 1995-2002 birth section IS a 1989-revision birth certificate; the map ALIASES the cohort raw names onto the natality V1-core 1989-revision recodes (H7 sibling-parity — reuses `_dmeduc_years_to_cat4` / `_mrace_detail_to_bridged4` / `_detail_order_to_recode9` / `_month_to_trimester` / `_pldel_to_facility`; do NOT re-derive). Authored as a SEPARATE function (not a shared helper with the 1989-1991 branch) so the 5c-i-verified 1989-1991 inline body stays byte-untouched (§9-#7; this file's existing explicit-per-era helper-duplication pattern). Per-era deltas vs the 5c-i 1989-1991 map (real-data-verified at the Convention-3 snapshot): `NPREVIST` (not `NPREVIS`); NO `DFEDUC` on the 1995-2002 birth section → `father_education_cat4` conservatively null; `RECWT`@223-230 present → `record_weight` float64 (1989-1991 had no RECWT); NO `MANNER` → `manner_of_death` null; the within_era ICD-9 (cohort 1995-1998) vs ICD-10 (1999-2002) split keyed on cohort **birth year** (§15.D DO step 1; the 5c-i resolved shape). A fail-closed `if _cohort_era(year) != "1995_2002": raise ValueError` guard (§2 — never silently mis-harmonize a wrong-era batch through the 1995-2002 map).
2. A 2-line `if era == "1995_2002": return _harmonize_cohort_1995_2002(batch, year)` dispatch prepended in `_harmonize_cohort_batch` BEFORE the existing `if era != "1989_1991":` guard; the NotImplementedError message + the function docstring reworded to reflect 1989-1991 + 1995-2002 implemented (remaining: 2003/2004 = 5c-ii-b; keyless 1983-1988 = 5c-iii). The 1989-1991 inline body byte-untouched (the cohort branch fires only for year ≤ 2004; the 1995-2002 branch only for 1995 ≤ year ≤ 2002 → the canonical v3 2005-2023 path AND the 5c-i-verified 1989-1991 path byte-identical; §9-#7-safe).

Plus a NEW sibling `DESIGN: tracks-current-state` SMOKE harness `natality/tests/test_linked_cohort_5cii_a_harmonize_smoke.py` (22 tests; Convention 1/2; sub-step isolation) + the L17 / Convention-2 / §4.2.1 minimal `tracks-current-state` Edit to `natality/tests/test_linked_cohort_5c_harmonize_smoke.py` (the now-implemented 1995/2002 NotImplementedError pins removed from `test_tier0_unimplemented_cohort_eras_failclosed`; 2003/2004 relabelled 5c-ii-b — bundled in the SAME commit per the PRE-FLIGHT HALT 9 / §4.2.1). NO `OUT_SCHEMA` change (80 cols unchanged — 5c-ii-a uses the 5c-i extended schema; the `harmonized_schema.csv` metadata edit + the v3→v4 version bump remain DO step 6 per Anti-Pattern #6). The parser dispatchers + `field_specs.py` + the 8 prior cohort harnesses (beyond the minimal 5c-i L17 edit) are byte-untouched (5c-ii-a is harmonize-side only; H10/HALT-13). **Zero canonical parquet / `harmonized_schema.csv` / validation-CSV mutation** (the harmonizer is NOT re-run at 5c-ii-a; 11/11 gate parquet SHAs byte-exact at VERIFY-A).

**One Convention-3 divergence surfaced at SMOKE + resolved BEFORE the commit** (L10-safe NEW dated PRE_FLIGHT_LOG addendum 2026-05-19T15:00:00Z; the 5b 2026-05-19T08:30:00Z + Task-1 2026-05-11T17:58:10Z precedent — a divergence between PRE-FLIGHT and final DO → a NEW dated entry, never a back-fill): the SMOKE (authored FIRST, run RED = collection ImportError vs the un-modified harmonizer — §9-#9/L3) ran Tier 0 = 48/48 PASS first-run; the 2 Tier-1 real-data tests FAILED the independent `infant_death (AGED-derived) == MATCHS-death` §7-#13 cross-check (`AGED=3, MATCHS=400` for 1999). A read-only `MATCHS × AGED-nonblank` cross-tab probe (n=3000 on real LinkCO90/95/99) established that the 1995-2002 denominator-plus MATCHS encoding differs from 1989-1991 on the SURVIVOR code (1989-1991: `3`=survivor; 1995-2002: `2`=surviving per `field_specs.py:1222`) but the linked-infant-death code is **uniformly `MATCHS == 1`** across BOTH eras (a *perfect* `AGED-nonblank ⟺ MATCHS==1` cross-tab: 1990 30/30, 1995 17/17, 1999 26/26). The harmonizer's `infant_death = AGED-non-blank` derivation is **CORRECT and UNCHANGED** (real data: ~0.57-1.0% deaths = the IMR ballpark); the defect was in the SMOKE harness's *independent cross-check*, which inherited the 5c-i `m in ("1","2")` framing (imprecise — for 1995-2002 it sweeps in code `2` = surviving). Resolved per §9-#4 / §2 (fix the test CORRECTLY, never loosen to pass): the cross-check → `m == "1"` (the era-correct AND era-independent linked-infant-death code; STRICTER, not looser — a test-correctness fix, no `harmonize_linked_v3.py` logic change). After the fix: SMOKE 50/50.

### Inputs consumed

- `PRE_FLIGHT_LOG.md` 2026-05-19T14:00:00Z (per-sub-step PRE-FLIGHT; before any code mutation; L10-safe) + the 2026-05-19T15:00:00Z addendum (the MATCHS-code cross-check correction). 9/9 forward-looking HALTs from `RECEIPTS/C8.18_step5c-i_2026-05-19T11-00-00Z.md` re-verified; 11/11 gate parquet SHAs byte-exact; canonical pytest baseline `213 passed, 1 skipped, 1 xfailed in 352.36s` (4-dir) preserved; the 5c-ii→5c-ii-a/5c-ii-b decomposition + a read-only real-data Convention-3 value-distribution probe + 4 within-task scope decisions surfaced + resolved before DO (Convention 3); L17 grep-scope of all 8 cohort harnesses = 1 stale pin (the 5c-i 1995/2002 NotImplementedError pins) → minimal `tracks-current-state` Edit bundled.
- `RECEIPTS/C8.18_step5c-i_2026-05-19T11-00-00Z.md` (the architecture + the resolved within_era ICD-9/ICD-10 cause-column shape + the 1989-1991 reference era).
- `natality/scripts/03_harmonize/harmonize_linked_v3.py` (the 5c-ii target — read in full) + `harmonize_v1_core.py` (the H7 sibling-parity reference: `is_pre2003` 1990-2002 == the 1989-rev recode family the 1995-2002 cohort uses).
- `natality/scripts/01_import/field_specs.py` `LINKED_BIRTH_1995_2002_FIELDS` + `LINKED_DEATH_1995_2002_FIELDS` + `LINKED_DENOMPLUS_RECLEN_1995_2002`=230 (state-on-disk 4a/5a, read-only).
- `natality/tests/test_linked_cohort_5c_harmonize_smoke.py` (read in full to scope the L17 stale pin).
- `~/Desktop/natality-harmonization/raw_data/linked/LinkCO95US.zip` (cohort 1995, ICD-9) + `LinkCO99US.zip` (cohort 1999, ICD-10) + `LinkCO90.zip` (the MATCHS cross-tab probe) — read-only, ≤3000 records each; SHA-anchored at C8.18 DO step 2.
- 11 gate parquets — SHA-verified byte-exact at the PRE-FLIGHT cheap-check + re-verified at VERIFY-A.

### Outputs produced

Git-tracked (this commit):

- `natality/scripts/03_harmonize/harmonize_linked_v3.py` — **additive** (`git diff --numstat` = **259 / 18**; 0 logic-deletion lines [the 18 deletions = the docstring + NotImplementedError-message reword, grep-confirmed]; the 5c-i 1989-1991 inline body + the 2005+ body byte-untouched, anchors intact; `OUT_SCHEMA` 80 cols unchanged).
- `natality/tests/test_linked_cohort_5cii_a_harmonize_smoke.py` — NEW; `DESIGN: tracks-current-state`; 22 tests (Tier 0: the 1995-2002 `_cohort_era` partition + the cohort-birth-year ICD-9/ICD-10 split (1996 ICD-9 / 2000 ICD-10 end-to-end + the 1995/1998 ICD-9 + 1999/2002 ICD-10 sub-era boundaries) + the per-era deltas (RECWT→record_weight; father_education_cat4 null; manner_of_death null) + 5 fail-closed NotImplementedError negatives (2003/2004=5c-ii-b, 1983-1988=5c-iii) + the direct-function wrong-era ValueError + the 1989-1991 regression-lock + the H7 sibling-parity recode equivalence; Tier 1: real `LinkCO95US` (ICD-9) + `LinkCO99US` (ICD-10) parse→harmonize with the independent `MATCHS==1` §7-#13 cross-check + the ICD default-null + record_weight-non-null).
- `natality/tests/test_linked_cohort_5c_harmonize_smoke.py` — the L17 / Convention-2 / §4.2.1 minimal `tracks-current-state` Edit (`git diff --numstat` 13/6: removed the now-implemented 1995/2002 NotImplementedError pins; 2003/2004 relabelled 5c-ii-b; docstring note).
- `PRE_FLIGHT_LOG.md` — the 5c-ii-a PRE-FLIGHT (14:00:00Z) + the 15:00:00Z MATCHS-code addendum.
- `RECEIPTS/C8.18_step5c-ii-a_2026-05-19T17-00-00Z.md` — this file.
- `DECISION_LOG.md` + `STATUS.md` — appended (the 5c-ii→5c-ii-a/5c-ii-b decomposition + the 1995-2002 MATCHS-encoding finding + new dated section).

No canonical parquet / `harmonized_schema.csv` / validation-CSV touched. Raw zips/guides gitignored + out-of-tree (read-only, unchanged). §15.D NOT edited this step (the broader model-clarification — now also folding 5c-ii→5c-ii-a/5c-ii-b + the 1995-2002 MATCHS-encoding finding — stays soft-flag (ii) §11 human-merge; the 3a/3b/4a/4b/4c/5a/5b/5c-i precedent).

### Five-phase trace

- **PRE-FLIGHT**: ✓ `PRE_FLIGHT_LOG.md` 2026-05-19T14:00:00Z (per-sub-step, before any code mutation; L10-safe) + the 2026-05-19T15:00:00Z addendum. 9/9 forward-looking HALTs re-verified; 11/11 gate SHAs byte-exact; pytest 213P+1S+1XF/352.36s baseline preserved; the 5c-ii→5c-ii-a/5c-ii-b decomposition + the real-data value-distribution probe + 4 scope decisions + the L17 grep-scope (1 stale pin → bundled) surfaced + resolved before DO (Convention 3).
- **SMOKE**: ✓ NEW harness authored FIRST (§9-#9). Run vs the **un-modified** harmonizer = collection ImportError (`cannot import name '_harmonize_cohort_1995_2002'`) — proves it genuinely requires the DO code (L3 / §9-#5, not a rubber-stamp). After DO: Tier 0 = 48/48 first-run; Tier 1 surfaced the §7-#13 cross-check MATCHS-code imprecision → resolved per §9-#4 (corrected the TEST — stricter `m == "1"` — not the harmonizer; L10-safe addendum) → **50/50** (the 5c-ii-a 22 + the L17-updated 5c-i 28).
- **DO**: ✓ additive `_harmonize_cohort_1995_2002` + the 2-line dispatch + the docstring/message reword + the new SMOKE harness + the L17 5c-i Edit. Single commit (PRE-FLIGHT + DO + RECEIPT; the C8.18 DO 3a-5c-i precedent).
- **VERIFY**: ✓ all 5 criteria (below).
- **RECEIPT**: ✓ this file.

### Verify results

- **VERIFY-A (zero canonical mutation)**: PASS — 11/11 gate parquet SHAs byte-exact vs the PRE-FLIGHT HALT 2 + the 5c-i receipt (full 64-hex compared programmatically, `/tmp/c8_18_s5cii_gate_verify.out`): nat `c8a740eb…`/`acb5c48a…`; `.v28_baseline` `230efed2…`/`e16ad532…`; linked-derived `9b828a4d…`; fetal-death `38e2cecb…`/`185c071e…` (via the `~/Desktop/fetal-death-harmonization-build/` tree, soft-flag (hh)); MM `5c22308b…`/`7c682668…`/`d98b4296…`/`adbec108…`. Threshold: 11/11 unchanged.
- **VERIFY-B (git scope)**: PASS — modified/new = `harmonize_linked_v3.py`, `test_linked_cohort_5cii_a_harmonize_smoke.py` (new), `test_linked_cohort_5c_harmonize_smoke.py` (L17 edit), `PRE_FLIGHT_LOG.md`, + `RECEIPTS/`/`DECISION_LOG.md`/`STATUS.md`. NO `.parquet`/`harmonized_schema.csv`/validation-CSV/`field_specs.py`/parser. Threshold: scope ⊆ {scripts, test, plan, state}.
- **VERIFY-C (additive / behavior-preserving)**: PASS — `git diff --numstat` `harmonize_linked_v3.py` **259 / 18**; the 18 deletions are EXCLUSIVELY the `_harmonize_cohort_batch` docstring reword (14) + the NotImplementedError message-string reword (4) — **0 logic-deletion lines** (`git diff | grep '^-' | grep -E 'out\[|return pa.|pc.|_get_col|opt_int|cols = set|...'` = 0). The 5c-i 1989-1991 inline body anchor (`out["underlying_cause_icd9"] = opt_str("UCOD")` + the 225-byte-1989-1991 comment) + the 2005+ body anchor (`is_post2013 = "OEGEST_COMB" in cols`) present unchanged → both byte-untouched (just shifted by the inserted function). `OUT_SCHEMA` 80 cols unchanged (no schema mutation; DO step 6 owns the schema-CSV + version bump). `test_linked_cohort_5c_harmonize_smoke.py` 13/6 = the minimal L17 `tracks-current-state` Edit. `python -c "import harmonize_linked_v3"` OK; `_harmonize_cohort_1995_2002` callable.
- **VERIFY-D (pytest count gate)**: PASS — canonical **4-dir** suite `fetal_death/ natality/ tests/ matched_multiples/` = **233 passed, 1 skipped, 1 xfailed in 344.68s**. Reconciles exactly: the 5c-i-close baseline 213P+1S+1XF **+** 22 new 5c-ii-a SMOKE **−** 2 (the 5c-i `test_tier0_unimplemented_cohort_eras_failclosed` parametrize 5→3, the L17 stale-pin trim) = 233P; zero regression (1 skipped + 1 xfailed unchanged; all prior cohort + canonical harmonize_linked tests still pass — the byte-untouched 1989-1991 + 2005+ bodies). Runtime 344.68s within the ~240-380s **advisory** band (soft-flag (kk); the COUNT is the gate).
- **VERIFY-E (SMOKE all PASS)**: PASS — SMOKE 50/50 (the 5c-ii-a 22 + the L17-updated 5c-i 28). Tier 0 48/48 first-run; the Tier-1 §7-#13 cross-check MATCHS-code imprecision resolved BEFORE the commit per §9-#4 (corrected the test, not the harmonizer; the L10-safe 15:00:00Z addendum) → Tier 1 PASS (real LinkCO95 ICD-9 + LinkCO99 ICD-10: schema == OUT_SCHEMA; data_year/cert_rev correct; the ICD split 100%; `infant_death (AGED) ⟺ MATCHS==1` exact; record_weight non-null float).

### Reproducibility

- `_harmonize_cohort_1995_2002` + `_cohort_era` + the reused recodes are deterministic pure functions; re-running yields identical output. The SMOKE re-derives from synthetic + the SHA-anchored real archives → deterministic.
- Zero canonical parquet rebuild (DO step 5c-ii-a is harmonize-side code only; the re-harmonize is DO step 6). H10 gate: 11/11 SHAs byte-exact.
- No new Python dependency (reuses pyarrow + the existing helpers; the C8.5a pinned-env SHA untouched).
- The architecture + the 1995-2002 map + the MATCHS-code finding are state-on-disk in `PRE_FLIGHT_LOG.md` 2026-05-19T14:00:00Z + the 15:00:00Z addendum + `DECISION_LOG.md` 2026-05-19T17:00:00Z.

### Cross-product re-probe (if applicable)

- N/A — DO step 5c-ii-a is additive linked harmonize-side code only; no cross-product artifact consumes it until DO step 6 (re-harmonize → v4). 11/11 gate parquet SHAs byte-exact (incl. fetal-death + natality + MM, untouched). The canonical v3 2005-2023 linked product + the 5c-i 1989-1991 path are unperturbed.

### Git

- Pre-DO rollback anchor: `C8.18-pre-do` @ `6632a15` (unchanged; the C8.18 DO rollback anchor).
- This commit: intermediate C8.18 DO step 5c-ii-a — **NO tag** (Convention 5; `C8.18-complete` is the final-sub-step-only tag).

### STATUS.md updated

- New section dated 2026-05-19T17:00:00Z at the top (newest-first).

### Self-check (§10) — what could I have gotten wrong that VERIFY wouldn't catch?

1. **The 1995-2002 per-field ALIAS is anchor-distribution-verified on 400 LinkCO95 + 400 LinkCO99 records + the synthetic Tier-0, not the full 8-year (1995-2002) column set.** A latent alias mis-map on a non-anchor field would not be caught at 5c-ii-a. *Mitigation*: the cohort raw names were read directly from the state-on-disk `field_specs.LINKED_BIRTH_/_DEATH_1995_2002_FIELDS` (4a/5a value-verified) + mapped onto the natality V1-core 1989-rev semantics (H7); one fixed-width layout spans 1995-2002 (field_specs-documented + 5a-verified). *Recommendation*: DO step 6 PRE-FLIGHT must value-distribution-verify the full 1995-2002 harmonized column set vs the NCHS published cohort Linked File report tables (the §15.D VERIFY criterion). Logged DECISION_LOG residual risk.
2. **`infant_death = AGED-non-blank` cross-checked == `MATCHS==1` on real 1995 (ICD-9) + 1999 (ICD-10) only** — 1996-1998 + 2000-2002 not exercised end-to-end at 5c-ii-a. *Mitigation*: the cross-tab is *perfect* on both sampled sub-era reps (1995 17/17, 1999 26/26) + the 1990 baseline (30/30); one fixed-width layout spans 1995-2002 (the ICD revision is the only sub-era variation, value-domain not byte-shift). *Recommendation*: DO step 6 per-year cohort IMR vs the NCHS cohort Linked File report (1995-2002). Logged DECISION_LOG residual risk.
3. **The ICD-9/ICD-10 split keys on the cohort birth year via `if year <= 1998`** (a Python branch on the file's cohort year). Correct because each NCHS 1995-2002 cohort file is a single birth year (probe confirmed `BIRYR` all == `year` == the file year); a hypothetical mixed-birth-year file would mis-route. *Mitigation*: NCHS cohort-linked files are single-birth-year by construction (BIRYR-verified); the SMOKE asserts the split on both sub-eras + the boundaries (1995/1998 ICD-9, 1999/2002 ICD-10). Logged DECISION_LOG residual risk.
4. **Composite multi-field blocks (MEDRISK/OBSTETRC/LABOR/DELMETH/NEWBORN/CONGENIT + preterm_recode3, father_race_ethnicity_5) are conservatively NULL at 5c-ii-a** — a downstream analysis expecting cohort-1995-2002 smoking/diabetes would get null. *Mitigation*: the natality is_pre1989 / 5c-i conservative-mapping precedent; documented soft-flag (gg) DO step 6 leaf decomposition; the SMOKE asserts the null. Logged DECISION_LOG residual risk.
5. **RECWT→record_weight parsed via `_to_float_or_null` (the same helper the 2005+ body uses).** A latent format/locale issue would surface at DO step 6. *Mitigation*: the real-data probe showed RECWT = "1.000000"/"1.487213" (clean float); the SMOKE asserts a non-null float ≥ 1.0 on real LinkCO95/99. Logged DECISION_LOG residual risk.
6. **The 5c-i harness `test_tier1_real_1990_parse_then_harmonize` still uses the imprecise `m in ("1","2")` MATCHS cross-check.** It is NOT stale (PASSES — real LinkCO90 den-plus carries no `MATCHS==2`) + OUT of 5c-ii-a's minimal-scope L17 bundle (§9-#7 — do not modify a non-stale verified harness beyond the stale-pin fix). A future divergence in the 1989-1991 numerator path could be masked by the looser code. *Mitigation*: documented in the 15:00:00Z addendum + here; the harmonizer's `infant_death = AGED-non-blank` is value-driven (no MATCHS assumption). *Recommendation*: DO step 6 PRE-FLIGHT (or a dedicated harness-precision pass) tighten the 5c-i cross-check to `m == "1"`. Logged DECISION_LOG residual risk + Forward-looking HALT.
7. **Behavior-preservation for the 5c-i 1989-1991 path + the 2005-2023 path is asserted via the additive diff (259/18, 0 logic-deletion, anchors intact, byte-untouched bodies) + the cohort-branch-year guards + the 233-baseline (incl. the explicit 1989-1991 regression-lock test in the new harness)**, NOT a full re-harmonize byte-diff (DO step 6). A latent perturbation visible only in the full re-harmonize would not be caught here. *Mitigation*: the 1995-2002 branch fires only for 1995 ≤ year ≤ 2002; the 1989-1991 body + 2005+ body byte-untouched; 11/11 gate SHAs byte-exact. *Recommendation*: DO step 6's "2005-2023 + per-era byte-clean regression" vs the v3 baseline is the durable §15.D check. Logged DECISION_LOG residual risk.

Each logged in `DECISION_LOG.md` 2026-05-19T17:00:00Z as a residual risk; the recommended checks are DO-step-6 PRE-FLIGHT items (not added to VERIFY without human sign-off per §10).

### Forward-looking HALTs for next session (C8.18 DO step 5c-ii-b)

1. `C8.18-pre-do`@`6632a15`; `C8.18-complete` NOT present (final-sub-step-only). `C8.17-complete` present. HEAD = this DO step 5c-ii-a commit (after `97d12fd`).
2. **11 gate parquet SHAs unchanged** (the same 11; fetal-death via the `~/Desktop/fetal-death-harmonization-build/` tree, soft-flag (hh)). Linked-derived `9b828a4d…` changes only at the later C8.18 re-harmonize DO step 6 (5a/5b/5c-* are parser/harmonize substrate only).
3. **Canonical pytest baseline is now `233 passed, 1 skipped, 1 xfailed`** (was 213P+1S+1XF; +22 new `test_linked_cohort_5cii_a_harmonize_smoke.py` − 2 the 5c-i L17 parametrize trim). The canonical suite is the **4-dir** `fetal_death/tests/ natality/tests/ tests/ matched_multiples/tests/`. Runtime ~340-380s (advisory band; the COUNT is the gate) → soft-flag (kk). README 3-dir/"56 passed" line stays stale → soft-flag (jj).
4. **`harmonize_linked_v3` now exposes** `_harmonize_cohort_1995_2002` (NEW) + `_cohort_era` + `_harmonize_cohort_batch` (1989-1991 + 1995-2002 implemented; 2003/2004 + the keyless 1983-1988 RAISE `NotImplementedError`) + the 2 within_era ICD-9 OUT_SCHEMA columns + the H7 recodes. **State-on-disk; do NOT re-derive.** **DO step 5c-ii-b = the 2003 + 2004 cohort birth-side maps** (the 2003-revision dual-certificate transition — the natality V1-core `is_2003revised` analog; `REVISION`@7 S=1989-unrevised/A=2003-revised; `MAGER41`@89-90 (2003) / `MAGER`@89-90 (2004, the 2003-sibling MAGER41 recode — NOT single-year age); `MBRACE`@139-140 already-bridged + `MRACEREC`@143; `MEDUC`@155 + `UMEDUC`@156-157 dual; `UMHISP`@148; `MAR`@153; `COMBGEST`@451-452; `DBWT`@467-470 (2003) / `BRTHWGT`@467-470 (2004); `SEX`@436; `DPLURAL`@423; `APGAR5`@415-416; `ATTEND`@408; `RESTATUS`@138; `PRECARE`@245-246; `UPREVIS`@270-271; death-side `AGED`@755-757 (2003) / @872-874 (2004), `UCOD` ICD-10, `UCODR130`, `MANNER`@761/@878 present → `manner_of_death`, `RECWT`@776-783/@893-900; the 4b/4c `LINKED_BIRTH_/_DEATH_2003/2004_FIELDS` state-on-disk). Cohort 2003/2004 = ICD-10 (deaths 2003-2005) → `underlying_cause_icd10`/`cause_recode_130` populated, `underlying_cause_icd9`/`cause_recode_61` null (≥1999 per the 5c-i shape). **DO step 5c-iii = the keyless 1983-1988 `link_segment` den/num → `infant_death`/`record_weight` one-row-per-birth encoding** (the highest-risk methodology-laden sub-step; the 5b construction model is state-on-disk + RESOLVED — do NOT re-open; the documented within-era structural difference = Phase-D D.4).
5. **The within_era ICD-9/ICD-10 cause-column-shape is RESOLVED + state-on-disk** (DECISION_LOG/PRE_FLIGHT_LOG 2026-05-19T11:00:00Z; §15.D DO step 1): `underlying_cause_icd9`/`cause_recode_61` (cohort birth-year ≤1998), `underlying_cause_icd10`/`cause_recode_130` (≥1999), keyed on cohort birth year. 5c-ii-a APPLIED it for 1995-2002 (≤1998 ICD-9 / ≥1999 ICD-10); 5c-ii-b applies it for 2003/2004 (all ICD-10). Do NOT re-open the shape.
6. **The `harmonized_schema.csv` metadata edit + the v3→v4 version bump is DO step 6** (Anti-Pattern #6). 5c-ii-a did NOT touch it (`OUT_SCHEMA` 80 cols unchanged). DO step 6 MUST widen the death-side rows' years_available 2005-2023 → 1983-2023 (1992-1994 gap) + add the `underlying_cause_icd9`/`cause_recode_61` rows + the ICD-9/10 null-pattern notes + bump v3→v4. If DO step 6's PRE-FLIGHT does not enumerate these, halt.
7. §15.D substrate-format reconcile applied at the 3a commit; the broader model-clarification — now ALSO folding the **5c-ii → 5c-ii-a/5c-ii-b decomposition + the 1995-2002 den-plus MATCHS-encoding finding (linked-infant-death code = MATCHS==1 uniform; survivor code era-dependent 3/2)** alongside 3a/3b + 4a/4b/4c + 5→5a/5b/5c + 5c→5c-i/5c-ii/5c-iii — remains soft-flag (ii) proposed-not-applied §11 human-merge (queued with the soft-flag-(w) batch). On-disk PRE_FLIGHT_LOG/DECISION_LOG/RECEIPTs authoritative meanwhile.
8. **soft-flag (gg) carries**: the cause-column SHAPE is resolved (HALT 5); the per-condition leaf decomposition of the composite MEDRISK/OBSTETRC/LABOR/DELMETH/NEWBORN/CONGENIT/ENTITY/RECORD spans (+ preterm_recode3, father_race_ethnicity_5) stays conservatively NULL → DO step 6 (the natality is_pre1989 / 5c-i precedent).
9. **L17 forward-looking discipline**: 5c-ii-a configured NO new parser dispatcher year (harmonize-side only; the parser handles 1995-2002 via 4a/5a). DO step 5c-ii-b implementing 2003/2004 MUST grep-scope ALL **9** cohort SMOKE harnesses' negative-era/`pytest.raises` sets — the **2003/2004 NotImplementedError pins go stale** in BOTH `test_linked_cohort_5cii_a_harmonize_smoke.py::test_tier0_unimplemented_eras_still_failclosed` AND `test_linked_cohort_5c_harmonize_smoke.py::test_tier0_unimplemented_cohort_eras_failclosed` when 5c-ii-b implements them → bundle the minimal `tracks-current-state` Edit into the SAME 5c-ii-b commit (the §4.2.1/Convention-2/L17 mechanism). pytest COUNT 233P+1S+1XF is the gate; runtime band advisory (soft-flag (kk)). **Additional**: the 5c-i harness `test_tier1_real_1990_parse_then_harmonize` `m in ("1","2")` MATCHS cross-check is imprecise-but-not-stale (PASSES; LinkCO90 has no MATCHS==2) — a recommended future tighten to `m == "1"` (DO step 6 PRE-FLIGHT or a harness-precision pass); out of 5c-ii-a scope per §9-#7.

### Notes for next session

- **C8.18 DO step 5c-ii-a is CLOSED.** `_harmonize_cohort_1995_2002` (the 1989-rev sibling of the 5c-i 1989-1991 reference era; the per-era deltas NPREVIST / no-DFEDUC / RECWT→record_weight / the cohort-birth-year ICD-9-ICD-10 split / no-MANNER) is state-on-disk + this receipt + `PRE_FLIGHT_LOG.md` 2026-05-19T14:00:00Z + the 15:00:00Z addendum. The within_era ICD-9/ICD-10 cause-column shape stays RESOLVED (do NOT re-open). Next per the 5c-ii→5c-ii-a/5c-ii-b decomposition: **DO step 5c-ii-b — the 2003 + 2004 cohort birth-side maps** (the 2003-rev dual-certificate transition; the natality V1-core `is_2003revised` analog; reuse the architecture + the resolved cause shape (all ICD-10); ~1 session), then **DO step 5c-iii — the keyless 1983-1988 `link_segment` encoding** (the highest-risk methodology-laden sub-step; the 5b model RESOLVED + state-on-disk; ~1 session), then DO step 6 (re-harmonize linked 1983-2023; v3→v4 + the `harmonized_schema.csv` edit + version bump) + DO step 7 (docs/metadata; tag `C8.18-complete`).
- §2 cheap-before-expensive + the 3a-substrate-first / §9-#8 / §9-#9 discipline governed 5c-ii-a (decompose 5c-ii by risk; ship the 1989-rev sibling of the verified reference era; reuse the H7 recodes; write the SMOKE FIRST; **fix the test — not the harmonizer — when SMOKE surfaced the §7-#13 cross-check MATCHS-code imprecision**, via an L10-safe dated PRE_FLIGHT_LOG addendum, §9-#4). Carry it.
- The §15.D model-clarification `[plan-update]` (soft-flag (ii)) is owed to the human (§11; queued with the soft-flag-(w) batch; now also folds 5c-ii→5c-ii-a/5c-ii-b + the 1995-2002 MATCHS-encoding finding); on-disk logs authoritative meanwhile.
- Runtime advisory-band (~340-380s clean) = soft-flag (kk); environmental cohort-Tier-1 I/O, not a canonical regression (11/11 gate SHAs byte-exact; the COUNT 233P+1S+1XF is the gate).
- `/tmp/c8_18_s5cii_*` scratch (gate-SHA + pytest output + the read-only probes) is OS-cleanable + reproducible.
