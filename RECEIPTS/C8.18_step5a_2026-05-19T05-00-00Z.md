# Receipt: C8.18 DO step 5a — cross-era denominator-member finder + den-plus parse verification for the 4 self-contained denominator-PLUS cohort eras (1989-1991, 1995-2002, 2003 [DEFLATE64], 2004)
## 2026-05-19T05:00:00Z

### What was done

Executed C8.18 **DO step 5a** under the full five-phase discipline in one session (PRE-FLIGHT + SMOKE + DO + VERIFY + RECEIPT; the C8.18 DO step 3a/3b/4a/4b/4c single-session-DO-step precedent). The §15.D **DO step 5** ("parser + `harmonize_linked.py` extensions per cohort era") was decomposed at the Convention-3 PRE-FLIGHT snapshot — before any code mutation — into three genuinely separable concerns of materially different risk (the 3a/3b + 4a/4b/4c per-concern-decomposition precedent): **5a** = cross-era denominator-member finder + verify the den-plus parse for the 4 self-contained denominator-PLUS cohort eras; **5b** = the 1983-1988 pure-two-file self-contained-numerator + aggregate-denominator construction + the numerator-member finder; **5c** = `harmonize_linked_v3.py` per-cohort-era field mapping + ICD-9 default-null/revision-tagged + `cause_recode_130` per-era (soft-flag (gg)). This session shipped **5a** (the lowest-risk verifiable increment; the 3a-denominator-substrate-first precedent).

5a broadened **`parse_linked_year._find_denomplus_member`** from a single `"DENOM"`-substring rule to a **behavior-preserving** cross-era finder: **Rule 1 (DENOM-first)** = the pre-5a rule verbatim (the FIRST member whose upper-name contains `"DENOM"`) — so the canonical 2004/2005+ `VS{yy}LKBC.DUSDENOM` selection is **byte-identical**; **Rule 2 (cross-era fallback, only if no `"DENOM"` member exists)** = the UNIQUE member whose upper-name contains `"DEN"` and not `"NUM"`/`"UNL"`/`"UNM"` (covers 1983-1991 `LinkCO{yy}USden.dat`, 1995-2002 `LinkCO{yy}US{Den,DEN}.dat`, 2003 `VS03LKBC.USDENPUB`); zero or >1 candidates → `RuntimeError` (§2 fail-closed). The `_layout_for_linked_year` / `_numerator_layout_for_linked_year` dispatchers + the 3a/3b/4a/4b/4c + 2005/2014 specs + `iter_parsed_records` / `run_parse` / `_slice_field` are **byte-untouched** (single diff hunk, confined to `_find_denomplus_member`; H10/HALT-13). Added the NEW sibling `DESIGN: tracks-current-state` SMOKE harness `natality/tests/test_linked_cohort_denmember_5a_smoke.py` (17 tests; Convention 1/2; kept separate for sub-step isolation). **Zero canonical parquet / schema / validation-CSV mutation** (11/11 gate parquet SHAs byte-exact at VERIFY-A).

**One material L13-extension-class finding, surfaced + resolved at the Convention-3 snapshot before any code mutation:** the prior `RECEIPTS/C8.18_step4b`/`4c` forward-notes framed "DO step 5 must wire the `7z` stream into the actual 2003 parse path" — a non-binding forward-note. The cheap-check (reading `zip_text_stream.iter_lines_from_zip`) showed the `7z x -so` fallback for `compress_type ∉ {STORED, DEFLATED}` is **ALREADY wired** (added for natality 2009-2013 DEFLATE64 / 2015 PPMd) and `parse_linked_year.iter_parsed_records` already calls it — so once `_find_denomplus_member` returns `VS03LKBC.USDENPUB`, the 2003 DEFLATE64 member streams automatically. A read-only pre-authoring probe confirmed this **first-run-clean on real data** (DOB_YY=2003, 0 bad-length/300). Not a §7 halt (the forward-note was non-binding; the cheap-check superseding it is exactly the Convention-3 mechanism — same class as the 4b/4c falsifications). Folded into soft-flag (ii) §11 human-merge. Like DO step 3b/4a/4b/4c, going straight from the verified probe to authoring produced code that the new SMOKE Tier 0/1 verified on real data on the FIRST run (no test loosened — §2 fail-closed; §9-#4).

### Inputs consumed

- `PRE_FLIGHT_LOG.md` 2026-05-19T05:00:00Z — per-sub-step PRE-FLIGHT (before any code mutation; L10-safe). 9/9 forward-looking HALTs from `RECEIPTS/C8.18_step4c` re-verified; 11/11 gate parquet SHAs byte-exact; canonical pytest baseline `147 passed, 1 skipped, 1 xfailed in 315.14s` (4-dir) preserved; the read-only pre-authoring probe value-distribution-verified the broadened finder + den-plus parse on real `LinkCO{89,95,02,03,04}` + `LinkCO05US` data FIRST-RUN-CLEAN; the §15.D DO step 5 → 5a/5b/5c decomposition + the DEFLATE64-at-2003-already-wired finding + 4 within-task scope decisions surfaced + resolved before DO (Convention 3).
- `RECEIPTS/C8.18_step4c_2026-05-18T23-00-00Z.md` + `DECISION_LOG.md` 2026-05-18T23:00:00Z + the 3a/3b receipts (the 9 forward-looking HALTs; the structural model: 1989-2004 = self-contained denominator-PLUS; 1983-1988 = pure two-file self-contained-numerator + aggregate-denominator, NO record-level public-use key).
- `natality/scripts/01_import/zip_text_stream.py` (`iter_lines_from_zip` — the pre-existing `7z x -so` fallback for `ct ∉ {0,8}`) + `parse_linked_year.py` (`_find_denomplus_member` — the single function 5a broadens; the dispatchers byte-untouched) + `field_specs.py` LINKED cohort reclen constants (state-on-disk; read-only).
- `~/Desktop/natality-harmonization/raw_data/linked/LinkCO{89,95,02,03,04,05}*.zip` — read-only, first ≤300 records of the den member per era (the pre-authoring probe + the SMOKE Tier 1); SHA-anchored at C8.18 DO step 2. 2003 = DEFLATE64 (`ct=9`), streamed via `/opt/homebrew/bin/7z` (p7zip 17.06; pre-existing dependency-of-record via `zip_text_stream`; no new dep).
- 11 gate parquets — SHA-verified byte-exact at PRE-FLIGHT entry cheap-check and re-verified at VERIFY-A.

### Outputs produced

Git-tracked (this commit):

- `natality/scripts/01_import/parse_linked_year.py` — `_find_denomplus_member` broadened (`git diff --numstat` = **46 insertions / 7 deletions**; the 7 deletions = the old 7-line single-`"DENOM"`-rule body, replaced by DENOM-first + the cross-era `"DEN"`-not-`NUM`/`UNL`/`UNM` fallback + an informative fail-closed `RuntimeError`). The diff is a **single hunk** `@@ -71,14 +71,53 @@`, confined to `_find_denomplus_member`; `_layout_for_linked_year` / `_numerator_layout_for_linked_year` / `iter_parsed_records` / `run_parse` / `_slice_field` are **byte-untouched** (grep-confirmed absent from the diff).
- `natality/tests/test_linked_cohort_denmember_5a_smoke.py` — NEW; `DESIGN: tracks-current-state`; 17 tests (Tier 0: 5 per-era resolution + 2 the-2004/2005-DENOM-rule regression-lock + 4 fail-closed L3 negatives; Tier 1: 5 real-data per-era end-to-end `iter_parsed_records` + 1 the-2005-regression-unchanged).
- `PRE_FLIGHT_LOG.md` — C8.18 DO step 5a PRE-FLIGHT entry (2026-05-19T05:00:00Z; before any code mutation; L10-safe) incl. the state-on-disk probe table.
- `RECEIPTS/C8.18_step5a_2026-05-19T05-00-00Z.md` — this file.
- `DECISION_LOG.md` + `STATUS.md` — appended (DO step 5a decomposition + finding + 4 decisions; new dated section).

No canonical parquet / `harmonized_schema.csv` / validation-CSV touched. Raw zips/guides are gitignored + out-of-tree (read-only, unchanged). §15.D NOT edited this step (the broader model-clarification — incl. the 5→5a/5b/5c decomposition + the DEFLATE64-at-2003-already-wired finding — stays soft-flag (ii) §11 human-merge, the 3a/3b/4a/4b/4c precedent).

### Five-phase trace

- **PRE-FLIGHT**: ✓ `PRE_FLIGHT_LOG.md` 2026-05-19T05:00:00Z (per-sub-step, before any code mutation; L10-safe). 9/9 forward-looking HALTs re-verified; 11/11 gate parquet SHAs byte-exact; canonical pytest 147P+1S+1XF/315.14s baseline preserved; the read-only probe verified the broadened-finder + den-plus parse FIRST-RUN-CLEAN on real data (incl. 2003 DEFLATE64 + the 2005 regression); the decomposition + finding + 4 decisions surfaced + resolved before DO (Convention 3).
- **SMOKE**: ✓ NEW harness authored FIRST (§9-#9). Run against the **un-broadened** finder = **8 failed, 9 passed** (the 4 cohort-era resolution tests + the 4 cohort Tier-1 tests RED — `_find_denomplus_member` raised for the 1983-2003 members; 2004/2005 + fail-closed negatives + 2005-regression already green): proves the harness genuinely tests the broadening claim (L3 / §9-#5, not a rubber-stamp). Tier 0 = synthetic real-`.zip` archives per era + L3 fail-closed negatives; Tier 1 = the real `iter_parsed_records` end-to-end path on 1989/1995/2002/2003[DEFLATE64]/2004 + the 2005 regression.
- **DO**: ✓ broadened `_find_denomplus_member` (behavior-preserving for 2005+) + the new SMOKE harness. Re-run = **17 passed** (first-run-clean; the probe had already proved the logic; no test loosened). Single commit (PRE-FLIGHT + DO + RECEIPT; the C8.18 DO 3a/3b/4a/4b/4c precedent).
- **VERIFY**: ✓ all 5 criteria (below).
- **RECEIPT**: ✓ this file.

### Verify results

- **VERIFY-A (zero canonical mutation)**: PASS — 11/11 gate parquet SHAs byte-exact vs PRE-FLIGHT HALT 2 + the 4c receipt VERIFY-A: natality `c8a740eb…`/`acb5c48a…`; `.v28_baseline` `230efed2…`/`e16ad532…`; linked-derived `9b828a4d…`; fetal-death `38e2cecb…`(harm)/`185c071e…`(deriv) (via the `~/Desktop/fetal-death-harmonization-build/` tree, soft-flag (hh)); MM `adbec108…`(harm)/`5c22308b…`(1995-1997 raw)/`7c682668…`(1995-2000 raw)/`d98b4296…`(2016-2020 raw). Threshold: 11/11 unchanged.
- **VERIFY-B (git scope)**: PASS — modified/new = `parse_linked_year.py`, `test_linked_cohort_denmember_5a_smoke.py` (new), `PRE_FLIGHT_LOG.md`, + `RECEIPTS/`/`DECISION_LOG.md`/`STATUS.md`. NO `.parquet`/`harmonized_schema.csv`/validation-CSV. Threshold: scope ⊆ {scripts, test, plan, state}.
- **VERIFY-C (behavior-preserving / no regression of the canonical path)**: PASS — `git diff --numstat` = `parse_linked_year.py` **46/7** (a single hunk confined to `_find_denomplus_member`; the 7 deletions = the old single-rule body, replaced by the DENOM-first + cross-era fallback — behavior change ONLY for the 1983-2003 cohort members the canonical v3 build does not parse); `field_specs.py` **0/0** (specs/dispatchers byte-untouched). The canonical 2004/2005+ `DUSDENOM` selection is byte-identical, asserted by `test_tier0_2004_2005_resolved_by_unchanged_DENOM_rule` (2 params) + `test_tier1_2005_regression_unchanged` + the sibling harnesses' `test_prior_substep_dispatchers_unregressed` (untouched dispatchers; PASS within the 147). No prior test pinned `_find_denomplus_member`; the new harness now locks the 2005-2015 regression.
- **VERIFY-D (pytest count gate)**: PASS — canonical **4-dir** suite `fetal_death/ natality/ tests/ matched_multiples/` = **164 passed, 1 skipped, 1 xfailed**. Reconciles exactly: the 4c-close baseline 147P+1S+1XF preserved (zero regression; 1 skipped + 1 xfailed unchanged; 147 still pass — incl. all 5 cohort sibling harnesses' dispatcher-unregressed tests, byte-untouched dispatchers) **+** 17 new 5a SMOKE all pass = 164P. **Runtime-band note (transparent; the 4b precedent):** the first VERIFY-D run measured 432.29s (concurrent ~13 GB gate-SHA hashing + the new 7z-DEFLATE64 Tier-1 streaming); a clean **quiescent re-run = `164 passed, 1 skipped, 1 xfailed in 389.42s`** (identical COUNT). 389s is ~2% above the ~240-380s **advisory** band — fully explained by the NEW harness's real-data Tier-1 (5 large-zip 200-record streams incl. the 2003 `7z` DEFLATE64 subprocess on the 3.2 GB `VS03LKBC.USDENPUB`), entirely in test code, NOT a canonical-pipeline regression (the pipeline is byte-untouched; 11/11 gate SHAs byte-exact; reproducibility intact). Per §8 L17 / HALT 9 / the 4b precedent the **COUNT is the gate** (PASS); the advisory band has been creeping as the cohort harnesses accumulate real-data Tier-1 (222→315→336→389s) → soft-flag (jj)/(kk) for a future re-baseline `[plan-update]`; not a §7 halt.
- **VERIFY-E (SMOKE all PASS)**: PASS — new 5a harness 17/17. Tier 0: 5 per-era den-member resolutions (1983-1991, 1995-2002 mixed/upper, 2003, 2004/2005+) + the 2-param DENOM-rule regression lock + 4 fail-closed L3 negatives (num+unl-only, 2003-num+unl-only, empty, ambiguous-two-DEN — all RAISE). Tier 1 (real SHA-anchored zips): the real `iter_parsed_records` end-to-end path resolves the era den member + parses 200 records with the cohort-year anchor (BIRYR/DOB_YY) == the cohort year for 1989/1995/2002/2003[DEFLATE64-via-7z]/2004; the 2005 den member still resolves via the unchanged DENOM rule.

### Reproducibility

- `_find_denomplus_member` is deterministic (pure function of the zip namelist); re-running yields identical member resolution. The SMOKE harness re-derives resolution from synthetic + real archives → deterministic.
- Zero canonical parquet rebuild (DO step 5a is parser-substrate only; the re-harmonize is DO step 6). H10 gate: 11/11 SHAs byte-exact.
- No new Python dependency (the 2003 DEFLATE64 path uses the pre-existing `zip_text_stream` `7z` fallback / `/opt/homebrew/bin/7z`; the C8.5a pinned-env SHA untouched).
- The read-only pre-authoring probe + the SMOKE Tier 1 are re-runnable from the SHA-anchored cohort zips (state-on-disk in `PRE_FLIGHT_LOG.md` 2026-05-19T05:00:00Z).

### Cross-product re-probe (if applicable)

- N/A — DO step 5a is additive linked parser-plumbing only; no cross-product artifact consumes it until DO step 5b/5c (parse/harmonize) / DO step 6 (re-harmonize). 11/11 gate parquet SHAs byte-exact (incl. fetal-death + natality + MM, untouched). The canonical v3 2005-2023 linked product is unperturbed (the 2005-2015 `parse_linked_year` selection is byte-identical; 2016-2023 uses `parse_linked_cohort_year`, a different finder).

### Git

- Pre-DO rollback anchor: `C8.18-pre-do` @ `6632a15` (unchanged; the C8.18 DO rollback anchor).
- This commit: intermediate C8.18 DO step 5a — **NO tag** (Convention 5; `C8.18-complete` is the final-sub-step-only tag).

### STATUS.md updated

- New section dated 2026-05-19T05:00:00Z at the top (newest-first).

### Self-check (§10) — what could I have gotten wrong that VERIFY wouldn't catch?

1. **The cross-era `"DEN"`-not-`NUM`/`UNL`/`UNM` fallback could mis-resolve on a member-naming convention not in the 6 probed eras** (e.g., a hypothetical cohort year whose den member contains "NUM"/"UNL"/"UNM" as a substring, or two "DEN" members). *Mitigation*: every C8.18 in-scope cohort era (1983-1991, 1995-2002, 2003, 2004) + the canonical 2005+ path was probed on the real SHA-anchored zip and resolves uniquely; the fail-closed `RuntimeError` (zero/>1 candidates) means a future surprise HALTs loudly rather than silently mis-picking (§2). *Recommendation*: DO step 5b/6 full-parse value-distribution-verifies each era's parsed parquet (row counts vs the guide-stated counts) — a mis-resolved member would surface as a wrong record count / reclen mismatch there. Logged DECISION_LOG residual risk.
2. **Behavior-preservation for 2005+ is asserted via member-name pattern, not a full canonical re-parse.** 5a does not re-run the 2005-2023 pipeline (that is DO step 6); the SMOKE asserts the 2004/2005 selection is byte-identical (DENOM-rule) + the dispatchers unregressed, but a latent perturbation that only manifests in a full 19-year re-harmonize would not be caught here. *Mitigation*: the diff is a single hunk confined to `_find_denomplus_member`, Rule 1 is the pre-5a rule verbatim, and `DUSDENOM` (2005-2015) is matched by Rule 1 exactly as before; 11/11 gate SHAs byte-exact. *Recommendation*: DO step 6 re-harmonize's 2005-2023 byte-clean regression (0/N column drift vs the v3 baseline) is the durable check (the §15.D VERIFY criterion). Logged DECISION_LOG residual risk.
3. **1989-1991 / 1995-2002 / 2003 / 2004 den-plus parse correctness is verified on the cohort-year anchor + reclen, not the full field set.** A non-anchor field could be mis-sliced and the cohort-year check would not catch it. *Mitigation*: the den-plus LAYOUTS were each value-distribution-verified field-by-field on real data at 3a/4a/4b/4c (state-on-disk); 5a only adds the member-finder + confirms reclen + encoding + alignment jointly via the cohort-year anchor (the layouts themselves were not re-derived). *Recommendation*: DO step 5c harmonize PRE-FLIGHT value-distribution-verifies the per-era field map before harmonizing. Logged DECISION_LOG residual risk; soft-flag (gg).
4. **The 1983-1988 path is NOT exercised by 5a** (its den member resolves — `LinkCO83USden.dat` via the cross-era rule — but `_layout_for_linked_year(1983-1988)` returns a births-only 91-byte layout with no death section; the self-contained-numerator + aggregate-denominator one-row-per-birth-model construction + the numerator-member finder are DO step 5b). 5a's Tier-1 deliberately excludes 1983-1988 (it is not a self-contained denominator-PLUS era). Not a defect — a scoped deferral, logged.
5. **Runtime advisory-band creep** (389s clean vs 240-380 band) is environmental/test-I/O, not a canonical regression, but if the band is treated as a hard gate by a future session it could be mis-read as a regression. *Mitigation*: documented transparently here + in STATUS HALT 9 + soft-flag for a band re-baseline `[plan-update]`; the COUNT is the gate (164P+1S+1XF, exact); 11/11 SHAs byte-exact.

Each logged in `DECISION_LOG.md` 2026-05-19T05:00:00Z as a residual risk; the recommended checks are DO-step-5b/5c/6 PRE-FLIGHT items (not added to VERIFY without human sign-off per §10).

### Forward-looking HALTs for next session (C8.18 DO step 5b)

1. `C8.18-pre-do`@`6632a15`; `C8.18-complete` NOT present (final-sub-step-only). `C8.17-complete` present. HEAD = this DO step 5a commit (after `7f09da0`).
2. **11 gate parquet SHAs unchanged** (the same 11; fetal-death via the `~/Desktop/fetal-death-harmonization-build/` tree, soft-flag (hh)). Linked-derived `9b828a4d…` changes only at the later C8.18 re-harmonize DO step 6 (5a/5b/5c are parser/harmonize substrate only).
3. **Canonical pytest baseline is now `164 passed, 1 skipped, 1 xfailed`** (was 147P+1S+1XF; +17 from the new `test_linked_cohort_denmember_5a_smoke.py`, `DESIGN: tracks-current-state`). The canonical suite is the **4-dir** `fetal_death/tests/ natality/tests/ tests/ matched_multiples/tests/`. Runtime advisory ~390s clean (creeping as cohort Tier-1 accumulates real-zip I/O; the COUNT is the gate) → soft-flag (kk). README 3-dir line stays stale → soft-flag (jj).
4. **`_find_denomplus_member` is now cross-era** (DENOM-first preserves 2005+ byte-identical; cross-era `"DEN"`-not-`NUM`/`UNL`/`UNM` fallback for 1983-2003; fail-closed on zero/>1). State-on-disk in `parse_linked_year.py`; do NOT re-derive. **DO step 5b = the 1983-1988 self-contained-numerator + aggregate-denominator construction + a numerator-member finder** (`"NUM"`-substring, the symmetric sibling of 5a's `"DEN"` rule; 1983-1991 `USnum`, 1995-2002 `USNum`/`USNUM`, 2003 `USNUMPUB`, 2004 `USNUMPUB`) — the methodology-laden one-row-per-birth-model decision for the pure-two-file 1983-1988 era (NO record-level public-use key; the 500-byte numerator IS self-contained; the 91-byte denominator is births-only). H7 fetal-death sibling-parity + the harmonized `infant_death`/`record_weight` semantics for the keyless construction are 5b PRE-FLIGHT methodology questions.
5. **Encoding = ASCII** — `_slice_field` `.decode("latin-1")` works for the cohort `.dat` (re-confirmed at 5a on real `LinkCO{89,95,02,03,04}` data).
6. **DEFLATE64-at-2003 is ALREADY wired** (`zip_text_stream.iter_lines_from_zip` shells to `7z x -so` for `ct ∉ {STORED, DEFLATED}`; `iter_parsed_records` already calls it; probe + SMOKE Tier-1 confirmed 2003 streams first-run-clean). NO new decompressor wiring needed at 5b/5c/6 for 2003. 2003 = DEFLATE64 (`ct=9`); 1983-1991 + 1995-2002 + 2004 + 2005+ = DEFLATE (`ct=8`, stdlib-fine).
7. §15.D substrate-format wording reconcile already APPLIED at the 3a commit; the broader §15.D model-clarification (3a/3b + 4a/4b/4c decomposition; the 5→5a/5b/5c decomposition; the DEFLATE64-at-2003-already-wired finding; the 4b/4c falsifications) remains **soft-flag (ii)** proposed-not-applied for §11 human-merge (queued with the soft-flag-(w) batch).
8. **soft-flag (gg)**: the numerator-only richer multiple-cause detail (`ENTITY`/`RECORD` axes; `cause_recode_130` per-era; ICD-9 1983-1998 vs ICD-10 1999+ underlying; the 2003-rev "Vers*" null-pattern) + the harmonized cause-column shape stay DO step 5c/6. The den-plus "plus" already carries per-infant `UCOD`/`UCODR130`/`AGED`/`AGER5`/`MANNER`/`RECWT` (5a-verified present for 1989-2004).
9. **L17 forward-looking discipline**: any later C8.18 sub-step (5b/5c/6) that configures a new parser/harmonize path or year MUST grep-scope ALL 6 cohort SMOKE harnesses' negative-year/`pytest.raises` sets and bundle the minimal `tracks-current-state` Edit into the same commit (the §4.2.1/Convention-2 mechanism). 5a added NO new dispatcher year (only the finder) so it introduced no stale-pin; 5b's numerator-member finder + 1983-1988 construction likewise must not (it touches the finder/parser, not the dispatcher year-set). pytest COUNT 164P+1S+1XF is the gate; runtime band advisory.

### Notes for next session

- **C8.18 DO step 5a is CLOSED.** `parse_linked_year._find_denomplus_member` is now cross-era (behavior-preserving for the canonical 2005+ path; the 1989-1991/1995-2002/2003-DEFLATE64/2004 self-contained denominator-PLUS eras' den member resolves + the den-plus parse is value-verified first-run-clean on real data), state-on-disk in `parse_linked_year.py` + this receipt + `PRE_FLIGHT_LOG.md` 2026-05-19T05:00:00Z. The "DO step 5 must wire the 7z stream for 2003" forward-note was FALSIFIED — it is already wired by `iter_lines_from_zip`. Next per the 5→5a/5b/5c decomposition: **DO step 5b — the 1983-1988 self-contained-numerator + aggregate-denominator construction + the numerator-member finder** (the methodology-laden one-row-per-birth-model decision for the keyless pure-two-file era; ~1 session), then **DO step 5c — `harmonize_linked_v3.py` per-cohort-era field mapping** (the cohort raw names `MATCHS`/`IDNUMBER`/`BIRYR`/`DMAGE`/… are entirely disjoint from the 2005+ names the harmonizer reads — a large mapping surface; ICD-9 default-null/revision-tagged; `cause_recode_130` per-era, soft-flag (gg); ~1-2 sessions), then DO step 6 (re-harmonize linked 1983-2023; v3→v4) + DO step 7 (docs/metadata; tag `C8.18-complete`).
- The §2 cheap-before-expensive + 3a-substrate-first discipline (verify the low-risk plumbing increment first; isolate the high-risk 1983-1988 keyless construction into its own sub-step with its own SMOKE) governed the 5→5a/5b/5c decomposition; carry it.
- The §15.D model-clarification `[plan-update]` (soft-flag (ii)) is owed to the human (§11; queued with the soft-flag-(w) batch; now also folds the 5→5a/5b/5c decomposition + the DEFLATE64-at-2003-already-wired finding); the on-disk PRE_FLIGHT_LOG/DECISION_LOG/RECEIPTs are authoritative in the interim.
- Runtime advisory-band creep (390s clean) is environmental/cohort-Tier-1-I/O, not a canonical regression (11/11 SHAs byte-exact); soft-flag (kk) for a future band re-baseline `[plan-update]`. The COUNT (164P+1S+1XF) is the gate.
- `/tmp/c8_18_s5*` scratch + the background gate-SHA/pytest output files are OS-cleanable + reproducible.
