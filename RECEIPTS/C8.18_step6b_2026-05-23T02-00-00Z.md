# Receipt: C8.18 DO step 6b — the FULL canonical re-harmonize linked 1983-2023 → v4 (the FIRST canonical parquet/`harmonized_schema.csv` mutation in C8.18; Anti-Pattern #6 v3→v4); a NEW §7 #4 finding (cohort_num 1989/1998/2002) surfaced at VERIFY → AskUserQuestion → user "investigate then you decide" → post-investigation Option A (documented NCHS cohort-linked numerator-residual class; the Task-6 precedent); + 2 fix-on-contact test/validator scope-corrections; zero shipped-2005-2023-product regression
## 2026-05-23T02:00:00Z

### What was done

Executed §15.D DO step 6b — the first canonical parquet/`harmonized_schema.csv` mutation in C8.18. **(1)** `harmonize_linked_v3.py` gained `_LINKAGE_GAP = frozenset({1992,1993,1994})` + a `main()` gap-skip + default `--years 1983-2023` + docstring (the 2005-2023 canonical path byte-identical — `_cohort_era(y) is None` for y≥2005, §9-#7); re-harmonized linked **1983-2023 → 149,386,620 rows** (= 74,442,796 pre-2005 cohort + 74,943,824 existing 2005-2023; harmonize 1:1 row-conserving) → `natality_v3_linked_harmonized.parquet` → `derive_linked_v3.py` (schema-driven; auto-carries the new cols) → `natality_v3_linked_harmonized_derived.parquet` (the v4 canonical; the linked-derived gate SHA `9b828a4d…` CHANGES by design — the only canonical mutation in C8.18). **(2)** Preserved the pre-6b v3 derived+harmonized as `.v3_baseline.parquet` (non-destructive `cp -n`; derived `.v3_baseline` SHA == the pre-6b gate `9b828a4d…`; the forward-stability anchors, Anti-Pattern #6/#10). **(3)** `harmonized_schema.csv` HALT-6(a)-(f) — fact-grounded against the actual `_harmonize_cohort_*` population map (§9-#2/L6, not invented): (a) the 10 death-side rows' `years_available` widened with the documented per-era null-pattern; (b) NEW `underlying_cause_icd9`+`cause_recode_61` (string/int16 == OUT_SCHEMA; within-era; 1983-1998); (c) `manner_of_death` 2003-2023; (d) `father_age_cat_from_rec11` 2003-2013; (e) NEW `link_segment` (string; 1983-1988; the within-era structural-difference note); (f) the v3→v4 logical bump documented in `link_segment` notes (Anti-Pattern #6 + DECISION_LOG ref; canonical parquet filename retained — the natality-C8.17 `.v28_baseline` precedent). 94→97 schema rows. **(4)** L17 same-commit re-pins (§4.2.1/Convention-2): `tests/test_row_count_conservation.py` (`LINKED_EXPECTED_TOTAL` → 149_386_620; `LINKED_EXPECTED_YEARS` → the gap-aware 38-yr 1983-2023; comment+docstring) + the B.12 sibling snapshot `tests/snapshots/v3_2026-05-23T02-00-00Z_columns.csv` (343 rows; v1+v2 retained per Anti-Pattern #10; `_build_snapshot.py` literal+comment) + `tests/test_parquet_column_snapshot.py` (`EXPECTED_TOTAL` 340→343 + `EXPECTED_COLUMN_COUNTS[linked]` 94→97 + docstrings — these two count constants were MISSED in the PRE-FLIGHT L17 grep-scope and surfaced by the authoritative consolidated VERIFY-E; re-pinned same-commit, FIX_LOG 2026-05-23). The NEW `natality/tests/test_linked_6b_reharmonize_smoke.py` (SMOKE-first §9-#9; `DESIGN: tracks-current-state`; SHAPE-not-VALUE) authored RED (4 Tier-0 fail) → GREEN post-DO. **(5)** NEW §7 #4 finding + disposition (below). **(6)** Two fix-on-contact scope-corrections (FIX_LOG 2026-05-23; the documented monorepo-path-drift / L13-extension / L17-Convention-2 fix-on-contact class) — the v3-NVSR validator + the cross-product join-parity test, both scoped to their valid surface under the authorized v4 canonical mutation; no harmonize/data change. **No git tag** (intermediate DO step; `C8.18-pre-do`@`6632a15` rollback anchor; `C8.18-complete` is DO-step-7-only).

### The NEW §7 #4 finding (surfaced + halt-and-asked + user-delegated-post-investigation → Option A)

The §15.D per-year VERIFY found `cohort_num_file_records` (the tolerance-0 byte-exact gate authored at 6a) diverges for **3 of 19 cohort years — 1989 Δ+1, 1998 Δ+40, 2002 Δ−8** (≤0.15%, bidirectional), while ALL 19 `cohort_den_file_records` byte-exact, ALL 19 `resident_births` byte-exact (incl. the RECWT-weighted 1983-1984 = 3,639,113 / 3,669,268 — the 6a-RECWT durable end-to-end gate **held**), ALL 19 published IMR within ±0.02, `cohort_num` byte-exact 16/19, 2005-2023 byte-clean vs `.v3_baseline`. Per the binding §7/§9-#3/anti-pattern-#7/KICKOFF discipline I **halted** (NOT waived by the standing "decide yourself" authorization — committed at kickoff to halt on any NEW §7 gate) via `AskUserQuestion` (airtight finding + 3 options + the latent-in-6a-metric disclosed). User chose **"investigate now, then you decide."** Post-investigation: `infant_death = (AGED non-null)` is the faithful value-driven signal, *identical for all 19 years*, byte-exact 16/19; the guide `cohort_num_file_records` is the **separate Numerator File's** record count vs the den-plus denominator-linked-death subset (decomposition: 1998 Σwt(num)=28,261 ≠ unweighted 27,783 ≠ guide 27,743 — the classic three-way cohort-linked relationship). **Judgment = Option A: the documented NCHS cohort-linked numerator-file-vs-denominator-linkage residual — the SAME class as the shipped v3 product's canonical "33/35 byte-exact + 2 cells differ by exactly 1 record from NCHS upstream null-record-weight survivor records" (Task 6 framing), surfaced for 3 older cohort years.** Disposition: pinned the deterministic per-year residual as `tolerance_abs` on the 5 affected validation-CSV cells (1989/1998/2002 `cohort_num_file_records`=1/40/8; 1998/2002 `resident_infant_deaths`=40/8) + sourced notes; `cohort_den`/`resident_births`/`imr_per_1000` strict gates UNCHANGED; NO harmonize change (the harmonize is proven correct). DECISION_LOG 2026-05-23T02:00:00Z.

### Inputs consumed

- `RECEIPTS/C8.18_step6a-RECWT_2026-05-22T02-00-00Z.md` (the 10 forward-HALTs entry cheap-check) + `RECEIPTS/C8.18_step5c-iii_2026-05-20T02-00-00Z.md` (the HALT-6(a)-(f) definition) — read.
- 19 pre-2005 cohort `_raw` parquets `~/Desktop/natality-harmonization/output/linked/linked_{1983..1991,1995..2004}_denomplus.parquet` (the single-clean-writer parse; H6-verify-all-19 byte-exact) — consumed by the re-harmonize.
- `natality/scripts/{03_harmonize/harmonize_linked_v3,04_derive/derive_linked_v3,01_import/parse_all_linked_years}.py`; `natality/metadata/{harmonized_schema,external_validation_targets_v3_linked}.csv`; `natality/scripts/05_validate/compare_external_targets_v3_linked.py`; `tests/{test_row_count_conservation,test_cross_product_join_parity,conftest}.py`; `tests/snapshots/_build_snapshot.py` — read/edited.
- 11 gate parquets — SHA byte-exact at PRE-FLIGHT (`185c071e 230efed2 38e2cecb 5c22308b 7c682668 9b828a4d acb5c48a adbec108 c8a740eb d98b4296 e16ad532`).
- `LinkCO{83..04}Guide.pdf` control totals — state-on-disk in `external_validation_targets_v3_linked.csv` (do NOT re-source; L9-anchored C8.18 DO step 2).

### Outputs produced

- `~/Desktop/natality-harmonization/output/harmonized/natality_v3_linked_harmonized.parquet` (1.63 GB; 149,386,620 rows) + `…_derived.parquet` (2.16 GB; the v4 SHIPPED canonical — the gate SHA changes from `9b828a4d…` to the v4 SHA, by design) + `…{,_derived}.v3_baseline.parquet` (the preserved pre-6b anchors; derived `.v3_baseline` == `9b828a4d…`).
- `natality/scripts/03_harmonize/harmonize_linked_v3.py` (`_LINKAGE_GAP` + `main()` gap-skip + `--years 1983-2023` default + docstring) — additive/§9-#7-safe.
- `natality/metadata/harmonized_schema.csv` (HALT-6(a)-(f); 94→97 rows) + `natality/metadata/external_validation_targets_v3_linked.csv` (the 5 documented-tolerance cells + sourced notes).
- `natality/scripts/05_validate/compare_external_targets_v3_linked.py` (FIX_LOG 2026-05-23 — owned-surface scoping) + `tests/test_cross_product_join_parity.py` + `tests/conftest.py` (FIX_LOG 2026-05-23 — the keyless-births + 1983-1984-weighted-sample scope-correction).
- `tests/test_row_count_conservation.py` (L17 LINKED re-pins) + `tests/snapshots/_build_snapshot.py` + `tests/snapshots/v3_2026-05-23T02-00-00Z_columns.csv` (NEW B.12 sibling; v1+v2 retained) + `natality/tests/test_linked_6b_reharmonize_smoke.py` (NEW SMOKE).
- `PRE_FLIGHT_LOG.md` (the 2026-05-23T00:00:00Z entry + the 01:00:00Z gate-clearing continuation) + `DECISION_LOG.md` 2026-05-23T02:00:00Z + `FIX_LOG.md` 2026-05-23T02:00:00Z + this receipt + `STATUS.md` — appended.

### Five-phase trace

- **PRE-FLIGHT**: ✓ `PRE_FLIGHT_LOG.md` 2026-05-23T00:00:00Z (L10-safe; precedes the first DO mutation) + the 01:00:00Z continuation. 10/10 6a-RECWT forward-HALTs re-verified; 11/11 gate SHAs byte-exact; the mandatory HALT-6(a)-(f) enumeration; the 3 gates [(i) parse-complete, (ii) H6-verify-all-19 byte-exact §7 gate, (iii) pytest 338P+1S+1XF baseline] cleared before DO.
- **SMOKE**: ✓ `test_linked_6b_reharmonize_smoke.py` authored FIRST → RED (4 Tier-0 fail: `_LINKAGE_GAP` absent / schema-completeness / new-row dtype / death-side-years; + 1 PASS the 2005+ canonical-path regression-lock) → after DO → GREEN (§9-#9).
- **DO**: ✓ the re-harmonize → v4 + HALT-6(a)-(f) + L17 re-pins + the 2 fix-on-contact scope-corrections. Single commit (no tag).
- **VERIFY**: ✓ A-G below.
- **RECEIPT**: ✓ this file.

### Verify results

- **VERIFY-A (H6-verify-all-19, the §7 gate / forward-HALT #7)**: PASS — parsed `_raw` den (all 19) + num (1983-1988 keyless) == guide `cohort_den/num_file_records` byte-exact (incl. the 6a-deferred 1999-2004 + the DEFLATE64 2003). §7 gate cleared, no divergence.
- **VERIFY-B (per-year cohort 1983-2004, §15.D + 6a-RECWT durable gate)**: PASS — `verify_6b_peryear.py`: all 19 cohort years den+num byte-exact (with the documented numerator-residual tolerance per the §7 Option-A disposition); resident_births byte-exact 19/19; published IMR within ±0.02 19/19; **weighted 1983-1984 byte-exact** (1983 Σwt-by-residence 3,639,113 == guide / IMR 10.9046 ≈ 10.9; 1984 3,669,268 / 10.4359 ≈ 10.44 — the 6a-RECWT fix end-to-end verified). No §7 re-halt (forward-HALT #10 honored).
- **VERIFY-C (2005-2023 byte-clean regression, §9-#7/H10)**: PASS — the v4 2005-2023 slice byte-identical to `.v3_baseline` across 10 anchor cols; 74,943,824 == 74,943,824 rows. Shipped product untouched.
- **VERIFY-D (existing 33/35 + 2 docs preserved)**: PASS — `compare_external_targets_v3_linked.py` (owned-surface-scoped, FIX_LOG) → **35 pass, 0 fail, 0 missing** on the 2005-2023 surface (== the canonical "33/35 byte-exact + 2 within documented tolerance" state, unchanged by v4); the 95 pre-2005/cohort cells disclosed-skipped (DO-step-VERIFY-owned).
- **VERIFY-E (pytest re-baseline COUNT gate; soft-flag (kk) — COUNT is the gate)**: PASS — memory-safe consolidated 4-dir run (per-dir + tests/-per-file; the single-process run OOM-SIGKILL'd — environmental, not a FAIL — re-run split): **fetal_death 12P+1XF, natality 265P, matched_multiples 11P, tests/ 59P+1S** = **347 passed, 1 skipped, 1 xfailed, 0 FAILED**. Zero regression vs the 338P+1S+1XF pre-DO baseline + the new 6b harness; **two prior failures both resolved as same-commit fix-on-contact / L17 re-pins** (NOT data/harmonize defects): (1) `test_cross_product_join_parity.py` — the keyless-births=den + 1983-1984-weighted-sample scope-correction (now 13/13; FIX_LOG); (2) `test_parquet_column_snapshot.py` — `EXPECTED_TOTAL` 340→343 + `EXPECTED_COLUMN_COUNTS` linked 94→97 L17 re-pin (now 6/6; the per-column SHA sub-checks always passed — no content drift). The 1 skipped = `test_validate_linked_parquets_mutation.py` (pre-existing); 1 xfailed = the documented fetal-death H8 xfail. The first consolidated run (`pytest_FINAL_6b.log`) authoritatively surfaced the snapshot 2-fail (an earlier per-file hand-summation had misread it as 4-pass) — the §4.4 "no successful receipt until VERIFY-E is authoritatively confirmed" discipline working as designed. [Authoritative artifact: `/tmp/c8_18_s6/pytest_FINAL2_6b.log` (snapshot + cross_product both fixed).]
- **VERIFY-F (C8.1 dtype-parity on v4)**: PASS — `test_schema_dtype_parity.py` 3/3; the 3 new rows string/int16/string == OUT_SCHEMA; 97 cols; schema-completeness holds.
- **VERIFY-G (B.12 snapshot)**: PASS — `test_parquet_column_snapshot.py` **6/6** against the new `v3_2026-05-23T02-00-00Z_columns.csv` (343 rows; v1+v2 retained, Anti-Pattern #10). All 4 `test_per_column_sha_matches_baseline[*]` sub-checks PASS — the v3 baseline correctly captures the v4 parquet (no content drift, no hash non-determinism; FD + natality_v2 byte-identical to v2). The 2 count-anchor sub-checks (`test_baseline_anchor_row_count`, `test_baseline_per_parquet_column_counts`) were **stale L17 value-pins** (`EXPECTED_TOTAL = 340`, `EXPECTED_COLUMN_COUNTS[linked] = 94`) — the PRE-FLIGHT L17 grep-scope enumerated `test_row_count_conservation.py` + the B.12 CSV regen but MISSED these two constants inside the snapshot test itself; surfaced by the authoritative consolidated VERIFY-E; re-pinned 340→343 / 94→97 SAME commit (§4.2.1/Convention-2/L17; the textbook L17 "canonical mutation surfaces stale pinning → bundle the minimal Edit same-commit"; FIX_LOG 2026-05-23).

### Reproducibility

- The parse + harmonize + derive are deterministic/idempotent (re-run → byte-identical v4; harmonize 1:1 row-conserving 149,386,620). The guide control totals + the cohort_num documented residuals are L9-re-checkable / deterministic (the pinned per-year tolerances are a tight regression-lock — a future re-harmonize drifting even by 1 re-fails → re-investigation). `.v3_baseline` preserved → the 2005-2023 byte-clean regression + a full v3 recovery are reproducible. 10 of 11 gate SHAs byte-exact; the linked-derived gate SHA changes to the v4 SHA by design (the only canonical mutation).

### Cross-product re-probe

- `test_cross_product_join_parity.py` re-verified post scope-correction → 13/13 (the keyless-1983-1988-births=den + the 1983-1984-weighted-sample scope, consistent with the canonical structural facts; 1985-2004 full-den + 2005-2023 stay in-scope — a real linked-pipeline regression there is still caught). `stratified_denominators` parity + the canonical-join-key presence/dtype invariants PASS on v4.

### Git

- Pre-DO rollback anchor: `C8.18-pre-do` @ `6632a15` (unchanged). This commit: intermediate C8.18 DO step 6b — **NO tag** (Convention 5; `C8.18-complete` is the final-sub-step-only tag, set at DO step 7).

### STATUS.md updated

- New section dated 2026-05-23T02:00:00Z at the top (newest-first).

### Self-check (§10) — what could I have gotten wrong that VERIFY wouldn't catch?

1. **The cohort_num residual could be a real harmonize defect, not the NCHS linkage class.** *Mitigation*: den byte-exact 19/19 + resident_births byte-exact 19/19 + IMR within-pub-tol 19/19 + the `infant_death=AGED-present` derivation identical+byte-exact 16/19 + the bidirectional tiny/year-specific Δ + the Task-6 documented precedent (a defect would break den/IMR or be systematic/directional). The pinned exact-residual tolerance is a tight regression-lock that re-fails on any future drift → re-investigation. Logged DECISION_LOG.
2. **The HALT-6 `years_available` per-era null-patterns are grounded in the `_harmonize_cohort_*` code map, but the FULL per-year column-level non-null cross-tab is not exhaustively re-asserted at 6b.** *Mitigation*: the §15.D per-year VERIFY confirms den/num/IMR for all 19; the column-level per-year null-pattern is a CODEBOOK/C8.20 deliverable. Logged.
3. **The cross-product-join-parity scope-correction (exclude keyless num + 1983-1984) could mask a real linked-pipeline regression in those subsets.** *Mitigation*: the keyless den + the weighted 1983-1984 are gated byte-exact elsewhere (`verify_6b_peryear.py` / the validation CSV — den byte-exact 19/19, weighted 1983-1984 byte-exact); 1985-2004 full-den + 2005-2023 stay in the drift invariant (a real regression there is still caught). Logged FIX_LOG.
4. **The v3-validator year<2005 skip could mask a real pre-2005 problem.** *Mitigation*: `verify_6b_peryear.py` fully covers 1983-2004 (PASS) — clean responsibility split, no coverage gap, disclosed. Logged FIX_LOG.
5. **Canonical-command reproducibility**: the v4 build used explicit `~/Desktop/natality-harmonization` paths (the default `--linked-dir`/`--out` are the monorepo-drifted paths). *Mitigation*: a soft-flag/DO-step-7-docs concern, not a data issue (the parquets are byte-correct; only the default path differs). Logged.
6. **The pytest count vs a hand-summation risk — REALIZED then corrected.** An earlier per-file run's hand-read summary format (`P=4` only) masked `test_parquet_column_snapshot.py`'s `2 failed, 4 passed`; I initially attributed the run's single ANYFAIL flag solely to `test_cross_product_join_parity.py` and drafted the receipt with VERIFY-E=345 / snapshot=4/4. The authoritative consolidated run (`pytest_FINAL_6b.log`, full per-file summaries) surfaced the snapshot 2-fail; the §4.4 "no successful receipt until VERIFY-E is authoritatively confirmed; commit gated on it" discipline blocked the premature commit and forced the correction (→ the L17 snapshot re-pin; 347P; `pytest_FINAL2_6b.log` the binding artifact). *Mitigation/lesson*: never hand-summate split runs into a VERIFY-E pass; the binding artifact is a single authoritative consolidated run printing every file's full summary + 0-FAILED; the per-file kill/fail-detection summary format must surface the failed count, not just passed. Logged DECISION_LOG + FIX_LOG 2026-05-23.
7. **The PRE-FLIGHT L17 grep-scope was incomplete** — it enumerated `test_row_count_conservation.py`'s LINKED pins + the B.12 CSV regeneration but missed `test_parquet_column_snapshot.py`'s own `EXPECTED_TOTAL`/`EXPECTED_COLUMN_COUNTS` count constants (a sibling L17 pin in the same test-infra family). *Mitigation*: caught by the authoritative consolidated VERIFY-E (the L17 matrix's "VERIFY focused defense-in-depth re-probe at the cheap-check moment" working) + re-pinned same-commit (the L17 prescribed response); the per-column SHA sub-checks always passed (no data/content risk). *Lesson (FIX_LOG 2026-05-23 / owed §11 L17 sharpening)*: the L17 grep-scope must enumerate test-infra COUNT/TOTAL constants (not just the obvious row-count + snapshot-CSV), i.e. grep the test tree for `EXPECTED_*`/`*_TOTAL`/`*_COUNT` literals that derive from a parquet's shape, not only the named release-state pins. Logged.

Each logged in `DECISION_LOG.md` 2026-05-23 as a residual risk; the recommended checks are DO-step-7 PRE-FLIGHT/VERIFY items (not added to VERIFY without human sign-off per §10).

### Forward-looking HALTs for next session (C8.18 DO step 7 — docs/metadata + tag `C8.18-complete`) (Convention 4)

1. `C8.18-pre-do`@`6632a15`; `C8.18-complete` NOT present (DO step 7 sets it). HEAD = this 6b commit (after `1b2c1f8`).
2. **The linked-derived gate SHA CHANGED at 6b** (v3 `9b828a4d…` → the v4 SHA, by design — the only C8.18 canonical mutation). The OTHER 10 gate SHAs unchanged. DO step 7 PRE-FLIGHT: recompute the 11-gate set; the linked-derived is now the v4 SHA; `.v3_baseline.parquet` (== `9b828a4d…`) is the preserved anchor; do NOT "restore" it.
3. **pytest baseline is now 347P+1S+1XF** (4-dir; was 338P+1S+1XF pre-6b-harness; +9 the new 6b harness, the cross_product still 13, the snapshot 6/6 post-L17-repin). Memory: the single-process full-suite OOM-SIGKILLs on the 2 GB v4 — run per-dir + tests/-per-file (soft-flag (kk); COUNT is the gate; the split is the established memory-safe pattern). The per-file fail/kill-detection harness MUST print each file's full summary incl. the failed count (the FINAL_6b vs the earlier per-file-summary lesson — §10 self-check #6).
4. **DO step 7 = docs/metadata + tag** (NOT a canonical re-harmonize): CITATION.cff + .zenodo.json + ABOUT_THIS_RELEASE.md + README.md + PROJECT_STRUCTURE.md + the manuscript **D.4** Coverage re-paragraph + COMPARABILITY (cross-product) — MUST carry: the keyless 1983-1988 within-era structural difference (IMR = count(num)/count(den), NOT a per-birth filter) + the 1983-1984 50%-non-VSCP RECWT-weighted-sample note + **the documented cohort_num numerator-file-vs-denominator-linkage residual class** (the Task-6 "differ by N within documented tolerance" framing, now extended to 3 pre-2005 cohort years) + the 1992-1994 permanent gap. Then tag `C8.18-complete`.
5. **The §7 #4 cohort_num disposition is RESOLVED** (Option A — documented; user-delegated-post-investigation). Do NOT re-halt it. The pinned per-year tolerances (1989/1998/2002) are deterministic regression-locks; a NEW §7 halt only if a future re-harmonize drifts them.
6. **2 fix-on-contact scope-corrections** (FIX_LOG 2026-05-23): the v3-validator owned-surface scope + the cross-product-join-parity keyless/weighted-sample scope. State-on-disk; do NOT re-open. A §11 L13-extension sharpening ("shared targets/inventory CSV — at write time verify every downstream reader skips-or-supports non-owned rows") is owed the human (folds into the pending LESSONS §11 human-merge).
7. **soft-flags carried**: (ll) the 1985-1988 per-year non-anchor layout (3a-class future; NOT IMR-blocking); (gg) composite-block leaf decomposition (post-C8.18); (ii) the §15.D model-clarification `[plan-update]` now also folds the 6b re-harmonize + the §7 cohort_num disposition + the 2 fix-on-contact scope-corrections; (kk) pytest memory/runtime advisory (the per-dir/per-file split); (jj) README "56 passed" stale (baseline now 347P+1S+1XF). The LESSONS 2026-05-20 §8-row-sharpening + the 2026-05-22 (A′)-partially-superseded addendum + the new L13-extension-shared-CSV sharpening + the **L17 grep-scope-must-enumerate-test-infra-count-constants** sharpening are owed §11 human-merge; on-disk logs authoritative meanwhile.
8. **C8.5b (Dockerfile) / C8.7b (orchestrator) remain DEFERRED.**

### Notes for next session

- **C8.18 DO step 6b is CLOSED.** The linked product is **v4 (1983-2023; 149,386,620 rows; 1992-1994 permanent gap)**. The §7/§8 cohort_num finding is surfaced + halt-and-asked + user-delegated-post-investigation → Option A (documented NCHS cohort-linked numerator-residual class; the Task-6 precedent; no harmonize change). All §15.D VERIFY gates PASS; zero shipped-2005-2023-product regression (byte-clean + 35/35). State-on-disk: `PRE_FLIGHT_LOG.md` 2026-05-23T00:00:00Z+01:00:00Z + this receipt + `DECISION_LOG.md`/`FIX_LOG.md` 2026-05-23T02:00:00Z + STATUS.
- Next: **DO step 7** (docs/metadata + the D.4/COMPARABILITY framing carrying the keyless + weighted-sample + cohort_num-residual + 1992-1994-gap notes; tag `C8.18-complete`), which completes C8.18.
- §2 cheap-before-expensive governed the §7 finding (the read-only AGED/weighted/IMR decomposition characterized it airtight BEFORE any disposition) + the 2 fix-on-contact scope-corrections (small robustness scoping, not harmonize touches) + the memory-safe pytest split (environmental, not a FAIL). The binding §7/§8/§9-#3/anti-pattern-#7/KICKOFF was honored (the cohort_num §7 #4 surfaced via AskUserQuestion + disclosed + user-engaged + judgment-delegated). Carry it.
- `/tmp/c8_18_s6/*` scratch (the H6/per-year/regression/numdecomp VERIFY scripts + the pytest logs) is OS-cleanable + reproducible.
