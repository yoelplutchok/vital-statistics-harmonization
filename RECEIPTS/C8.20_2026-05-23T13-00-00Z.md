# Receipt: C8.20

## 2026-05-23T13:00:00Z

### What was done

Shipped C8.20 (CODEBOOK extensions, E.7) under the full five-phase discipline. Authored a deterministic, idempotent, read-only builder `scripts/_build_codebook_extensions.py` that, per documented harmonized variable, renders the three §15.D subsections — (i) per-era value-distribution panel, (ii) sentinel-code disambiguation table, (iii) era-by-era coding-scheme diff — into a marker-delimited generated appendix in both `fetal_death/CODEBOOK.md` (73 variables, 7 documented fetal-death layout eras, 1982–2024) and `natality/docs/CODEBOOK.md` (84 natality variables over 6 natality eras 1968–2024 + 13 linked-v4 death-side variables over 5 linked eras 1983–2023 with the documented 1992–1994 gap = 97 variable blocks). Every count is derived from the gate-verified parquet (H8/L6: zero invented numbers; per-era `n` reconciles byte-exact to the parquet filtered row count); era boundaries are documented NCHS-layout constants cited inline. Added FAQ pointers to both products' FAQ.md, three minimal L11 fix-on-contact scope notes (the stale fetal-death "V2.0 1992-2022" head + the two natality "tracked at C8.20, not yet reflected" notes now resolved/pointing to the appendix), the SHAPE-not-VALUE SMOKE harness (`tests/test_codebook_extensions_smoke.py`, `DESIGN: tracks-current-state`). Zero canonical-state mutation. Architecture/scope recorded in DECISION_LOG 2026-05-23T11:00:00Z.

### Inputs consumed

- `~/Desktop/fetal-death-harmonization-build/output/harmonized/fetal_death_derived.parquet`: sha256=`185c071ec76ab8aa…` (canonical -build tree; the flat `~/Desktop/fetal-death-harmonization/` tree is a stale different build `90af89b9…` and was NOT used — documented monorepo-path-drift class)
- `~/Desktop/natality-harmonization/output/harmonized/natality_v2_harmonized_derived.parquet`: sha256=`acb5c48a9abf82ac…` (natality v3.0.0; 201,161,456 rows)
- `~/Desktop/natality-harmonization/output/harmonized/natality_v3_linked_harmonized_derived.parquet`: sha256=`f630d8cf20db72ea…` (linked v4.0.0; 149,386,620 rows; 1992–1994 gap)
- `fetal_death/harmonized_schema.csv` (73 harmonized variable rows), `natality/metadata/harmonized_schema.csv` (97 rows incl. linked death-side) — canonical variable enumeration + `notes`/`allowed_values` text
- `fetal_death/COMPARABILITY.md`, `natality/docs/COMPARABILITY.md`, `README.md` — documented era-boundary catalogs (era partition = cited documented constants)

### Outputs produced

- `scripts/_build_codebook_extensions.py`: sha256=`11c443969eb72e60…` (NEW; deterministic/idempotent/read-only builder)
- `tests/test_codebook_extensions_smoke.py`: sha256=`4d389011b5aaa7cd…` (NEW; 6 tests; SHAPE-not-VALUE; `DESIGN: tracks-current-state`)
- `fetal_death/CODEBOOK.md`: sha256=`eb6ace53f222dafb…` (generated appendix = 2512 block lines, 73 var blocks × 3 subsections; + 1 L11 scope note)
- `natality/docs/CODEBOOK.md`: sha256=`88c5afbb3591eb04…` (generated appendix = 3110 block lines, 97 var blocks × 3 subsections; + 2 L11 scope-note edits)
- `fetal_death/FAQ.md`: sha256=`1707554e252f0734…` (marker-delimited pointer block)
- `natality/docs/FAQ.md`: sha256=`9e8a6912e480ae7e…` (marker-delimited pointer block)

### Five-phase trace

- PRE-FLIGHT: ✓ (PRE_FLIGHT_LOG.md 2026-05-23T11:00:00Z — one upfront, L10-safe; 11-gate SHAs byte-exact; Convention-3 surfaced d1/d2/d3 + fetal-death path-drift, resolved at cheap-check; committed + `C8.20-pre-do`@`743b4b4`)
- SMOKE: ✓ Tier 0 (4 synthetic-fixture tests: pure render, idempotency, no-wallclock, marker-splice) PASS; Tier 1 (fetal-death real render: 73 vars×3 subsections, byte-identical re-run, per-era n reconciles to parquet — V2 1992-2002 = 700,704) PASS; Tier 2 (natality+linked: 97 vars×3, 0 unresolved cross-links, linked 1989-2004 correctly excludes 1992-1994 gap) PASS
- DO: ✓ builder + both CODEBOOK appendices + 2 FAQ pointers + 3 L11 scope notes (commits `743b4b4`..C8.20-complete)
- VERIFY: ✓ (criteria below)
- RECEIPT: ✓ (this file)

### Verify results

- §15.D V1 — both CODEBOOK.md extended, 3 subsections per variable: PASS (FD 73/73/73; natality 97/97/97 — subsection counts equal variable counts)
- §15.D V2 — cross-link integrity, all internal anchors resolve: PASS (FD 73 links / 73 ids / 0 unresolved / 0 malformed; natality 97/97/0/0)
- §15.D V3 — deterministic builder, re-running produces identical CODEBOOK byte-exact: PASS (authoritative final-state re-run incl. post-head-edit: all 4 targets byte-identical, sha-diff empty)
- §15.D V4 — no test-suite regression (pure docs task): PASS (memory-safe split TOTAL = 353 passed, 1 skipped, 1 xfailed, 0 failed; the pre-existing 347P+1S+1XF baseline preserved with zero regression + the new smoke file's 6 passed = 347+6=353)
- Additive invariant — zero canonical-state mutation: PASS (`git diff C8.20-pre-do` = 4 docs only + 2 NEW code files; 4 input-parquet mtimes unchanged → read-only builder; no parquet/`harmonized_schema.csv`/validation-target/metadata-CSV touched)
- H8/L6 — every appendix number parquet-derived: PASS (per-era `n` reconciles byte-exact to `dataset.count_rows(filter=year∈era)` for residence_status/link_segment/infant_death across sampled eras of all 3 products; era boundaries are cited documented constants, the only non-derived values)
- §7 halt conditions: none tripped

### Reproducibility

- Re-run `uv run python scripts/_build_codebook_extensions.py --product all` → both CODEBOOKs + both FAQs byte-identical (verified twice; final-state idempotency confirmed post head-edits — head edits are outside the regex-replaced marker region by construction).
- No wall-clock / timestamp / abspath in the committed marker block (provenance = input basename + sha256[:12] + rowcount + builder path). Bit-identical on the pinned `uv.lock` env.

### Cross-product re-probe (if applicable)

- N/A for data — C8.20 is additive docs + a read-only builder; the settled envelope (natality v3.0.0 + linked v4.0.0 + fetal-death v2.4.0 + matched-multiples) is unchanged. C8.21/C8.22 "Dependencies: … C8.20" satisfied (no envelope change).

### Git

- Pre-DO tag: `C8.20-pre-do`, commit=`743b4b4`
- Post-RECEIPT tag: `C8.20-complete`, commit=<the C8.20 DO commit this receipt ships in>

### STATUS.md updated

- New section dated 2026-05-23T13:00:00Z marking C8.20 complete (prepended, newest-first).

### Self-check — what could I have gotten wrong that VERIFY wouldn't catch?

1. **The builder is self-consistent but its value/sentinel/era-diff LOGIC is only validated against itself** (SMOKE/idempotency prove determinism + per-era `n`, not that the top-8 cells / sentinel rows / era-diff add-drop sets are independently correct). Mitigation: a fresh-eyes adversarial audit prompt (independent recomputation, not importing the builder) has been prepared for a separate session per the user's request (KICKOFF audit-variant; AUDITS/). This is the primary residual risk and is explicitly deferred to that audit, not silently accepted.
2. **`pc.value_counts` null handling**: if it did not count nulls, per-era `n` would be < parquet rowcount. Mitigated empirically — `n` reconciles byte-exact to `count_rows` for columns that DO have nulls (e.g. natality typed cols), so nulls are counted into the `""` key. Still, an unusual dtype could behave differently; the audit's n-conservation check (every era, every product) closes this.
3. **`_fmt_val` representation collisions** (float 25.0 vs int 25; bool vs string "true"): possible distinct source codes collapsing to one key. Not exhaustively probed here; flagged as audit check #4.
4. **Era-partition coverage**: a parquet year falling outside all era bands would silently vanish from every panel. Mitigated for sampled eras (n reconciles) but NOT exhaustively asserted for every year; flagged as audit check #3 (H6).
5. **L11 scope-note numbers** (fetal-death "2,427,233 rows / 43 yrs / 7 eras") hand-written from the PRE-FLIGHT probe; if the probe drifted they could be stale. Mitigated: probe was the gate-verified parquet; audit check #8 re-verifies independently.
(See NEXT_STEPS.md §8. No addition to any VERIFY without human sign-off per §10.)

### Forward-looking HALTs for next session (C8.21 PRE-FLIGHT) (Convention 4)

- FH1: `C8.20-pre-do`@`743b4b4` + `C8.20-complete` set → C8.20 CLOSED. Next per KICKOFF Tier 3+5 = **C8.21 (Stata + SAS quickstart pointer files, C.3, ~0.5 session)**, then C8.22 (cross-tab CSVs), then Phase D.
- FH2: **11-gate parquet SHAs unchanged** (fetal-death derived `185c071e…`/harmonized `38e2cecb…`; natality v3.0.0 `c8a740eb…`/`acb5c48a…`; `.v28_baseline` `230efed2…`/`e16ad532…`; linked-v4 derived `f630d8cf…`/harmonized `ea89ab3c…`/`.v3_baseline` `9b828a4d…`; MM `adbec108…`/`5c22308b…`/`7c682668…`/`d98b4296…`). C8.20 additive; settled envelope unchanged.
- FH3: **pytest baseline now 353 passed, 1 skipped, 1 xfailed** via the memory-safe split (per-dir fetal_death/ matched_multiples/ natality/ tests/mutations/, + `tests/` per-file; bare `pytest tests/` collects nothing — soft-flag (kk)). The +6 vs the prior 347P is exactly `tests/test_codebook_extensions_smoke.py`. README "56 passed" still stale (soft-flag (jj)).
- FH4: **A fresh-eyes adversarial C8.20 audit is pending in a separate user-run session** (prompt delivered in chat; KICKOFF audit-variant; writes to `AUDITS/C8.20_<UTC>.md`). If that audit reports a blocker, treat per §8/§11 (HALT + options) before C8.21 DO. C8.20 artifacts are committed/tagged so the audit reviews the shipped state.
- FH5: **Canonical fetal-death parquet path = `~/Desktop/fetal-death-harmonization-build/output/harmonized/`** (gate-SHA-matched). The flat `~/Desktop/fetal-death-harmonization/` tree is stale (`90af89b9…`/`f09beb4a…`) — never consume it. Pinned in the builder + DECISION_LOG.
- FH6: The fetal-death CODEBOOK/COMPARABILITY hand-authored bodies still describe **V2.0 1992-2022** (only a minimal L11 scope note + the generated appendix carry the v2.4.0 1982-2024 evidence). The full-body v2.4.0 re-paragraph remains a tracked **Phase-D doc-sync deferral** (not C8.20/C8.21 scope).
- FH7: Owed §11 human-merge items carry unchanged (the §15.D model-clarification `[plan-update]`, soft-flag (ii); the LESSONS 2026-05-20/2026-05-22 + L13-extension-shared-CSV + L17 grep-scope sharpenings). C8.20 added NO LESSONS entry by design (no new mistake class — the §15.D spec executed as designed).

### Notes for next session

- C8.20 is CLOSED. Tier 3+5 now **5/7 done** (C8.16+C8.17+C8.18+C8.19+C8.20); C8.21 (Stata/SAS pointer) + C8.22 (cross-tab CSVs) remain, then Phase D. Cumulative Phase C ≈ 41-42 of 51-71 (cap 86 intact; C8.20 came in ~1 session, under the 2-4 estimate — the marker-appendix architecture avoided a multi-session per-variable hand-rewrite).
- The C8.20 deliverable is a clean separation: hand-authored CODEBOOK bodies untouched; all per-era evidence in the regenerable appendix. To refresh after any future envelope change: `uv run python scripts/_build_codebook_extensions.py --product all` (byte-identical unless the parquets changed — then it correctly reflects the new envelope; the SMOKE is SHAPE-not-VALUE so it does not stale-pin).
- `/tmp/c8_20_*` scratch (gate-SHA, build/idem/verify logs, the audit pre-stage that was not run) is OS-cleanable + reproducible.
- The fresh-eyes audit is the recommended next assurance step but is **independent of C8.21** — C8.21 may proceed; if the audit later finds a C8.20 issue it is handled via FIX_LOG/§11 against the committed state.
