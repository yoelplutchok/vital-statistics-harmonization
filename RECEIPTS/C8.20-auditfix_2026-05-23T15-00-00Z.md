# Receipt: C8.20-auditfix
## 2026-05-23T15:00:00Z

### What was done
Remediated the lone finding of the user-run fresh-eyes adversarial audit
`AUDITS/C8.20_20260519T152156Z.md` (minor, non-blocking, no-HALT; class L6/H8
precision/representation loss — explicitly NOT a new mistake class) at root
cause. `scripts/_build_codebook_extensions.py` now detects floating-point
columns by pyarrow dtype and renders, for those columns only, subsection (i)
as a per-era **exact summary-statistics** panel (n, non-null, null/blank %,
distinct, min, p25, median, mean, p75, max — deterministic nearest-rank
quantile on the weighted ECDF, `%.6g`, method disclosed inline) and
subsection (iii) as a "continuous numeric — no discrete code frame" line;
subsection (ii) (sentinel disambiguation) unchanged. This eliminates the
6-sig-fig key-collision class for the *entire* float family (not just
`record_weight`) and is the correct codebook representation for a continuous
variable. Both CODEBOOK appendices regenerated. Zero canonical-state mutation.
Chosen over the audit's accept-as-documented / widen-float-format options per
the user's "best quality (within reason)" mandate (DECISION_LOG 15:00:00Z).

### Inputs consumed
- `AUDITS/C8.20_20260519T152156Z.md` (Check 4 finding + verdict)
- fetal-death derived parquet sha256[:12]=`185c071ec76a` (canonical `-build/output/harmonized/`)
- natality v3.0.0 derived parquet sha256[:12]=`acb5c48a9abf`
- linked v4.0.0 derived parquet sha256[:12]=`f630d8cf20db`
- `scripts/_build_codebook_extensions.py` @ `C8.20-complete` (`0e514c0`)

### Outputs produced
- `scripts/_build_codebook_extensions.py` — `import math`; new `_float_summary` + `_fmt_stat`; `render_appendix` float branch ((i)/(iii) + 2 kwargs); `scan_product` float-detect + raw multiset + 4-tuple; 3 `main()` call sites
- `tests/test_codebook_extensions_smoke.py` — +2 float Tier-0 tests (SHAPE-not-VALUE; exact hand-fixture)
- `natality/docs/CODEBOOK.md` — regenerated marker block; exactly 2 var blocks changed (`bmi_prepregnancy` f32 natality block; `record_weight` f64 linked block)
- `fetal_death/CODEBOOK.md`, `fetal_death/FAQ.md`, `natality/docs/FAQ.md` — **byte-identical** (0 float cols / static FAQ): a VERIFY signal
- `FIX_LOG.md` 2026-05-23T15:00:00Z (L6/H8); `DECISION_LOG.md` 2026-05-23T15:00:00Z; `PRE_FLIGHT_LOG.md` 2026-05-23T15:00:00Z; this receipt; `STATUS.md` 2026-05-23T15:00:00Z

### Five-phase trace
- PRE-FLIGHT: ✓ (`PRE_FLIGHT_LOG.md` 2026-05-23T15:00:00Z — PROCEED; gate SHAs byte-exact; float blast-radius enumerated; back-fill-free, precedes the first DO commit)
- SMOKE: ✓ (Tier 0: float fixture authored + run **red** vs unmodified builder — `AttributeError: no _float_summary`, 2 failed / 6 passed — §9-#9 fixture-first; then **8 passed** post-builder-edit; Tier 1/2: real CODEBOOK marker-block/subsection/anchor re-assert green post-DO)
- DO: ✓ (commits `cf2a59e` [pre-do: PRE-FLIGHT+DECISION_LOG+SMOKE] .. the C8.20-auditfix-complete DO commit [builder+regenerated CODEBOOK+FIX_LOG+receipt+STATUS])
- VERIFY: ✓ (A–H below)
- RECEIPT: ✓ (this file)

### Verify results
- VERIFY-A scope: PASS — `git diff --name-only C8.20-auditfix-pre-do` = `scripts/_build_codebook_extensions.py` + `natality/docs/CODEBOOK.md` only (SMOKE test committed in the pre-do commit)
- VERIFY-B gate SHAs byte-exact: PASS — `185c071ec76a`/`acb5c48a9abf`/`f630d8cf20db` unchanged ⇒ zero canonical-state mutation
- VERIFY-C fetal CODEBOOK + both FAQ byte-identical: PASS (`git diff --stat` empty)
- VERIFY-D collision tail removed: PASS — no `1.00714` / `_(+510/397/262 more codes)_` for any float var
- VERIFY-E n-conservation (audit Check-2 H6): PASS — per-era `n` in the new stats panel = era rowcount (22,189,258 / 52,253,538 / 37,006,070 / 7,986,908 / 29,950,846)
- VERIFY-F determinism: PASS — `--check ×2` over the full ~350M-row envelope → both `WOULD-CHANGE: (none)` (twice: pre- and post- the inline method-note edit) — §15.D criterion + audit Check 5 preserved
- VERIFY-G pytest: PASS — full memory-safe per-file split (34 files) = **355P + 1S + 1XF** = STATUS C8.20 baseline 353P+1S+1XF **+2** (the 2 intended float Tier-0); 0 failed/0 error; parquet-suite (snapshot 6P / row_count 16P / canonical_filter 12P / join_parity 13P / mutations / mm 11P) unaffected
- VERIFY-H independent recompute (L6/H8): PASS — DuckDB + independent numpy nearest-rank (NOT importing the builder) byte-matched the rendered `record_weight` cells for 1983-1988, 2005-2013, 2016-2023 across n/non-null/distinct/min/p25/median/mean/p75/max

### Reproducibility
- Re-run `uv run python scripts/_build_codebook_extensions.py --product all` then `--check ×2` → byte-identical (`WOULD-CHANGE: (none)`). Deterministic; the C8.20 §15.D idempotency contract preserved.

### Cross-product re-probe (if applicable)
- C8.21/C8.22/Phase D: unaffected — zero canonical-state mutation; settled pre-Zenodo envelope unchanged (3 gate SHAs byte-exact). The CODEBOOK appendix is regenerable; no parquet/schema dependency changed.

### Git
- Pre-DO tag: `C8.20-auditfix-pre-do`, commit=`cf2a59e`
- Post-RECEIPT tag: `C8.20-auditfix-complete`, commit=<the DO/receipt commit>

### STATUS.md updated
- New section dated 2026-05-23T15:00:00Z at the top marking C8.20-auditfix complete; next task C8.21

### Self-check
What could I have gotten wrong that VERIFY wouldn't catch?
1. **Quantile-method semantics misread.** A reader could expect linear-interpolation (Type-7) quantiles and misread p25/median/p75. *Mitigation:* the method (nearest-rank ⌈p·N⌉ on the weighted ECDF, `%.6g`) is now disclosed inline in the rendered (i) header for every float var **and** in `_float_summary`'s docstring + DECISION_LOG; for these distributions (modal value 1.0 for weights; tight BMI clusters) interpolated vs nearest-rank differ negligibly. Residual: low.
2. **Sentinel-in-stats.** Summary stats are over raw non-null values; if a per-column sentinel that is NOT in `GLOBAL_SENTINELS` exists and is not schema-nulled, it would inflate max/mean. *Mitigation:* the only float vars are `record_weight` (no sentinel; weight≥1) and `bmi_prepregnancy` (schema note "Sentinel 99.9→null" — already nulled upstream; observed max 69.9 confirms). Deliberately not trimmed (§7-#7 no sentinel-as-data presumption); (ii) still surfaces sentinels; the inline note states this. Residual: low; logged.
3. **A future float column with a true sentinel** would get raw stats including the sentinel. *Mitigation:* (ii) still flags GLOBAL_SENTINELS; this is a documented design choice, not a silent bug; revisitable via DECISION_LOG. Residual: low (forward-looking note).
(See §8 — class L6/H8; the fresh-eyes audit is the designed defense-in-depth and it worked.)

### Forward-looking HALTs for next session
- Forward-looking HALT 1: `C8.20-auditfix-pre-do` (`cf2a59e`) + `C8.20-auditfix-complete` tags set ⇒ the audit finding is CLOSED against committed state; the C8.20 audit is fully dispositioned. Next = **C8.21**.
- Forward-looking HALT 2: 3 gate parquet SHAs unchanged (`185c071ec76a`/`acb5c48a9abf`/`f630d8cf20db`) — settled pre-Zenodo envelope intact; C8.20-auditfix was docs/script-only.
- Forward-looking HALT 3: pytest baseline is now **355P+1S+1XF** (was 353P+1S+1XF; +2 = the float Tier-0 in `tests/test_codebook_extensions_smoke.py`) via the memory-safe per-file split (a bare `pytest tests/` collects nothing — soft-flag (kk); `tests/mutations/` + `matched_multiples/tests/` + `fetal_death/tests/` + `natality/tests/` are part of the split).
- Forward-looking HALT 4: the CODEBOOK appendix is regenerated by `scripts/_build_codebook_extensions.py`; any future envelope change must re-run it (`--product all`) and re-VERIFY `--check ×2` = `(none)`.
- Forward-looking HALT 5: owed §11 human-merge items + Phase-D deferrals carry **unchanged** (C8.20-auditfix added zero canonical state, no LESSONS by design). C8.5b/C8.7b DEFERRED.

### Notes for next session
- C8.20 (incl. its audit) is fully closed. Per-variable historical evidence now uses the correct representation for continuous variables (exact per-era summary statistics) and the lossy 6-sig-fig frequency-table class is eliminated builder-wide.
- Next: **C8.21 — Stata + SAS quickstart pointer files (C.3, ~0.5 session)**, then C8.22, then Phase D.
- §2/§9-#7/§9-#9 honored (cheap audit re-probe; root-cause builder fix not a doc patch; SHAPE-not-VALUE fixture authored red-before-green). The user's "best quality" bar drove root-cause + inline self-describing provenance over the cheaper accept-as-doc option.
