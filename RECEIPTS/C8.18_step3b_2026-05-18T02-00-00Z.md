# Receipt: C8.18 DO step 3b — cohort 1983-1991 NUMERATOR (`*num*` birth+death+ICD-9+cert-match-key) `field_specs.py` layout authoring + SMOKE Tier-0/1 value-distribution verify
## 2026-05-18T02:00:00Z

### What was done

Executed C8.18 DO step 3b under the full five-phase discipline in one session (PRE-FLIGHT + SMOKE + DO + VERIFY + RECEIPT; C8.18 DO step 3a single-session-DO-step precedent). Authored the **byte-exact additive** pre-1990 cohort-linked NUMERATOR layouts in `natality/scripts/01_import/field_specs.py`, reconstructed from the `LinkCO83Guide.pdf` / `LinkCO89Guide.pdf` **DETAIL code-outline** (L13-extension; the C8.18 DO step 3a SMOKE-Tier-1 catch is the governing precedent — the p13/pp18-19 element-span summary is composite + NOT trusted): `LINKED_NUM_DEATH_1983_1988_FIELDS` (27 fields, the 500-byte numerator's mortality section locs 194-500), `LINKED_NUM_DEATH_1989_1991_FIELDS` (17 fields, the 535-byte numerator's mortality section locs 226-535), and the reclen constants `LINKED_NUM_RECLEN_1983_1988 = 500` / `LINKED_NUM_RECLEN_1989_1991 = 535`. Added an **additive** pure `_numerator_layout_for_linked_year` helper to `parse_linked_year.py` (the existing `_layout_for_linked_year` denominator dispatcher + the 2005/2014 + 3a-denominator branches byte-untouched — H10/HALT-13). Added the NEW sibling `DESIGN: tracks-current-state` SMOKE harness `natality/tests/test_linked_cohort_1983_1991_numerator_smoke.py` (13 tests; Convention 1/2; parallel to the 3a denominator harness, kept separate for sub-step isolation). **Zero canonical parquet / schema / validation-CSV mutation** (11/11 gate parquet SHAs byte-exact at VERIFY-A). The cert-match-key HALT-4 finding was resolved: **1989-1991** = `(MATCHS@1, IDNUMBER@2-6)` (already in the 3a-authored `LINKED_BIRTH_1989_1991_FIELDS`; the DO-step-5 numerator↔denominator-plus join key); **1983-1988** = NO record-level public-use key (the 500-byte numerator is self-contained — locs 1-91 are the deceased infant's birth covariates byte-identical to the 91-byte denominator; the standard pre-2005 cohort-IMR construction is self-contained-numerator + aggregate-denominator). Numerator reclens are byte-exact-confirmed against the guide-stated counts: `LinkCO83USnum.dat` 19,931,408 / 502 = 39,704 (= LinkCO83Guide p12); `LinkCO89USnum.dat` 20,730,885 / 537 = 38,605 (= LinkCO89Guide p17). Unlike DO step 3a (where the pp18-19 summary FAILed first), going straight to the detail code-outline produced a byte-exact layout that SMOKE Tier 1 verified on real data on the FIRST run (no test loosened — §2 fail-closed; §9-#4).

### Inputs consumed

- `PRE_FLIGHT_LOG.md` 2026-05-18T00:30:00Z — 9 forward-looking HALTs from `RECEIPTS/C8.18_step3a` (9/9 re-verified) + both full numerator layouts captured as state-on-disk substrate (so DO did not re-read the guides for already-captured positions) + 3 within-task scope decisions.
- `RECEIPTS/C8.18_step3a_2026-05-17T23-30-00Z.md` + `DECISION_LOG.md` 2026-05-17T23:30:00Z / T20:00:00Z (the 3a denominator layouts + structural model + DO-step-3/3a/3b split).
- `raw_docs/linked/LinkCO83Guide.pdf` (139 pp; text-extractable, no OCR — L12-extension re-confirmed, 0 empty pages): p12 (numerator 500 B / 39,704), p13-14 List of Data Elements, p32 (locs 92-193 = numerator-only RESERVED), pp33-45 "Mortality Part/Section of Linked Record" detail code-outline (locs 194-500).
- `raw_docs/linked/LinkCO89Guide.pdf` (192 pp; text-extractable, no OCR — L12-extension re-confirmed, 0 empty pages): p17 (numerator 535 B / 38,605; unlinked 535 B / 1,029), p20 (birth/death-cert boundary; MATCHS@1, IDNUMBER@2-6), pp46-56 "Mortality Section of Linked Record" detail code-outline (locs 213-535).
- `raw_data/linked/LinkCO{83,85,88}.zip` member `LinkCO{YY}USnum.dat` (500-byte) + `LinkCO{89,90,91}.zip` member `LinkCO{YY}USnum.dat` (535-byte) — read-only, first ~300 records each for SMOKE Tier 1; SHA-anchored at DO step 2.
- `natality/scripts/01_import/field_specs.py` + `parse_linked_year.py` (sibling substrate; the 3a-denominator + 2005/2014 LINKED specs).
- 11 gate parquets — SHA-verified byte-exact at PRE-FLIGHT entry cheap-check and re-verified at VERIFY-A.

### Outputs produced

Git-tracked (this commit):

- `natality/scripts/01_import/field_specs.py` — **purely additive** (`git diff --numstat` = 111 insertions / **0 deletions**; existing LINKED 2005/2014 + 3a-denominator tuple lists/constants byte-untouched): `LINKED_NUM_DEATH_1983_1988_FIELDS` (27 fields, 194-500), `LINKED_NUM_RECLEN_1983_1988 = 500`, `LINKED_NUM_DEATH_1989_1991_FIELDS` (17 fields, 226-535), `LINKED_NUM_RECLEN_1989_1991 = 535`, with a source-citation header block.
- `natality/scripts/01_import/parse_linked_year.py` — **purely additive** (`git diff --numstat` = 44 insertions / **0 deletions**): 4 new imports + the pure `_numerator_layout_for_linked_year(year)` helper. `_layout_for_linked_year` (denominator) + 2005/2014/3a branches byte-identical; `test_denominator_dispatcher_unregressed` PASS asserts `_layout_for_linked_year(1983)==91`/`(1989)==225`/`(2008)==900`/`(2018)==1384`/`(1994)`→`ValueError`.
- `natality/tests/test_linked_cohort_1983_1991_numerator_smoke.py` — NEW; `DESIGN: tracks-current-state`; 13 tests (5 SHAPE structural + 2 Tier-0 synthetic-with-L3-negative + 6 Tier-1 real-data parametrized).
- `PRE_FLIGHT_LOG.md` — C8.18 DO step 3b PRE-FLIGHT entry (2026-05-18T00:30:00Z; before any `field_specs.py` mutation; L10-safe) incl. both state-on-disk numerator layouts.
- `RECEIPTS/C8.18_step3b_2026-05-18T02-00-00Z.md` — this file.
- `DECISION_LOG.md` + `STATUS.md` — appended (DO step 3b choices + new dated section).

No canonical parquet / `harmonized_schema.csv` / validation-CSV touched. Raw zips are gitignored + unchanged (read-only). §15.D NOT edited this step (the substrate-format wording was already reconciled at the 3a commit; the broader model-clarification stays soft-flag (ii) §11 human-merge).

### Five-phase trace

- **PRE-FLIGHT**: ✓ `PRE_FLIGHT_LOG.md` 2026-05-18T00:30:00Z (per-sub-step, before any `field_specs.py` mutation; L10-safe). 9/9 forward-looking HALTs re-verified; 11/11 gate parquet SHAs byte-exact; 3 within-task scope decisions surfaced + resolved before DO (Convention 3).
- **SMOKE**: ✓ Tier 0 (synthetic 500-byte + 535-byte recovery + L3 position-shift negative: PASS) → Tier 1 (real `LinkCO{83,85,88}USnum.dat` 500-byte + `LinkCO{89,90,91}USnum.dat` 535-byte). **13/13 PASS on the FIRST run** (the detail-code-outline reconstruction was byte-exact; no test loosened — contrast DO step 3a where the pp18-19 summary FAILed first).
- **DO**: ✓ additive `field_specs.py` + `parse_linked_year.py` + new SMOKE harness. Single commit (PRE-FLIGHT + DO + RECEIPT; C8.18 DO3a precedent).
- **VERIFY**: ✓ all 5 criteria (below).
- **RECEIPT**: ✓ this file.

### Verify results

- **VERIFY-A (zero canonical mutation)**: PASS — 11/11 gate parquet SHAs byte-exact vs PRE-FLIGHT HALT 2 + the DO step 3a receipt VERIFY-A: natality `c8a740eb…a6237153`/`acb5c48a…28856974`; `.v28_baseline` `230efed2…33ccebac`/`e16ad532…77c41d44`; linked-derived `9b828a4d…5a08b777`; fetal-death `38e2cecb…99c5cf48`(harm)/`185c071e…a7968a09`(deriv) (via the `-build` tree); MM `adbec108…45dc1549`/`5c22308b…d39205d1`/`7c682668…edd61f5d`/`d98b4296…6a543261`. Threshold: 11/11 unchanged.
- **VERIFY-B (git scope)**: PASS — modified/new = `field_specs.py`, `parse_linked_year.py`, `test_linked_cohort_1983_1991_numerator_smoke.py`, `PRE_FLIGHT_LOG.md`, + `RECEIPTS/`/`DECISION_LOG.md`/`STATUS.md`. NO `.parquet`/`harmonized_schema.csv`/validation-CSV. Threshold: scope ⊆ {scripts, test, plan, state}.
- **VERIFY-C (additive-only)**: PASS — `git diff --numstat` = `field_specs.py` 111/**0**, `parse_linked_year.py` 44/**0** (zero deletion/modification lines; purely additive). The 2005/2014 + 3a-denominator specs + `_layout_for_linked_year` byte-untouched; `test_denominator_dispatcher_unregressed` PASS.
- **VERIFY-D (pytest count gate)**: PASS — canonical **4-dir** suite `fetal_death/ natality/ tests/ matched_multiples/` = **111 passed, 1 skipped, 1 xfailed in 300.29s**. Reconciles exactly: the 3a-close baseline 98P+1S+1XF preserved (zero regression; 1 skipped + 1 xfailed unchanged; 98 still pass) **+** 13 new numerator SMOKE all pass = 111P. Runtime 300.29s within the ~240-380s band.
- **VERIFY-E (SMOKE all PASS)**: PASS — 13/13 on the first run. Tier 1 value-distribution (L13-extension, sentinel-aware): BIRYR == cohort year for all 300×6 records (encoding ASCII + alignment + block-size jointly); MATCHS ∈ {1,2} (numerator = linked infant deaths only, 3=surviving never appears); year-of-death ∈ {cohort, cohort+1}; ICD-9 UCOD numeric 001-999 plausible; entity-axis condition counts 00-20; numerator counts ≪ denominator.

### Reproducibility

- `field_specs.py` constants are declarative; the SMOKE harness re-derives block size empirically (data + CR/LF) + value-distribution-verifies → deterministic, re-running yields identical results.
- Zero canonical parquet rebuild (DO step 3b is layout substrate only). H10 gate: 11/11 SHAs byte-exact.
- Numerator reclens cross-checked byte-exact against the guide-stated record counts (LinkCO83 19,931,408/502=39,704; LinkCO89 20,730,885/537=38,605).

### Git

- Pre-DO rollback anchor: `C8.18-pre-do` @ `6632a15` (unchanged; the C8.18 DO rollback anchor).
- This commit: intermediate C8.18 DO step 3b — **NO tag** (Convention 5; `C8.18-complete` is the final-sub-step-only tag).

### STATUS.md updated

- New section dated 2026-05-18T02:00:00Z at the top (newest-first).

### Self-check (§10) — what could I have gotten wrong that VERIFY wouldn't catch?

1. **Composite ENTITY / RECORDAX spans** (1983-1988 ENTITY@240-379, RECORDAX@382-481; 1989-1991 ENTITY@263-402, RECORDAX@405-504) are authored as single composite spans; the per-condition 7-byte (entity) / 5-byte (record) sub-positions + the ICD-9-code sub-field (pos 3-6 entity / pos 1-4 record) + the nature-of-injury flag were NOT individually value-distribution-verified (only the count fields NENTITY/NRECORD around them, plus the scalar UCOD@231-234 / @219-222 ICD-9). A composite span could be off by a byte and the anchor checks would not catch it. *Mitigation*: span boundaries are byte-exact from the detail code-outline (each "Nth Condition" location is explicitly tabulated pp44-45 / pp49-50), the count fields + the scalar UCOD value-verify, and the SHAPE non-overlap/in-bounds checks pass with the spans tightly bracketed by value-verified neighbors (UCOD/UCODR61 before, RESSTATD/reclen after). *Recommendation*: DO step 5 value-distribution-verify each entity/record-axis condition sub-field before harmonizing the multiple-cause detail. Logged DECISION_LOG 2026-05-18T02:00:00Z residual risk.
2. **1983-1988 numerator natality section (locs 1-91) reuse of `LINKED_BIRTH_1983_1988_FIELDS`** is asserted from the NCHS "Denominator Record AND Natality Section of Linked Record" shared-detail title + the p13/p14 List-of-Data-Elements numerator-birth column == denominator column, and SMOKE-Tier-1-verified only on the BIRYR@2-5 anchor (== cohort year). A non-anchor natality field could in principle differ between the compact denominator and the numerator's natality section; the 92-193 numerator-only-reserved gap is from LinkCO83Guide p32 (not value-probed beyond the BIRYR alignment). *Mitigation*: BIRYR alignment + MATCHS domain on real numerator data confirm the 1-91 layout aligns; the guide explicitly states the shared layout. *Recommendation*: DO step 5 full-parse value-distribution-verify the numerator natality section against `LINKED_BIRTH_1983_1988_FIELDS`. Logged DECISION_LOG residual risk.
3. **Mid-file encoding/layout shift** sampled only first ~300 records/file (extremely unlikely for a fixed-width NCHS public-use `.dat`; the file_size % block == 0 check holds for the whole file). DO step 5 full parse surfaces any anomaly via row-count + value-distribution.
4. **1989-1991 numerator death = `LINKED_DEATH_1989_1991_FIELDS` (213-225) + `LINKED_NUM_DEATH_1989_1991_FIELDS` (226-535)** assembled in the helper; the 213-225 reuse is from the LinkCO89Guide pp46-47 detail (the numerator's 213-225 == the denominator-plus's). If a byte differed between the denominator-plus "plus" and the numerator at 213-225, only the AGED/UCOD anchors (value-verified) would catch it. Low residual risk (the guide documents one shared "Denominator-Plus Record and Mortality Section of Numerator (Linked) Record" detail for 213-225).
5. **The cert-match-key finding is a layout/methodology note, not wired** — `_numerator_layout_for_linked_year` returns the layout substrate only; the actual num/den construction + the 1989-1991 (MATCHS,IDNUMBER) join + the harmonized v4 row model are DO step 5 forward dependencies, not DO step 3b defects.

Each logged in `DECISION_LOG.md` 2026-05-18T02:00:00Z as a residual risk; recommended checks are DO-step-5 PRE-FLIGHT items (not added to VERIFY without human sign-off per §10).

### Forward-looking HALTs for next session (C8.18 DO step 4)

1. `C8.18-pre-do`@`6632a15`; `C8.18-complete` NOT present (final-sub-step-only). `C8.17-complete` present. HEAD = this DO step 3b commit (after `94c423e`).
2. **11 gate parquet SHAs unchanged** (the same 11; fetal-death via the `~/Desktop/fetal-death-harmonization-build/` tree, soft-flag (hh)). Linked-derived `9b828a4d…` changes only at the later C8.18 re-harmonize DO step (3a/3b are layout-substrate only).
3. **Canonical pytest baseline is now `111 passed, 1 skipped, 1 xfailed`** (was 98P+1S+1XF; +13 from the new `test_linked_cohort_1983_1991_numerator_smoke.py`, `DESIGN: tracks-current-state`). The canonical suite is the **4-dir** `fetal_death/tests/ natality/tests/ tests/ matched_multiples/tests/`. The README 3-dir "56 passed" line stays stale → soft-flag (jj) (pre-existing L11-class docs; out-of-scope; Phase-D/C8.x docs-refresh `[plan-update]`).
4. `field_specs.py` `LINKED_NUM_DEATH_1983_1988_FIELDS`(500)/`LINKED_NUM_DEATH_1989_1991_FIELDS`(535)/reclen constants are byte-exact + real-data value-verified. **DO step 4 = `field_specs.py` layout reconstruction for cohort 1995-2004** (1989-rev birth; ICD-9 1995-1998 / ICD-10 1999-2004; 2003-2004 cohort 1989+2003-rev mix), per the scope-corrected §15.D DO step 4. DO step 4 must NOT re-derive the 1983-1991 denominator OR numerator layouts (state-on-disk in `field_specs.py` + this receipt + `RECEIPTS/C8.18_step3a`).
5. **Encoding = ASCII (verified at 3a + re-confirmed at 3b on `*num*`)** — the 2005+ parser's `_slice_field` `.decode("latin-1")` works for pre-1990 public-use `.dat`.
6. **Structural model (complete for 1983-1991)**: 1983-1988 = pure two-file (91-byte births-only denominator + 500-byte self-contained numerator → DO step 5 = self-contained-numerator + aggregate-denominator IMR construction); 1989-1991 = 225-byte denominator-PLUS single-file + 535-byte numerator (join key `(MATCHS@1, IDNUMBER@2-6)` for the DO-step-5 num↔denom-plus richer-death-detail join). `_find_denomplus_member` (`"DENOM"`) won't match `LinkCO{YY}USden.dat` (`"DEN"`) / `USnum.dat` (`"NUM"`) — a DO step 5 parser fix.
7. §15.D substrate-format wording reconcile already APPLIED at the 3a commit; the broader §15.D model-clarification (3a/3b split; 1983-1988 pure-two-file self-contained-numerator vs 1989-1991 denominator-plus + (MATCHS,IDNUMBER) join; num/den → denominator-plus-equivalent) remains **soft-flag (ii)** proposed-not-applied for §11 human-merge (queued with the soft-flag-(w) batch; the 3b numerator/match-key refinement folds into the SAME (ii) note).
8. **soft-flag (gg) refined**: the FULL entity/record-axis multiple-cause detail is now layout-captured (1983-1988: NENTITY@238-239, ENTITY@240-379, NRECORD@380-381, RECORDAX@382-481; 1989-1991: NENTITY@261-262, ENTITY@263-402, NRECORD@403-404, RECORDAX@405-504) as composite spans; per-condition decomposition + `cause_recode_130` per-era + the exact ICD-9-era harmonized cause-column shape stay DO step 5/6 PRE-FLIGHT.
9. pytest runtime ~300s (in the ~240-380s band; the band is the gate, not the absolute number).

### Notes for next session

- **C8.18 DO step 3 (3a denominator + 3b numerator) is CLOSED.** Both pre-1990 cohort-linked physical layouts (1983-1988 91-byte den + 500-byte num; 1989-1991 225-byte denom-plus + 535-byte num) are authored byte-exact + real-data value-verified, state-on-disk in `field_specs.py` + the 3a/3b receipts. Next per the scope-corrected §15.D: **DO step 4 — cohort 1995-2004 `field_specs.py` layout reconstruction** (1989-rev birth; ICD-9 1995-1998 / ICD-10 1999-2004; 2003-2004 1989+2003-rev mix; ~2-3 sessions).
- The L13-extension "reconstruct from the DETAIL code-outline, value-distribution-verify on real data, do NOT trust the element-span summary" discipline (the 3a SMOKE-Tier-1 catch) governed 3b and produced a first-run-clean layout; carry it to DO step 4.
- The §15.D model-clarification `[plan-update]` (soft-flag (ii)) is owed to the human (§11; queued with the soft-flag-(w) batch); the on-disk PRE_FLIGHT_LOG/DECISION_LOG/RECEIPTs are authoritative in the interim.
- `/tmp/c8_18_3b_*` scratch (PDF extracts + sniff scripts) + background gate-SHA/pytest output files are OS-cleanable + reproducible.
