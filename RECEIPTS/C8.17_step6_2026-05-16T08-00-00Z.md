# Receipt: C8.17 DO step 6 — canonical 1968-2024 re-harmonize + `certificate_revision` enum widen + natality v3.0.0 bump
## 2026-05-16T08:00:00Z

### What was done

The canonical natality parquet was re-harmonized + re-derived across the full
**1968-2024 (57-year) envelope** using the DO5b-extended monorepo
`harmonize_v1_core.py` (the additive `is_pre1989` branch for 1968-1988 +
1989→`is_pre2003`), preserving the prior v2.8 (1990-2024, 138,819,655-row)
parquets as `.v28_baseline.parquet` forward-stability anchors. The
`certificate_revision` schema enum was widened with the new `unrevised_1968`
value (1968-1988 era; AskUserQuestion 2026-05-15 Q2 Option A) and the natality
release version decision was set to **v3.0.0 (major)** per AskUserQuestion
2026-05-16T06:45:00Z (cert-revision era boundary + universal row-count/SHA
change = the H10 reproducibility-gate cascade §15.D names as the major-bump
trigger). The recon surfaced a 5th divergence (**D5**: a separate stale
build-repo `~/Desktop/natality-harmonization/scripts/`) which was resolved
empirically (the monorepo harmonizer @ C8.17-pre-do is byte-identical to the
build-repo copy that produced the current canonical parquet; `derive_v1_core`
identical too) — DO step 6 runs the monorepo scripts with explicit build-dir
path args (the DO5a/5b-established pattern). The new canonical parquet is
201,161,456 rows / 57 years; the 1990-2024 slice is **byte-clean** (0/35-year
content drift vs `.v28_baseline`); 183/183 NVSR preserved; the 7
non-natality-v2 canonical parquet SHAs are byte-exact (only natality_v2
changed). Convention-2 / L17 bundled re-pins (`test_row_count_conservation.py`
NATALITY pins, B.12 v2 baseline, the 22 pre-1990 `file_inventory.csv`
`imported` flips, the cert schema row) ship in this same commit. Per §15.D's
step split, version-string propagation across `.zenodo.json` / CITATION.cff /
ABOUT_THIS_RELEASE.md / README.md / PROJECT_STRUCTURE.md + the broader smoke
EXPECTED_* / per-column years_available refresh is **DO step 7** (Anti-Pattern
#6 satisfied here by this receipt's paired DECISION_LOG entry + the schema↔
version-decision pairing).

### Inputs consumed

- `~/Desktop/natality-harmonization/output/yearly_clean/natality_<1968-1989>_raw.parquet` (22; DO5a) + `natality_<1990-2024>_core.parquet` (35)
- `natality/scripts/03_harmonize/harmonize_v1_core.py` (DO5b-extended; sha-tracked in git @ `89cc052`) + `natality/scripts/04_derive/derive_v1_core.py` (byte-identical monorepo↔build-repo)
- Preserved `.v28_baseline`: `natality_v2_harmonized.v28_baseline.parquet` sha256=`230efed2ac34c794638aceaa777a31…`; `natality_v2_harmonized_derived.v28_baseline.parquet` sha256=`e16ad5323d68e28d401518f1ff56b1…`

### Outputs produced

- `~/Desktop/natality-harmonization/output/harmonized/natality_v2_harmonized.parquet` — NEW, sha256=`c8a740eb48d4f3de66759da27eef94143c315846885bf905a88cbc0fa6237153`, rows=201,161,456, cols=71 (pre-derive; gitignored build artifact)
- `~/Desktop/natality-harmonization/output/harmonized/natality_v2_harmonized_derived.parquet` — NEW SHIPPED/H10-gated, sha256=`acb5c48a9abf82ac78e6bf210d6be5d62cba6afae271b978b0e53ed528856974`, rows=201,161,456, cols=84 (gitignored build artifact)
- `.v28_baseline.parquet` ×2 — preserved forward-stability anchors (`230efed2…` / `e16ad53…`)
- `natality/metadata/harmonized_schema.csv` — MODIFIED (certificate_revision row: `allowed_values` +`unrevised_1968`, `years_available` 1968-2024, label/raw_source/derivation/notes extended; `type` UNCHANGED)
- `natality/metadata/file_inventory.csv` — MODIFIED (22 pre-1990 rows `imported` false→true; 22 ins/22 del, all other content byte-preserved)
- `tests/test_row_count_conservation.py` — MODIFIED (`NATALITY_EXPECTED_TOTAL` 138_819_655→201_161_456; `NATALITY_EXPECTED_YEARS` range(1990,2025)→range(1968,2025))
- `tests/snapshots/_build_snapshot.py` — MODIFIED (output literal → `v2_2026-05-16T08-00-00Z_columns.csv`)
- `tests/snapshots/v2_2026-05-16T08-00-00Z_columns.csv` — NEW B.12 baseline (340 rows; natality_v2 84-col block updated; fetal_death ×2 + linked byte-identical to v1; v1 retained per Anti-Pattern #10)
- `PRE_FLIGHT_LOG.md` — MODIFIED (DO step 6 PRE-FLIGHT addendum, prepended; L10-safe, pre-DO)

### Five-phase trace

- **PRE-FLIGHT**: ✓ Entry cheap-check 13/14 HALTs (HALT 7 pytest deferred to VERIFY, precedented). Recon surfaced D5 → resolved by empirical byte-identity proof (`git show 12fc20e:…harmonize_v1_core.py` ≡ build-repo copy; derive identical). Field-value snapshot of every canonical artifact in `PRE_FLIGHT_LOG.md` addendum 2026-05-16T07:00:00Z **before any DO mutation**. AskUserQuestion 2026-05-16T06:45:00Z → v3.0.0 major.
- **SMOKE / safety gate**: ✓ Fail-closed by baseline-preservation-first (`cp -p` → `.v28_baseline`, SHAs matched the pre-DO6 canonical → the overwrite is fully reversible). Strictly cheaper + stronger than a separate pre-run; the §15.D anchor criterion is the VERIFY gate.
- **DO**: ✓ re-harmonize (`--years 1968-2024`, 201,161,456 rows / 57 yrs / cert sums) → re-derive (row-count-preserving) → cert schema row + 2 test re-pins + B.12 re-snap + 22 inventory `imported` flips.
- **VERIFY**: ✓ all 5 criteria (below).
- **RECEIPT**: ✓ (this file).

### Verify results

- **A — 1990-2024 byte-clean anchor (§15.D / L5)**: PASS. All 35 years 1990-2024 content-**EQUAL** (`pyarrow.Table.equals`, exact incl. nulls; no sort fallback needed → row-order also preserved) between the new derived parquet and `.v28_baseline`. new[1990-2024] total = baseline total = **138,819,655** exactly. **0/35-year column drift** on the post-1989 slice.
- **B — NVSR**: PASS. `compare_external_targets_v1.py` on the new derived parquet → **183/183 PASS, 0 FAIL**. (Pre-1990 NVSR cells are a future incremental task per §15.D + EXPLORATION_REPORT OCR-friction; not added here.)
- **C — H10 7-SHA gate**: PASS. fetal_death_harmonized `38e2cecb…`, fetal_death_derived `185c071e…`, natality_v3_linked_derived `9b828a4d…`, matched_multiples_harmonized `adbec108…`, MM 1995-1997 `5c22308b…`, MM 1995-2000 `7c682668…`, MM 2016-2020 `d98b4296…` — all byte-exact. ONLY natality_v2 changed (new `acb5c48a…` derived).
- **D — pytest SHAPE (Convention 1)**: PASS. Cache-cleared `pytest fetal_death/ natality/ tests/ matched_multiples/ -p no:cacheprovider` = **85 passed, 1 skipped, 1 xfailed in 288.22s** (count exact; +50s vs the 238s v2.8 baseline expected — natality fixtures now read 201M rows; Convention 1: count is the gate, not timing). The first post-rebuild run surfaced 1 FAIL (`test_natality_inventory_years_match_schema_years_available`) — the **correctly-firing** Convention-2 cross-check the test's own docstring scheduled for "C8.17 DO step 6"; root-cause-fixed by flipping the 22 pre-1990 `imported` flags (the data IS now imported), NOT by re-pinning the test (Anti-Pattern #5). Clean re-run confirmed 85P+1S+1XF.
- **E — envelope sanity**: PASS. 201,161,456 = 138,819,655 (v2.8 1990-2024) + 62,341,801 (DO5a 1968-1989) exactly; 57 contiguous years; certificate_revision = `unrevised_1968` 58,296,108 (= 62,341,801 − 1989's 4,045,693, i.e. the 1968-1988 envelope) + `unrevised_1989` 74,716,846 + `revised_2003` 67,442,145 + `unknown` 706,357 = 201,161,456 (sums; 1968-1988→unrevised_1968, 1989→unrevised_1989 confirming the is_pre1989/is_pre2003 routing at full scale).

### Reproducibility

Deterministic + idempotent: the monorepo harmonizer + derive over the immutable yearly parquets reproduce byte-identical output (the 1990-2024 anchor is the empirical proof — re-deriving the existing envelope produced content-identical results). `.v28_baseline` retained for forward-stability diffing. Re-run cost ≈ ~20 min harmonize + ~8 min derive.

### Cross-product re-probe

7 non-natality-v2 canonical parquet SHAs byte-exact (Criterion C) → fetal-death, linked, matched-multiples provably untouched. B.12 v2 baseline corroborates (their per-column SHAs byte-identical to v1).

### Git

- Pre-DO tag: `C8.17-pre-do` @ `12fc20e`.
- Post-RECEIPT tag: **NONE** — DO step 7 (docs / version-string propagation / smoke EXPECTED_* refresh) still pending; `C8.17-complete` set only after DO step 7 (Convention 5 intermediate-DO-step precedent).
- Commit: ~5-line Convention 5 summary; full narrative here + DECISION_LOG 2026-05-16T08:00:00Z + STATUS 2026-05-16T08:00:00Z.

### Self-check (§10) — what could I have gotten wrong that VERIFY wouldn't catch?

1. **Pre-1990 NVSR is unvalidated.** 183/183 covers only 1990-2024 (the targets CSV has no pre-1990 rows). The 1968-1989 harmonized values are SMOKE-distribution-plausible (DO5b Tier-2) + L13-extension-anchored but NOT cross-checked against published NCHS *Vital Statistics of the United States* annual-volume counts (paper-only; OCR friction per EXPLORATION_REPORT §A.2). Residual risk: a systematic pre-1990 mapping error (e.g., a sentinel leak in an un-Tier-2'd year) would not be caught by VERIFY. Mitigation: pre-1990 NVSR is a documented future incremental task; soft-flag (aa) flags maternal_age full-envelope sentinel-leak as a DO-step-7-or-later check.
2. **`unrevised_1968` is now in the canonical parquet but the schema-version STRING is still `v2.8.0` everywhere except this receipt/DECISION_LOG.** Anti-Pattern #6 is satisfied (schema edit ↔ DECISION_LOG-recorded version decision), but a consumer reading `.zenodo.json` today sees `v2.8.0` against a v3.0.0-shaped parquet. This is the **intended DO step 6/7 split** (§15.D), but if DO step 7 is skipped/delayed the repo is internally version-inconsistent. Mitigation: forward-looking HALT 6 + STATUS Notes make the DO step 7 version-string propagation a hard prerequisite for `C8.17-complete`.
3. **The 1990-2024 anchor used `data_year`-filtered per-year `Table.equals`.** If the harmonizer reordered rows WITHIN a year (not across years), a per-year `equals` would catch it (it did not trigger the sort fallback → order identical). But if BOTH the new build and `.v28_baseline` shared an identical latent ordering bug, the anchor would pass while both are wrong. Extremely low risk (`.v28_baseline` is the NVSR-validated shipped v2.8 parquet; 183/183 still pass). Documented.
4. **B.12 v2 baseline's 3 "unchanged" parquets were re-hashed from the live files, not copied from v1.** I verified the v2 rows for fetal_death ×2 + linked are byte-identical to v1 (string-equality of the CSV rows) — so the re-hash reproduced identical SHAs, corroborating those parquets are untouched. If a parquet had silently drifted, the v2 baseline would have absorbed the drift as "new normal" — but Criterion C (independent file-SHA gate) independently proves they did not drift. Defense-in-depth holds.
5. **`certificate_revision` for 1989 = `unrevised_1989`** (routed through is_pre2003). 1989 IS the 1989-revision certificate, so this is correct — but a reader expecting "all pre-1990 = unrevised_1968" might be surprised. The schema `derivation_rule` now documents "1968-1988: unrevised_1968; 1989-2002: unrevised_1989" explicitly. Not a bug; documented.

### Forward-looking HALTs for next session (C8.17 DO step 7 PRE-FLIGHT)

1. `C8.17-pre-do`@`12fc20e`; `C8.17-complete` NOT present (set at DO step 7 end — the final DO step).
2. **Canonical natality SHAs are now `c8a740eb…` (harmonized) / `acb5c48a…` (derived)** — DO step 7 must NOT re-harmonize; it is docs-only. If these SHAs differ at DO step 7 PRE-FLIGHT, an unauthorized rebuild happened — HALT.
3. **7 non-natality-v2 parquet SHAs byte-exact** (`38e2cecb`/`185c071e`/`9b828a4d`/`adbec108`/`5c22308b`/`7c682668`/`d98b4296`) — must stay byte-exact through DO step 7 (docs-only).
4. **`.v28_baseline.parquet` ×2 preserved** (`230efed2…`/`e16ad53…`) — forward-stability anchor; do not delete (Anti-Pattern #10).
5. **DO step 7 scope (§15.D)**: propagate `v2.8.0`→`v3.0.0` across `natality/.zenodo.json` + CITATION.cff + `natality/docs/ABOUT_THIS_RELEASE.md` + `natality/README.md` + monorepo `PROJECT_STRUCTURE.md` + `README.md` "four products" table (natality 35→57 yrs / 138.8M→201.2M rows); refresh any broader C8.1 release-smoke EXPECTED_YEAR_ROWS / per-column `years_available` cells now populated 1968+; then tag `C8.17-complete`.
6. **Internal version inconsistency until DO step 7**: the canonical parquet is v3.0.0-shaped but version strings still say v2.8.0 (intended split; self-check 2). DO step 7 is a hard prerequisite for `C8.17-complete`.
7. **B.12 latest baseline is now `v2_2026-05-16T08-00-00Z_columns.csv`** (`latest_baseline_path()` picks it; v1 retained). DO step 7 does NOT re-snap (no parquet change).
8. **`test_row_count_conservation.py` NATALITY pins = 201_161_456 / range(1968,2025)** — DO step 7 must not perturb (no rebuild).
9. **pytest baseline 85P+1S+1XF ≈ 288s** (201M-row fixtures; system-variance band ~240-380s; count is the gate).
10. **Soft-flag (bb) NEW**: the stale build-repo `~/Desktop/natality-harmonization/scripts/` (+ `_drive_natality_benchmark.py` pointing at it) lacks all C8.17 work; out-of-C8.17-scope cleanup (Phase D / C8.7b candidate). (aa)/(w)/(x)/(z)/(u) carry; (aa) maternal_age full-envelope sentinel-leak check is a DO-step-7-or-later candidate.
11. Tier 3+5 ≈ 2.3/7 tasks; cumulative Phase C ≈ 25 of 51-71 sessions (cap 86).
12. §15.D §1358 wording soft-flag (w) + the broader §15.D step-6/7 boundary clarification are next-`[plan-update]` items (Convention 5; not bundled with DO commits).

### Notes for next session

- DO step 7 is docs-only + the `C8.17-complete` tag. No rebuild, no re-harmonize, no re-derive. Verify the 2+7 parquet SHAs unchanged at PRE-FLIGHT.
- The v3.0.0 decision is RECORDED (DECISION_LOG 2026-05-16T08:00:00Z); DO step 7 PROPAGATES the string + updates the manuscript-facing counts (natality 1968-2024, 57 yrs, 201,161,456 rows). Manuscript Coverage re-paragraph is Phase D D.4 (already planned), not DO step 7.
- `/tmp/c8_17_step6/` scratch (logs + anchor/smoke scripts) is OS-cleanable; reproducible.

### §10 self-check answered: yes (5 residual risks above; logged in DECISION_LOG 2026-05-16T08:00:00Z).
