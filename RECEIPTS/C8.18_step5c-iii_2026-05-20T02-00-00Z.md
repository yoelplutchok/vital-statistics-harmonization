# Receipt: C8.18 DO step 5c-iii — the keyless 1983-1988 `link_segment` den/num one-row-per-birth harmonized encoding (+ a bundled, human-authorized §7 root-cause fix to RESOLVED-5b `run_parse`)
## 2026-05-20T02:00:00Z

### What was done

Executed C8.18 **DO step 5c-iii** under the full five-phase discipline in one session (PRE-FLIGHT + SMOKE + DO + VERIFY + RECEIPT; the 3a/3b/4a/4b/4c/5a/5b/5c-i/5c-ii-a/5c-ii-b single-session-DO-step precedent). This is the LAST of the §15.D DO step 5c → 5c-i/5c-ii/5c-iii decomposition — **all cohort eras (1983-1991 + 1995-2004) are now harmonize-implemented**.

**5c-iii harmonize-side (the planned deliverable)** — additive to `natality/scripts/03_harmonize/harmonize_linked_v3.py` (`git diff --numstat` = **328 / 8**; the 8 deletions = the `_harmonize_cohort_batch` docstring bullet reword + the `NotImplementedError`→`RuntimeError` dispatch-guard reword — **0 era-body logic-deletion lines**, grep-confirmed; the 5c-i 1989-1991 inline body + 5c-ii-a `_harmonize_cohort_1995_2002` + 5c-ii-b `_harmonize_cohort_2003_2004` + the 2005+ `_harmonize_batch` array entries byte-untouched, all 8 anchors intact):

1. **`("link_segment", pa.string())` appended to `OUT_SCHEMA`** (80→81; the v3→v4 ADDITIVE code-schema extension — the 5c-i within_era-ICD-9-columns precedent; the 5b receipt self-check #6 mandate "5c must preserve the keyless-era den/num provenance") + the matching `pa.nulls(batch.num_rows, type=pa.string())  # link_segment` appended to the 2005+ `_harmonize_batch` explicit `from_arrays` list (the dict-pattern 1989-1991/1995-2002/2003-2004 era functions auto-default it NULL via `{f.name: pa.nulls for f in OUT_SCHEMA}` — byte-untouched). Value `"den"`/`"num"` for 1983-1988; NULL for every other era (single denominator-plus / 2005+ — faithful "not applicable: exactly one segment"). The `harmonized_schema.csv` metadata row + the v3→v4 version bump = DO step 6 (Anti-Pattern #6; `OUT_SCHEMA` is code, not the metadata CSV).
2. **`_mrace1digit_to_bridged4(mrace)`** (NEW helper) — the 1968-1988 1-DIGIT detail-race → bridged-4 (0/4/5/6/8→4 Asian-PI; 1→1; 2→2; 3→3; 7/9→null). **Byte-identical to `harmonize_v1_core._mrace1digit_to_bridged4`** (H7 sibling-parity on the shared `maternal_race_bridged` concept; SMOKE-asserted equal).
3. **`_harmonize_cohort_1983_1988(batch, year)`** (NEW; the RESOLVED 5b two-file model — do NOT re-open) — `is_num = pc.equal(link_segment, "num")`; `infant_death` = `if_else(is_num, True, NULL)` (den → NULL/unknown un-linkable aggregate denominator = the documented within-era structural difference / Phase-D D.4; num → True linked-infant-death). Death-side gated on `is_num`: `age_at_death_days`=NULL (the 1983-1988 numerator has **NO AGED** — only the AGER5 recode; faithful "not on this file"); `age_at_death_recode5`=AGER5; `underlying_cause_icd9`=UCOD (ICD-9 numeric — probe-confirmed 0/3000 alpha-prefixed); `cause_recode_61`=UCODR61; ICD-10 cols NULL (cohort ≤1998 = ICD-9; the §15.D DO step 1 / 5c-i resolved shape, do NOT re-open); `manner_of_death`=NULL (no MANNER in the 1983-1988 numerator). Birth-side (shared locs 1-91, BOTH segments) = the natality `is_pre1989` 1968/1978-rev H7 ALIAS: `certificate_revision`="unrevised_1968"; native NCHS `LIVORD9`/`TOTORD9` recode-9 (present 1983-1988, unlike the 1989-1991 detail-only era → faithful + H7-parity); `marital_status`=DMAR (the LINKED file carries it — unlike natality's pre-1989 public-use file); `_mrace1digit_to_bridged4(DMRACE)`; `_dmeduc_years_to_cat4(DMEDUC/DFEDUC)`; `_month_to_trimester(DMPCB)`; `_pldel_to_facility(PLDEL)`; DGEST 17-47∪99 keep "lmp" (H7 parity); DFAGE 10-98 keep; DMAGE/DPLURAL/CSEX/DBIRWT/APGAR5/NPREVIS/BIRATTND. **Conservative NULL** (H7 `is_pre1989` + soft-flag (gg) + the (A′) finding): hispanic_origin/maternal_hispanic/father_hispanic/maternal_race_ethnicity_5 (2-digit ORMOTH/ORFATH crosswalk non-byte-stable & unverified — 1983 all "88", 1988 "00"-dominant); **record_weight** (the (A′) RECWT@91 era-instability); the composite DELMTH/MEDRISK/OBSTETRC/LABOR/ABNORMNB/ENTITY/RECORDAX spans + smoking/diabetes/hypertension/preterm_recode3 (soft-flag (gg)). Fail-closed `if _cohort_era(year)!="1983_1988": raise ValueError`.
4. A 2-line `if era == "1983_1988": return _harmonize_cohort_1983_1988(batch, year)` dispatch prepended in `_harmonize_cohort_batch` (before the `if era != "1989_1991":` guard); the docstring + the now-unreachable-for-real-cohort-years `NotImplementedError`→a defensive `RuntimeError` reworded (ALL cohort eras implemented; the residual §2 fail-closed is the `_cohort_era` ValueError for the 1992-1994 permanent NCHS linkage gap / pre-1983, raised UPSTREAM).

**Bundled §7 root-cause fix to RESOLVED-5b `parse_linked_year.run_parse` (human-authorized; the binding §7/§8 halt-and-ask satisfied)** — additive (`git diff --numstat` = **65 / 5**; the 5 deletions = the `pa.Table.from_pylist(rows/buffer)` (×3) + `pq.ParquetWriter(str(out), tbl.schema)` (×2) lines refactored into a behavior-preserving `_tbl()` closure + a `_schema`-aware writer): a NEW `_expected_parsed_schema(year)` returns the deterministic den∪num column union for **1983-1988 ONLY** (from the parser's OWN `_layout_for_linked_year` + `_numerator_layout_for_linked_year` dispatchers — no data scan, no new layout logic) and **`None` for every homogeneous year** → `run_parse` passes the explicit unified schema to `from_pylist(..., schema=)` + `ParquetWriter(out, schema)` for 1983-1988; the non-1983-1988 path is **provably byte-identical** to the original (`_expected_parsed_schema(1990/1996/2003/2004/2010/2015/2023) is None` → `_tbl(recs)`==`pa.Table.from_pylist(recs)`, writer==`tbl.schema`; §9-#7 — the byte-exact shipped single-member parquets unperturbed). See "The §7 finding" below.

Plus a NEW sibling `DESIGN: tracks-current-state` SMOKE harness `natality/tests/test_linked_cohort_5ciii_harmonize_smoke.py` (47 tests; Convention 1/2; sub-step-isolation; Tier-0 synthetic + Tier-1 real `LinkCO83`/`LinkCO88` + the bundled `run_parse`-fix coverage) + the 3 L17 / Convention-2 / §4.2.1 minimal `tracks-current-state` Edits bundled in the SAME commit per PRE-FLIGHT HALT 9 (5c-iii implemented the LAST unimplemented cohort era → the keyless-1983-1988 `NotImplementedError` pins in `test_linked_cohort_5cii_b_harmonize_smoke.py` + `_5cii_a_harmonize_smoke.py` + `_5c_harmonize_smoke.py` went stale; each reframed to the genuine residual `_cohort_era` ValueError for the 1992-1994 gap / pre-1983 + the module-docstring `tracks-current-state` notes). **Zero canonical parquet / `harmonized_schema.csv` / validation-CSV / `field_specs.py` mutation** (the harmonizer is NOT re-run at 5c-iii; 11/11 gate parquet SHAs byte-exact at VERIFY-A).

### The §7 finding (surfaced + halt-and-asked + human-authorized Option A)

The 5c-iii SMOKE Tier-1 (real `LinkCO83`/`LinkCO88`) initially failed: `pa.Table.from_pylist`/`pa.RecordBatch.from_pylist` infer the Arrow schema from the **FIRST record's keys ONLY** (empirically confirmed: 20 den-then-20-num rows → `UCOD` + the entire numerator section dropped). `parse_linked_year.run_parse` (RESOLVED-5b code) materializes via `pa.Table.from_pylist(rows/buffer)`; `_iter_two_file_1983_1988` yields ALL den rows (47 keys, NO death section) BEFORE any num row (74 keys, WITH the locs 194-500 ICD-9 mortality section) → at DO step 6 the 1983-1988 `_raw` parquet build is defective (single-list path **silently drops the entire numerator ICD-9/age-recode section**; the default chunked path **crashes** at the den→num boundary; 1989-2004/2005+ single-member parsing unaffected). This is a latent defect in **already-completed RESOLVED-5b work** whose root-cause fix is in `parse_linked_year.py` — **outside 5c-iii's declared harmonize-side scope** (§7-#17; "5b RESOLVED — do NOT re-open"; §8 "halt for human approval if the lesson reveals a bug in already-completed work"). Per the binding §7/§8/anti-pattern-#3/KICKOFF, this was surfaced to the human via AskUserQuestion with the airtight finding + 3 concrete options; the human responded *"whatever is the best, use your judgment"* (the §7/§8 halt-and-ask satisfied — surfaced + options + human response with the bug-in-completed-work explicitly disclosed). Judgment: **Option A** (bundle the minimal surgical root-cause fix; §2 fail-closed + anti-pattern #7; the keyless encoding is un-exercisable end-to-end without it; parser-substrate-only, zero canonical mutation). The §9-#4 SMOKE-Tier-1 test-construction was independently fixed (mis-copied the 5c-ii-b homogeneous-dict pattern; corrected to build the explicit unified den∪num batch — the harmonize code is correct, 120/120 GREEN; only the test-input construction was wrong; the H6 `n_den>n_num` assertion mis-sized to the full-file property → corrected to "both segments present", the den≫num conservation being the DO-step-6 §15.D NCHS-per-year-cohort-count VERIFY). Full narrative: `PRE_FLIGHT_LOG.md` 2026-05-20T02:00:00Z addendum (A″)/(B″) + the LESSONS entry.

### Inputs consumed

- `PRE_FLIGHT_LOG.md` 2026-05-20T00:00:00Z (main, before any code mutation; L10-safe) + the 2026-05-20T02:00:00Z addendum (the §7 finding + Option-A resolution + the §9-#4 test-construction fix; the 5b 08:30Z / 5c-ii-a 15:00Z dated-addendum precedent). 9/9 forward-looking HALTs from `RECEIPTS/C8.18_step5c-ii-b` re-verified; 11/11 gate parquet SHAs byte-exact; canonical pytest baseline `251 passed, 1 skipped, 1 xfailed in 352.85s` (4-dir) preserved; the read-only real-data Convention-3 probe (`/tmp/c8_18_s5ciii/probe_*.py` on real `LinkCO83/85/88`) + 6 within-task scope decisions (A)-(F) + the (A′) RECWT@91 finding + (A″) the §7 run_parse finding surfaced + resolved.
- `RECEIPTS/C8.18_step5c-ii-b_2026-05-19T23-00-00Z.md` (the 9 forward-looking HALTs) + `RECEIPTS/C8.18_step5b_2026-05-19T08-00-00Z.md` (the RESOLVED keyless two-file construction model + self-check #1/#3/#6 — the den/num harmonized semantics + the segment-distinction-preservation 5c mandate + the per-year non-anchor verification routed to 5c/6) + `RECEIPTS/C8.18_step3a` (self-check #2 — non-anchor 1983-1988 fields transcribed from the 1983 layout, NOT individually value-verified for 1984-1988 → DO step 5/6).
- `natality/scripts/03_harmonize/harmonize_linked_v3.py` (the 5c-iii target; read in full) + `harmonize_v1_core.py` (the H7 `is_pre1989` 1968/1978-rev reference: `_mrace1digit_to_bridged4`/`_dmeduc_years_to_cat4`/cert_rev "unrevised_1968"/native LBORD_R9/marital-NULL[public-use]/hispanic-NULL/DGESTAT 17-47∪99/DFAGE 10-98; read lines 206-287 + 620-819).
- `natality/scripts/01_import/parse_linked_year.py` (`run_parse` + `iter_parsed_records`/`_iter_two_file_1983_1988` + the layout dispatchers — the §7-fix target; read) + `field_specs.py` `LINKED_BIRTH_1983_1988_FIELDS`(91) + `LINKED_NUM_DEATH_1983_1988_FIELDS`(194-500) + reclen constants (state-on-disk 3a/3b; read-only, NOT edited).
- `~/Desktop/natality-harmonization/raw_data/linked/LinkCO{83,85,88}.zip` (read-only, ≤5000 den + ≤3000 num/year via the probes + ≤500/segment Tier-1; SHA-anchored at C8.18 DO step 2).
- 11 gate parquets — SHA-verified byte-exact at the PRE-FLIGHT cheap-check + re-verified at VERIFY-A.

### Outputs produced

Git-tracked (this commit):

- `natality/scripts/03_harmonize/harmonize_linked_v3.py` — additive (**328 / 8**; 0 era-body logic-deletion lines; the 8 anchors intact: `def _harmonize_cohort_1995_2002`/`_2003_2004`/`_1983_1988`/`_mrace1digit_to_bridged4` + `out["underlying_cause_icd9"] = opt_str("UCOD")` [5c-i] + `out["prenatal_visits"] = opt_int("NPREVIST"` [5c-ii-a] + `is_post2013 = "OEGEST_COMB" in cols` [2005+] + `("link_segment", pa.string())`; `OUT_SCHEMA` 80→81).
- `natality/scripts/01_import/parse_linked_year.py` — additive (**65 / 5**; the §7 root-cause fix: `_expected_parsed_schema` + the behavior-preserving `run_parse` `_tbl`/`_schema` wiring; non-1983-1988 byte-identical, §9-#7 — the human-authorized scope expansion).
- `natality/tests/test_linked_cohort_5ciii_harmonize_smoke.py` — NEW; `DESIGN: tracks-current-state`; 47 tests.
- `natality/tests/test_linked_cohort_5cii_b_harmonize_smoke.py` / `_5cii_a_harmonize_smoke.py` / `_5c_harmonize_smoke.py` — minimal L17 `tracks-current-state` Edits (the keyless-1983-1988 `NotImplementedError`-pin tests reframed to the genuine residual `_cohort_era` ValueError + docstring notes).
- `PRE_FLIGHT_LOG.md` (the 5c-iii main entry + the 02:00:00Z addendum) + this `RECEIPTS/C8.18_step5c-iii_…` + `DECISION_LOG.md` 2026-05-20 + `LESSONS.md` (the L13-extension/H6 recurrence note) + `STATUS.md` — appended.

No canonical parquet / `harmonized_schema.csv` / validation-CSV / `field_specs.py` touched. Raw zips/guides gitignored + out-of-tree (read-only, unchanged). §15.D NOT edited this step (the broader model-clarification — now also folding the 5c-iii keyless encoding + the (A′) RECWT@91 finding + the §7 `run_parse` fix — stays soft-flag (ii) §11 human-merge; the 3a-5c-ii-b precedent).

### Five-phase trace

- **PRE-FLIGHT**: ✓ `PRE_FLIGHT_LOG.md` 2026-05-20T00:00:00Z (main; before any code mutation; L10-safe) + the 02:00:00Z addendum (the §7 finding + Option-A + the §9-#4 test fix, before the commit; L10-safe — a NEW dated entry, not a back-fill). 9/9 forward-looking HALTs re-verified; 11/11 gate SHAs byte-exact; pytest 251P+1S+1XF/352.85s baseline; the Convention-3 probe + 6 scope decisions + the (A′)/(A″) findings + the L17 grep-scope (3 stale pins) surfaced + resolved before DO.
- **SMOKE**: ✓ NEW harness authored FIRST (§9-#9). Run vs the **un-modified** harmonizer = collection ImportError (`cannot import name '_harmonize_cohort_1983_1988'`) — proves it genuinely requires the DO code (L3/§9-#5). After DO: 110 pass / 2 fail (the §7 finding surfaced — the cheap-check working). After the §7 root-cause fix + the §9-#4 test-construction fix: **120/120 first-run** on the 4 cohort harmonize harnesses (no test loosened — the H6 reframe is the correctly-sized SMOKE invariant; the §7 fix is the root-cause fix, not a workaround).
- **DO**: ✓ additive `link_segment`→OUT_SCHEMA + the 2005+ pa.nulls entry + `_mrace1digit_to_bridged4` + `_harmonize_cohort_1983_1988` + the dispatch + the docstring/RuntimeError reword + the bundled `parse_linked_year` §7 fix + the new SMOKE harness + the 3 L17 Edits. Single commit (the 3a-5c-ii-b precedent).
- **VERIFY**: ✓ all 5 criteria (below).
- **RECEIPT**: ✓ this file.

### Verify results

- **VERIFY-A (zero canonical mutation)**: PASS — 11/11 gate parquet SHAs byte-exact vs the PRE-FLIGHT HALT 2 (sorted-prefix set equality, `/tmp/c8_18_s5ciii/gate.out` vs the VERIFY recompute): `185c071e 230efed2 38e2cecb 5c22308b 7c682668 9b828a4d acb5c48a adbec108 c8a740eb d98b4296 e16ad532`. Threshold: 11/11 unchanged.
- **VERIFY-B (git scope)**: PASS — modified/new = `harmonize_linked_v3.py`, `parse_linked_year.py` (the human-authorized §7 fix), `test_linked_cohort_5ciii_harmonize_smoke.py` (new), the 3 L17-edited harnesses, `PRE_FLIGHT_LOG.md`, + `RECEIPTS/`/`DECISION_LOG.md`/`LESSONS.md`/`STATUS.md`. NO `.parquet`/`harmonized_schema.csv`/validation-CSV/`field_specs.py`. Threshold: scope ⊆ {scripts, test, plan, state} + the authorized `parse_linked_year.py` expansion.
- **VERIFY-C (additive / behavior-preserving)**: PASS — `git diff --numstat` `harmonize_linked_v3.py` **328 / 8** (0 era-body logic-deletion lines; the only non-comment deletion = the documented `NotImplementedError(`→`RuntimeError(` dispatch-guard reword; 8 anchors intact → the 5c-i + 5c-ii-a + 5c-ii-b + 2005+ bodies byte-untouched, just shifted + gaining the auto-defaulted `link_segment`=NULL); `parse_linked_year.py` **65 / 5** (the 5 deletions = the `from_pylist`/`ParquetWriter` lines refactored into the behavior-preserving `_tbl`/`_schema` wiring; `_expected_parsed_schema(non-1983-1988) is None` → the homogeneous-year path provably byte-identical, §9-#7). `OUT_SCHEMA` 80→81. `python -c "import harmonize_linked_v3"` OK; `_harmonize_cohort_1983_1988`/`_mrace1digit_to_bridged4`/`_expected_parsed_schema` callable.
- **VERIFY-D (pytest count gate)**: PASS — canonical **4-dir** suite `fetal_death/ natality/ tests/ matched_multiples/` = **303 passed, 1 skipped, 1 xfailed in 392.70s** (`/tmp/c8_18_s5ciii/pytest_verify.out`). Reconciles EXACTLY: the 5c-ii-b-close baseline 251P+1S+1XF **+** 47 new `test_linked_cohort_5ciii_harmonize_smoke.py` **+** 5 (the 3 L17 reframes: `_5cii_b` [1983,1985,1988]→[1992,1993,1994,1979] +1; `_5cii_a` +1; `_5c` [(1985,"5c-iii")]→[1992,1993,1994,1979] +3) = 303P; zero regression (1 skipped + 1 xfailed unchanged; ALL prior tests pass — the byte-untouched prior-era harmonize bodies + the behavior-preserving `run_parse` refactor for homogeneous years + the 5b `test_tier1_2005_regression_unchanged` + the parser dispatcher-unregressed tests). 392.70s within the ~340-680s **advisory** band → soft-flag (kk) (the COUNT 303P+1S+1XF is the gate; 11/11 gate SHAs byte-exact = zero canonical regression).
- **VERIFY-E (SMOKE all PASS)**: PASS — the 4 cohort harmonize harnesses **120/120** first-run after the §7 + §9-#4 fixes. Tier 0 (the den infant_death-NULL / num infant_death-True + ICD-9 + no-AGED two-segment encoding; the birth-side H7 `is_pre1989` ALIAS; `_mrace1digit_to_bridged4` edges + H7 parity; `link_segment` ∈ OUT_SCHEMA + NULL for other eras; the residual gap/pre-1983 fail-closed; the 1989-1991/1995-2002/2003-2004 regression-locks; `_expected_parsed_schema` unified-1983-1988 / None-homogeneous) + Tier 1 (real `LinkCO83`/`LinkCO88` parsed via the 5b den/num union then harmonized; `test_tier1_run_parse_preserves_numerator_section` — the §7 fix end-to-end: run_parse → read-back parquet → UCOD/AGER5/UCODR61 survive → harmonize → ICD-9 populated on num rows).

### Reproducibility

- `_harmonize_cohort_1983_1988` + `_mrace1digit_to_bridged4` + `_expected_parsed_schema` + the reused recodes are deterministic pure functions; re-running yields identical output. The SMOKE re-derives from synthetic + the SHA-anchored real archives → deterministic.
- Zero canonical parquet rebuild (DO step 5c-iii is harmonize-side code + the parser-substrate §7 fix; the re-harmonize is DO step 6). H10 gate: 11/11 SHAs byte-exact.
- No new Python dependency (reuses pyarrow + the existing helpers; the C8.5a pinned-env SHA untouched).
- The architecture + the 1983-1988 map + the (A′) RECWT@91 finding + the §7 `run_parse` fix are state-on-disk in `PRE_FLIGHT_LOG.md` 2026-05-20T00:00:00Z + the 02:00:00Z addendum + `DECISION_LOG.md` 2026-05-20 + `LESSONS.md`.

### Cross-product re-probe (if applicable)

- N/A — DO step 5c-iii is additive linked harmonize-side code + the parser-substrate §7 fix; no cross-product artifact consumes it until DO step 6 (re-harmonize → v4). 11/11 gate parquet SHAs byte-exact (incl. fetal-death + natality + MM, untouched). The canonical v3 2005-2023 linked product + the 5c-i 1989-1991 + 5c-ii-a 1995-2002 + 5c-ii-b 2003/2004 paths are unperturbed (link_segment auto-defaults NULL there); the homogeneous-year `run_parse` path is provably byte-identical.

### Git

- Pre-DO rollback anchor: `C8.18-pre-do` @ `6632a15` (unchanged; the C8.18 DO rollback anchor).
- This commit: intermediate C8.18 DO step 5c-iii — **NO tag** (Convention 5; `C8.18-complete` is the final-sub-step-only tag, set at DO step 7).

### STATUS.md updated

- New section dated 2026-05-20T02:00:00Z at the top (newest-first).

### Self-check (§10) — what could I have gotten wrong that VERIFY wouldn't catch?

1. **The 1983-1988 birth-side per-field ALIAS is anchor-verified on the ≤5000-den/≤3000-num real probe + ≤500/segment Tier-1 + synthetic, not the full 6-year set.** A latent non-anchor alias mis-map (the L13-extension class — the RECWT@91 (A′) finding proves the 1983-guide-only layout is NOT byte-stable for non-anchor fields across 1984-1988) would not be caught at 5c-iii. *Mitigation*: anchors (BIRYR/MATCHS/CSEX/DPLURAL/DBIRWT) 3a-real-data-verified; the probe value-distribution-checked ~24 non-anchors (all era-consistent EXCEPT RECWT@91, conservatively NULLed); the H7 `is_pre1989` recodes are byte-identical to the natality sibling. *Recommendation*: **soft-flag (ll)** — DO step 6 PRE-FLIGHT MUST value-distribution-verify the FULL per-year (1984-1988) 1983-1988 harmonized set vs the NCHS published cohort Linked File report tables (the §15.D VERIFY criterion; the 3a self-check #2 + 5b self-check #3 already routed this here). Logged DECISION_LOG residual risk.
2. **The §7 `run_parse` fix is verified on real `LinkCO83` (single-list path, max_rows=300) end-to-end, NOT on the full-file chunked production path (`chunk_rows=250_000`, ~3.8M den + ~38K num).** A latent chunk-boundary issue under the explicit `_schema` (e.g., a num record with an unexpected extra key) would not be caught at 5c-iii. *Mitigation*: `_expected_parsed_schema` is derived from the parser's OWN layout dispatchers (the exact field set `_iter_two_file_1983_1988` emits); `from_pylist(schema=)` empirically fills missing keys with null + honors the schema; the writer is fixed to the full union schema from the first chunk. *Recommendation*: DO step 6's actual 1983-1988 re-harmonize (full-file, chunked) + the NCHS-per-year-cohort-count VERIFY is the durable end-to-end gate (forward HALT 9). Logged DECISION_LOG residual risk.
3. **`infant_death`=NULL for den / True for num is the RESOLVED-5b model; the cohort 1983-1988 IMR = count(num)/count(den) per stratum — NOT a per-birth `infant_death` filter (the 1989+ form).** A downstream analyst who naively filters `infant_death==True` + counts all rows as births would double-count (the num birth-covariates re-represent dead infants also in den). *Mitigation*: the explicit `link_segment` column makes the within-era structural difference loud + self-documenting (the 5b self-check #6 mandate satisfied). *Recommendation*: the COMPARABILITY/schema-CSV note + the manuscript Coverage paragraph = the Phase-D D.4 deliverable (forward HALT 6e + the carried-forward D.4). Logged DECISION_LOG residual risk.
4. **`record_weight` conservatively NULL for ALL 1983-1988 (the (A′) RECWT@91 era-instability resolution).** If a downstream analysis expected a 1983-1988 inflation weight it gets null. *Mitigation*: the cohort IMR = num/den does not use record_weight; the 5c-i/1989-1991 precedent had NULL record_weight; `field_specs.py` left UNTOUCHED (the substrate is still correct for 1983-1985; the byte-91 1986-1988 question + the full per-year non-anchor re-verification is routed to soft-flag (ll) DO step 6). Logged DECISION_LOG residual risk.
5. **Hispanic (origin/maternal/father/race_ethnicity_5) + the composite blocks + smoking/diabetes/etc. conservatively NULL** — a downstream analysis expecting cohort-1983-1988 Hispanic/smoking/delivery-method gets null. *Mitigation*: the natality `is_pre1989` / 5c-i / 5c-ii conservative-mapping precedent; the 2-digit ORMOTH crosswalk is non-byte-stable & unverified; documented soft-flag (gg) DO step 6 leaf decomposition; the SMOKE asserts the null. Logged DECISION_LOG residual risk.
6. **The §7 finding revealed a real defect in already-completed RESOLVED-5b `run_parse`** — the 5b receipt CLAIMED "Lossless; H6 row-count conservation" but the materialization was NOT lossless for the heterogeneous den/num union. *Mitigation*: surfaced via the binding §7/§8 halt-and-ask (AskUserQuestion, 3 concrete options, the bug-in-completed-work explicitly disclosed); human-authorized Option A; the LESSONS entry + the §8-row-sharpening proposal documents the H6/L13-extension recurrence (a heterogeneous-union materialized via first-record-schema-inference silently drops the late-arriving segment's columns). *Recommendation*: §11 human-merge of the LESSONS-proposed §8 row; DO step 6's NCHS-per-year-cohort-count VERIFY is the durable end-to-end gate. Logged DECISION_LOG residual risk + LESSONS.
7. **`_harmonize_cohort_1983_1988` reads `link_segment` via `_get_col` (required).** If a future parser change drops `link_segment` for 1983-1988, the harmonizer fail-closes (KeyError) — correct (§2), but a silent rename would surface only at DO step 6. *Mitigation*: the SMOKE Tier-1 asserts every 1983-1988 parsed row carries `link_segment ∈ {den,num}` + the `_expected_parsed_schema` includes it; the 5b construction is RESOLVED + state-on-disk. Logged DECISION_LOG residual risk.
8. **Behavior-preservation for the 5c-i/5c-ii-a/5c-ii-b/2005-2023 paths + the homogeneous-year `run_parse`** is asserted via the additive diff (anchors intact, byte-untouched bodies, `_schema is None` byte-identity) + the 303-baseline (incl. the explicit regression-lock + dispatcher-unregressed tests), NOT a full re-harmonize/re-parse byte-diff (DO step 6). *Mitigation*: the 1983-1988 branch fires only for year ∈ {1983..1988}; `_expected_parsed_schema(homogeneous) is None`; 11/11 gate SHAs byte-exact. *Recommendation*: DO step 6's 2005-2023 + per-era byte-clean regression vs the v3 baseline is the durable §15.D check. Logged DECISION_LOG residual risk.

Each logged in `DECISION_LOG.md` 2026-05-20 as a residual risk; the recommended checks are DO-step-6 PRE-FLIGHT items (not added to VERIFY without human sign-off per §10).

### Forward-looking HALTs for next session (C8.18 DO step 6 — re-harmonize linked 1983-2023 v3→v4 + the `harmonized_schema.csv` edit + version bump)

1. `C8.18-pre-do`@`6632a15`; `C8.18-complete` NOT present (final-sub-step-only). `C8.17-complete` present. HEAD = this DO step 5c-iii commit (after `f940b46`).
2. **11 gate parquet SHAs unchanged** (the same 11; fetal-death via the `~/Desktop/fetal-death-harmonization-build/` tree, soft-flag (hh)). Linked-derived `9b828a4d…` changes ONLY at the DO step 6 re-harmonize (5c-iii is harmonize/parser substrate only).
3. **Canonical pytest baseline is now `303 passed, 1 skipped, 1 xfailed`** on the **4-dir** suite (was 251P+1S+1XF; +47 the new 5c-iii harness + 5 the 3 L17 reframes). Runtime ~340-680s advisory (soft-flag (kk); the COUNT is the gate). README 3-dir/"56 passed" stays stale → soft-flag (jj).
4. `harmonize_linked_v3` exposes `_harmonize_cohort_1983_1988` (NEW) + `_mrace1digit_to_bridged4` (NEW) + `_harmonize_cohort_2003_2004` + `_harmonize_cohort_1995_2002` + `_cohort_era` + `_harmonize_cohort_batch` (**ALL cohort eras 1983-1991 + 1995-2004 implemented**; the residual §2 fail-closed is the `_cohort_era` ValueError for the 1992-1994 gap / pre-1983 + each function's wrong-era ValueError + the defensive `_harmonize_cohort_batch` `RuntimeError`) + `OUT_SCHEMA` **81 cols** (the NEW `link_segment` appended last). `parse_linked_year` exposes `_expected_parsed_schema` + the §7-fixed `run_parse`. **State-on-disk; do NOT re-derive / do NOT re-open** the 5b construction, the within_era ICD-9/ICD-10 shape, or the §7 fix.
5. The within_era ICD-9/ICD-10 cause-column-shape is **RESOLVED + state-on-disk** (§15.D DO step 1; 5c-i/ii-a/ii-b/iii applied it). 1983-1988 = **ICD-9** (`underlying_cause_icd9`=UCOD / `cause_recode_61`=UCODR61 on num rows; ICD-10 cols null; **NO `age_at_death_days`** — only `age_at_death_recode5`=AGER5). Do NOT re-open.
6. **The `harmonized_schema.csv` metadata edit + the v3→v4 version bump = DO step 6** (Anti-Pattern #6). 5c-iii did NOT touch it. DO step 6 PRE-FLIGHT MUST enumerate (halt if it does not): (a) widen the death-side rows' `years_available` 2005-2023 → 1983-2023 (with the 1992-1994 gap); (b) add the `underlying_cause_icd9`/`cause_recode_61` rows + the ICD-9(≤1998)/ICD-10(≥1999) null-pattern note; (c) widen `manner_of_death` `years_available` (cohort 2003-2004; null 1983-2002 incl. 1983-1988); (d) widen `father_age_cat_from_rec11` `years_available` (cohort 2003-2004); (e) **NEW** add the `link_segment` schema row (den/num for 1983-1988, null elsewhere) + the within-era structural-difference note (1983-1988 IMR = count(num)/count(den), NOT a per-birth `infant_death` filter — the D.4 Coverage item) + the 1983-1988 `record_weight`/hispanic null-pattern + the "no `age_at_death_days`, AGER5-only" note + cert_rev "unrevised_1968" for 1983-1988; (f) bump v3→v4.
7. **NEW critical DO-step-6 gate**: the §7 `run_parse` fix is in place but DO step 6 MUST value-distribution-verify the actual 1983-1988 `_raw` parquet (full-file `run_parse`, chunked) preserves the FULL numerator ICD-9 section + the den/num row counts (H6: den == NCHS-stated births; num == NCHS-stated infant deaths) vs the NCHS published cohort Linked File report — the durable end-to-end check that the §7 fix + the RESOLVED-5b construction are correct. If the 1983-1988 num section is empty / the den≈num counts are wrong / the per-year cohort IMR diverges from NCHS → §7 halt.
8. soft-flag (gg) carries: the cause-column SHAPE resolved; the composite-block leaf decomposition (DELMTH/MEDRISK/OBSTETRC/LABOR/ABNORMNB/ENTITY/RECORDAX + smoking/diabetes/hypertension/preterm_recode3 + the 2-digit ORMOTH/ORFATH Hispanic crosswalk) stays conservatively NULL → DO step 6. **NEW soft-flag (ll)**: the RECWT@91 byte-91 1986-1988 layout-instability + the FULL per-year (1984-1988) non-anchor byte-stability re-verification → DO step 6 PRE-FLIGHT (the 3a self-check #2 + 5b self-check #3 routed it here; the NCHS-per-year-cohort-count VERIFY is the durable root-cause gate; `field_specs.py` UNTOUCHED — a per-year layout reconstruction, if needed, is a 3a-class task surfaced at DO step 6).
9. §15.D substrate-format reconcile applied at the 3a commit; the broader model-clarification — now ALSO folding the **5c-iii keyless 1983-1988 harmonized encoding + the `link_segment` OUT_SCHEMA column + the (A′) RECWT@91 conservative-NULL + the bundled §7 `run_parse` root-cause fix** alongside 3a/3b + 4a/4b/4c + 5→5a/5b/5c + 5c→5c-i/5c-ii/5c-iii + 5c-ii→5c-ii-a/5c-ii-b + the 2003/2004 MATCHS/FLGND finding — remains soft-flag (ii) proposed-not-applied §11 human-merge. On-disk PRE_FLIGHT_LOG/DECISION_LOG/RECEIPT/LESSONS authoritative meanwhile.
10. **L17 forward-looking discipline**: 5c-iii implemented the LAST unimplemented cohort era → NO `NotImplementedError` pin remains in any cohort harness (the 3 reframed to the `_cohort_era` ValueError gap/pre-1983 residual). DO step 6 (the re-harmonize + the `harmonized_schema.csv` edit + the v3→v4 version bump) MUST grep-scope ALL cohort/linked harnesses for any v3/v4 version pin, `OUT_SCHEMA`-col-count literal (none found — the harnesses assert `schema == OUT_SCHEMA` relatively), or `harmonized_schema.csv`-row pin, and bundle the minimal `tracks-current-state` Edit into the SAME DO-step-6 commit (§4.2.1/Convention-2/L17). pytest COUNT 303P+1S+1XF is the gate; runtime band advisory (soft-flag (kk)).

### Notes for next session

- **C8.18 DO step 5c-iii is CLOSED.** ALL cohort eras (1983-1991 + 1995-2004) are harmonize-implemented. `_harmonize_cohort_1983_1988` (the RESOLVED 5b two-file den/num encoding; den→infant_death NULL, num→True+ICD-9; the H7 `is_pre1989` 1968/1978-rev birth-side ALIAS; record_weight/hispanic conservatively NULL) + `_mrace1digit_to_bridged4` + the `link_segment` OUT_SCHEMA column + the bundled §7 `run_parse` root-cause fix are state-on-disk + this receipt + `PRE_FLIGHT_LOG.md` 2026-05-20T00:00:00Z + the 02:00:00Z addendum + `DECISION_LOG.md` 2026-05-20 + `LESSONS.md`. Next per the 5c→5c-i/5c-ii/5c-iii decomposition: **DO step 6 — re-harmonize linked 1983-2023; preserve the v3 parquet as `.v3_baseline`; v3→v4 + the `harmonized_schema.csv` edit (HALT 6 (a)-(f)) + version bump per Anti-Pattern #6 + Convention-2/L17 re-pins**, then DO step 7 (CITATION.cff/.zenodo.json/ABOUT_THIS_RELEASE/README/PROJECT_STRUCTURE/manuscript refs; tag `C8.18-complete`).
- §2 cheap-before-expensive + the §9-#9 SMOKE-first + the binding §7/§8 halt-and-ask discipline governed 5c-iii (the read-only Convention-3 probe pre-resolved the substrate + caught the RECWT@91 (A′) instability; the SMOKE caught the §7 `run_parse` defect at the cheap-check; the §7/§8 condition was surfaced + halt-and-asked + human-authorized before the root-cause fix; the §9-#4 fix-the-test-correctly discipline distinguished the test-construction bug from the real §7 defect). Carry it.
- The §15.D model-clarification `[plan-update]` (soft-flag (ii)) is owed to the human (§11; queued with the soft-flag-(w) batch; now also folds 5c-iii + the §7 `run_parse` fix + the (A′) RECWT@91 finding); on-disk logs authoritative meanwhile. The LESSONS-proposed §8 row (H6/L13-extension sharpening) is owed §11 human-merge.
- Runtime 392.70s within the advisory band (soft-flag (kk)); the COUNT 303P+1S+1XF is the gate.
- `/tmp/c8_18_s5ciii/*` scratch (gate-SHA + pytest baseline/verify + the 3 read-only probes) is OS-cleanable + reproducible.
