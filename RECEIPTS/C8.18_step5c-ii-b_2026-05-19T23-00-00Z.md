# Receipt: C8.18 DO step 5c-ii-b — the 2003 + 2004 cohort birth-side maps (the 2003-revision dual-certificate transition)
## 2026-05-19T23:00:00Z

### What was done

Executed C8.18 **DO step 5c-ii-b** under the full five-phase discipline in one session (PRE-FLIGHT + SMOKE + DO + VERIFY + RECEIPT; the C8.18 DO step 3a/3b/4a/4b/4c/5a/5b/5c-i/5c-ii-a single-session-DO-step precedent). §15.D **DO step 5c-ii** was decomposed at the 5c-ii-a Convention-3 snapshot → **5c-ii-a** (1995-2002; the 1989-rev sibling, SHIPPED 2026-05-19T17:00:00Z) + **5c-ii-b** (2003 + 2004; the 2003-revision dual-certificate transition — the natality V1-core `is_2003revised` analog). This session shipped **5c-ii-b**.

5c-ii-b added **additively** to `natality/scripts/03_harmonize/harmonize_linked_v3.py` (`git diff --numstat` = **327 insertions / 9 deletions**; the 9 deletions are ALL the `_harmonize_cohort_batch` docstring 2003/2004-bullet reword + the `NotImplementedError` message-string reword — **0 logic-deletion lines**, grep-confirmed; the 5c-i 1989-1991 inline body + the 5c-ii-a `_harmonize_cohort_1995_2002` + the 2005+ `_harmonize_batch` body are byte-untouched, anchors intact):

1. **`_mager41_to_age(mager41)`** (NEW helper) — maps the 2003-rev MAGER41 (01-41) age-of-mother recode to single-year age (`code 1→14`; `codes 2-41→code+13`; `99|>41→null`). **Byte-identical to the `harmonize_v1_core` inline `is_2003revised` MAGER41 conversion** (H7 sibling-parity; SMOKE-asserted equal). The cohort 2003 `MAGER41` AND the cohort 2004 `MAGER` @89-90 are BOTH this recode-41 (NOT a single-year age — field_specs 4b/4c + the 2026-05-19T20:00:00Z real-data probe).
2. **`_harmonize_cohort_2003_2004(batch, year)`** (NEW) — the 2003 + 2004 cohort denominator-plus birth-side map. ONE function handles BOTH years (ONE §15.D sub-step 5c-ii-b, ONE certificate revision; the 2003-vs-2004 deltas are pure raw-name aliasing — `MAGER41`↔`MAGER` @89-90, `DBWT`↔`BRTHWGT` @467-470, the death match-status `MATCHS`↔`FLGND` not consumed by the AGED-value-driven harmonizer — resolved via `_get_col_optional` name-fallback, the natality V1-core single `is_2003revised` branch pattern; §9-#8). The birth section ALIASES the cohort raw names onto the natality V1-core 2003-rev recodes (H7 sibling-parity — reuses `_meduc_to_cat4`/`_meduc_rec_to_cat4`/`_month_to_trimester`/`_fagerec11_to_cat`/`_pldel_to_facility`/`_mrace_detail_to_bridged4` + the new `_mager41_to_age`; do NOT re-derive). Dual-certificate handled faithfully via the natality V1-core coalesce: `certificate_revision` = `has_MEDUC→revised_2003 ; else has_MRACE→unrevised_1989 ; else unknown`; `maternal_education_cat4` = `if_else(is_null(_meduc_to_cat4(MEDUC)), _meduc_rec_to_cat4(MEDUC_REC), _meduc_to_cat4(MEDUC))`; `prenatal_care_start_month` = `if_else(is_null(PRECARE), MPCB, PRECARE)` — byte-identical to natality V1-core 2003-rev. Per-era deltas vs the 5c-ii-a 1995-2002 map: `MANNER` present → `manner_of_death` (NEW); `gestational_age_weeks_source` = "combined" (2003-rev COMBGEST; schema-documented; vs 5c-ii-a's "lmp"); ALL ICD-10 (cohort birth-year 2003/2004 ≥ 1999 → `underlying_cause_icd10`/`cause_recode_130`; the ICD-9 cols null — the 5c-i resolved shape, §15.D DO step 1); `father_age_cat_from_rec11` populated via `_fagerec11_to_cat(FAGEREC11)` (the 2003-rev within-era enrichment). A fail-closed `if _cohort_era(year) not in ("2003","2004"): raise ValueError`.
3. A 2-line `if era in ("2003","2004"): return _harmonize_cohort_2003_2004(batch, year)` dispatch prepended in `_harmonize_cohort_batch` AFTER the 5c-ii-a `if era == "1995_2002":` line, BEFORE the existing `if era != "1989_1991":` guard; the `NotImplementedError` message + the `_harmonize_cohort_batch` docstring reworded (now only the keyless 1983-1988 = 5c-iii remains unimplemented). The 5c-i 1989-1991 + 5c-ii-a 1995-2002 + 2005+ bodies byte-untouched (the 2003/2004 branch fires only for year ∈ {2003,2004} → the canonical v3 2005-2023 path + the 5c-i 1989-1991 + the 5c-ii-a 1995-2002 paths byte-identical; §9-#7-safe).

Plus a NEW sibling `DESIGN: tracks-current-state` SMOKE harness `natality/tests/test_linked_cohort_5cii_b_harmonize_smoke.py` (22 tests; Convention 1/2; sub-step isolation) + the L17 / Convention-2 / §4.2.1 minimal `tracks-current-state` Edits to **two** existing harnesses, bundled in the SAME commit per the PRE-FLIGHT HALT 9: (i) `test_linked_cohort_5cii_a_harmonize_smoke.py::test_tier0_unimplemented_eras_still_failclosed` `[2003,2004,1983,1985,1988]`→`[1983,1985,1988]` (2003/2004 now implemented); (ii) `test_linked_cohort_5c_harmonize_smoke.py::test_tier0_unimplemented_cohort_eras_failclosed` `[(1985,"5c-iii"),(2003,"5c-ii-b"),(2004,"5c-ii-b")]`→`[(1985,"5c-iii")]`; **+ scope decision (E)** the 5c-ii-a-receipt-recommended §9-#4 harness-precision tighten `test_linked_cohort_5c_harmonize_smoke.py::test_tier1_real_1990_parse_then_harmonize` `m in ("1","2")`→`m == "1"` (the era-correct AND era-independent linked-infant-death code; STRICTER not looser; the 5c-i harness is edited anyway for (ii) → a same-commit within-task harness-precision Edit; consumes DECISION_LOG 2026-05-19T17:00:00Z residual-risk #6). NO `OUT_SCHEMA` change (80 cols unchanged — the `harmonized_schema.csv` metadata edit + the v3→v4 version bump remain DO step 6 per Anti-Pattern #6). **Zero canonical parquet / `harmonized_schema.csv` / validation-CSV mutation** (the harmonizer is NOT re-run at 5c-ii-b; 11/11 gate parquet SHAs byte-exact at VERIFY-A).

**No Convention-3 divergence surfaced at SMOKE** (unlike 5c-ii-a/5b). The read-only real-data probe on `LinkCO03US.zip`+`LinkCO04US.zip` at the Convention-3 PRE-FLIGHT snapshot pre-resolved the §7-#13 match-status code (the cross-tab established the linked-infant-death code is uniformly `MATCHS`/`FLGND`==`1` across 2003/2004, re-confirming the 5c-ii-a finding) — so the SMOKE Tier-1 cross-check used the era-correct `== "1"` from the start; Tier 0 + Tier 1 = 68/68 first-run.

### Inputs consumed

- `PRE_FLIGHT_LOG.md` 2026-05-19T20:00:00Z (per-sub-step PRE-FLIGHT; before any code mutation; L10-safe). 9/9 forward-looking HALTs from `RECEIPTS/C8.18_step5c-ii-a_2026-05-19T17-00-00Z.md` re-verified; 11/11 gate parquet SHAs byte-exact; canonical pytest baseline `233 passed, 1 skipped, 1 xfailed in 405.78s` (4-dir) — the COUNT is the gate (soft-flag (kk) runtime); the real-data Convention-3 probe + 5 within-task scope decisions (A)-(E) surfaced + resolved before DO; L17 grep-scope of all 9 cohort harnesses = exactly the 2 stale-pin Edits the 5c-ii-a receipt HALT 9 predicted + the recommended precision tighten.
- `RECEIPTS/C8.18_step5c-ii-a_2026-05-19T17-00-00Z.md` (the 9 forward-looking HALTs + the byte-position guidance for 2003/2004 + the MATCHS==1-uniform finding).
- `natality/scripts/03_harmonize/harmonize_linked_v3.py` (the 5c-ii-b target — read in full) + `harmonize_v1_core.py` (the H7 sibling-parity reference: the `is_2003revised` 2003-2024 path — read lines 1062-1400).
- `natality/scripts/01_import/field_specs.py` `LINKED_BIRTH_2003_FIELDS`/`LINKED_DEATH_2003_FIELDS`/`LINKED_DENOMPLUS_RECLEN_2003`=783 + `LINKED_BIRTH_2004_FIELDS`/`LINKED_DEATH_2004_FIELDS`/`LINKED_DENOMPLUS_RECLEN_2004`=900 (state-on-disk 4b/4c/5a, read-only).
- `natality/metadata/harmonized_schema.csv` (the canonical mapping definitions cross-checked for H7 parity; NOT edited at 5c-ii-b — HALT 6).
- `natality/tests/test_linked_cohort_5cii_a_harmonize_smoke.py` + `test_linked_cohort_5c_harmonize_smoke.py` (the 5c-ii-b harness template + the L17 stale-pin + the precision-tighten targets; read in full).
- `~/Desktop/natality-harmonization/raw_data/linked/LinkCO03US.zip` (cohort 2003, DEFLATE64; 7z stream) + `LinkCO04US.zip` (cohort 2004, DEFLATE) — read-only, ≤4000 records each; SHA-anchored at C8.18 DO step 2.
- 11 gate parquets — SHA-verified byte-exact at the PRE-FLIGHT cheap-check + re-verified at VERIFY-A.

### Outputs produced

Git-tracked (this commit):

- `natality/scripts/03_harmonize/harmonize_linked_v3.py` — **additive** (`git diff --numstat` = **327 / 9**; 0 logic-deletion lines [the 9 deletions = the docstring + NotImplementedError-message reword, grep-confirmed]; the 5c-i 1989-1991 + 5c-ii-a 1995-2002 + 2005+ bodies byte-untouched, anchors intact; `OUT_SCHEMA` 80 cols unchanged).
- `natality/tests/test_linked_cohort_5cii_b_harmonize_smoke.py` — NEW; `DESIGN: tracks-current-state`; 22 tests (Tier 0: the 2003/2004 `_cohort_era` partition + the S-rev + A-rev dual-cert end-to-end + the MAGER41→age recode edges + the all-ICD-10 cause shape + the per-era deltas + the keyless-1983-1988 fail-closed negative + the direct-function wrong-era ValueError + the 1989-1991 + 1995-2002 regression-locks + the H7 sibling-parity recode equivalence; Tier 1: real `LinkCO03US` (DEFLATE64) + `LinkCO04US` parse→harmonize with the independent `match-status==1` §7-#13 cross-check + ICD-10-only + record_weight + manner_of_death).
- `natality/tests/test_linked_cohort_5cii_a_harmonize_smoke.py` — L17 minimal `tracks-current-state` Edit (`git diff --numstat` 9/6: stale-pin (i)).
- `natality/tests/test_linked_cohort_5c_harmonize_smoke.py` — L17 minimal `tracks-current-state` Edit + scope decision (E) (`git diff --numstat` 31/19: stale-pin (ii) + the `m in {1,2}`→`m == "1"` precision tighten + docstring/comment rewords).
- `PRE_FLIGHT_LOG.md` — the 5c-ii-b PRE-FLIGHT (2026-05-19T20:00:00Z).
- `RECEIPTS/C8.18_step5c-ii-b_2026-05-19T23-00-00Z.md` — this file.
- `DECISION_LOG.md` + `STATUS.md` — appended (the 5c-ii-b 2003+2004 map + the 2003/2004 MATCHS/FLGND-encoding finding + new dated section).

No canonical parquet / `harmonized_schema.csv` / validation-CSV touched. Raw zips/guides gitignored + out-of-tree (read-only, unchanged). §15.D NOT edited this step (the broader model-clarification — now also folding 5c-ii-b 2003+2004 + the 2003/2004 MATCHS/FLGND-encoding finding — stays soft-flag (ii) §11 human-merge; the 3a/3b/4a/4b/4c/5a/5b/5c-i/5c-ii-a precedent).

### Five-phase trace

- **PRE-FLIGHT**: ✓ `PRE_FLIGHT_LOG.md` 2026-05-19T20:00:00Z (per-sub-step, before any code mutation; L10-safe). 9/9 forward-looking HALTs re-verified; 11/11 gate SHAs byte-exact; pytest 233P+1S+1XF/405.78s COUNT-baseline preserved; the real-data Convention-3 probe + 5 scope decisions (A)-(E) + the L17 grep-scope (2 stale pins + 1 precision tighten) surfaced + resolved before DO (Convention 3).
- **SMOKE**: ✓ NEW harness authored FIRST (§9-#9). Run vs the **un-modified** harmonizer = collection ImportError (`cannot import name '_harmonize_cohort_2003_2004'`) — proves it genuinely requires the DO code (L3 / §9-#5, not a rubber-stamp). After DO: Tier 0 + Tier 1 = **68/68 first-run** (the new 5c-ii-b 22 + the L17-edited 5c-ii-a 20 + 5c-i 26); no Convention-3 divergence (the probe pre-resolved the match-status code); no test loosened (the (E) tighten is STRICTER).
- **DO**: ✓ additive `_mager41_to_age` + `_harmonize_cohort_2003_2004` + the 2-line dispatch + the docstring/message reword + the new SMOKE harness + the 2 L17 Edits + the (E) precision tighten. Single commit (PRE-FLIGHT + DO + RECEIPT; the C8.18 DO 3a-5c-ii-a precedent).
- **VERIFY**: ✓ all 5 criteria (below).
- **RECEIPT**: ✓ this file.

### Verify results

- **VERIFY-A (zero canonical mutation)**: PASS — 11/11 gate parquet SHAs byte-exact vs the PRE-FLIGHT HALT 2 (sorted-set equality, `/tmp/c8_18_s5cii_b/gate_verify.out`): nat `c8a740eb…`/`acb5c48a…`; `.v28_baseline` `230efed2…`/`e16ad532…`; linked-deriv `9b828a4d…`; fetal-death `38e2cecb…`/`185c071e…` (via the `~/Desktop/fetal-death-harmonization-build/` tree, soft-flag (hh)); MM `5c22308b…`/`7c682668…`/`d98b4296…`/`adbec108…`. Threshold: 11/11 unchanged.
- **VERIFY-B (git scope)**: PASS — modified/new = `harmonize_linked_v3.py`, `test_linked_cohort_5cii_b_harmonize_smoke.py` (new), `test_linked_cohort_5cii_a_harmonize_smoke.py` (L17 (i)), `test_linked_cohort_5c_harmonize_smoke.py` (L17 (ii) + (E)), `PRE_FLIGHT_LOG.md`, + `RECEIPTS/`/`DECISION_LOG.md`/`STATUS.md`. NO `.parquet`/`harmonized_schema.csv`/validation-CSV/`field_specs.py`/parser. Threshold: scope ⊆ {scripts, test, plan, state}.
- **VERIFY-C (additive / behavior-preserving)**: PASS — `git diff --numstat` `harmonize_linked_v3.py` **327 / 9**; the 9 deletions are EXCLUSIVELY the `_harmonize_cohort_batch` docstring 2003/2004-bullet reword + the NotImplementedError message-string reword — **0 logic-deletion lines** (grep of `out[`/`return pa.`/`pc.`/`opt_int`/`opt_str`/`_get_col`/`raise`/`def`/`= _`/`cols = ` deletion-lines = 0). All 6 anchors present: `def _harmonize_cohort_1995_2002` + `def _harmonize_cohort_2003_2004` + `def _mager41_to_age` + `out["underlying_cause_icd9"] = opt_str("UCOD")` (the 5c-i 1989-1991 body anchor) + `is_post2013 = "OEGEST_COMB" in cols` (the 2005+ body anchor) + `out["prenatal_visits"] = opt_int("NPREVIST"` (the 5c-ii-a 1995-2002 body anchor) → the 5c-i + 5c-ii-a + 2005+ bodies byte-untouched (just shifted by the inserted functions). `OUT_SCHEMA` 80 cols unchanged (no schema mutation; DO step 6 owns the schema-CSV + version bump). `python -c "import harmonize_linked_v3"` OK; `_harmonize_cohort_2003_2004` + `_mager41_to_age` callable.
- **VERIFY-D (pytest count gate)**: PASS — canonical **4-dir** suite `fetal_death/ natality/ tests/ matched_multiples/` = **251 passed, 1 skipped, 1 xfailed in 676.57s** (`/tmp/c8_18_s5cii_b/pytest_verify.out`). Reconciles exactly: the 5c-ii-a-close baseline 233P+1S+1XF **+** 22 new 5c-ii-b SMOKE **−** 2 (the 5c-ii-a `test_tier0_unimplemented_eras_still_failclosed` parametrize 5→3, the L17 (i) trim) **−** 2 (the 5c-i `test_tier0_unimplemented_cohort_eras_failclosed` parametrize 3→1, the L17 (ii) trim) = 251P; zero regression (1 skipped + 1 xfailed unchanged; all prior cohort + canonical harmonize_linked tests pass — the byte-untouched 1989-1991 + 1995-2002 + 2005+ bodies + the explicit regression-lock tests). Runtime 676.57s over the ~340-380s **advisory** band → soft-flag (kk) (environmental: the new cohort Tier-1 real-data parses incl. 2003 DEFLATE64 via the 7z stream + machine load; the COUNT is the gate; 11/11 gate SHAs byte-exact = zero canonical regression; a future `[plan-update]` should re-baseline the band).
- **VERIFY-E (SMOKE all PASS)**: PASS — SMOKE 68/68 (the 5c-ii-b 22 + the L17-updated 5c-ii-a 20 + 5c-i 26). Tier 0 + Tier 1 first-run; no Convention-3 divergence (the probe pre-resolved the match-status code); the (E) tighten STRICTER (real LinkCO90 still 30/30 — no MATCHS==2; the 5c-i regression-lock holds).

### Reproducibility

- `_mager41_to_age` + `_harmonize_cohort_2003_2004` + `_cohort_era` + the reused recodes are deterministic pure functions; re-running yields identical output. The SMOKE re-derives from synthetic + the SHA-anchored real archives → deterministic.
- Zero canonical parquet rebuild (DO step 5c-ii-b is harmonize-side code only; the re-harmonize is DO step 6). H10 gate: 11/11 SHAs byte-exact.
- No new Python dependency (reuses pyarrow + the existing helpers; the C8.5a pinned-env SHA untouched).
- The architecture + the 2003/2004 map + the 2003/2004 MATCHS/FLGND==1 finding are state-on-disk in `PRE_FLIGHT_LOG.md` 2026-05-19T20:00:00Z + `DECISION_LOG.md` 2026-05-19T23:00:00Z.

### Cross-product re-probe (if applicable)

- N/A — DO step 5c-ii-b is additive linked harmonize-side code only; no cross-product artifact consumes it until DO step 6 (re-harmonize → v4). 11/11 gate parquet SHAs byte-exact (incl. fetal-death + natality + MM, untouched). The canonical v3 2005-2023 linked product + the 5c-i 1989-1991 + the 5c-ii-a 1995-2002 paths are unperturbed.

### Git

- Pre-DO rollback anchor: `C8.18-pre-do` @ `6632a15` (unchanged; the C8.18 DO rollback anchor).
- This commit: intermediate C8.18 DO step 5c-ii-b — **NO tag** (Convention 5; `C8.18-complete` is the final-sub-step-only tag).

### STATUS.md updated

- New section dated 2026-05-19T23:00:00Z at the top (newest-first).

### Self-check (§10) — what could I have gotten wrong that VERIFY wouldn't catch?

1. **The 2003/2004 per-field ALIAS is anchor-verified on 4000 LinkCO03 + 4000 LinkCO04 (probe) + 600-row real Tier-1 + the synthetic Tier-0, not the full 2-year column set.** A latent alias mis-map on a non-anchor field would not be caught at 5c-ii-b. *Mitigation*: the cohort raw names were read directly from the state-on-disk `field_specs.LINKED_BIRTH_/_DEATH_2003/2004_FIELDS` (4b/4c value-verified byte-exact) + mapped onto the natality V1-core 2003-rev semantics (H7, schema-`derivation`-cross-checked). *Recommendation*: DO step 6 PRE-FLIGHT must value-distribution-verify the full 2003+2004 harmonized column set vs the NCHS published cohort Linked File report tables (the §15.D VERIFY criterion). Logged DECISION_LOG residual risk.
2. **The public-use cohort 2003/2004 den-plus first-4000 probe sample is 100% REVISION='S' (1989-unrevised).** The A-rev (2003-revised) path is exercised ONLY by the synthetic Tier-0 (`test_tier0_2003_arev_cert_and_meduc`), not by real data at 5c-ii-b — a latent A-rev-only bug (e.g., MEDUC value-domain edge) would not surface here. *Mitigation*: the dual-cert coalesce is byte-identical to the natality V1-core `is_2003revised` path (H7; `_meduc_to_cat4`/`_meduc_rec_to_cat4` SMOKE-asserted == natality V1-core); the synthetic A-rev test asserts `revised_2003` + `_meduc_to_cat4(MEDUC)`. *Recommendation*: DO step 6 per-year cohort IMR + the cert-revision distribution vs the NCHS cohort Linked File report (the A-rev share is small in 2003-2004 by NCHS design — only a few states adopted the 2003 cert). Logged DECISION_LOG residual risk.
3. **`maternal_race_bridged` uses `_mrace_detail_to_bridged4(MRACE detail)` + race_bridge_method "approximate_pre2003" (the 5c-i/5c-ii-a sibling-consistent choice), NOT the A-rev MBRACE/MRACEREC nchs_bridged path.** For the S-dominant cohort this preserves the race signal (MBRACE is blank on S-rev rows); the richer A-rev NCHS-bridged precision is deferred. *Mitigation*: consistent with the verified 5c-i/5c-ii-a maps + the natality `is_pre2003` approximate-bridge precedent; MRACE detail is populated on both revisions (probe-verified). *Recommendation*: soft-flag (gg) DO step 6 leaf/precision pass (the A-rev MBRACE-preferred bridge + per-row race_bridge_method). Logged DECISION_LOG residual risk.
4. **`infant_death = AGED-non-blank` cross-checked == match-status=="1" on real 2003 (`MATCHS`) + 2004 (`FLGND`) ≤600-row Tier-1 only.** A latent disagreement deeper in the file would not be caught at 5c-ii-b. *Mitigation*: the n=4000 Convention-3 probe cross-tab was *perfect* on both years (2003 21/21 MATCHS==1⟺AGED; 2004 19/19 FLGND==1⟺AGED) + the 5c-ii-a/5c-i finding generalizes (uniform across ALL eras); the Tier-1 asserts row-for-row agreement. *Recommendation*: DO step 6 per-year cohort IMR vs the NCHS cohort Linked File report (1983-2004). Logged DECISION_LOG residual risk.
5. **Composite multi-field blocks + smoking/diabetes/hypertension/preterm_recode3/father_race_ethnicity_5/father_education_cat4/payment/prior_cesarean conservatively NULL at 5c-ii-b** — a downstream analysis expecting cohort-2003/2004 smoking/diabetes/delivery-method would get null. *Mitigation*: the natality is_pre1989 / 5c-i / 5c-ii-a conservative-mapping precedent; documented soft-flag (gg) DO step 6 leaf decomposition; the SMOKE asserts the null. Logged DECISION_LOG residual risk.
6. **`gestational_age_weeks_source` = "combined" for 2003/2004 (the per-era delta vs 5c-ii-a's "lmp").** If a downstream cross-era groupby treats the source string as comparable across eras it could be surprised. *Mitigation*: schema-documented ("combined for 2003-2013"); the source string is a metadata annotation, not an analytic key; the 2005+ linked body also uses "combined". Logged DECISION_LOG residual risk.
7. **`father_age_cat_from_rec11` is now populated for cohort 2003/2004 but its `harmonized_schema.csv` `years_available` still says 2005-2013; `manner_of_death` populated for 2003/2004 but its years_available says "2005-2023 (linked)"; the death-side ICD-9 rows are not yet in the schema CSV.** A schema-vs-data mismatch would be a latent H8/L13 doc-drift if DO step 6 forgets. *Mitigation*: HALT 6 + the Forward-looking HALT below explicitly enumerate the DO-step-6 schema-CSV widenings (death-side years_available 2005-2023→1983-2023 incl. the 1992-1994 gap + the `underlying_cause_icd9`/`cause_recode_61` rows + `manner_of_death`/`father_age_cat_from_rec11` years_available + the ICD-9/10 null-pattern notes) + the v3→v4 bump (Anti-Pattern #6). Logged DECISION_LOG residual risk + Forward-looking HALT.
8. **Behavior-preservation for the 5c-i 1989-1991 + 5c-ii-a 1995-2002 + 2005-2023 paths is asserted via the additive diff (327/9, 0 logic-deletion, anchors intact, byte-untouched bodies) + the cohort-branch-year guards + the 251-baseline (incl. the explicit 1989-1991 + 1995-2002 regression-lock tests)**, NOT a full re-harmonize byte-diff (DO step 6). *Mitigation*: the 2003/2004 branch fires only for year ∈ {2003,2004}; the three prior bodies byte-untouched; 11/11 gate SHAs byte-exact. *Recommendation*: DO step 6's "2005-2023 + per-era byte-clean regression" vs the v3 baseline is the durable §15.D check. Logged DECISION_LOG residual risk.

Each logged in `DECISION_LOG.md` 2026-05-19T23:00:00Z as a residual risk; the recommended checks are DO-step-6 PRE-FLIGHT items (not added to VERIFY without human sign-off per §10).

### Forward-looking HALTs for next session (C8.18 DO step 5c-iii)

1. `C8.18-pre-do`@`6632a15`; `C8.18-complete` NOT present (final-sub-step-only). `C8.17-complete` present. HEAD = this DO step 5c-ii-b commit (after `8497e92`).
2. **11 gate parquet SHAs unchanged** (the same 11; fetal-death via the `~/Desktop/fetal-death-harmonization-build/` tree, soft-flag (hh)). Linked-derived `9b828a4d…` changes only at the later C8.18 re-harmonize DO step 6 (5a/5b/5c-* are parser/harmonize substrate only).
3. **Canonical pytest baseline is now `251 passed, 1 skipped, 1 xfailed`** (was 233P+1S+1XF; +22 new `test_linked_cohort_5cii_b_harmonize_smoke.py` − 2 the 5c-ii-a L17 trim − 2 the 5c-i L17 trim). The canonical suite is the **4-dir** `fetal_death/tests/ natality/tests/ tests/ matched_multiples/tests/`. Runtime ~340-680s (advisory band; the COUNT is the gate; 676.57s this VERIFY = soft-flag (kk) environmental — the new cohort Tier-1 real-data I/O incl. 2003 DEFLATE64). README 3-dir/"56 passed" line stays stale → soft-flag (jj).
4. **`harmonize_linked_v3` exposes** `_mager41_to_age` (NEW) + `_harmonize_cohort_2003_2004` (NEW) + `_harmonize_cohort_1995_2002` + `_cohort_era` + `_harmonize_cohort_batch` (1989-1991 + 1995-2002 + 2003 + 2004 implemented; **only the keyless 1983-1988 RAISES `NotImplementedError`**) + the 2 within_era ICD-9 OUT_SCHEMA columns + the H7 recodes. **State-on-disk; do NOT re-derive.** **DO step 5c-iii = the keyless 1983-1988 `link_segment` den/num → `infant_death`/`record_weight` one-row-per-birth harmonized encoding** (the highest-risk methodology-laden sub-step; the 5b two-file `link_segment` construction model is state-on-disk + RESOLVED — do NOT re-open; the documented within-era structural difference = Phase-D **D.4**: `link_segment="den"` rows = one row per birth with `infant_death` null/unknown (un-linkable aggregate denominator); `link_segment="num"` rows = the self-contained linked-infant-death detail surface; the harmonized encoding must NOT fabricate a record-level join — COMPARABILITY/schema notes + the manuscript Coverage paragraph are the D.4 deliverable). After 5c-iii: DO step 6 (re-harmonize linked 1983-2023; v3→v4 + the `harmonized_schema.csv` edit + version bump) + DO step 7 (docs/metadata; tag `C8.18-complete`).
5. **The within_era ICD-9/ICD-10 cause-column-shape is RESOLVED + state-on-disk** (DECISION_LOG/PRE_FLIGHT_LOG 2026-05-19T11:00:00Z; §15.D DO step 1): `underlying_cause_icd9`/`cause_recode_61` (cohort birth-year ≤1998), `underlying_cause_icd10`/`cause_recode_130` (≥1999). 5c-ii-a applied it for 1995-2002; 5c-ii-b applied it for 2003/2004 (all ICD-10). 5c-iii applies it for the keyless **1983-1988** (cohort birth-year ≤1998 → **ICD-9**: `underlying_cause_icd9`/`cause_recode_61` populated, the ICD-10 cols null). Do NOT re-open the shape.
6. **The `harmonized_schema.csv` metadata edit + the v3→v4 version bump is DO step 6** (Anti-Pattern #6). 5c-ii-b did NOT touch it (`OUT_SCHEMA` 80 cols unchanged). DO step 6 MUST: (a) widen the death-side rows' `years_available` 2005-2023 → 1983-2023 (with the 1992-1994 gap); (b) add the `underlying_cause_icd9`/`cause_recode_61` rows + the ICD-9/10 null-pattern notes; (c) widen `manner_of_death` `years_available` to include cohort 2003-2004 (null 1983-2002 — no MANNER on the 1989-1991/1995-2002/1983-1988 den-plus); (d) widen `father_age_cat_from_rec11` `years_available` 2005-2013 → 2003-2013 (now also populated for cohort 2003/2004 via 5c-ii-b); (e) bump v3→v4. If DO step 6's PRE-FLIGHT does not enumerate (a)-(e), halt.
7. §15.D substrate-format reconcile applied at the 3a commit; the broader model-clarification — now ALSO folding the **5c-ii-b 2003+2004 map + the 2003/2004 den-plus MATCHS/FLGND-encoding finding (linked-infant-death code = match-status==1 uniform across ALL eras; survivor code era-dependent: 3 for 1989-1991, 2 for 1995-2002/2003/2004)** alongside 3a/3b + 4a/4b/4c + 5→5a/5b/5c + 5c→5c-i/5c-ii/5c-iii + 5c-ii→5c-ii-a/5c-ii-b — remains soft-flag (ii) proposed-not-applied §11 human-merge (queued with the soft-flag-(w) batch). On-disk PRE_FLIGHT_LOG/DECISION_LOG/RECEIPTs authoritative meanwhile.
8. **soft-flag (gg) carries**: the cause-column SHAPE is resolved (HALT 5); the per-condition leaf decomposition of the composite MEDRISK/OBSTETRC/LABOR/DELMETH/NEWBORN/CONGENIT/ENTITY/RECORD spans (+ preterm_recode3, father_race_ethnicity_5, smoking, diabetes/hypertension, payment, prior_cesarean, the A-rev MBRACE-preferred bridge) stays conservatively NULL → DO step 6.
9. **L17 forward-looking discipline**: 5c-ii-b configured NO new parser dispatcher year (harmonize-side only; the parser handles 2003/2004 via 4b/4c/5a). DO step 5c-iii implementing the keyless 1983-1988 harmonized encoding MUST grep-scope ALL **10** cohort SMOKE harnesses — the `1985`/`1983-1988` keyless `NotImplementedError` pins go stale in BOTH `test_linked_cohort_5cii_b_harmonize_smoke.py::test_tier0_keyless_1983_1988_still_failclosed` AND `test_linked_cohort_5cii_a_harmonize_smoke.py::test_tier0_unimplemented_eras_still_failclosed` AND `test_linked_cohort_5c_harmonize_smoke.py::test_tier0_unimplemented_cohort_eras_failclosed` when 5c-iii implements 1983-1988 → bundle the minimal `tracks-current-state` Edit into the SAME 5c-iii commit (§4.2.1/Convention-2/L17). pytest COUNT (now 251P+1S+1XF) is the gate; runtime band advisory (soft-flag (kk)).

### Notes for next session

- **C8.18 DO step 5c-ii-b is CLOSED.** `_harmonize_cohort_2003_2004` (the 2003-rev dual-cert transition; the natality V1-core `is_2003revised` analog; one function for 2003+2004; the per-era deltas MANNER→manner_of_death / "combined" gest-source / all-ICD-10 / father_age_cat_from_rec11 / the dual-cert MEDUC/UMEDUC+PRECARE/MPCB coalesce) + `_mager41_to_age` are state-on-disk + this receipt + `PRE_FLIGHT_LOG.md` 2026-05-19T20:00:00Z + `DECISION_LOG.md` 2026-05-19T23:00:00Z. The within_era ICD-9/ICD-10 cause-column shape stays RESOLVED (do NOT re-open). Next per the 5c→5c-i/5c-ii/5c-iii decomposition: **DO step 5c-iii — the keyless 1983-1988 `link_segment` harmonized encoding** (the highest-risk methodology-laden sub-step; the 5b two-file construction model RESOLVED + state-on-disk; the D.4 within-era structural difference; ~1 session), then DO step 6 (re-harmonize linked 1983-2023; v3→v4 + the schema-CSV edit + version bump per HALT 6) + DO step 7 (docs/metadata; tag `C8.18-complete`).
- §2 cheap-before-expensive + the 3a-substrate-first / §9-#8 / §9-#9 discipline governed 5c-ii-b (one function for the one 2003-rev sub-step; reuse the natality V1-core 2003-rev recodes via H7 sibling-parity NOT re-derive; write the SMOKE FIRST; pre-resolve the §7-#13 match-status code via the read-only Convention-3 probe so NO divergence surfaced at SMOKE; bundle the recommended §9-#4 harness-precision tighten into the same commit since the harness was edited anyway for L17). Carry it.
- The §15.D model-clarification `[plan-update]` (soft-flag (ii)) is owed to the human (§11; queued with the soft-flag-(w) batch; now also folds 5c-ii-b 2003+2004 + the 2003/2004 MATCHS/FLGND-encoding finding); on-disk logs authoritative meanwhile.
- Runtime advisory-band exceedance (676.57s this VERIFY vs ~340-380s) = soft-flag (kk); environmental cohort-Tier-1 real-data I/O (incl. 2003 DEFLATE64 via 7z), not a canonical regression (11/11 gate SHAs byte-exact; the COUNT 251P+1S+1XF is the gate). A future `[plan-update]` should re-baseline the band.
- `/tmp/c8_18_s5cii_b/*` scratch (gate-SHA + pytest baseline/verify + the read-only probe) is OS-cleanable + reproducible.
