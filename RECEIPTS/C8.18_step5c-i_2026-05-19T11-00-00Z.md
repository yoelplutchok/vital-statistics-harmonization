# Receipt: C8.18 DO step 5c-i — the per-cohort-era harmonize architecture + the within_era ICD-9 cause-column shape + the 1989-1991 reference era end-to-end
## 2026-05-19T11:00:00Z

### What was done

Executed C8.18 **DO step 5c-i** under the full five-phase discipline in one session (PRE-FLIGHT + SMOKE + DO + VERIFY + RECEIPT; the C8.18 DO step 3a/3b/4a/4b/4c/5a/5b single-session-DO-step precedent). §15.D **DO step 5c** ("`harmonize_linked_v3.py` per-cohort-era field mapping + the keyless-era harmonized encoding + ICD-9 default-null/revision-tagged + `cause_recode_130` per-era") was decomposed at the Convention-3 PRE-FLIGHT snapshot — before any code mutation — into three separable concerns of materially different risk (the 5→5a/5b/5c + 4→4a/4b/4c + 3→3a/3b per-concern precedent + §2 cheap-before-expensive + §9-#8): **5c-i** = the per-cohort-era harmonize architecture + the within_era ICD-9 cause-column-shape decision + the **1989-1991 reference era** end-to-end (the lowest-risk verifiable substrate-first increment); **5c-ii** = the 1995-2002 + 2003 + 2004 cohort birth-side maps (the 2003-rev transition; medium risk); **5c-iii** = the keyless 1983-1988 `link_segment` den/num → `infant_death`/`record_weight` one-row-per-birth encoding (highest risk; the documented within-era structural difference = Phase-D D.4). This session shipped **5c-i**.

5c-i added **additively** to `natality/scripts/03_harmonize/harmonize_linked_v3.py` (`git diff --numstat` = **378 insertions / 1 deletion**; the 1 deletion is the existing 2005+ `from_arrays` line `underlying_cause_icd10, cause_recode_130, manner_of_death,` split to insert the 2 new columns at their correct OUT_SCHEMA position — NOT a logic deletion; **0 logic-deletion lines**, grep-confirmed; the existing 2005+ `_harmonize_batch` body byte-untouched):

1. **`_dmeduc_years_to_cat4`** + **`_mrace_detail_to_bridged4`** (NEW; byte-identical to the `harmonize_v1_core` siblings — H7 fetal-death/natality sibling-parity; duplicated locally per this file's existing "Arrow helper functions (same as harmonize_v1_core.py)" pattern; the SMOKE asserts the equivalence by feeding the SAME array to both).
2. **`_detail_order_to_recode9`** (NEW; the cohort detail birth order DLIVORD/DTOTORD → the natality 9-category recode shape 1-7|8|9; SMOKE-edge-verified).
3. **`_cohort_era(year)`** (NEW; the year→era classifier: 1983-1988/1989-1991/1995-2002/2003/2004 → era str; ≥2005 → `None` (the canonical path; the cohort branch must NOT fire there → 2005-2023 byte-identical, §9-#7-safe); 1992-1994 (the permanent NCHS linkage gap) + pre-1983 → `ValueError`, §2 fail-closed, mirroring the parser `_layout_for_linked_year` dispatcher discipline).
4. **`_harmonize_cohort_batch(batch, year)`** (NEW; the 1989-1991 reference-era mapping = the cohort raw names aliased onto the natality V1-core 1989-revision recodes — H7 sibling-parity; the other cohort eras RAISE `NotImplementedError` "configured at DO step 5c-ii / 5c-iii" — §2 fail-closed; a premature DO step 6 on those years halts loudly, never silently mis-harmonizes).
5. **2 NEW `OUT_SCHEMA` columns** `underlying_cause_icd9` (string, within_era) + `cause_recode_61` (int16, within_era) inserted after `cause_recode_130` — the **within_era ICD-9 cause-column shape decision** (H7 fetal-death sibling-parity: `cause_icd10` is within_era there too; ICD-9/10 are NOT fabricated into one cross-era column). `underlying_cause_icd10` / `cause_recode_130` go NULL for cohort birth-year 1983-1998 (the ICD-9 era; §15.D DO step 1 default-null + revision-tagged, DECISION_LOG 2026-05-17T05:30:00Z); the 2 new columns carry the ICD-9 signal there and are NULL for 1999+. The existing 2005+ `from_arrays` appends 2 `pa.nulls` at the new schema position (the 2005-2023 ICD-10 slice → the 2 ICD-9 columns are 100% null = the intended v3→v4 **additive** schema extension; existing 2005-2023 column VALUES byte-identical).
6. A top-of-`_harmonize_batch` cohort dispatch: `if _cohort_era(year) is not None: return _harmonize_cohort_batch(batch, year)` — the existing 2005+ body below (`cols = set(...)` onward) byte-untouched (§9-#7-safe; the cohort branch fires only for year ≤ 2004; the canonical v3 2005-2023 build is byte-identical).

Plus a NEW sibling `DESIGN: tracks-current-state` SMOKE harness `natality/tests/test_linked_cohort_5c_harmonize_smoke.py` (30 tests; Convention 1/2; sub-step isolation). The parser `_layout_for_linked_year`/`_numerator_layout_for_linked_year` dispatchers + `parse_linked_year.py` + `field_specs.py` + the 7 prior cohort harnesses are **byte-untouched** (5c-i is harmonize-side only; H10/HALT-13). **Zero canonical parquet / `harmonized_schema.csv` / validation-CSV mutation** (the harmonizer is NOT re-run at 5c-i; 11/11 gate parquet SHAs byte-exact at VERIFY-A).

**The within_era ICD-9 cause-column-shape decision** (the §15.D DO step 1 "a within-era ICD-9 harmonized representation, exact column shape decided at DO step 5/6 PRE-FLIGHT, H7 fetal-death sibling-parity, soft-flag (gg)") was resolved at the Convention-3 snapshot before any code mutation: add `underlying_cause_icd9` + `cause_recode_61` (within_era), keep `underlying_cause_icd10`/`cause_recode_130` (null for the ICD-9 era), key the split on cohort **birth year** (≤1998 ICD-9, ≥1999 ICD-10) per §15.D DO step 1. The `harmonized_schema.csv` metadata edit + the years_available widening + the v3→v4 version bump is DO step 6 (Anti-Pattern #6: schema-CSV edit paired with the version bump); the `OUT_SCHEMA` code change at 5c-i is the harmonize-code authoring (the parquet is not re-run; zero canonical mutation).

No Convention-3 divergence surfaced between PRE-FLIGHT and final DO (unlike 5b). SMOKE was authored FIRST, run RED (collection ImportError vs the un-modified harmonizer — §9-#9 / L3), then GREEN **30/30 first-run** after DO — including the real-LinkCO90 Tier-1 with the independent MATCHS-vs-AGED §7-#13 cross-check (no test loosened — §2 / §9-#4).

### Inputs consumed

- `PRE_FLIGHT_LOG.md` 2026-05-19T11:00:00Z (per-sub-step PRE-FLIGHT; before any code mutation; L10-safe). 9/9 forward-looking HALTs from `RECEIPTS/C8.18_step5b` re-verified; 11/11 gate parquet SHAs byte-exact (`/tmp/c8_18_s5c_gate.out`); canonical pytest baseline `183 passed, 1 skipped, 1 xfailed in 311.98s` (4-dir) preserved; the 5c→5c-i/5c-ii/5c-iii decomposition + the cause-column-shape decision + 4 within-task scope decisions surfaced + resolved before DO (Convention 3); L17 grep-scope of all 7 cohort harnesses = no stale pin.
- `RECEIPTS/C8.18_step5b_2026-05-19T08-00-00Z.md` + `RECEIPTS/C8.18_step5a_2026-05-19T05-00-00Z.md` (the §15.D DO step 5 → 5a/5b/5c decomposition + the keyless 1983-1988 construction model = 5c-iii forward item).
- `natality/scripts/03_harmonize/harmonize_linked_v3.py` (the 5c target — read in full) + `harmonize_v1_core.py` (the **H7 sibling-parity reference**: the `is_pre1989`/`is_pre2003` cohort-style era logic + `_dmeduc_years_to_cat4`/`_mrace_detail_to_bridged4`/`_month_to_trimester`/`_pldel_to_facility` reusable recodes — era branch + helpers read).
- `natality/scripts/01_import/parse_linked_year.py` + `field_specs.py` (`LINKED_BIRTH_1989_1991_FIELDS` + `LINKED_DEATH_1989_1991_FIELDS` — the cohort `_raw` parquet column names; state-on-disk 3a/3b, read-only).
- `natality/metadata/harmonized_schema.csv` (the canonical death-side column definitions; the v4 extension + version bump = DO step 6) + `fetal_death/harmonized_schema.csv` (`cause_icd10` within_era — the H7 sibling-parity precedent).
- `~/Desktop/natality-harmonization/raw_data/linked/LinkCO90.zip` — read-only, first ≤300 records of the denominator-plus member (SMOKE Tier 1); SHA-anchored at C8.18 DO step 2.
- 11 gate parquets — SHA-verified byte-exact at the PRE-FLIGHT cheap-check + re-verified at VERIFY-A.

### Outputs produced

Git-tracked (this commit):

- `natality/scripts/03_harmonize/harmonize_linked_v3.py` — **additive** (`git diff --numstat` = **378 / 1**; the 1 deletion = the `from_arrays` line reformat to position the 2 new columns; 0 logic-deletion lines; the existing 2005+ `_harmonize_batch` body byte-untouched, grep-confirmed; the cohort branch fires only for year ≤ 2004).
- `natality/tests/test_linked_cohort_5c_harmonize_smoke.py` — NEW; `DESIGN: tracks-current-state`; 30 tests (Tier 0: the `_cohort_era` partition + 5 fail-closed ValueError negatives [1992-1994 gap + pre-1983] + the OUT_SCHEMA within_era-ICD-9 shape + the H7 sibling-parity recode equivalence + the detail-order-recode9 edges + the 1989-1991 reference-era end-to-end mapping + 5 fail-closed NotImplementedError negatives [5c-ii/5c-iii eras]; Tier 1: the real `LinkCO90` parse→harmonize with the independent MATCHS-vs-AGED §7-#13 cross-check + the ICD-9 default-null).
- `PRE_FLIGHT_LOG.md` — the C8.18 DO step 5c-i PRE-FLIGHT entry (2026-05-19T11:00:00Z).
- `RECEIPTS/C8.18_step5c-i_2026-05-19T11-00-00Z.md` — this file.
- `DECISION_LOG.md` + `STATUS.md` — appended (the 5c→5c-i/5c-ii/5c-iii decomposition + the within_era ICD-9 cause-column-shape decision + new dated section).

No canonical parquet / `harmonized_schema.csv` / validation-CSV touched. Raw zips/guides gitignored + out-of-tree (read-only, unchanged). §15.D NOT edited this step (the broader model-clarification — now also folding 5c→5c-i/5c-ii/5c-iii + the within_era ICD-9 cause-column shape — stays soft-flag (ii) §11 human-merge; the 3a/3b/4a/4b/4c/5a/5b precedent).

### Five-phase trace

- **PRE-FLIGHT**: ✓ `PRE_FLIGHT_LOG.md` 2026-05-19T11:00:00Z (per-sub-step, before any code mutation; L10-safe). 9/9 forward-looking HALTs re-verified; 11/11 gate SHAs byte-exact; pytest 183P+1S+1XF/311.98s baseline preserved; the decomposition + the cause-shape decision + 4 scope decisions + the L17 grep-scope (clean) surfaced + resolved before DO (Convention 3).
- **SMOKE**: ✓ NEW harness authored FIRST (§9-#9). Run vs the **un-modified** harmonizer = **collection ImportError** (`cannot import name '_cohort_era'`) — proves it genuinely requires the DO code (L3 / §9-#5, not a rubber-stamp). After DO = **30 passed first-run** (no Convention-3 divergence; no test loosened — §2 / §9-#4). Tier 0 = the era partition + fail-closed negatives + the OUT_SCHEMA shape + the H7 sibling-parity recode equivalence + the synthetic 1989-1991 end-to-end mapping; Tier 1 = the real `LinkCO90` parse→harmonize with the independent MATCHS-vs-AGED cross-check.
- **DO**: ✓ additive helpers + `_cohort_era` + `_harmonize_cohort_batch` + the 2 within_era ICD-9 OUT_SCHEMA columns + the top-of-`_harmonize_batch` cohort dispatch + the new SMOKE harness. Single commit (PRE-FLIGHT + DO + RECEIPT; the C8.18 DO 3a/3b/4a/4b/4c/5a/5b precedent).
- **VERIFY**: ✓ all 5 criteria (below).
- **RECEIPT**: ✓ this file.

### Verify results

- **VERIFY-A (zero canonical mutation)**: PASS — 11/11 gate parquet SHAs byte-exact vs the PRE-FLIGHT HALT 2 + the 5b receipt: natality `c8a740eb…`/`acb5c48a…`; `.v28_baseline` `230efed2…`/`e16ad532…`; linked-derived `9b828a4d…`; fetal-death `38e2cecb…`/`185c071e…` (via the `~/Desktop/fetal-death-harmonization-build/` tree, soft-flag (hh)); MM `adbec108…`/`5c22308b…`/`7c682668…`/`d98b4296…`. Threshold: 11/11 unchanged.
- **VERIFY-B (git scope)**: PASS — modified/new = `harmonize_linked_v3.py`, `test_linked_cohort_5c_harmonize_smoke.py` (new), `PRE_FLIGHT_LOG.md`, + `RECEIPTS/`/`DECISION_LOG.md`/`STATUS.md`. NO `.parquet`/`harmonized_schema.csv`/validation-CSV. Threshold: scope ⊆ {scripts, test, plan, state}.
- **VERIFY-C (additive / behavior-preserving)**: PASS — `git diff --numstat` = `harmonize_linked_v3.py` **378 / 1**. The single deletion is the existing 2005+ `from_arrays` line `underlying_cause_icd10, cause_recode_130, manner_of_death,` split to insert the 2 new (always-null-for-2005+) columns at their correct OUT_SCHEMA position — **0 logic-deletion lines** (`git diff | grep '^-' | grep -v <that-one-line>` = 0). The existing 2005+ `_harmonize_batch` body is byte-untouched (the cohort dispatch is a 2-line prepend before the unchanged `cols = set(...)`; the cohort branch never fires for year ≥ 2005); the canonical v3 2005-2023 product's existing-column VALUES are byte-identical (the only delta is 2 appended all-null columns = the intended v3→v4 additive schema extension, version-bumped at DO step 6; the parquet is NOT re-run at 5c-i → VERIFY-A 11/11 byte-exact).
- **VERIFY-D (pytest count gate)**: PASS — canonical **4-dir** suite `fetal_death/ natality/ tests/ matched_multiples/` = **213 passed, 1 skipped, 1 xfailed in 257.23s**. Reconciles exactly: the 5b-close baseline 183P+1S+1XF preserved (zero regression — incl. all 7 prior cohort harnesses + the canonical `harmonize_linked` tests; the byte-untouched 2005+ body) **+** 30 new 5c-i SMOKE all pass = 213P. Runtime 257.23s within the ~240-380s **advisory** band (soft-flag (kk); the COUNT is the gate; 257s < the 311.98s baseline — the new SMOKE is ~0.07s, the variance is environmental I/O).
- **VERIFY-E (SMOKE all PASS)**: PASS — new 5c-i harness **30/30** first-run. Tier 0: the `_cohort_era` partition (11 eras/None) + 5 fail-closed ValueError (1992-1994 gap + pre-1983) + the OUT_SCHEMA within_era-ICD-9 shape + the H7 sibling-parity recode equivalence (vs `harmonize_v1_core`) + the detail-order-recode9 edges + the synthetic 1989-1991 end-to-end (planted anchors + the ICD-9 default-null) + 5 fail-closed NotImplementedError (5c-ii/5c-iii eras). Tier 1 (real SHA-anchored `LinkCO90.zip`): parse 300 records → harmonize → schema == extended OUT_SCHEMA; data_year all 1990; certificate_revision all `unrevised_1989`; `underlying_cause_icd10`/`cause_recode_130` 100% null (the ICD-9 era default-null); maternal_age ∈ [10,54]; infant_sex ∈ {M,F,null}; **infant_death (AGED-derived) == MATCHS∈{1,2}** row-for-row (the independent §7-#13 cross-check PASSED on real data — validates the infant_death derivation); death rows carry the ICD-9 underlying cause.

### Reproducibility

- `_cohort_era` / `_dmeduc_years_to_cat4` / `_mrace_detail_to_bridged4` / `_detail_order_to_recode9` / `_harmonize_cohort_batch` are deterministic pure functions of their inputs; re-running yields identical output. The SMOKE re-derives from synthetic + real archives → deterministic.
- Zero canonical parquet rebuild (DO step 5c-i is harmonize-side code only; the re-harmonize is DO step 6). H10 gate: 11/11 SHAs byte-exact.
- No new Python dependency (reuses pyarrow + the codebase's existing helper-duplication pattern; the C8.5a pinned-env SHA untouched).
- The SMOKE Tier 1 is re-runnable from the SHA-anchored `LinkCO90.zip`; the architecture + the cause-column-shape decision are state-on-disk in `PRE_FLIGHT_LOG.md` 2026-05-19T11:00:00Z + `DECISION_LOG.md` 2026-05-19T11:00:00Z.

### Cross-product re-probe (if applicable)

- N/A — DO step 5c-i is additive linked harmonize-side code only; no cross-product artifact consumes it until DO step 6 (re-harmonize → v4). 11/11 gate parquet SHAs byte-exact (incl. fetal-death + natality + MM, untouched). The canonical v3 2005-2023 linked product is unperturbed.

### Git

- Pre-DO rollback anchor: `C8.18-pre-do` @ `6632a15` (unchanged; the C8.18 DO rollback anchor).
- This commit: intermediate C8.18 DO step 5c-i — **NO tag** (Convention 5; `C8.18-complete` is the final-sub-step-only tag).

### STATUS.md updated

- New section dated 2026-05-19T11:00:00Z at the top (newest-first).

### Self-check (§10) — what could I have gotten wrong that VERIFY wouldn't catch?

1. **The 1989-1991 birth-side recodes are sibling-parity-asserted on the recode functions but the per-field ALIAS (cohort name → harmonized column) is not exhaustively value-distribution-verified across the full 1989-1991 file** — SMOKE Tier-1 verifies the anchor distributions (maternal_age range, infant_sex enum, cert_rev, the ICD-9 default-null, infant_death⟺MATCHS) on 300 records, not every column on the full 3 years. A latent alias mis-map on a non-anchor field (e.g. a wrong `GESTAT`-vs-`GESTAT10` choice) would not be caught at 5c-i. *Mitigation*: the cohort raw names were read directly from the state-on-disk `field_specs.LINKED_BIRTH_1989_1991_FIELDS` (3a value-verified) + mapped onto the natality V1-core 1989-rev semantics (H7). *Recommendation*: DO step 6 PRE-FLIGHT must value-distribution-verify the full 1989-1991 harmonized column set vs the NCHS published cohort Linked File report tables (the §15.D VERIFY criterion "per-year cohort counts 1983-2004 match … byte-exact"). Logged DECISION_LOG residual risk.
2. **`infant_death = AGED-non-blank`** is value-driven (no MATCHS-code assumption); the SMOKE Tier-1 cross-checked it == `MATCHS∈{1,2}` on real `LinkCO90` (1990) only — 1989 + 1991 not exercised end-to-end at 5c-i. A year where AGED-blank ≠ MATCHS-3 would mis-flag IMR. *Mitigation*: the cross-check PASSED on real 1990 data; the rule is the same all 3 years (one fixed-width layout); the SMOKE asserts the equivalence (a disagreement is a surfaced §7-#13 halt). *Recommendation*: DO step 6 re-harmonize VERIFY = per-year cohort IMR vs the NCHS cohort Linked File report (1989/1990/1991). Logged DECISION_LOG residual risk.
3. **Composite multi-field blocks (MEDRISK / OBSTETRC / LABOR / DELMTH / CONGENIT) + a few detail fields (preterm_recode3, father_race_ethnicity_5) are conservatively NULL at 5c-i** — a downstream analysis expecting e.g. cohort-1989-1991 smoking/diabetes would get null. *Mitigation*: this is the natality `is_pre1989` conservative-mapping precedent (high-confidence anchors mapped; composites null + flagged); the leaf decomposition is the documented soft-flag (gg) DO step 5c-ii/6 item. Not a silent deviation: the SMOKE asserts the null + the receipt/DECISION_LOG document it. Logged DECISION_LOG residual risk.
4. **The within_era ICD-9 cause-column-shape decision is implemented in `OUT_SCHEMA` (code) but the `harmonized_schema.csv` metadata is NOT yet edited** (Anti-Pattern #6: the schema-CSV edit + the v3→v4 version bump = DO step 6). If DO step 6 forgets to widen `underlying_cause_icd10`/`cause_recode_130` years_available + add the 2 new rows + the null-pattern note + bump the version, the metadata would drift from the v4 parquet. *Mitigation*: documented as a DO step 6 Forward-looking HALT (below) + a DECISION_LOG residual risk; the cause-column-shape decision is fully state-on-disk (PRE_FLIGHT_LOG/DECISION_LOG 2026-05-19T11:00:00Z).
5. **Behavior-preservation for the canonical 2005-2023 path is asserted via the additive diff (378/1, 0 logic-deletion lines, byte-untouched body) + the cohort-branch-fires-only-for-year≤2004 guard + the 183-baseline-preserved suite**, NOT a full 2005-2023 re-harmonize byte-diff (DO step 6). The 2 new always-null OUT_SCHEMA columns change the v4 parquet's column SET for 2005-2023 (intended additive extension), not existing values; a latent perturbation visible only in the full re-harmonize would not be caught here. *Mitigation*: the cohort branch never fires for year ≥ 2005 (the existing body byte-untouched); 11/11 gate SHAs byte-exact. *Recommendation*: DO step 6's "2005-2023 byte-clean regression: 0/N existing columns drift" vs the v3 baseline is the durable §15.D check. Logged DECISION_LOG residual risk.
6. **5c-i scopes ONLY the 1989-1991 reference era**; 1995-2002/2003/2004/1983-1988 RAISE NotImplementedError. A DO step 6 (or a careless caller) running the harmonizer over a non-1989-1991 cohort year before 5c-ii/5c-iii lands will HALT loudly (correct fail-closed) — but the sequence MUST land 5c-ii + 5c-iii before DO step 6. *Mitigation*: the NotImplementedError message names the owning sub-step; documented as a Forward-looking HALT + STATUS in-progress. Logged DECISION_LOG residual risk.

Each logged in `DECISION_LOG.md` 2026-05-19T11:00:00Z as a residual risk; the recommended checks are DO-step-6 PRE-FLIGHT items (not added to VERIFY without human sign-off per §10).

### Forward-looking HALTs for next session (C8.18 DO step 5c-ii)

1. `C8.18-pre-do`@`6632a15`; `C8.18-complete` NOT present (final-sub-step-only). `C8.17-complete` present. HEAD = this DO step 5c-i commit (after `383692a`).
2. **11 gate parquet SHAs unchanged** (the same 11; fetal-death via the `~/Desktop/fetal-death-harmonization-build/` tree, soft-flag (hh)). Linked-derived `9b828a4d…` changes only at the later C8.18 re-harmonize DO step 6 (5a/5b/5c-* are parser/harmonize substrate only).
3. **Canonical pytest baseline is now `213 passed, 1 skipped, 1 xfailed`** (was 183P+1S+1XF; +30 from the new `test_linked_cohort_5c_harmonize_smoke.py`, `DESIGN: tracks-current-state`). The canonical suite is the **4-dir** `fetal_death/tests/ natality/tests/ tests/ matched_multiples/tests/`. Runtime ~257-380s (advisory band; the COUNT is the gate) → soft-flag (kk). README 3-dir/"56 passed" line stays stale → soft-flag (jj).
4. **`harmonize_linked_v3` now exposes** `_cohort_era` + `_harmonize_cohort_batch` (1989-1991 only; the other cohort eras RAISE `NotImplementedError`) + the 2 within_era ICD-9 OUT_SCHEMA columns (`underlying_cause_icd9` string + `cause_recode_61` int16) + the local H7-sibling-parity recodes (`_dmeduc_years_to_cat4`/`_mrace_detail_to_bridged4`/`_detail_order_to_recode9`). **State-on-disk; do NOT re-derive.** **DO step 5c-ii = the 1995-2002 + 2003 + 2004 cohort birth-side maps** (the 2003-rev transition; reuse the 5c-i architecture + the within_era ICD-9 columns — note cohort 1999-2004 = ICD-10 → `underlying_cause_icd10`/`cause_recode_130` populated, `underlying_cause_icd9`/`cause_recode_61` null; cohort 1995-1998 = ICD-9 → the inverse). **DO step 5c-iii = the keyless 1983-1988 `link_segment` den/num → `infant_death`/`record_weight` one-row-per-birth encoding** (the highest-risk methodology-laden sub-step; the documented within-era structural difference = Phase-D D.4; the 5b construction model is state-on-disk + RESOLVED — do NOT re-open).
5. **The within_era ICD-9 cause-column-shape decision is RESOLVED + state-on-disk** (DECISION_LOG/PRE_FLIGHT_LOG 2026-05-19T11:00:00Z): `underlying_cause_icd9` + `cause_recode_61` (within_era, populated cohort birth-year ≤1998, null ≥1999); `underlying_cause_icd10`/`cause_recode_130` null ≤1998. Key the ICD-9/ICD-10 split on cohort **birth year** (§15.D DO step 1). 5c-ii/5c-iii implement it for their eras; do NOT re-open the shape.
6. **The `harmonized_schema.csv` metadata edit + the v3→v4 version bump is DO step 6** (Anti-Pattern #6): DO step 6 MUST widen `underlying_cause_icd10`/`cause_recode_130` years_available + null-pattern notes + add the `underlying_cause_icd9`/`cause_recode_61` rows (within_era) + the death-side rows' years_available 2005-2023 → 1983-2023 (with the 1992-1994 gap) + bump v3→v4. If DO step 6's PRE-FLIGHT Field-value snapshot does not enumerate these schema-CSV edits, halt.
7. §15.D substrate-format reconcile applied at the 3a commit; the broader model-clarification — now ALSO folding the **5c→5c-i/5c-ii/5c-iii decomposition + the within_era ICD-9 cause-column shape (`underlying_cause_icd9`/`cause_recode_61`)** alongside the 3a/3b + 4a/4b/4c + 5→5a/5b/5c decomposition — remains **soft-flag (ii)** proposed-not-applied for §11 human-merge (queued with the soft-flag-(w) batch). On-disk PRE_FLIGHT_LOG/DECISION_LOG/RECEIPTs authoritative in the interim.
8. **soft-flag (gg) narrowed**: the cause-column SHAPE is resolved (HALT 5); the per-condition leaf decomposition of the composite MEDRISK/OBSTETRC/LABOR/DELMTH/CONGENIT/ENTITY/RECORD spans (+ preterm_recode3, father_race_ethnicity_5) stays conservatively NULL → DO step 5c-ii/5c-iii/6 (the natality is_pre1989 conservative-mapping precedent).
9. **L17 forward-looking discipline**: 5c-i added NO new parser dispatcher year (harmonize-side only) → introduced NO stale pin in the 7 cohort parser harnesses; the new 5c-i harness pins no future-year case. DO step 5c-ii/5c-iii/6 configuring a new harmonize era MUST grep-scope ALL 8 cohort SMOKE harnesses' negative-era/`pytest.raises` sets (incl. the new 5c-i `_cohort_era` ValueError + the `_harmonize_cohort_batch` NotImplementedError pins — those become stale when 5c-ii/5c-iii implement their eras: bundle the minimal `tracks-current-state` Edit into the same commit, the §4.2.1/Convention-2/L17 mechanism). Manuscript Coverage re-paragraph (1983-2023 linked incl. the keyless 1983-1988 within-era note) = Phase-D D.4. pytest COUNT 213P+1S+1XF is the gate; runtime band advisory (soft-flag (kk)).

### Notes for next session

- **C8.18 DO step 5c-i is CLOSED.** The per-cohort-era harmonize architecture (`_cohort_era` + `_harmonize_cohort_batch`) + the within_era ICD-9 cause-column shape (`underlying_cause_icd9`/`cause_recode_61`) + the 1989-1991 reference era end-to-end are state-on-disk + this receipt + `PRE_FLIGHT_LOG.md` 2026-05-19T11:00:00Z. The cause-column-shape decision is RESOLVED (H7 sibling-parity; do NOT re-open). Next per the 5c→5c-i/5c-ii/5c-iii decomposition: **DO step 5c-ii — the 1995-2002 + 2003 + 2004 cohort birth-side maps** (reuse the 5c-i architecture; the 2003-rev transition; ~1 session), then **DO step 5c-iii — the keyless 1983-1988 `link_segment` encoding** (the highest-risk methodology-laden sub-step; ~1 session), then DO step 6 (re-harmonize linked 1983-2023; v3→v4 + the `harmonized_schema.csv` edit + version bump) + DO step 7 (docs/metadata; tag `C8.18-complete`).
- §2 cheap-before-expensive + the 3a-substrate-first / §9-#8 / §9-#9 discipline governed 5c-i (decompose by risk; verify the architecture + the lowest-risk reference era first; write the fixture before the code; the H7-sibling-parity reuse of the natality V1-core recodes rather than re-deriving). Carry it.
- The §15.D model-clarification `[plan-update]` (soft-flag (ii)) is owed to the human (§11; queued with the soft-flag-(w) batch; now also folds the 5c→5c-i/5c-ii/5c-iii decomposition + the within_era ICD-9 cause-column shape); on-disk PRE_FLIGHT_LOG/DECISION_LOG/RECEIPTs authoritative in the interim.
- Runtime advisory-band (~257-380s) = soft-flag (kk); environmental, not a canonical regression (11/11 gate SHAs byte-exact; the COUNT 213P+1S+1XF is the gate).
- `/tmp/c8_18_s5c_*` scratch (gate-SHA + pytest output files) is OS-cleanable + reproducible.
