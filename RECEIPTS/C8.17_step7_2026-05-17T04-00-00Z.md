# Receipt: C8.17 DO step 7 — docs-only version-string propagation v2.8.0→v3.0.0 + `C8.17-complete` (the FINAL C8.17 sub-step)
## 2026-05-17T04:00:00Z

### What was done

Propagated the natality **v2.8.0 → v3.0.0** version decision (recorded in DECISION_LOG 2026-05-16T08:00:00Z at DO step 6) into the consumer-facing documentation surface, and updated the manuscript-facing headline counts (natality **1990–2024 / 35 years / 138,819,655 → 1968–2024 / 57 years / 201,161,456**). Docs-only: zero parquet / schema / test / script mutation (re-verified — all 11 gate parquet SHAs byte-exact, git scope is 18 markdown/json/R/cff files). §15.D scopes DO step 7 to 6 files; a Convention-3 PRE-FLIGHT Field-value snapshot surfaced ~10 more consumer-facing files carrying the same stale strings, so a scope divergence was raised via AskUserQuestion 2026-05-17 → user chose **Option A "Honest propagation"** (the 6 §15.D Tier-A files + the mechanical version/count swaps in the consumer files; substantive content — CODEBOOK per-variable rows, the pre-1990 comparability narrative, schema per-column `years_available` — explicitly deferred to C8.20 / soft-flag (aa) with header scope-notes instead of misleading bare swaps). 17 files edited + the PRE-FLIGHT addendum. Tier-C historically-correct "v2.8.0 column-rename" references, the v2.7.0-latest-deposited framing, validation-scope (183/183 = 1990–2024), and unrebuilt-artifact year-ranges (residents-only convenience parquet, the C8.10c 1990–2024 notebook) were deliberately preserved (and annotated). This closes **C8.17** in full (DO steps 1–7 all shipped).

### Inputs consumed

- `RECEIPTS/C8.17_step6_2026-05-16T08-00-00Z.md` — 12 forward-looking HALTs (entry cheap-check; all verified).
- DECISION_LOG 2026-05-16T08:00:00Z — the recorded v2.8.0→v3.0.0 semver decision DO step 7 propagates.
- 11 gate parquets (SHA-verified byte-exact at PRE-FLIGHT and re-verified at VERIFY; see below).
- The pre-edit content of the 17 scoped files (Field-value snapshot in PRE_FLIGHT_LOG addendum 2026-05-17T03:30:00Z).

### Outputs produced

Docs-only; no binary artifacts. Git-tracked files MODIFIED (18; `git diff --stat`):

- **Tier A (6 §15.D-named):** `natality/.zenodo.json` (version/title/description/notes), `CITATION.cff` (in-repo-state line), `natality/docs/ABOUT_THIS_RELEASE.md` (headline, output-table rows, validation-summary, "what does NOT claim", reproduce block), `natality/README.md` (coverage/metrics/Zenodo-version line/parquet table/reproduce block/citation pin + a pre-1990-layout pointer line), `PROJECT_STRUCTURE.md` (natality subproject line), `README.md` (mirror line + four-products Natality row).
- **Tier B (Option-A consumer files):** `CHANGELOG.md` (new C8.17/v3.0.0 entry under v1.1-WIP Data extensions; [v1.0] history untouched), `VERSION_ROADMAP.md` (Currently-shipped table; Planned section deliberately untouched — pre-existing-stale, soft-flagged), `docs/NCHS_SOURCE_MANIFEST.md` (inventory-count/coverage-envelope/process-narrative lines; cross-product joint-coverage clause left + soft-flagged), `docs/COMPARABILITY.md` (cross-product timeline + product-version label), `natality/docs/GETTING_STARTED.md` (header/output-table/rebuild block), `natality/quickstart.R` (header + the ~138.8M→~201.2M comment caught at VERIFY), `natality/docs/FAQ.md` (coverage-summary lines only), `natality/docs/VALIDATION.md` (v3.0.0 scope note + reproduce harmonize-years; 183/183=1990–2024 left accurate), `natality/REPRODUCING.md` (deposit-contents line), `natality/docs/CODEBOOK.md` (header scope-note), `natality/docs/COMPARABILITY.md` (header scope-note).
- `PRE_FLIGHT_LOG.md` — DO step 7 PRE-FLIGHT addendum (prepended, L10-safe, pre-DO).

### Five-phase trace

- **PRE-FLIGHT**: ✓ `PRE_FLIGHT_LOG.md` addendum 2026-05-17T03:30:00Z. 12/12 forward-looking HALTs verified (HALT 9 pytest deferred to VERIFY, precedented). All 11 gate parquet SHAs computed on-disk byte-exact vs the DO-step-6 receipt → docs-only precondition proven. Convention-3 Field-value snapshot of every scoped file. D-class scope divergence surfaced (§7-#17) → AskUserQuestion 2026-05-17 → **Option A "Honest propagation"** Convention-3 plan-amendment, documented before any DO mutation.
- **SMOKE**: ✓ Per-file exact old→new string enumeration via targeted Reads (L1-clean: every file Read-verified to exist; exact unique old-strings captured for surgical Edits). Discriminating principle defined: swap coverage/version/record-count; preserve validation-scope, Tier-C historically-correct, and unrebuilt-artifact year-ranges.
- **DO**: ✓ 17 scoped files + addendum edited via surgical `Edit`. Zero parquet/schema/test/script mutation (verified `git diff --name-only` → no `.py`/`.csv`/schema/tests/scripts/parquet).
- **VERIFY**: ✓ all 5 criteria (below).
- **RECEIPT**: ✓ (this file) + DECISION_LOG + STATUS appended; `C8.17-complete` tag.

### Verify results

- **A — git scope docs-only + parquet mtimes unchanged**: PASS. `git diff --stat` = 17 docs + `PRE_FLIGHT_LOG.md`; zero `.py`/`.csv`/schema/test/script/parquet. Canonical parquet mtimes unchanged (harmonized May 16 22:17, derived May 16 22:29) — no rebuild occurred. threshold: zero non-docs files; value: zero.
- **B — H10 11-SHA gate (docs-only precondition)**: PASS. Re-shasum at VERIFY byte-exact vs DO-step-6 receipt AND vs PRE-FLIGHT: canonical natality `c8a740eb…`/`acb5c48a…`; `.v28_baseline` `230efed2…`/`e16ad53…`; 7 non-natality-v2 `38e2cecb…`/`185c071e…`/`9b828a4d…`/`adbec108…`/`5c22308b…`/`7c682668…`/`d98b4296…`. 11/11 unchanged.
- **C — pytest SHAPE (Convention 1; HALT 9)**: PASS. Cache-cleared `uv run pytest fetal_death/ natality/ tests/ matched_multiples/ -p no:cacheprovider` = **85 passed, 1 skipped, 1 xfailed in 380.64s** (exit 0). Count is the gate (Convention 1); 380.64s within the documented ~240–380s system-variance band; docs-only changes provably cannot perturb the parquet/schema/CSV-reading test surface (Criterion A).
- **D — residual stale-string audit**: PASS. `git grep` over the 17 scoped files for `138,819,655`/`138.8M`/`v2.8.0`/`35 year`/`1990-2024`-coverage. Every residual classified as a documented intentional retention (CHANGELOG L22 provenance equation `201,161,456 = 138,819,655 + 62,341,801`; CHANGELOG [v1.0] L84/L68/L74/L100 history; Tier-C "built on the v2.8.0 column-name rename" in CITATION/README/VERSION_ROADMAP; `docs/COMPARABILITY.md:70` historically-correct column-name attribution; the C8.10c notebook 1990–2024 panel descriptions; the soft-flagged VERSION_ROADMAP "Planned" section). **One accidental miss caught + fixed**: `natality/quickstart.R:8` `~138.8M rows` → `~201.2M rows` (VERIFY did its job — L7/no-rubber-stamp). Post-fix re-sweep: only the 3 documented intentional retentions remain.
- **E — version-string internal consistency**: PASS. `201,161,456`/`1968–2024`/`57 years`/`v3.0.0` present across all key surfaces (positive `git grep -c`); v2.7.0-latest-deposited framing preserved; **no edit claims v3.0.0 is on Zenodo** (every version line says "not yet deposited" / "pending unified-HVS Zenodo deposit").

### Reproducibility

Deterministic + idempotent — docs-only text edits; no build step. Re-running VERIFY (`git diff --stat`, `shasum`, `pytest`, `git grep`) reproduces the same results. Canonical artifacts unchanged (Criterion B), so the C8.17 DO-step-6 reproducibility guarantee carries forward unaltered.

### Cross-product re-probe

7 non-natality-v2 canonical parquet SHAs byte-exact (Criterion B) → fetal-death, linked, matched-multiples provably untouched. No downstream task depends on DO step 7 docs output beyond the next session's reads.

### Git

- Pre-DO tag: `C8.17-pre-do` @ `12fc20e` (set at C8.17 start; not re-tagged at intermediate DO steps per Convention 5).
- Post-RECEIPT tag: **`C8.17-complete`** — set at this commit (DO step 7 is the final C8.17 sub-step; the internal version inconsistency from the DO-step-6/7 split is now resolved).
- Commit: ~5-line summary per Convention 5; full narrative here.

### STATUS.md updated

New section dated 2026-05-17T04:00:00Z marking C8.17 complete (all DO steps 1–7 shipped); next task = C8.18 (linked 1983–2004 backward extension) per KICKOFF Tier 3+5 sequence.

### Self-check (§10) — what could I have gotten wrong that VERIFY wouldn't catch?

1. **An accidental stale-string miss in a file I didn't grep, or a context VERIFY's regexes didn't cover** (e.g., a "1990" written without "–2024", or a spelled-out "thirty-five years"). Mitigation: VERIFY-D grepped the union of `138,819,655|138.8M|138.8 million|v2.8.0|35 year|35 contiguous|1990-2024 (35`; it caught the one real miss (quickstart.R:8). Residual risk: a paraphrased count VERIFY's patterns missed. Plausible-but-low; the headline numerics (`201,161,456`/`1968–2024`/`57`) were positively confirmed present in every key surface.
2. **A judgment call mis-drawn on the discriminating principle** — e.g., I preserved `docs/COMPARABILITY.md` lines 74/158 (C8.10c "35-year 1990-2024 panel") as accurate-unrebuilt-artifact descriptions; if a reader expects those to track the product envelope they may read them as stale. Documented as deliberate (the notebook is genuinely a 1990–2024 artifact; rebuilding it is C8.10-class). Same for the residents-only convenience parquet annotations.
3. **The CODEBOOK/COMPARABILITY "header only" instruction realized as a scope-note rather than a bare `(1990–2024)`→`(1968–2024)` swap.** A bare swap would have over-claimed (the per-variable body + pre-1990 comparability are genuinely 1990–2024-scoped, soft-flag (aa)). The scope-note is the honest realization of Option A's "headers only" but is a micro-deviation from a literal swap — recorded in DECISION_LOG so the choice is auditable.
4. **`.zenodo.json`/ABOUT "multiple record layouts spanning three birth-certificate revisions (1968, 1989, 2003)"** — I rephrased the old "five record layouts / two|three certificate revisions" qualitatively rather than fabricating a precise layout count (L6 no-invention). "Three birth-certificate revisions" is derivable (1968-rev + 1989-rev + 2003-rev); the exact distinct-layout count is intentionally not asserted (it is a soft-flag-(t)-adjacent framing question).
5. **The VERSION_ROADMAP "Planned" section is wholesale-stale (L11) and I deliberately did NOT fix it** (out of C8.17 scope; pre-existing; broader than natality-version). If a reader treats that section as current they will see shipped items marked "scoped/stub". Mitigated by a soft-flag + forward-looking HALT; not introduced by C8.17.

Each residual risk logged in DECISION_LOG 2026-05-17T04:00:00Z.

### Forward-looking HALTs for next session (C8.18 PRE-FLIGHT)

1. `C8.17-pre-do` @ `12fc20e`; **`C8.17-complete` IS now present** (set at this DO step 7 commit). C8.17 is fully closed (DO steps 1–7).
2. **9 canonical/baseline parquet SHAs unchanged** — canonical natality `c8a740eb…`(harm)/`acb5c48a…`(deriv); `.v28_baseline` `230efed2…`/`e16ad53…`; 7 non-natality-v2 (`38e2cecb`/`185c071e`/`9b828a4d`/`adbec108`/`5c22308b`/`7c682668`/`d98b4296`). C8.18 touches the LINKED product; the natality_v2 + fetal-death + matched-multiples SHAs must stay byte-exact through C8.18 (the linked `9b828a4d…` SHA WILL change at C8.18 — that is the intended canonical linked re-harmonize, the symmetric sibling of C8.17's natality_v2 change).
3. **B.12 latest baseline = `tests/snapshots/v2_2026-05-16T08-00-00Z_columns.csv`** (v1 retained). C8.18 will re-snap (linked parquet changes) → new `v3_<UTC>_columns.csv`.
4. **`tests/test_row_count_conservation.py` LINKED pins** (not the NATALITY pins, which are now `201_161_456`/`range(1968,2025)` and must NOT be perturbed by C8.18) will need a Convention-2/L17 same-commit re-pin at C8.18 DO (linked total/years change). pytest baseline 85P+1S+1XF (~240–380s band; count is the gate).
5. **Documentation deferrals owed (soft-flags; NOT C8.18-blocking):** (cc-NEW) the pre-1990 per-variable CODEBOOK extension + the pre-1990 natality COMPARABILITY narrative + `harmonized_schema.csv` per-column `years_available` 1968+ refresh → **task C8.20** / soft-flag (aa); (dd-NEW) `VERSION_ROADMAP.md` "Planned" section is wholesale-stale (pre-existing L11 — fetal-death V2.1/V3, joint-use, notebook statuses all shipped) → a dedicated L11 roadmap-refresh / `[plan-update]`; (ee-NEW) `docs/NCHS_SOURCE_MANIFEST.md` "Cross-product joint coverage" clause + the residents-only convenience parquet (`scripts/06_convenience/`) regeneration to the 1968–2024 envelope → Phase-D deposit-prep / cross-product-COMPARABILITY follow-up. A dedicated v2.8.0→v3.0.0 natality migration guide = C8.11/Phase-D-class. (bb)/(aa)/(w)/(x)/(z)/(u) carry.
6. **§15.D §1358 wording soft-flag (w) + the §15.D DO-step-6/7-boundary clarification** remain the next `[plan-update]` items (Convention 5; NOT bundled with any DO commit). KICKOFF/NEXT_STEPS unedited this step.
7. Tier 3+5 ≈ 2.5/7 tasks done (C8.16 + C8.17); cumulative Phase C ≈ 25.5 of 51–71 sessions (effort-ceiling cap 86 intact). C8.18 is the largest single remaining pre-Zenodo task (8–14 sessions per §15.D).

### Notes for next session

- **C8.17 is COMPLETE.** Next per KICKOFF Tier 3+5 sequence: **C8.18 — linked birth-infant death 1983–2004 backward extension** (29 source files; cohort-linked 1983–1991 + cohort/period-linked 1995–2004; permanent 1992–1994 gap; cohort-vs-period publishing-design decision; linked v3 → v4). It is the symmetric sibling of C8.17 on the linked product and modestly benefits from C8.17's 1978-cert layout work.
- DO step 7 was docs-only; no rebuild. The v3.0.0 string is now consistent across the consumer surface; the only `v2.8.0` strings that remain are historically-correct (the column-rename landed at v2.8.0) or append-only history.
- The §10 self-check's 5 residual risks + the 3 new soft-flags (cc/dd/ee) are the documentation debt knowingly carried from the Option-A scope boundary; none block C8.18.
