# Receipt: C8.18 DO step 2 — download 19 cohort source zips + 19 cohort user-guide PDFs + SHA-anchor + metadata extension
## 2026-05-17T18:30:00Z

### What was done

Executed C8.18 DO step 2 (the scope-corrected §15.D cohort-only download sub-step) under the full five-phase discipline. Downloaded the **19 cohort-linked NCHS source zips** (9 × `LinkCO{83..91}.zip` 1983-1991 no-`US`-suffix + 10 × `LinkCO{95..04}US.zip` 1995-2004; Σ 1,858,192,447 B ≈ 1.85 GB) + **19 cohort user-guide PDFs** (`LinkCO{83..91,95..04}Guide.pdf`) to the sibling-of-existing `~/Desktop/natality-harmonization/raw_data/linked/` + `raw_docs/linked/` locations; recorded SHA-256 + uncompressed member layouts for every artifact; SHA-anchored all 19 zips into `docs/NCHS_SOURCE_MANIFEST.md` §3 (19→38 linked rows; total manifest 122→141); added 19 `<YYYY>_linked` rows to `natality/metadata/file_inventory.csv` (`imported=false` — downloaded, not yet harmonized); Convention-2/L17 same-commit re-pin of `tests/test_source_zip_sha_stability.py` (`tracks-current-state`: count anchor 122→141, linked 19→38). The §15.D Task C8.18 scope-correction `[plan-update]` (soft-flag (ff)) was **merged this session** as the standalone commit `df0675f` (§11 step 3) before DO step 2, so this sub-step ran against a self-consistent plan. Zero canonical parquet / schema / harmonize-or-parse-script mutation (download + inventory only, the C8.17 DO step 1 shape).

### Inputs consumed

- `RECEIPTS/C8.18_step1_2026-05-17T05-30-00Z.md` — 7 forward-looking HALTs (entry cheap-check; 7/7 verified).
- `NEXT_STEPS.md` §15.D Task C8.18 (post-`df0675f` scope-corrected, cohort-only / 19 zips).
- On-disk sibling substrate (L1-extension anchor): `natality/metadata/file_inventory.csv` 2005-2015 `cohortlinkedus/LinkCO{05..15}US.zip` rows; `docs/NCHS_SOURCE_MANIFEST.md` §3; `raw_docs/linked/LinkCO05Guide.pdf` (367,848 B on disk = the verified-correct guide-URL anchor).
- 19 NCHS cohort zips + 19 cohort guide PDFs (HTTP 200; SHAs recorded at download — net-new manifest rows, no prior SHA claim).
- 11 gate parquets — SHA-verified byte-exact at PRE-FLIGHT and re-verified at VERIFY.

### Outputs produced

Raw artifacts (gitignored; outside the monorepo tree; SHA-recorded in `docs/NCHS_SOURCE_MANIFEST.md` §3):

- `~/Desktop/natality-harmonization/raw_data/linked/LinkCO{83..91}.zip` (9) + `LinkCO{95..04}US.zip` (10) — 19 zips, Σ 1,858,192,447 B. Per-zip SHA-256 in the manifest §3 table + `/tmp/c8_18_step2_manifest.tsv`.
- `~/Desktop/natality-harmonization/raw_docs/linked/LinkCO{83..91,95..04}Guide.pdf` — 19 cohort user-guide PDFs (sizes 1.2 MB–23.7 MB; Σ ≈ 0.14 GB).

Git-tracked (this commit):

- `natality/metadata/file_inventory.csv` — +19 rows (`1983_linked`…`1991_linked`, `1995_linked`…`2004_linked`), inserted before `2005_linked` (chronological within the cohort-linked block; C8.17 "insert before first existing block row" precedent). 76→95 data rows; 19→38 `_linked` rows. All 8-col well-formed (csv-parsed). `imported=false`; `file_format` records "record byte-width = C8.18 DO step 3" (not fabricated — DO step 3 reconstructs it from the guides); `notes` carries member names + uncompressed sizes + correctly-identified denominator/births member.
- `docs/NCHS_SOURCE_MANIFEST.md` — §3 header 19→38 / "cohort years 1983-2023, permanent 1992-1994 gap"; three-era naming-convention preamble; loud 1992-1994-gap paragraph; 19 new SHA rows (chronological, before 2005); coverage caveat corrected (the prior "pre-2005 NOT processed" superseded by C8.18). 122→141 total manifest rows.
- `tests/test_source_zip_sha_stability.py` — `DESIGN: tracks-current-state` Convention-2/L17 same-commit anchor re-pin: docstring `(122 = 43 + 57 + 19 + 3)`→`(141 = 43 + 57 + 38 + 3)` + C8.18 provenance sentence; `assert len(rows) == 122`→`141` (+message); section-count docstring + `counts == {... "linked": 38 ...}`.
- `PRE_FLIGHT_LOG.md` — C8.18 DO step 2 PRE-FLIGHT entry (2026-05-17T17:30:00Z; before any DO mutation; L10-safe).
- `RECEIPTS/C8.18_step2_2026-05-17T18-30-00Z.md` — this file.
- `DECISION_LOG.md` + `STATUS.md` — appended (DO step 2 choices + new dated section).

### Five-phase trace

- **PRE-FLIGHT**: ✓ `PRE_FLIGHT_LOG.md` C8.18 DO step 2 entry 2026-05-17T17:30:00Z. 7/7 forward-looking HALTs PASS. 11/11 gate parquet SHAs byte-exact (fetal-death via the canonical `~/Desktop/fetal-death-harmonization-build/output/harmonized/` tree — a wrong-path read of the stale secondary copy was caught + corrected at the cheap-check, not a §7-#18 regression). 19 zip URLs + 19 guide PDF URLs all HTTP 200 (L1-extension sibling-derived; guide-URL anchored on the byte-size-matching on-disk `LinkCO05Guide.pdf`). Convention-3 Field-value snapshot of the 3 mutated artifacts = no divergence. Written before any DO mutation (L10-safe).
- **SMOKE**: ✓ Tier-1-equivalent (~3% of the 19-file run): downloaded `LinkCO83.zip` (smallest) + `LinkCO83Guide.pdf` (oldest, highest OCR-risk) + `LinkCO96Guide.pdf`; sizes byte-exact vs probed content-length; zip valid (`LinkCO83USnum.dat` + `LinkCO83USden.dat`); SHA-anchoring works; **L12-extension text-layer probe before any OCR claim** — `LinkCO83Guide.pdf` 139/139 non-empty pages / 433,983 chars (clean record-layout text), `LinkCO96Guide.pdf` 262/272 / 584,523 chars → **no OCR needed** (same scanned-with-embedded-text-layer pattern as the C8.17/fetal-death precedent).
- **DO**: ✓ Resumable background download of all 19 zips + 19 PDFs (idempotent skip-if-correct-size; SMOKE's 3 reused); per-file size validated vs probed content-length; `ALLDONE rc=0 n_manifest=38`, zero FAIL/RETRY/REDL/SIZE_MISMATCH. 3 metadata artifacts extended via surgical Edits. Commits: this DO step 2 commit (download + metadata).
- **VERIFY**: ✓ all criteria (below).
- **RECEIPT**: ✓ (this file) + DECISION_LOG + STATUS appended. **No tag** at this intermediate C8.18 sub-step (Convention 5 / C8.17 DO-step precedent; `C8.18-complete` only at the final C8.18 sub-step). `C8.18-pre-do`@`6632a15` remains the C8.18 DO rollback anchor.

### Verify results

- **A — all 38 artifacts downloaded + size-valid**: PASS. 19 zips + 19 PDFs on disk; every download size == its probed `content-length` (validated per-file in the download script); `ALLDONE rc=0`; zero FAIL/SIZE_MISMATCH/RETRY/REDL in `/tmp/c8_18_step2_dl.log`. value: 38/38; threshold: 38/38.
- **B — manifest SHA-anchor integrity + Convention-1 structural anchor**: PASS. `docs/NCHS_SOURCE_MANIFEST.md` parses (via the test's own regex) to exactly **141 rows = {fetal:43, natality:57, linked:38, mm:3}**, matching the re-pinned anchors. `test_source_zip_sha_stability.py` (`test_manifest_anchor_row_count` + `test_manifest_section_row_counts` + `test_source_zip_sha_matches_manifest`) PASS within the 85-passed suite → every on-disk linked zip's SHA == manifest byte-exact (all 38, incl. the 19 net-new).
- **C — H10 gate (zero canonical-state mutation)**: PASS. 11/11 gate parquet SHAs byte-exact vs documented: natality `c8a740eb…`/`acb5c48a…`; `.v28_baseline` `230efed2…`/`e16ad532…`; linked-derived `9b828a4d…`; fetal-death `38e2cecb…99c5cf48`/`185c071e…a7968a09` (canonical `-build` tree); MM `adbec108…`/`5c22308b…`/`7c682668…`/`d98b4296…`. `git status` = 4 state/metadata files + receipt/decisionlog/status; zero `.py`-pipeline/`.parquet`/`harmonized_schema.csv`/validation-CSV.
- **D — pytest Convention-1 count gate**: PASS. `uv run pytest fetal_death/ natality/ tests/ matched_multiples/ -p no:cacheprovider` = **85 passed, 1 skipped, 1 xfailed in 347.03s** (== baseline; within the ~240-380s system-variance band; the Convention-2/L17 anchor re-pin is bundled into this DO commit and the SHA-stability test passes — not a regression). `test_inventory_invariants` (natality inventory↔schema) PASS — the 19 new `<YYYY>_linked` rows are excluded by the `_read_inventory_year_set` `_linked`-skip (the C8.17 DO step 1 lesson already internalized), so they did not perturb it.
- **E — CSV well-formed**: PASS. `csv.reader` parses `file_inventory.csv` to 95 data rows, every row 8 cols, 0 malformed; 19 new rows all `imported=false`; linked years 1983-2023 with the 1992-1994 gap visibly absent.

### Reproducibility

Deterministic + idempotent: re-running `/tmp/c8_18_step2_dl.sh` skips files already present at the correct size (no re-download); the inventory/manifest rows were generated deterministically from the download manifest + `unzip -l` member sizes (`/tmp/c8_18_step2_*.txt`). The 19 zip SHAs are NCHS-upstream-stable (the SHA-stability test re-verifies every run). `/tmp/c8_18_step2_*` scratch is OS-cleanable + reproducible. Canonical parquets unchanged (Criterion C) → the C8.17 reproducibility guarantee carries forward.

### Cross-product re-probe

11 gate parquet SHAs byte-exact (Criterion C) → natality_v2, fetal-death, linked-derived, matched-multiples provably untouched. The linked-derived `9b828a4d…` changes only at the later C8.18 re-harmonize DO step (intended; not DO step 2). No downstream task consumes DO step 2 output beyond the next C8.18 sub-step (DO step 3 layout reconstruction reads these raw zips + guides).

### Git

- Pre-DO tag: `C8.18-pre-do` @ `6632a15` (set at C8.18 start; the C8.18 DO rollback anchor; not re-tagged at intermediate sub-steps per Convention 5). The standalone `[plan-update]` `df0675f` sits between `6632a15` and this DO step 2 commit (a non-DO §11 commit).
- Post-RECEIPT tag: **none** — DO step 2 is an intermediate C8.18 sub-step; `C8.18-complete` is set only at the final C8.18 sub-step (Convention 5 / C8.17 DO-step precedent).
- Commit: ~5-line summary per Convention 5; full narrative here.

### STATUS.md updated

New section dated 2026-05-17T18:30:00Z: C8.18 DO step 2 closed (19 cohort zips + 19 guide PDFs downloaded, SHA-anchored, metadata extended); next = C8.18 DO step 3 (cohort 1983-1991 layout-CSV reconstruction; 1978-rev birth + ICD-9; sibling of natality 1978-1988 from C8.17 DO step 4).

### Self-check (§10) — what could I have gotten wrong that VERIFY wouldn't catch?

1. **The 19 zips download byte-clean but a member is internally truncated/wrong-era.** Mitigation: zip integrity confirmed on the SMOKE sample (`unzip -l` member listing); `unzip -l` central-directory member sizes recorded for all 19 (consistent num+den[+unl] cohort structure per era). Residual: the *record-level* byte-width + field semantics are unverified at DO step 2 by design — that is the explicit DO step 3 deliverable (L13-extension value-distribution verify), surfaced as a forward-looking HALT, not silently assumed. `file_format` says "record byte-width = C8.18 DO step 3", not a fabricated number.
2. **Denominator-vs-numerator member mislabel in `file_inventory.csv` notes.** A first-pass generator positionally labeled the 1983-1991 `*num.dat` as the denominator; caught at the cheap-check (L13-extension discipline) and regenerated with name-based `den` detection — every year's denominator is now the `*den*`/`*DEN*`/`*DENOM*`/`*DENPUB*` member. Residual: low; verified by spot-check across 1983/1989/1991/1995/2002/2003/2004.
3. **Guide-URL sibling-derivation wrong (downloaded a wrong-but-200 PDF).** Mitigation: the guide base path was anchored on the on-disk `LinkCO05Guide.pdf` whose probed `content-length` (367,848) equals the on-disk file size byte-for-byte → the URL form is the verified sibling, not a hallucinated path that merely returns 200. L12 text-layer probe further confirms the 1983 + 1996 PDFs are the actual linked-file documentation (title pages read "Linked Birth/Infant Death Data Set: 1983 Birth Cohort" / "1996 Linked Birth/Infant Death Birth Cohort").
4. **`imported=false` could be wrong if a downstream test expects new `_linked` rows in a coverage set.** Mitigation: `test_inventory_invariants` skips `_linked` keys entirely (verified by reading the test + the 85-passed suite); `imported=false` is semantically correct (not yet in the v3 parquet — the v3→v4 re-harmonize is a later C8.18 DO step). Residual: low; the C8.17 pre-1990 precedent is identical (download=false, flipped at re-harmonize).
5. **Stale fetal-death secondary copy confusion recurs at a later step.** The canonical fetal-death gate path is the `-build` tree; the non-`build` `~/Desktop/fetal-death-harmonization/*.parquet` is a stale May-4 secondary. Recorded here + in the PRE_FLIGHT_LOG so the next session's gate-SHA check uses the correct path (sibling of the C8.17 D5 stale-build-repo class; soft-flag candidate).

Each residual risk logged in DECISION_LOG 2026-05-17T18:30:00Z.

### Forward-looking HALTs for next session (C8.18 DO step 3 PRE-FLIGHT)

1. `C8.18-pre-do` @ `6632a15` present; `C8.18-complete` NOT present (final-sub-step-only). `C8.17-complete` present. The `[plan-update]` `df0675f` + this DO step 2 commit are on `main` after `6632a15`.
2. **11 gate parquet SHAs unchanged at DO step 3 entry** — natality `c8a740eb…`/`acb5c48a…`; `.v28_baseline` `230efed2…`/`e16ad532…`; linked-derived `9b828a4d…`; fetal-death `38e2cecb…99c5cf48`/`185c071e…a7968a09` **(read from `~/Desktop/fetal-death-harmonization-build/output/harmonized/` — NOT the stale `~/Desktop/fetal-death-harmonization/` secondary)**; MM `adbec108…`/`5c22308b…`/`7c682668…`/`d98b4296…`. The linked-derived `9b828a4d…` changes only at the later C8.18 re-harmonize DO step (NOT step 3).
3. **19 cohort zips + 19 guide PDFs on disk + SHA-anchored**: `raw_data/linked/LinkCO{83..91}.zip` + `LinkCO{95..04}US.zip`; `raw_docs/linked/LinkCO{83..91,95..04}Guide.pdf`. Manifest §3 = 38 linked rows; total 141; `test_source_zip_sha_stability.py` anchors = 141 / linked 38. DO step 3 must NOT re-download (idempotent skip).
4. **DO step 3 = cohort 1983-1991 layout reconstruction** (1978-rev birth cert + ICD-9; sibling of natality 1978-1988 from C8.17 DO step 4). Substrate: `LinkCO{83..91}USden.dat` (births denominator — the primary harmonized file) + `LinkCO{83..91}USnum.dat` (infant-death numerator); guide = `LinkCO{83..91}Guide.pdf` (text-extractable, no OCR — L12 confirmed). L13-extension: byte-position verify is NOT sufficient — value-distribution-verify each anchor field. Record byte-width is unknown until read from the guides (the DO step 3 deliverable; `file_inventory.csv` `file_format` currently says so explicitly).
5. ICD-9/10 default-null + revision-tagged settled (DECISION_LOG 2026-05-17T05:30:00Z); the exact ICD-9-era harmonized cause column shape + `cause_recode_130` per-era availability = DO step 5/6 PRE-FLIGHT (soft-flag (gg)).
6. `tests/test_row_count_conservation.py` NATALITY pins (`201_161_456`/`range(1968,2025)`) must NOT be perturbed; LINKED pins (`74_943_824`/`range(2005,2024)`) get the Convention-2/L17 same-commit re-pin at the later C8.18 re-harmonize DO step (NOT step 3). B.12 latest = `v2_2026-05-16T08-00-00Z_columns.csv`; C8.18 re-snaps at re-harmonize. pytest baseline 85P+1S+1XF (~240-380s; count is the gate).
7. Tier 3+5 ≈ 2.5/7 (C8.16+C8.17 done; C8.18 DO steps 1+2 of 7 done); cumulative Phase C ≈ 27 of 51-71 (effort-ceiling cap 86 intact).

### Notes for next session

- **C8.18 DO step 2 is closed.** 19 cohort source zips (~1.85 GB) + 19 cohort user-guide PDFs SHA-anchored on disk; the 3 metadata artifacts agree (file_inventory 38 linked / manifest 141 / SHA-stability anchors 141·38). The §15.D `[plan-update]` is merged (`df0675f`) — plan and on-disk decision agree; soft-flag (ff) RESOLVED.
- **Next per §15.D (scope-corrected): DO step 3 — cohort 1983-1991 layout-CSV reconstruction** (2-3 sessions; 1978-rev birth + ICD-9; sibling-derive from C8.17's natality 1978-1988 layout work where applicable; L13-extension value-distribution verify each anchor field; the highest-value early DO-step for the cohort extension).
- Soft-flag candidate: the stale `~/Desktop/fetal-death-harmonization/*.parquet` secondary (non-`build`) vs the canonical `-build` tree — record so future gate-SHA checks use the `-build` path (sibling of the C8.17 D5 class; not C8.18-blocking).
- `/tmp/c8_18_step2_*` scratch (dl script + log + manifest + row generators) is OS-cleanable + reproducible.
